#!/usr/bin/env python3
# Created: 24:08:2026 - 16:20:00
# Last updated: 24:08:2026 - 03:10:00
# Module: tools
# File: tools/gen_cornhall_plan.py
#
# Responsibility:
# - ЧЕРТЁЖ КОРНХОЛЛА (город №4 эпохи «12 городов», ПИЛОТ, этап 1). Рисует
#   схему в docs/CORNHALL_PLAN.html и ЭКСПОРТИРУЕТ её в docs/CORNHALL_PLAN.json
#   — единственный источник композиции для tools/gen_city.py (паспорт города
#   лежит в tools/cities/cornhall.py и композиции не знает).
#
# ЧЕМ ЭТОТ ЧЕРТЁЖ ОТЛИЧАЕТСЯ ОТ ВАЙТРАНОВСКОГО, И ЭТО ВЕСЬ СМЫСЛ ЭТАПА 1.
# Пилот не строит НИ ОДНОГО нового рецепта и НИ ОДНОГО нового умения движка:
# меняются только три рычага — стиль (какие рецепты куда), посадка (этот файл)
# и огонь (паспорт). Посадка здесь другая по четырём пунктам:
#   1. КОЛЬЦО ПРАВИЛЬНОЕ. У Вайтрана стена идёт по кромке рельефа и потому
#      кривая; здесь равнина, защищать нечего, и кольцо — овал (a=170, b=150,
#      24 вершины) с четырьмя воротами ровно на две оси трактов.
#   2. ГЛАВНАЯ ФОРМА — УЛИЦА, А НЕ ПЛОЩАДЬ. Рынок Вайтрана это прямоугольник
#      14x11; здесь рыночная улица 17 м между фасадами и 240 м в длину, и вся
#      застройка города — два фасадных ряда по её красной линии.
#   3. РЯД СПЛОШНОЙ. Щель фасадов 0.0-0.3 (у Вайтрана 0.35-0.7), поворот дома
#      ±4 гр. от красной линии, за фасадом бургажная полоса 12-20 м до
#      задворочного переулка. Это даёт ленту, а не рассыпанные дома.
#   4. ЯРМАРОЧНОЕ ПОЛЕ СНАРУЖИ. У одних ворот, с постоянными каменными тумбами
#      (city-kerb2), которые вне сезона стоят голыми — город показывает своё
#      время года.
#
# ВОДЫ ЗДЕСЬ НЕТ, И ТЕПЕРЬ ЭТО ЗАКОННО. Пилот держал за южными воротами ручей
# с мостом единственно затем, чтобы прогон не упал: gen_city звал bridge_setup
# безусловно, а тот меряет water_at() — при пустом списке русел IndexError на
# первой строке main(). Волна Г сделала воду НЕОБЯЗАТЕЛЬНОЙ (bridge_setup
# отдаёт None, стадии моста молчат), и обход снят: Корнхолл стоит на хлебной
# равнине без единого русла, как и записано в его паспорте.
#
# UPD:
# - 24:08:2026 - 16:20:00: Создан (пилот эпохи, этап 1). Кольцо-овал, четверо
#   ворот, рыночная улица с двумя фасадными рядами, задворочные переулки,
#   fringe belt у стены, три предместья лентами вдоль трактов, ярмарочное поле
#   с каменными тумбами, ручей с мостом за южными воротами.
# - 24:08:2026 - 18:10:00: ВОЛНА Г, ЗАХОД 1 — РУЧЕЙ И МОСТ УБРАНЫ. Они стояли
#   здесь не по замыслу, а затем, чтобы gen_city не упал на городе без воды;
#   вода стала необязательной, и обход снят. Кузница переименована в вид
#   «kuznya»: имя вида больше не диктует механизм.
# - 24:08:2026 - 19:40:00: ВОЛНА Г, ЗАХОД 2 — ДВЕ ТАБЛИЦЫ, ПЕРЕПИСАННЫЕ С
#   ПОЛКИ РУКАМИ (FRONT и FACE_OFF), заменены чтением манифеста
#   assets/houses/INDEX.txt. В FRONT уже жила ошибка: у мельницы (дверь на -X)
#   фронт с глубиной стояли переставленными. Точка дома уходит в чертёж как
#   точка КРАСНОЙ ЛИНИИ; отступ дверной точки делает генератор (FACE_ON_LINE).
#   Шаг рядов пояса у стены вернулся к общегородскому — разрежённость держит
#   правило GAP_ZONES паспорта.
# - 24:08:2026 - 22:00:00: ВОЛНА Г, ЗАХОД 5 — ДВА ОБХОДА ЯРМАРКИ СНЯТЫ.
#   Поле вернулось к 120x90 (сжатие до 84x78 было подгонкой места под то, что
#   умел его пол; теперь пол мостит рядами), а съезд на ярмарку из
#   14-метрового огрызка стал настоящим съездом до самого поля.
# - 24:08:2026 - 03:10:00: ЭТАП 2 — ЗАСТРОЙКА ПЕРЕЕХАЛА НА ПАКЕТ cornhall-*.
#   Чертёж не знает о рецептах ничего (габарит и вынос лица берёт из
#   манифеста, вид -> рецепт называет паспорт), поэтому здесь изменилось
#   только ОДНО: КАКИЕ ВИДЫ и КУДА. Что прибавилось на карте:
#     * рыночный фронт — щипец на улицу (jetty/house/manor вместо city-shop);
#     * ДВА ПРОГОНА АРКАДЫ по 3 тела на каждом торге — крытый ряд двумя
#       длинными сторонами (§4.6), одиннадцать тел всего;
#     * гражданский центр на площади-упоре: ратуша с башней, весовая палата
#       напротив, каланча в её дворе, храм в глубине (§4.7, 4.8, 4.17);
#     * пивоварня у восточного пояса (§4.10-4.11);
#     * ТРИ постоялых двора у ворот (§4.12), ЧЕТЫРЕ голубятни во дворах
#       усадеб (§4.15), ЧЕТЫРЕ амбара на грибах (§4.5), ДВА гончарных горна
#       снаружи у ворот (§4.18), ТРИ ветряка-столбовки на валу (§4.9);
#     * пояс у стены и предместья — солома (cornhall-house-s-thatch), дальний
#       хутор остался на старом city-house-s-old (он беднее).
#   БАШНЯ РАТУШИ больше не отдельное тело: у cornhall-townhall она своя.
#   Мельница city-mill снята вместе с ней — ветряки на валу и есть §4.9, а
#   водяная мельница на равнине без единого русла стояла необъяснимо.
"""Чертёж Корнхолла: docs/CORNHALL_PLAN.json + .html."""
import json
import math
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
SPAN = 512
SC = 1.5                       # масштаб картинки: 512 м -> 768 px

# =============================================================================
# КОЛЬЦО СТЕН: ОВАЛ, А НЕ КРОМКА РЕЛЬЕФА
# =============================================================================
CX, CZ, A, B = 256.0, 292.0, 170.0, 150.0
NV = 24                        # вершин кольца: шаг 15 гр. — на нём лежат и
                               # четверо ворот (0/90/180/270), и восемь башен


def ell(deg):
    """Точка овала. Знак у sin — МИНУС, и это не вкус: цепь гнутых прогонов
    кладёт бугор кладки наружу только при отрицательной знаковой площади
    контура (у Вайтрана -7680), то есть при обходе по часовой в чарте."""
    t = math.radians(deg)
    return (CX + A * math.cos(t), CZ - B * math.sin(t))


WALL = [tuple(round(v, 1) for v in ell(i * 360.0 / NV)) for i in range(NV)]
_area = sum(x0 * z1 - x1 * z0 for (x0, z0), (x1, z1)
            in zip(WALL, WALL[1:] + [WALL[0]])) / 2.0
assert _area < 0, f"обход кольца не по часовой (площадь {_area:.0f})"

GATES = [{"pos": list(ell(90)), "name": "ярмарочные"},     # север
         {"pos": list(ell(0)), "name": "пивоварские"},     # восток
         {"pos": list(ell(270)), "name": "скотные"},       # юг
         {"pos": list(ell(180)), "name": "мельничные"}]    # запад
TOWERS = [[round(v, 1) for v in ell(d)]
          for d in (30, 60, 120, 150, 210, 240, 300, 330)]

# =============================================================================
# УЛИЦЫ
# =============================================================================
# РЫНОЧНАЯ УЛИЦА — ОСЕВАЯ ГОРОДА. Ломаная гуляет на ±0.6 м: улица не должна
# быть чертёжной прямой, но и уходить от красной линии ей нельзя — фасады
# стоят на 8.5 м от осевой, а полоса мостовой полуширины 6.5 м, и метровый
# увод осевой загнал бы фасадный ряд в проезжую часть.
MAIN = [(256.0, 146.0), (256.6, 176.0), (255.4, 206.0), (256.4, 240.0),
        (255.6, 272.0), (256.4, 300.0), (255.4, 334.0), (256.6, 368.0),
        (255.6, 402.0), (256.0, 438.0)]
CROSS = [(92.0, 292.0), (130.0, 291.4), (170.0, 292.4), (210.0, 291.6),
         (256.0, 292.0), (300.0, 291.6), (340.0, 292.4), (380.0, 291.4),
         (420.0, 292.0)]
WBACK = [(222.0, 182.0), (221.4, 232.0), (222.4, 282.0), (221.6, 332.0),
         (222.4, 382.0), (222.0, 412.0)]
EBACK = [(290.0, 182.0), (290.6, 232.0), (289.4, 282.0), (290.4, 332.0),
         (289.6, 382.0), (290.0, 412.0)]
NROAD = [(256.0, 138.0), (256.4, 108.0), (255.6, 74.0), (256.4, 40.0),
         (256.0, 0.0)]
SROAD = [(256.0, 446.0), (256.6, 470.0), (256.0, 487.0), (255.6, 512.0)]
WROAD = [(82.0, 292.0), (56.0, 291.4), (28.0, 292.4), (0.0, 292.0)]
EROAD = [(430.0, 292.0), (458.0, 292.6), (486.0, 291.6), (512.0, 292.0)]
# СЪЕЗД НА ЯРМАРКУ ДОВЕДЁН ДО САМОГО ПОЛЯ. Он был ОГРЫЗКОМ в 14 м, и это
# было вынужденно: поле мостилось целиком классом мостовой, съезд снаружи
# кольца идёт классом стёжки, и приёмка честно считала пересечение «чужим
# классом внутри контура площадки» (10.25% при пороге 0). Пилот увёл ленту за
# кромку поля. Волна Г мостит поле РЯДАМИ, контур замощённого — это полосы
# под тумбами, и съезду больше не с чем пересекаться: он входит на поле, как
# и положено съезду.
FAIRLANE = [(256.0, 124.0), (250.0, 114.0), (242.0, 106.0), (232.0, 100.0)]

ROADS = [
    {"mat": "stone", "w": 11.0, "pts": MAIN},        # 0 — рыночная улица (ось)
    {"mat": "stone", "w": 8.0, "pts": CROSS},        # 1 — поперечная
    {"mat": "dirt", "w": 3.0, "pts": WBACK},         # 2 — задворочный западный
    {"mat": "dirt", "w": 3.0, "pts": EBACK},         # 3 — задворочный восточный
    {"mat": "stone", "w": 6.0, "pts": NROAD},        # 4 — северный тракт
    {"mat": "stone", "w": 6.0, "pts": SROAD},        # 5 — южный тракт (мост)
    {"mat": "stone", "w": 6.0, "pts": WROAD},        # 6 — западный тракт
    {"mat": "stone", "w": 6.0, "pts": EROAD},        # 7 — восточный тракт
    {"mat": "dirt", "w": 4.0, "pts": FAIRLANE},      # 8 — съезд на ярмарку
]

# =============================================================================
# ЗАСТРОЙКА
# =============================================================================
# ГАБАРИТ И ВЫНОС ЛИЦА — ИЗ МАНИФЕСТА ПОЛКИ, А НЕ ИЗ ТАБЛИЦЫ ЗДЕСЬ
# =============================================================================
# ЗДЕСЬ СТОЯЛИ ДВЕ ТАБЛИЦЫ, ПЕРЕПИСАННЫЕ РУКАМИ С ПОЛКИ (находка пилота №9):
# FRONT — фронт и глубина каждого рецепта, FACE_OFF — вынос лица за дверную
# точку. Обе описывают ДЕТАЛЬ, а не композицию, и обе были будущим
# расхождением: полку перепекают каждую волну, а таблицу поправить забудут.
# Так уже и случилось — у мельницы фронт с глубиной стояли переставленными
# местами (12.25/8.54 при двери на -X), и самопроверка чертежа мерила её тело
# поперёк себя.
# Теперь оба числа берутся у ЕДИНСТВЕННОГО источника габаритов —
# assets/houses/INDEX.txt (колонки g*, door_axis, face_off), а какой рецепт
# стоит за каким видом, говорит паспорт города. Чертёж по-прежнему не знает
# ничего о характере города: он спрашивает у паспорта одну таблицу KIND.
sys.path.insert(0, os.path.join(ROOT, "tools"))
from cities.cornhall import KIND        # noqa: E402  (после правки sys.path)


def _shelf():
    """Манифест полки: строка на рецепт, разбор dict(zip(cols, split()))."""
    cols, rows = None, {}
    with open(os.path.join(ROOT, "assets/houses/INDEX.txt"),
              encoding="utf-8") as f:
        for line in f:
            if line.startswith("# columns:"):
                cols = line.split(":", 1)[1].split()
            elif cols and line.strip() and not line.startswith("#"):
                row = dict(zip(cols, line.split()))
                rows[row["file"]] = row
    return rows


def _num(v):
    return None if v == "-" else float(v)


SHELF = _shelf()
# ФРОНТ — ПОПЕРЁК ОСИ ДВЕРИ, ГЛУБИНА — ВДОЛЬ НЕЁ. У city-longhall дверь на +X,
# у city-mill на -X: их фронт это габарит по z, а не по x, и таблица «w, d»
# для них означала бы дом, повёрнутый на 90 гр. от своей улицы.
FRONT, FACE_OFF = {}, {}
for _kind, _rec in KIND.items():
    _r = SHELF[_rec]
    _w = _num(_r["gx1"]) - _num(_r["gx0"])
    _d = _num(_r["gz1"]) - _num(_r["gz0"])
    _along_x = _r["door_axis"] in ("+X", "-X")
    FRONT[_kind] = (round(_d if _along_x else _w, 2),
                    round(_w if _along_x else _d, 2))
    FACE_OFF[_kind] = _num(_r["face_off"]) or 0.0

HOUSES = []


def put(kind, x, z, deg, door, name=None, walk=None):
    """(x,z) — точка КРАСНОЙ ЛИНИИ, где стоит ЛИЦО дома, и ТОЧКА УХОДИТ В
    ЧЕРТЁЖ КАК ЕСТЬ. Отступ дверной точки внутрь квартала на face_off делает
    ГЕНЕРАТОР (паспорт объявляет FACE_ON_LINE): прежде эту поправку считал
    здешний put() по переписанной руками таблице — то есть чертёж брал на себя
    замер детали. Схемное пятно передаётся нулевым: его единственное дело в
    house_frame — вынос двери от центра схемы, а он нам не нужен."""
    HOUSES.append({"x": round(x, 2), "z": round(z, 2),
                   "w": 0, "d": 0,
                   "deg": round(deg, 1), "door": door,
                   "walk": list(walk) if walk else None,
                   "name": name or "", "kind": kind})


def row(x_or_z, a, b, door, pattern, gap=0.2, axis="z", jitter=4.0, walk=None):
    """ФАСАДНЫЙ РЯД ПО КРАСНОЙ ЛИНИИ. Идёт от a до b, беря рецепты из
    pattern по кругу и подставляя следующий, пока хватает места; поворот
    каждого дома — ±jitter гр. вокруг линии ряда (норматив Корнхолла ±4).
    Щель gap — НОРМАТИВ РЯДА (0.0-0.3 у рынка, 1.0-2.0 в fringe belt); тот же
    норматив стоит в паспорте как GAP_M и держится расталкиванием тел."""
    t, i, out = a, 0, 0
    while True:
        kind = pattern[i % len(pattern)]
        front = FRONT[kind][0]
        if t + front > b:
            break
        deg = ((i * 37) % int(jitter * 2 + 1)) - jitter
        c = t + front / 2.0
        if axis == "z":
            put(kind, x_or_z, c, deg, door, walk=walk)
        else:
            put(kind, c, x_or_z, deg, door, walk=walk)
        t += front + gap
        i += 1
        out += 1
    return out


# --- РЫНОЧНАЯ УЛИЦА: ДВА СПЛОШНЫХ ФАСАДНЫХ РЯДА ------------------------------
# Красные линии на 8.5 м от осевой: полоса мостовой 13 м (полуширина 6.5) плюс
# 2 м запаса, чтобы тело не входило в проезжую часть на изломах осевой.
#
# ЭТАП 2: РЯД СТОИТ ЩИПЦОМ НА УЛИЦУ, И ЭТО ДАРОМ. У всего жилья пакета
# cornhall-* конёк идёт вдоль Z, а дверь — в южном щипце; дом, посаженный
# дверной гранью на красную линию, поворачивается к улице ступенчатым щипцом
# сам. Прежде здесь стоял city-shop с коньком ВДОЛЬ улицы: ряд читался
# сплошной кровлей, а не чередой щипцов, и это и была главная причина, по
# которой пилот выходил «Вайтраном другого цвета».
#
# УЛИЦА СТАЛА УЖЕ, ХОТЯ НИ ОДНО ЧИСЛО ПОСАДКИ НЕ ТРОНУТО. Между красными
# линиями по-прежнему 17 м, но фасады выросли с 5.4 (city-shop) до 10.7-13.9,
# а дом с нависающими этажами съедает ещё 1.4 м вверху — отношение высоты к
# ширине улицы 0.8, то самое, ради которого §4.2 заказан.
WFACE, EFACE = 247.5, 264.5
n_row = 0
# УШИРЕНИЯ РЯДА ПОД КОЛОДЦЫ. У улицы обочины нет, а тело колодца 2.80x2.74,
# поставленное в полосу, — это стоп ходока (урок Вайтрана, две правки чертежа
# подряд). Поэтому ряд разорван дважды: z=214..240 на западной стороне и
# z=342..368 на восточной; в разрывах стоят колодцы северного и южного торга.
n_row += row(WFACE, 180.0, 214.0, "E", ["jetty", "house", "manor"])
n_row += row(EFACE, 180.0, 232.0, "W", ["house", "jetty", "manor", "jetty"])
# КРЫТЫЙ РЯД ДВУМЯ ДЛИННЫМИ СТОРОНАМИ ТОРГА (§4.6). Аркада — единственный
# рецепт школы, стоящий ДЛИННОЙ стороной к улице, и ставится он не поштучно, а
# ПРОГОНОМ: галерея на кирпичных столбах читается только как непрерывная
# колоннада. Шесть тел даёт 73 м крытого прохода на северном торге и столько
# же на южном — обе длинные стороны каждого гнезда.
n_row += row(WFACE, 240.0, 282.0, "E", ["arcade"])
n_row += row(EFACE, 236.0, 274.0, "W", ["arcade"])
n_row += row(WFACE, 318.0, 358.0, "E", ["arcade"])
n_row += row(EFACE, 322.0, 342.0, "W", ["arcade"])
# ИЗНОС В САМОМ РЯДУ, А НЕ ТОЛЬКО У СТЕНЫ (§3: «этажность 3-4 у рынка, 1-2 у
# стены» — но достаток внутри одного ряда тоже не ровный). Ветхий близнец
# большого дома стоит на ЮЖНОМ плече, дальнем от гражданского центра: там
# улица уже уходит к скотным воротам, и обветшавший купеческий дом на ней
# объясним. На северном плече, у ярмарочных ворот, его нет — туда съезжаются.
n_row += row(WFACE, 360.0, 406.0, "E", ["jetty", "house", "manor-old", "house"])
n_row += row(EFACE, 368.0, 406.0, "W", ["arcade", "jetty", "manor-old"])

# --- ГРАЖДАНСКИЙ ЦЕНТР НА ПЛОЩАДИ-УПОРЕ --------------------------------------
# ПЛОЩАДЬ — ЭТО ЗАМОЩЁННЫЙ ПЕРЕКРЁСТОК (PAVE_BLOCKS паспорта), а не разрыв
# ряда: у Корнхолла главная форма — улица, и площадь у него получается там, где
# улица пересекает поперечную. Гражданские тела стоят по её четырём углам.
#
# РАТУША ФАСАДОМ НА РЫНОЧНУЮ УЛИЦУ, БАШНЕЙ НА ПЛОЩАДЬ. Башня сидит в ЗАПАДНОМ
# торце рецепта (локальные x 0..4.6), а при двери на запад локальный +X ложится
# на юг — то есть башня приходится на СЕВЕРНЫЙ конец зала, ровно в угол
# площади. Это проверено осью, а не глазом: yaw_for_door("+Z", 1, 0) = 90 гр.,
# и при нём +X_лок = (cos90, -sin90) = (0, +1).
# ЛИЦО НА ЛИНИИ, А НЕ ДВЕРЬ (FACE_ON_LINE паспорта): у ратуши face_off 3.17 —
# дверь сидит в задней стене ЛОДЖИИ, на 3.17 м вглубь от кромки её столбов.
# Поставь сюда дверную точку — и три кирпичных столба лоджии встали бы в
# проезжую часть.
put("hall", EFACE, 310.0, 0.0, "W", "РАТУША", walk=(EFACE - 4.0, 310.0))
# ХРАМ ОТСТУПАЕТ ОТ КРАСНОЙ ЛИНИИ НА 11.5 м, И ЭТО ЕДИНСТВЕННОЕ МЕСТО, ГДЕ
# РЯД РАЗОРВАН РАДИ ЗДАНИЯ. Двор-партер (механизм forecourt) строится ОТ
# ДВЕРИ НАРУЖУ на 3.6..13.6 м, а у храма, стоящего лицом на улицу, эти метры
# лежат на проезжей части: первый прогон дал «двор храма: 0 предметов» —
# ставить было негде, а не нечем. Отступив, храм получает паперть между собой
# и улицей, а улица — единственный разрыв фасадного ряда на все 240 м.
put("temple", 236.0, 306.0, 0.0, "E", "ХРАМ", walk=(240.0, 306.0))
# ВЕСОВАЯ ПАЛАТА — НА ТОРЦЕ ПЛОЩАДИ (§4.7), НАПРОТИВ РАТУШИ ЧЕРЕЗ ПЕРЕКРЁСТОК.
# У неё дверь в ЗАПАДНОЙ стене (door_axis -X манифеста), значит «лицом на
# улицу» для неё и значит door="W"; приёмное крыльцо с двумя арками, куда
# заводят телегу под весы, смотрит на юг, во двор.
put("weigh", EFACE, 283.0, 0.0, "W", "весовая палата",
    walk=(EFACE - 4.0, 283.0))
# КАЛАНЧА ПРИ РАТУШЕ (§4.17) — В ЕЁ ДВОРЕ, А НЕ НА КРАСНОЙ ЛИНИИ. Депо с
# воротами обслуживает задворочный переулок; на площади ей делать нечего, а
# смотровой обход на 15.6 м всё равно виден с неё поверх конька ратуши.
put("firetower", 280.0, 306.0, 0.0, "W", "каланча")

# --- ПОПЕРЕЧНАЯ УЛИЦА: КОРОТКИЕ РЯДЫ ----------------------------------------
NFACE, SFACE = 286.0, 298.0
row(NFACE, 150.0, 200.0, "S", ["house", "thatch", "house"], gap=0.6, axis="x")
row(SFACE, 320.0, 372.0, "N", ["house", "thatch", "house"], gap=0.6, axis="x")
# ПИВОВАРНЯ (§4.10) — У ПОЯСА, А НЕ У ВОДЫ, И ЭТО НАЗВАНО. Паспорт §4 просит
# «квартал у воды»; воды у Корнхолла нет ни капли (техблок: «Воды нет —
# пожарные водоёмы плитами»), поэтому квартал взят второй по смыслу — восточный
# пояс у пивоварских ворот, куда возят солод и откуда вывозят бочки. Сушильная
# труба на 13.6 м держит этот квартал и НЕ спорит с башней ратуши (22.6).
put("brewery", 330.0, 286.0, 0.0, "S", "ПИВОВАРНЯ", walk=(330.0, 290.0))

# --- FRINGE BELT: РЕДКИЙ РЯД У САМОЙ СТЕНЫ -----------------------------------
# ЗДЕСЬ СОЛОМА, И ТЕПЕРЬ ОНА НАСТОЯЩАЯ. До этапа 2 «солома у стены» держалась
# тем, что city-house-s-old печётся соломенным; теперь у стены стоит
# cornhall-house-s-thatch — то же тело школы, что и на рынке, но под соломой и
# с более длинным свесом щипца. Градиент §3 («черепица в центре, солома у
# стены») читается ОДНОЙ формой в двух кровлях, а не двумя разными домами.
# ШАГ РЯДА ЗДЕСЬ ОБЩЕГОРОДСКОЙ, А РАЗРЕЖЁННОСТЬ ДЕРЖИТ ПРАВИЛО (GAP_ZONES).
row(190.0, 214.0, 250.0, "E", ["thatch", "thatch"])
row(322.0, 214.0, 250.0, "W", ["thatch", "thatch"])
row(190.0, 334.0, 370.0, "E", ["thatch", "thatch"])
row(322.0, 334.0, 370.0, "W", ["thatch", "thatch"])
put("kuznya", 306.0, 180.0, -3.0, "W", "кузница", walk=(302.0, 180.0))

# --- ПОСТОЯЛЫЕ ДВОРЫ У ВОРОТ, ТРИ ШТУКИ (§4.12) ------------------------------
# ПРОЕЗДНАЯ АРКА РАБОТАЕТ ТОЛЬКО У ВОРОТ, и потому дворы стоят там, а не на
# рынке: гружёная телега въезжает с тракта, не разгружаясь на улице. Тело
# 13.1 x 21.5 — самое глубокое в городе, на рыночной улице оно не помещается
# в бургажную полосу (12-20 м) вовсе, и это ещё один довод за ворота.
put("inn", 246.0, 170.0, 0.0, "E", "постоялый двор ярмарочных ворот",
    walk=(250.0, 170.0))
put("inn", 380.0, 282.0, 0.0, "S", "постоялый двор пивоварских ворот",
    walk=(380.0, 286.0))
put("inn", EFACE, 416.0, 0.0, "W", "постоялый двор скотных ворот",
    walk=(EFACE - 4.0, 416.0))

# --- ГОЛУБЯТНИ ВО ДВОРАХ УСАДЕБ, ЧЕТЫРЕ ШТУКИ (§4.15) ------------------------
# ГОЛУБЯТНЯ СТОИТ В БУРГАЖНОЙ ПОЛОСЕ, а не на улице: она ЧУЖОЙ двор украшает.
# Полоса здесь 12-13 м между задней стеной фасадного ряда и задворочным
# переулком, тело голубятни 3.7 x 3.7 — она встаёт посередине двора и ни с чем
# не спорит, а её 11.2 м торчат над коньками рядового дома (10.7).
put("dovecote", 229.0, 196.0, 0.0, "W", "голубятня")
put("dovecote", 229.0, 264.0, 0.0, "W", "голубятня")
put("dovecote", 283.0, 200.0, 0.0, "E", "голубятня")
put("dovecote", 283.0, 390.0, 0.0, "E", "голубятня")

# --- АМБАРЫ НА ГРИБАХ: ЗАДВОРКИ И ПРЕДМЕСТЬЯ (§4.5) --------------------------
put("granary", 234.0, 350.0, 0.0, "E", "амбар на грибах")
put("granary", 40.0, 258.0, 0.0, "E", "амбар на грибах")
put("granary", 470.0, 340.0, 0.0, "W", "амбар на грибах")
put("granary", 240.0, 490.0, 0.0, "E", "амбар на грибах")

# --- ТРИ ПРЕДМЕСТЬЯ ЛЕНТАМИ ВДОЛЬ ТРАКТОВ (снаружи стены) -------------------
# ДАЛЬНИЙ ХУТОР БЕДНЕЕ СОЛОМЕННОГО ГОРОДСКОГО ДОМА, и потому здесь остаётся
# старый city-house-s-old: соломенный cornhall стоит на кирпичном цоколе с
# фахверковым вторым ярусом — для избы у тракта он слишком богат. Это НЕ
# «недоделанная замена», а второй шаг того же градиента §3.
row(52.0, 264.0, 282.0, "S", ["thatch", "hut"], gap=8.0, axis="z")
row(52.0, 302.0, 326.0, "N", ["hut", "barn-old"], gap=8.0, axis="z")
row(460.0, 264.0, 282.0, "S", ["hut", "thatch"], gap=8.0, axis="z")
row(460.0, 302.0, 326.0, "N", ["barn-old", "hut"], gap=8.0, axis="z")
row(244.0, 456.0, 478.0, "E", ["thatch", "hut"], gap=7.0)
row(268.0, 456.0, 478.0, "W", ["hut", "thatch"], gap=7.0)
put("barn", 220.0, 470.0, 0.0, "E", "скотный баз")

# --- ТРИ ВЕТРЯКА-СТОЛБОВКИ НА ВАЛУ ЗА СТЕНОЙ (§4.9) --------------------------
# ОБРАЗ ГОРОДА С ТРАКТА ЗА КИЛОМЕТР (§8, точка притяжения №3), и потому они
# СНАРУЖИ кольца: внутри их крылья упёрлись бы в кладку, а с тракта их бы
# закрыла та же кладка. Точки взяты по овалу с коэффициентом 1.14 — это 12-20 м
# от кривой стены при её толщине 2.25, то есть настоящий вал, а не приставленная
# к кладке мельница. Западная дуга выбрана не на глаз: у западных ворот имя
# «мельничные», и оно с этого дня перестало быть только словом.
put("windmill", 119.0, 171.1, 0.0, "N", "ВЕТРЯК")
put("windmill", 73.9, 233.5, 0.0, "N", "ВЕТРЯК")
put("windmill", 78.0, 352.0, 0.0, "N", "ВЕТРЯК")

# --- ГОНЧАРНЫЕ ГОРНЫ У ВОРОТ, СНАРУЖИ (§4.18) --------------------------------
# ОГНЕННОЕ РЕМЕСЛО ЗА СТЕНУ ВЫНОСИЛ ЛЮБОЙ ПОЖАРНЫЙ РЕГЛАМЕНТ, и Корнхолл им же
# объясняет свою черепицу. Тело горна стоит ЗА своей огневой точкой (FIRE_SPOTS
# паспорта, ref fires.kiln_*), а не на ней: топочная арка смотрит на юг, огонь
# горит в 2 м перед ней. Проверено правилом, а не глазом — spot_free отвергает
# точку внутри тела, и горн, поставленный НА свою точку, вытолкнул бы
# собственный огонь на 3.6-7.0 м вбок.
put("kiln", 238.0, 130.0, 0.0, "S", "гончарный горн")
put("kiln", 442.0, 270.0, 0.0, "S", "гончарный горн")

# ПРЕДМЕСТЬЕ У ТРАКТА НЕ ДОЛЖНО ЗАХОДИТЬ НА ПОЛОСУ: дорожку от двери сюда не
# рисуем (лента предместья и есть дорога), кроме мельницы и постоялых дворов.
for h in HOUSES:
    if h["walk"] == [0, 0]:
        h["walk"] = None

# =============================================================================
# ТОЧКИ ПРИТЯЖЕНИЯ (их состав объявляет паспорт; здесь только координаты)
# =============================================================================
MARKET_N = {
    "rect": [246.0, 190.0, 20.0, 102.0],       # полотно северного торга
    # КОЛОДЕЦ СТОИТ В НИШЕ ФАСАДНОГО РЯДА, А НЕ НА ПРОЕЗЖЕЙ ЧАСТИ. Урок
    # Вайтрана записан двумя правками чертежа подряд: тело колодца 2.80x2.74,
    # попавшее в полосу главной улицы, — это стоп бота, а не украшение. У
    # улицы обочины нет по определению, поэтому ряд РАЗОРВАН на z=216..238 и
    # отодвинут на запад: колодец встаёт в уширение, в 1.6 м от кромки полосы.
    "well": [245.6, 226.0],
    # ЛОТКИ ОДНОЙ ЛИНИЕЙ ВДОЛЬ ЗАПАДНОГО РЯДА, двумя гнёздами (север и юг):
    # проезд по середине улицы остаётся свободным на 6 м — это торговая
    # УЛИЦА, по ней ездят, а не площадь, где стоят.
    "stalls": [[251.0, 240.0], [251.0, 246.0], [251.0, 252.0],
               [251.0, 258.0], [251.0, 264.0]],
}
MARKET_S = {
    "rect": [246.0, 300.0, 20.0, 98.0],        # полотно южного торга
    "well": [266.4, 354.0],                    # в уширении восточного ряда
    "stalls": [[261.0, 322.0], [261.0, 328.0], [261.0, 334.0],
               [261.0, 376.0], [261.0, 382.0], [261.0, 388.0]],
}
# ГНЁЗДА ОГНЯ, НАЗВАННЫЕ ПОИМЁННО (правило О1: гнездо не ближе 60 м к соседу).
# Гончарные горны стоят СНАРУЖИ у ворот — огненное ремесло за стену выносил
# любой пожарный регламент, и Корнхолл им же объясняет свою черепицу.
FIRES = {"kiln_n": [238.0, 132.0], "kiln_e": [442.0, 276.0],
         "fairyard": [244.0, 124.0]}
FAIR = {
    # ПОЛЕ ВЕРНУЛОСЬ К СВОЕМУ РАЗМЕРУ. Пилот сжал его до 84x78, потому что
    # пол места умел только залить прямоугольник целиком, и сотня метров
    # сплошного камня читалась с земли пустырём; сжатый контур был обходом —
    # место подгоняли под то, что умеет его пол. Волна Г мостит РЯДАМИ
    # (floor.mode = rows в паспорте), и поле снова 120x90 — трава с
    # каменными полосами под постоянными рядами тумб.
    "rect": [136.0, 26.0, 120.0, 90.0],
    "well": [232.0, 70.0],
    # ЯРМАРОЧНЫЕ ТУМБЫ: постоянные каменные основания под сезонные навесы,
    # вне сезона стоят голыми. Рецепт — city-kerb2 (бордюрный камень 2 м);
    # ставит их механизм торгового ряда, у каждой тумбы своя пара сзади.
    "tumbs": [[x, z] for z in (40.0, 58.0, 76.0, 94.0)
              for x in (150.0, 170.0, 190.0, 210.0)],
}

# =============================================================================
# ЗЕМЛЯ: РАВНИНА С МЯГКИМ МИКРОРЕЛЬЕФОМ
# =============================================================================
# Ни горы, ни реки: перепад по всей карте — три метра, и он нужен только
# затем, чтобы плоскость не читалась столом.
# ОТМЕТКА 24 ВЗЯТА ПО ЗАМЕРУ, А НЕ НАЗНАЧЕНА: прибор probe_ground по
# натуральной земле стенда на этом квадрате даёт 20.37..27.56, среднее 24.19,
# в пятне города 24.24. Дельты relief считаются ОТ натуральной земли, и
# отметка, взятая с потолка (первый заход: 30), подняла бы всю карту на шесть
# метров — то есть обрубила бы её по краю шестиметровым уступом в мир.
ZONES = [{"h": 24, "pts": [(0, 0), (512, 0), (512, 512), (0, 512)]},
         {"h": 25, "pts": [(300, 40), (460, 20), (500, 140), (420, 200),
                           (330, 150)]},
         {"h": 23, "pts": [(20, 340), (140, 320), (200, 420), (120, 500),
                           (30, 460)]},
         {"h": 25, "pts": [(60, 40), (200, 30), (230, 120), (120, 160),
                           (40, 120)]},
         {"h": 23, "pts": [(330, 400), (470, 380), (500, 470), (390, 500)]}]
MICRO = [{"dh": 1, "pts": [(150, 200), (190, 190), (200, 230), (160, 240)]},
         {"dh": -1, "pts": [(320, 210), (370, 200), (380, 250), (330, 255)]},
         {"dh": 1, "pts": [(120, 260), (160, 255), (165, 300), (125, 305)]},
         {"dh": -1, "pts": [(350, 320), (400, 310), (410, 360), (360, 365)]},
         {"dh": 1, "pts": [(200, 440), (250, 430), (255, 470), (205, 475)]},
         {"dh": -1, "pts": [(280, 120), (330, 110), (340, 150), (290, 155)]}]

# ЗЕЛЕНЬ: НА ХЛЕБНОЙ РАВНИНЕ ДЕРЕВЬЕВ МАЛО, А В ГОРОДЕ ИХ НЕТ ВОВСЕ.
# Это тоже посадка, а не флора: у Вайтрана внутри стен стоит Гилдергрин и
# кусты грядок, здесь внутри кольца НЕ РАСТЁТ НИЧЕГО — сплошная застройка и
# камень. Роща одна, у западного тракта, и она держит горизонт.
OAK_GLADE = {"oak": [44, 372], "trees": [[32, 360], [56, 358], [62, 380],
                                         [50, 392], [30, 386], [66, 366]]}
GARDENS = [[268, 34], [268, 56], [40, 268], [40, 316], [470, 270], [470, 314]]
TREES = ([{"kind": "birch", "x": x, "z": z} for x, z in
          [(96, 214), (104, 226), (88, 236), (438, 206), (446, 218),
           (432, 232), (150, 462), (166, 474), (356, 466)]]
         + [{"kind": "spruce", "x": x, "z": z} for x, z in
            [(24, 120), (36, 136), (18, 150), (492, 130), (478, 146),
             (500, 160), (30, 500), (48, 492)]]
         + [{"kind": "oak", "x": x, "z": z} for x, z in
            [(120, 96), (352, 130), (472, 420), (96, 440)]]
         + [{"kind": "bush", "x": x, "z": z} for x, z in
            [(210, 470), (300, 468), (150, 150), (370, 160), (60, 220),
             (450, 230), (200, 500), (320, 502)]])

# =============================================================================
# АВТОПРОВЕРКИ ЧЕРТЕЖА
# =============================================================================
problems = []


def in_walls(x, z):
    inside = False
    n = len(WALL)
    j = n - 1
    for i in range(n):
        xi, zi = WALL[i]
        xj, zj = WALL[j]
        if (zi > z) != (zj > z) and x < (xj - xi) * (z - zi) / (zj - zi) + xi:
            inside = not inside
        j = i
    return inside


def body_rect(h):
    """Пятно тела: лицо на красной линии, тело растёт назад во двор."""
    fw, fd = FRONT[h["kind"]]
    r = math.radians(h["deg"])
    base = {"S": (0, 1), "N": (0, -1), "E": (1, 0), "W": (-1, 0)}[h["door"]]
    dx = base[0] * math.cos(r) - base[1] * math.sin(r)
    dz = base[0] * math.sin(r) + base[1] * math.cos(r)
    cx, cz = h["x"] - dx * fd / 2.0, h["z"] - dz * fd / 2.0
    return cx, cz, fw, fd


for i in range(len(HOUSES)):
    ax, az, aw, ad = body_rect(HOUSES[i])
    for j in range(i + 1, len(HOUSES)):
        bx, bz, bw, bd = body_rect(HOUSES[j])
        if math.hypot(ax - bx, az - bz) < (min(aw, ad) + min(bw, bd)) / 2 * 0.92:
            problems.append(
                f"ПЕРЕСЕЧЕНИЕ: {HOUSES[i]['kind']} ({ax:.0f},{az:.0f}) и "
                f"{HOUSES[j]['kind']} ({bx:.0f},{bz:.0f})")
for h in HOUSES:
    cx, cz, w, d = body_rect(h)
    for rd in ROADS:
        for (x0, z0), (x1, z1) in zip(rd["pts"], rd["pts"][1:]):
            vx, vz = x1 - x0, z1 - z0
            L2 = vx * vx + vz * vz
            t = max(0.0, min(1.0, ((cx - x0) * vx + (cz - z0) * vz) / L2))
            dd = math.hypot(cx - (x0 + vx * t), cz - (z0 + vz * t))
            if dd < rd["w"] / 2 + min(w, d) / 2 * 0.8:
                problems.append(f"НА ДОРОГЕ: {h['kind'] or 'дом'} "
                                f"({cx:.0f},{cz:.0f}) до оси {dd:.1f} м")
                break
inside = sum(1 for h in HOUSES if in_walls(h["x"], h["z"]))
green_in = [t for t in TREES if in_walls(t["x"], t["z"])]
if green_in:
    problems.append(f"ЗЕЛЕНЬ ВНУТРИ КОЛЬЦА: {len(green_in)} — у Корнхолла "
                    f"внутри стен не растёт ничего")
if problems:
    print("ПРОБЛЕМЫ ЧЕРТЕЖА:")
    for p in sorted(set(problems)):
        print(" -", p)
else:
    print("проверки чисты: пересечений нет, тел на полосах нет, зелени "
          "внутри кольца нет")
print(f"построек {len(HOUSES)} (внутри кольца {inside}, предместья "
      f"{len(HOUSES) - inside}); фасадный ряд рыночной улицы: {n_row}")

# =============================================================================
# КАРТИНКА
# =============================================================================
svg = ['<svg viewBox="0 0 768 768" xmlns="http://www.w3.org/2000/svg" '
       'style="width:100%;max-width:900px;background:#e6e3cf;'
       'border:1px solid #999">']
svg.append('<rect width="768" height="768" fill="#d9d8b4"/>')
for zn in ZONES[1:]:
    p = " ".join(f"{x*SC:.0f},{z*SC:.0f}" for x, z in zn["pts"])
    svg.append(f'<polygon points="{p}" fill="#00000010"/>')
fx, fz, fw, fd = FAIR["rect"]
svg.append(f'<rect x="{fx*SC}" y="{fz*SC}" width="{fw*SC}" height="{fd*SC}" '
           f'fill="#cfc7a2" stroke="#8f855f" stroke-dasharray="5,4"/>')
svg.append(f'<text x="{(fx+6)*SC}" y="{(fz+12)*SC}" font-size="11" '
           f'font-weight="bold" fill="#6d6142">ЯРМАРОЧНОЕ ПОЛЕ</text>')
for x, z in FAIR["tumbs"]:
    svg.append(f'<rect x="{x*SC-3}" y="{z*SC-1.5}" width="6" height="3" '
               f'fill="#8d8880"/>')
for _m in (MARKET_N, MARKET_S):
    mx, mz, mw, md = _m["rect"]
    svg.append(f'<rect x="{mx*SC}" y="{mz*SC}" width="{mw*SC}" '
               f'height="{md*SC}" fill="#c9c4ae"/>')
for rd in ROADS:
    p = " ".join(f"{x*SC:.1f},{z*SC:.1f}" for x, z in rd["pts"])
    col = "#b5b1a8" if rd["mat"] == "stone" else "#b79a6a"
    svg.append(f'<polyline points="{p}" fill="none" stroke="{col}" '
               f'stroke-width="{rd["w"]*SC:.1f}" stroke-linecap="round"/>')
wp = " ".join(f"{x*SC:.1f},{z*SC:.1f}" for x, z in WALL)
svg.append(f'<polygon points="{wp}" fill="none" stroke="#6d6a63" '
           f'stroke-width="5" stroke-linejoin="round"/>')
for x, z in TOWERS:
    svg.append(f'<circle cx="{x*SC:.0f}" cy="{z*SC:.0f}" r="5" fill="#57544e"/>')
for g in GATES:
    x, z = g["pos"]
    svg.append(f'<circle cx="{x*SC:.0f}" cy="{z*SC:.0f}" r="7" '
               f'fill="#f3e8c8" stroke="#8a6b2f" stroke-width="2"/>')
    svg.append(f'<text x="{x*SC+9:.0f}" y="{z*SC+4:.0f}" font-size="10" '
               f'fill="#6b5320">{g["name"]}</text>')
COL = {"temple": "#e8dcc0", "hall": "#8c5a44", "brewery": "#a2624a",
       "weigh": "#b06b4e", "firetower": "#7a7065", "inn": "#9c6247",
       "arcade": "#c07a52", "jetty": "#c8895c", "manor": "#c8a06a",
       "manor-old": "#b0885e", "house": "#d3c4a2", "old": "#b09a70",
       "thatch": "#d8c88e", "hut": "#b09a70",
       "granary": "#a98f60", "dovecote": "#8f8375", "windmill": "#8a6a40",
       "kiln": "#7d5b48", "barn": "#a98f60", "barn-old": "#9c8455",
       "kuznya": "#8f8375"}
for h in HOUSES:
    cx, cz, w, d = body_rect(h)
    svg.append(f'<g transform="translate({cx*SC:.1f},{cz*SC:.1f}) '
               f'rotate({h["deg"]:.0f})">'
               f'<rect x="{-w*SC/2:.1f}" y="{-d*SC/2:.1f}" '
               f'width="{w*SC:.1f}" height="{d*SC:.1f}" '
               f'fill="{COL.get(h["kind"], "#c8a06a")}" stroke="#4b3a26" '
               f'stroke-width="0.8"/></g>')
    if h["name"]:
        svg.append(f'<text x="{cx*SC+5:.0f}" y="{cz*SC-4:.0f}" font-size="9" '
                   f'fill="#3d2f1c">{h["name"]}</text>')
for x, z in OAK_GLADE["trees"] + [OAK_GLADE["oak"]]:
    svg.append(f'<circle cx="{x*SC:.0f}" cy="{z*SC:.0f}" r="5" fill="#6f8f3f"/>')
for t in TREES:
    svg.append(f'<circle cx="{t["x"]*SC:.0f}" cy="{t["z"]*SC:.0f}" r="3" '
               f'fill="#7da052"/>')
svg.append('<g stroke="#00000018" stroke-width="1">')
for i in range(0, 513, 64):
    svg.append(f'<line x1="{i*SC}" y1="0" x2="{i*SC}" y2="768"/>')
    svg.append(f'<line x1="0" y1="{i*SC}" x2="768" y2="{i*SC}"/>')
svg.append('</g>')
for i in range(0, 513, 128):
    svg.append(f'<text x="{i*SC+3}" y="12" font-size="10" '
               f'fill="#00000055">{i}</text>')
    svg.append(f'<text x="3" y="{i*SC+12}" font-size="10" '
               f'fill="#00000055">{i}</text>')
svg.append('</svg>')

html = """<!doctype html>
<!--
Created: 24:08:2026 - 16:20:00
Last updated: 24:08:2026 - 03:10:00
Module: docs
File: docs/CORNHALL_PLAN.html
Responsibility: чертёж Корнхолла (генерируется tools/gen_cornhall_plan.py;
правки вносить в генератор чертежа, не сюда).
UPD:
- 24:08:2026 - 16:20:00: Создан (пилот эпохи «12 городов», этап 1).
- 24:08:2026 - 18:10:00: Волна Г: ручей и мост сняты (вода стала
  необязательной), кузница переименована в вид «kuznya».
- 24:08:2026 - 19:40:00: Волна Г: габарит и вынос лица берутся из манифеста
  полки; шаг пояса у стены отдан правилу щели.
- 24:08:2026 - 22:00:00: Волна Г: ярмарочное поле вернулось к 120x90, съезд
  доведён до поля (мощение рядами).
- 24:08:2026 - 03:10:00: Этап 2: застройка переехала на пакет cornhall-*
  (щипец на улицу, два прогона аркады, ратуша с башней на площади-упоре,
  весовая, каланча, пивоварня, три постоялых двора у ворот, четыре
  голубятни, четыре амбара на грибах, два горна снаружи, три ветряка).
-->
<meta charset="utf-8"><title>Схема Корнхолла (этап 2)</title>
<body style="max-width:960px;margin:1.5em auto;font:15px/1.55 -apple-system,
sans-serif;color:#222;background:#faf8f4;padding:0 1em">
<h1 style="margin:.2em 0">Корнхолл — чертёж (этап 2, своя школа)</h1>
<p>Ноль новых рецептов и ноль новых умений движка. Меняются три рычага:
<b>стиль</b> (какие из имеющихся рецептов куда ложатся), <b>посадка</b> (этот
чертёж) и <b>огонь</b> (паспорт tools/cities/cornhall.py).</p>
""" + "\n".join(svg) + """
<h2>Что здесь другое, чем в Вайтране</h2>
<ul>
<li><b>Кольцо правильное</b> — овал 340&times;300 с четырьмя воротами ровно на
две оси трактов; у Вайтрана стена идёт по кромке рельефа.</li>
<li><b>Главная форма — улица, а не площадь</b>: рыночная улица 240 м длиной,
17 м между фасадами, два сплошных фасадных ряда по красной линии.</li>
<li><b>Ряд сплошной</b>: щель фасадов 0.0&ndash;0.3 м (у Вайтрана 0.35&ndash;0.7),
поворот дома &plusmn;4&deg;, за фасадом бургажная полоса 12&ndash;20 м до
задворочного переулка.</li>
<li><b>Достаток разложен наоборот</b>: дранка и черепица в центре (лавки,
усадьбы, храм, ратуша), солома &mdash; у стены и в предместьях.</li>
<li><b>Внутри кольца не растёт ничего</b>; ярмарочное поле с каменными тумбами
вынесено за северные ворота.</li>
</ul>
</body>"""

open(os.path.join(ROOT, "docs/CORNHALL_PLAN.json"), "w",
     encoding="utf-8").write(json.dumps({
         "rivers": [],
         "wall": WALL, "towers": TOWERS, "gates": GATES,
         "roads": ROADS, "houses": HOUSES,
         "market_n": MARKET_N, "market_s": MARKET_S,
         "fair": FAIR, "fires": FIRES,
         "oak_glade": OAK_GLADE, "gardens": GARDENS, "trees": TREES,
         "zones": ZONES, "micro": MICRO,
     }, ensure_ascii=False, indent=1))
open(os.path.join(ROOT, "docs/CORNHALL_PLAN.html"), "w",
     encoding="utf-8").write(html)
print("чертёж записан: docs/CORNHALL_PLAN.json + .html")
