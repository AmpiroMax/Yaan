/*
Created: 20:08:2026 - 13:40:00
Last updated: 28:08:2026 - 19:55:00
Module: tools
File: tools/forge_houses.cpp

Responsibility:
- КУЗНИЦА ГОТОВЫХ ПОСТРОЕК (заказ 20.08: «повторить домики с демки новой
  механикой + Г-образный + П-образный, зарегистрировать как готовые
  постройки»). Пять домов собираются ЧЕРЕЗ HouseGraph API и пишутся
  каноническим write_house в assets/houses/*.dfh — тем же текстом, который
  читает редактор, загрузка карты и тест проходимости.

Usage:
    dfn_houses                    испечь ВСЮ полку и переписать манифест
    dfn_houses --only <маска>     испечь только совпавшие (* и ?), чужого не тронуть
    dfn_houses --list [--only м]  перечислить реестр и остановиться
    dfn_houses --check [<полка>]  судья связности по всей полке

Key items:
- Forge: рука над графом (вершины, цепочки, контуры, балки).
- log/frame/stone replica + l_house + u_house: рецепты домов.
- pack_demo / pack_city: СЕКЦИИ РЕЕСТРА по пакетам; городской пакет будущего
  города добавляет свою и зовёт её в house_recipes().

Dependencies:
- Uses: engine/world (HouseGraph, HouseFile, HouseMesh — константы двери),
  tools/recipe_book.h (реестр, ключи), tools/house_manifest.h (INDEX.txt).
- Used by: цель dfn_houses; артефакты читают сцена build.scene и тесты;
  манифест assets/houses/INDEX.txt читает генератор города.

Notes:
- ЛОКАЛЬНЫЕ КООРДИНАТЫ: начало — северо-западный угол, дверь смотрит на +Z
  (юг), как у домов из деталей (gen_house_demo.py). Вершины все Free с явной
  высотой: постройка ставится на ровную полку, и высота из рельефа сделала бы
  файл недетерминированным.
- Числа этажа/двери — из HOUSES.md через HouseStyle.h (STOREY у демки 3.25;
  здесь этаж 2.8 — кратен подъёму ступени 0.175 ровно в 16 ступеней).

AI Agents Notice (must follow):
- Follow docs/ARCHITECTURE.md strictly. Правки рецептов — только здесь,
  артефакты .dfh перегенерировать, не править руками.
*/
/*
UPD:
- 20:08:2026 - 13:40:00: Создана: три повтора демки, Г-образный, П-образный с двумя маршами.
- 20:08:2026 - 17:30:00: Конёк-бревно и стропила у двускатов; кровля рядами; детали и износ на всех пяти домах; лица стен наружу (крыльцо строилось в комнате).
- 20:08:2026 - 18:40:00: Сруб венцами; южные фасады с окнами по бокам двери; скаты Г-дома сели на стены + клинья-фронтоны; конёк и стропильные торцы П-дому; износ камня 0.7.
- 20:08:2026 - 19:05:00: Стропильные торцы — над карнизом с выпуском (не «верёвки» по фасаду); у П-дома — из-под южных свесов, а не в небо.
- 20:08:2026 - 20:30:00: Марши у-дома — доски с зазорами; потолок бара с балками; пол сруба ветхий.
- 20:08:2026 - 21:20:00: Стропила двускатов — ПОД настилом (лежали поверх гонта, приёмка №3).
- 20:08:2026 - 22:40:00: Одна вершина на точку (дедуп): угол тянет и стену, и столб.
- 21:08:2026 - 01:50:00: eaves_trim: двойной кант карниза (тёсовая доска + слега) вдоль нижних кромок скатов двускатов.
- 21:08:2026 - 00:45:00: Тон дранки среднего ряда (скат был чёрным сверху); сруб wear 0.55.
- 21:08:2026 - 01:10:00: ГОРОДСКОЙ НАБОР ВАЙТРАНА — 17 рецептов: сегмент стены
  с боевым ходом и зубцами (crenellated_run), башня и донжон с шатром из
  четырёх треугольников, ворота с щеками и перемычкой, лестницы улиц (3 м и
  один марш на 6 м подъёма, open=2 — стык двух маршей дважды ловил бота в
  щель), плазы 12/20, мост с опорами в русле и заездами-аппарелями по торцам
  (без пандуса ступень настила 0.85 м непроходима), кольцо-бортик дерева,
  лавка с верандой, стойло, колодец, лонгхолл в венцах, храм с портиком и
  замок-керн (низ камень, верх фахверк, подиум с двумя маршами, крылья).
  Числа — docs/WHITERUN_RESEARCH.md; артефакты assets/houses/city-*.dfh.
- 21:08:2026 - 13:05:00: Приёмка глазами (кадры города, 21.08): прилавок —
  столбы 0.07 -> 0.10, тент 0.06 -> 0.10, дощатая юбка под столешницей
  («плоские карточки без опор»); лавка — прогон по верху столбов веранды и
  навес заведён выше и глубже под свес («навес на одной тумбе», «щель между
  скатами»).
- 21:08:2026 - 15:20:00: ТРИ НОВЫХ ВИДА ДОМОВ (заказ 21.08): city-house-l —
  большой жилой 10x8 в два этажа (каменный цоколь 0.525 = три ступени, низ
  кладка, верх фахверк, окна на обоих этажах, крыльцо-площадка с открытым
  маршем open=1 и навесом на столбах); city-manor — Г-образная усадьба
  14x7 + 7x8 с двориком в углу, входом В УГЛУ под навесом и оградой дворика
  с проездом; city-barn — амбар-хлев 12x9 с воротами 3.2 м БЕЗ створки
  (проём набран стенами и перемычкой: раскладка знает только створку 1.0 м),
  сеновалом на 2.6 с маршем и асимметричной кровлей — конёк сдвинут к югу,
  северный скат уходит до 0.475 м над землёй и накрывает стойла.
  Рука gable_roof_z: двускат с коньком ВДОЛЬ Z — без перпендикулярного
  конька две кровли усадьбы не сходятся ендовой. Отдельный близнец, а не
  флаг у gable_roof: кузница переписывает все .dfh каждым прогоном.
- 21:08:2026 - 16:40:00: РАЗНЫЙ ИЗНОС У СОСЕДЕЙ (заказ 21.08: «у зданий
  города должны быть РАЗЛИЧНЫЕ эффекты износа»). Aging — параметр рецепта, а
  НЕ второй рецепт: пять жилых тел (малый дом, лавка, большой дом, усадьба,
  амбар) печатаются дважды одним кодом, ухоженным и запущенным. Прибавка
  +0.28 к СОБСТВЕННОМУ износу детали, зажатая в полосу 0.65..0.8 (у амбара
  0.72..0.8) — рельеф рецепта (цоколь трёт сильнее карниза) переживает
  старение, иначе ветхий дом стал бы равномерно серым, а равномерность
  читается плохой текстурой, не возрастом. У -old сняты ставни и не
  выложена завалинка-цоколь: это ЕДИНСТВЕННЫЕ признаки ухода, которые
  ElementParams умеет выключить — шаг обшивки живёт в WallStyle по имени
  стиля и из .dfh недостижим. Полоса выбрана выше порога 0.7, на котором
  отрисовка уводит деталь в ВЫВЕТРЕННЫЙ ряд атласа. Все 31 прежний .dfh
  после правки байт-в-байт: свежий вызов обязан дать ту же строку, что
  стояла литералом.
- 21:08:2026 - 20:55:00: СОЛИДНЫЙ ЗАМОК (заказ 21.08: «замок сделать более
  солидным»). Прежний city-keep — зал 20x12 с коньком 11.6 — рядом с
  донжоном 14 читался сараем, а §8 гайда просит доминанту в 2-3 раза выше
  второго объекта. Переписан forge_keep: зал 26x14, ЯРУСЫ по ставкирке —
  каменный стилобат 0..2.9 (толщина 0.9 против прежних 0.5), два фахверковых
  яруса до нижнего карниза 9.8, широкие нижние полы 9.8 -> 12.4 и ФОНАРЬ
  глубиной 7 (вместо 14) со своим двускатом, конёк 18.8. Один скат от 9.8 до
  18.8 дал бы полсилуэта гонта — доминанта читается ступенями, каждая уже
  нижней. Четыре угловые башенки-контрфорса 2.5x2.5 до нижнего карниза с
  шатриками, заведённые НА угол на 0.2 (тангенциальная отрывается щелью).
  Портал: терраса с бортиками, ОДИН широкий марш 12 м (open=2; стык двух
  маршей дважды ловил бота), две башенки-фланкера 6.6 и две колонны под
  навесом. Резной конёк: охлупень с выпуском 1.1 и подвесами на торцах,
  перекрещённые доски на щипцах. Верхняя кромка нижних пол держится
  торцовыми треугольниками в плоскостях x=0 и x=W — check_roof_support видит
  опору, находок 0. Пол верхнего зала — ДВА прямоугольника вокруг
  лестничного колодца: невыпуклый контур пришлось бы триангулировать веером.
  Плиты двора остались на 0.02 при толщине 0.2 (контур растёт в обе стороны
  от своей высоты — 0.3 дало бы порог 0.15 на парадном дворе).
- 22:08:2026 - 14:30:00: ГОРОД ПО УТВЕРЖДЁННОЙ СХЕМЕ (docs/WHITERUN_PLAN.json)
  — ДВА НОВЫХ РЕЦЕПТА, прежние 40 .dfh байт-в-байт.
  city-mill — водяная мельница 7x7: глухой каменный подклет 1.2, фахверк до
  карниза 3.6, двускат-дранка (конёк 5.5), дверь на ЗАПАД к дороге с
  площадкой и маршем 1.2 (уклон 26.6 гр.). КОЛЕСО на восточной стене —
  восьмигранник рукой forge_well/tree_ring: круг из прямых брусьев, гнутого
  обода в графе нет. Два обода R 2.2 в плоскостях x = W+0.4 и W+1.2 (ширина
  0.8, выступ за стену ровно 1.2), спицы к ступице в плоскости каждого обода,
  восемь лопастей-досок 1.55..2.35 по радиусу, ось-балка от подклета наружу и
  два подкоса-подшипника от стены. Центр на 0.7 при R 2.2 — низ обода на -1.5,
  под срезом берега: колесо обязано доставать до воды.
  city-keep-s — компактный замок ПО СХЕМЕ: корпус 15x8, стилобат-камень 1.6,
  два фахверковых яруса до карниза 7.4, нижние полы к ФОНАРЮ глубиной 4 (из 8)
  и его двускат — конёк 13.8; два крыла 5x6 у торцов (габарит 25x8), карниз
  4.2, конёк 6.0; вход по центру юга на уровень стилобата, крыльцо-площадка и
  ОДИН марш open=2 на 1.6 (стык двух маршей дважды ловил бота). Прежний
  city-keep 26x14 не тронут — принятая доминанта живёт своим файлом, а ярусная
  лямбда tier() вынесена в общую руку keep_tier() (порядок стен север-восток-
  запад-юг — часть контракта: по нему нумеруются вершины, и перестановка
  перепекла бы принятый .dfh). Ей же добавлена сторона двери: мельница
  смотрит на запад, замок — на юг.
  ГЛУХОЙ НИЗ У ОБОИХ: build_house_mesh сказал «проём просит 2.10 м по высоте,
  стена 1.60» — окно 2.1 и дверь 2.05 в цоколь 1.2-1.6 не влезают. Проёмы
  ушли в ярус над цоколем, как у принятого city-house-l с его 0.525. У крыльев
  ради этого карниз поднят 3.4 -> 4.2, а подъём ската срезан 2.6 -> 1.8, чтобы
  конёк остался на 6.
- 22:08:2026 - 14:45:00: МОЩЕНИЕ УЛИЦ, семейство city-cobble (претензия тяжести 3:
  у города ноль метров уличного покрытия). Четыре участка одним рецептом
  forge_cobble: 24x6 главная (норматив ширины 6 м), 8x6 вставка на поворот,
  24x4 второстепенный проход, 16x16 рыночный квадрат. Прежние 42 .dfh
  байт-в-байт — рецепт новый, ни одна принятая постройка не тронута.
  ПОЧЕМУ НЕ ПЛАЗА. У forge_plaza бордюр — это СТЕНА 0.22 по периметру. Площадь
  обходят, улицу переходят ПОПЕРЁК в любой точке, и на такой борт капсула
  взбирается (бот уже застревал на кромке плиты). Здесь по периметру наклонный
  поясок 0.30 м: сверху вровень с мостовой, снаружи ровно на нуле земли, уклон
  15 гр. — на него входят, а не влезают, и ступени нет НИ С ОДНОЙ стороны.
  ФАСКА ВНУТРЬ, а не наружу: габарит ровно w x d, две плиты встык не наезжают
  поясками (z-fighting) и не вылезают за клетку раскладки.
  КАМЕНЬ ОТДЕЛЬНЫМИ ПЛИТАМИ поверх основания: ровный серый прямоугольник — это
  пол, а не мостовая. Шов 5 см (глубина 3 см: верх камня 0.08, верх основания
  0.05), разнобой высот ВОСЕМЬ МИЛЛИМЕТРОВ (больше — капсула спотыкается о
  собственную мостовую), поворот текстуры покамённо по хэшу клетки. Дрожь от
  хэша, не от rand(): артефакты лежат в git и обязаны перепекаться байт-в-байт.
  НОВЫХ КЛЕТОК АТЛАСА НЕТ: mat=3 (Stone) — та же, что носит плаза;
  PARTS_ATLAS_REVISION входит в ключ кэша, и заводить строку ради мостовой не
  моё решение. Второстепенный проход отличается ОДЕЖДОЙ, а не вторым рецептом:
  камень мельче (1.7 против 2.0), тон тёмный, износ 0.62 — выше порога 0.7 по
  сумме с покамённой прибавкой, где отрисовка уводит деталь в выветренный ряд.
- 22:08:2026 - 15:10:00: Мощение: ещё три участка по ЗАМЕРУ ТРАССЫ раскладчика, а не
  «на всякий случай». Главная улица города — ломаная 11 узлов, между изломами
  10-14 м, и плита 24 м на изломе даёт веер с клиньями травы между соседками:
  city-cobble12x6 ложится по прямым, city-cobble6x6 — на изломы и подход к
  воротам. city-cobble14x11 — ПО ФАКТИЧЕСКОМУ рынку чертежа: жёсткую плиту
  раскладкой не режут, лишние два на пять метров от 16x16 ушли бы под соседние
  постройки. Прежние 48 .dfh байт-в-байт.
- 22:08:2026 - 16:20:00: ТРУБЫ (претензия критика [13]: ноль труб на город; гайд §8 —
  вид держит гребёнка на линии крыш). Девять .dfh, кровли не тронуты: труба
  добавляется ПОВЕРХ, Line с квадратным профилем — у графа нет тела «труба», а
  каменный брус ею и является. Цена 24 треугольника на трубу.
  ТОЧКА ТРУБЫ — ВХОД ОТ РАССТАНОВКИ, А НЕ ВЫБОР КУЗНИЦЫ. Очаг ставит furnish;
  труба мимо очага — это дым из одного угла при огне в другом, и читается это
  багом, а не вольностью. Числа взяты у раскладчика: глухая стена z=0 у всех,
  кроме Йоррваскра. city-house-s (2.25, 1.05); city-shop (1.15, 1.10) — восток
  занят прилавком, а у ветхой это КУЗНИЦА, горн по середине стены (3.0, 1.10);
  city-house-l (5.0, 1.10); city-manor (11.0, 1.10) — восточная половина
  главного корпуса, запад занят крылом.
  АСИММЕТРИЯ ПО ДОСТАТКУ, А НЕ ПО ЭКОНОМИИ: труба — маркер достатка, поэтому у
  city-house-s каменный стояк с напуском только у УХОЖЕННОЙ версии, а у ветхой
  осталась низкая обкладка дымового отверстия по скату. Разнота силуэта даром,
  бьёт разом по [13] и по [14] («один силуэт на всю застройку»).
  У ЙОРРВАСКРА ДЫМНИК, А НЕ ТРУБА: очаг посреди зала (8.0, 4.0) стоит ровно под
  коньком, и каменный стояк пришлось бы вести сквозь конёк-бревно. Нордский
  медовый зал с очагом в полу выводит дым люком в коньке под деревянным
  колпаком — тёмный тёсаный брус с широким напуском (mat 0), не камень.
  АДДИТИВНОСТЬ ДОКАЗАНА, А НЕ ОБЕЩАНА: DFN_FORGE_NO_VENTS=1 печёт библиотеку без
  труб, и все 51 .dfh сходятся с догробным состоянием байт-в-байт. Свойство
  остаётся проверяемым навсегда, а не живёт один вечер в чужой голове.
  В БЭКЛОГ, отдельной строкой: у forge_plaza бордюр — СТЕНА 0.22 м по периметру,
  и профиль высоты поперёк края даёт 22 см ОТВЕСНО (у city-cobble на том же
  приборе 2.6-3.2 см). Это дефект принятых city-plaza12/20, а не фон: он задним
  числом объясняет класс застреваний бота на кромках. Чинить не сейчас.
- 22:08:2026 - 17:05:00: ОБКЛАДКА ВЕТХОГО ДОМИКА ТОНУЛА В КРОВЛЕ — приёмка архитектора
  «перекрытие обязано быть положительным» поймала настоящий дефект. Высота
  отсчитывалась от ОСИ обкладки, а скат под ней поднимается на её же ширину:
  на верхней по скату кромке оставалось 10 см (мерено: верх 3.72 против ската
  3.62 в 26 см выше). Отсчёт от cz + r — стало 3.84, выступ 0.22 сверху и 0.46
  снизу. Изменился ОДИН .dfh, аддитивность под DFN_FORGE_NO_VENTS цела.
  Запас 0.22, а не заказанные 0.35, потому что roof_y_at даёт СРЕДИННУЮ
  плоскость ската, а видимый верх выше неё на полтолщины настила плюс ряд
  дранки — мерено 0.13. Обкладке этого хватает: 0.35 читались бы обрубком
  трубы, а заказана обкладка отверстия.
  ТАБЛИЦА ПЕРЕКРЫТИЙ (конёк / верх / перекрытие, мерено build_house_mesh, за
  конёк взят верх ОХЛУПНЯ — он и есть линия силуэта, а не верх настила):
  house-s 4.46/5.03/+0.57; shop 4.76/5.38/+0.62 (обе версии); house-l
  9.19/9.90/+0.72 (обе); manor 8.66/9.43/+0.77 (обе); longhall 6.36/7.23/+0.87.
  У house-s-old +0.00 НАМЕРЕННО: обкладка (3.84) ниже конька, силуэт ветхого
  дома не ломается — в этом и есть асимметрия достатка.
  В БЭКЛОГ, отдельной строкой: претензия критика [13] звучала «ни печной трубы,
  НИ ДЫМА». Трубы закрыты, ДЫМ — НЕТ: это система частиц, которой в движке нет.
  [13] закрыта НАПОЛОВИНУ, и считать её закрытой целиком нельзя.
- 22:08:2026 - 18:10:00: НАКЛОННЫЕ КУСКИ МОСТОВОЙ city-cobble{6x6,8x6}-s{04,06,08,10}.
  Заказ раскладчика по замеру: подъём к замку идёт 8.3%, и приёмка «пройти ось,
  не сходя с мощения» на нём проваливалась (29.3% покрытия, разрыв 33 м). Причина
  не в раскладке, а в геометрии: плоская жёсткая плита 6 м на 8% разбегается с
  землёй на 0.48 м — либо висит краем, либо даёт соседке ступеньку в полметра.
  ПЛОСКИМИ КУСКАМИ НЕПРЕРЫВНО ЗАМОСТИТЬ СКЛОН НЕЛЬЗЯ.
  Параметр slope (подъём на метр вдоль локального +X) ведёт ВСЮ плиту: основание,
  камни и все четыре фаски считаются через Y(y,x) = y + slope*x. Все контуры
  остаются плоскими (у трапеции фаски плоскость y = slope*x + g(z)), поэтому
  находок построения 0 и круговой прогон сходится.
  СТЫК ДВУХ НАКЛОННЫХ СОВПАДАЕТ ПО ПОСТРОЕНИЮ: внешняя кромка куска на x=w
  поднята ровно на slope*w, то есть ровно туда, где раскладчик сажает следующий
  кусок по профилю. Замерено: 0.482 против 0.483 — расхождение 1 мм.
  ШАГ 2% ВЫБРАН ПО ОСТАТКУ, а не на глаз: раскладка берёт ближайший уклон, промах
  не больше 1%, на шестиметровом куске это 6 см — вчетверо ниже её же порога
  ступени 0.20 м; на восьмиметровом 8 см, всё ещё под порогом.
  slope = 0 даёт y + 0*x, то есть РОВНО y: прежние семь плит и все остальные 44
  постройки перепечены бит в бит (сверено хешами до и после).
- 22:08:2026 - 19:35:00: Наклонные до 20%: +6x6-s{12,14,16,18,20} и 8x6-s{12,14}.
  Потолок 10% ПРОБИТ ЗАМЕРОМ раскладчика, а не поднят на всякий случай: 8.3% —
  это СРЕДНЕЕ по подъёму, а по окну 6 м тридцать метров идут 11.5-12.6%, и у
  кромки рынка 21%. С прежним потолком промах на 12.6% давал 0.16 м, на 21% —
  0.66 м, то есть разрыв мощения ровно у выхода с рынка и провал связности.
  Восьмиметровые остановлены на 14%: на длинном куске круче — это подъём за
  метр, а такому месту место короткому. Прежние 59 .dfh бит в бит.
- 22:08:2026 - 23:20:00: МАРШ И ПОДПОРНАЯ КРОМКА под южную грань рынка (заказ
  раскладчика по замеру: клин 1.30 м на западном конце до нуля у x=122).
  city-steps6-h12/-h08 — ширина 6.0 в ширину мостовой (марш обязан читаться
  продолжением улицы, а не калиткой), подъём вдоль локального +X и origin у
  НИЖНЕЙ ступени — та же арифметика посадки, что у city-cobble-sNN, чтобы цепь
  отметок вдоль ломаной велась одним правилом. Пандус под ступенями НЕ кладу:
  fill=6 строит его движок (HouseStairs.cpp, ходьба по 26-градусному пандусу),
  а второй под первым дал бы спор коллайдеров.
  ЗАМЕРЕНО И СКАЗАНО ВСЛУХ: подъём ЯКОРЕЙ ровно паспортный (1.200/0.800), но
  ходибельная поверхность стоит на 0.176 выше origin у подошвы и на 0.151 выше
  верхнего якоря. Посадка цепью по паспорту оставляет на стыке 0.025 м — втрое
  под порогом 0.20, но это НЕ ноль, и раскладчик должен знать число, а не
  узнать его прибором.
  city-kerb2-h02..h12 — отрезок 2.0 x 0.35, origin в нижнем углу ЛИЦЕВОЙ грани
  (ось отодвинута на полтолщины: тело стены сидит симметрично вокруг оси).
  Верх копинга = h + 0.053. ШАГ ВЫСОТ 0.2, А НЕ 0.4: раскладчик берёт ближайшую,
  промах — половина шага, и при 0.4 это ровно 0.20 — ТОЧНО тот порог ступени, по
  которому он же и бракует. Запас обязан быть ПОД порогом, а не на нём.
  Прежние 66 .dfh бит в бит.
- 23:08:2026 - 03:30:00: city-steps6-h12r2 — ВЫЛЕТ РОВНО 2.000. Правило раскладчика
  по замеру: вылет марша обязан быть ЦЕЛЫМ ЧИСЛОМ МЕТРОВ. Ниша под маршем
  режется в поле высот, поле — сетка шагом 1 м, и кромка между строк непредставима:
  билинейная выборка тянет соседнюю строку, а на подошве 57% десятая доля строки
  стоит 5.7 см. У h12 (вылет 2.100) верхняя кромка садится на z=143.9 и даёт отход
  -0.051 при заданных -0.020; расчёт (доля*уклон*шаг = 0.9*0.571*1.0) сошёлся с
  замером до миллиметра. При 2.000 обе кромки на целых строках, квантование
  исчезает. h12 не тронут — он годен там, где кромки и так between-free.
  ПОДСТУПЕНКИ ЗАДАТЬ ИЗ РЕЦЕПТА НЕЛЬЗЯ, и я это раньше писал неверно: движок
  сам берёт steps = round(подъём / HOUSE_STAIR_RISE_M=0.175) (HouseStairs.cpp:85),
  отсюда у h12r2 семь подступенков по 0.1714 и проступь 0.2857, а не заказанные
  шесть по 0.200. 2R+T = 0.63 — в удобной полосе. Рецепт правит подъём и вылет,
  дробление — движок.
  Поправки поверхности замерены и совпали с h12: +0.176 у подошвы, +0.151 над
  верхним якорем.
- 23:08:2026 - 04:20:00: city-steps6-h14r2 — под ВЫРОСШИЙ перепад кромки террасы
  (1.23 -> 1.35: мостовую рынка посадили заподлицо, +0.147 -> +0.030, а улица
  осталась). Подъём 1.400 при вылете 2.000 садится в сетку лучше прочих: 1.400/0.175
  ровно 8, подступенок выходит канонический 0.175 без остатка, проступь 0.250,
  2R+T = 0.600.
  ПОПРАВКИ ПОВЕРХНОСТИ У КАЖДОГО МАРША СВОИ — замерил, а не перенёс. У h14r2
  +0.180 у подошвы и +0.155 над верхним якорем против +0.176/+0.151 у h12r2:
  разница 4 мм, потому что дробление другое (8 подступенков по 0.175 против семи
  по 0.1714), а с ним и рельеф ступеней. Ходибельный подъём 1.375.
  Раскладчик просил сказать прямо, если поправки те же. ОНИ НЕ ТЕ ЖЕ, и его
  правило «без замеренных поправок не сажаю» этим и оправдалось: перенос чисел
  с соседнего рецепта дал бы ошибку в 4 мм на ровном месте.
- 22:08:2026 - 20:25:00: ГНУТЫЕ ПРОГОНЫ СТЕНЫ, семейство city-wall-arc{8,12,16}-
  s{000,025,050,100,200} — 15 рецептов (замечание владельца №6: «стены
  расставлены криво, видны стыки, а должно гладко идти»). Прежние 76 .dfh
  бит-в-бит.
  ДИАГНОЗ ЗАМЕРЕН. Отрезок цепочки — самостоятельное тело (HouseWalls.cpp:
  «в углу стоит столб, стены упираются в его ось»), поэтому на изломе с
  поворотом θ по ЛИЦУ раскрывается клин ГЛУБИНОЙ В ПОЛТОЛЩИНЫ и шириной
  2*(th/2)*tan(θ/2). У кольца чертежа 14 изломов с поворотами 13.9..49.2 гр.,
  то есть четырнадцать вырезов 0.21..0.55 м шириной и 0.60 м глубиной.
  ЗАМЕР КЛИНА, А НЕ ДОВОД: дверь дозы DFN_FORGE_ARC_NO_LAP=1 печёт те же дуги
  без выпуска торцов, и 401 луч по радиусу в лицо arc12-s100 на высоте 3 м
  даёт медиану радиуса 19.144 в обеих руках, но min 19.100 с нахлёстом против
  18.521 без него: провал 0.045 м (это рельеф кладки) против 0.624 м, проб
  глубже 0.40 м — 0 против 5.
  ОСЬ — ОПИСАННАЯ ЛОМАНАЯ, А НЕ ВПИСАННАЯ. Звенья лежат на касательных, точки
  касания — концы поддуг, крайние звенья выходят из САМИХ концов дуги вдоль
  касательной. Поэтому торец прогона строго перпендикулярен касательной и два
  прогона с непрерывной касательной смыкаются без уступа; вписанная ломаная
  дала бы торец, повёрнутый на полуугол поддуги.
  ЗВЕНО 1.33..1.85 м — ВЫВЕДЕНО. Не длиннее 2.0 м по хорде и не дальше 0.05 м
  от дуги (описанная отходит на R*(sec δ − 1)); у самой крутой дуги семейства
  (хорда 8, стрела 2, R = 5.0) второе условие и режет звено до 1.333.
  СТРЕЛА 0.25 — ПО ЗАМЕРУ ЧЕРТЕЖА, а не для симметрии: кольцо поворачивает на
  360 гр. за 316.2 м, то есть 1.139 гр./м, и куску 12 м средней кривизны
  положено 13.7 гр. (стрела 0.36). На одних стрелах 0.5 кольцо замкнулось бы
  за 226 м вместо 316.
  ОДИН ИЗГИБ НА ВСЁ КОЛЬЦО: во всех 14 изломах чертежа направление
  поворачивает в сторону inw = cross(Y, d), знак один — зеркальное семейство
  кольцу не нужно. Стрела отводит середину НАРУЖУ (+Z): у выпуклого кольца
  наружная поверхность выпукла, центр кривизны лежит в городе.
  ЧЕГО СЕМЕЙСТВО НЕ УМЕЕТ, и это сказано вслух: раскладка кладки у стены —
  ПРОЛЁТНАЯ (HouseWalls.cpp: u отсчитывается от начала своего отрезка), значит
  на каждом звене ряды блоков начинаются заново, и вертикальный шов идёт по
  всей высоте каждые ~1.8 м. Это на порядок мельче четырнадцати вырезов по
  полметра, но это НЕ НОЛЬ, и рецептом это не лечится — фазу раскладки задаёт
  движок.
  ПРЯМОЙ ЧЛЕН СЕМЕЙСТВА СВЕРЕН С ПРИНЯТЫМ: city-wall-arc12-s000 против
  city-wall12 — те же 27900 вершин, 9300 треугольников, 19 частей и тот же
  габарит; расходятся ровно четыре строки — циклический сдвиг обхода полки
  боевого хода (тот же четырёхугольник, тот же обход).
- 22:08:2026 - 21:05:00: АРКА МОСТА city-bridge-arc ([N2], тяжесть 3, четыре
  круга подряд). Прежний city-bridge не тронут, все 91 прежний .dfh бит-в-бит.
  «ПАРАПЕТ ОДИН» — ЭТО НЕ ОТСУТСТВУЮЩИЙ ПАРАПЕТ, А ОДИНАКОВЫЙ ПОРЯДОК ВЕРШИН.
  Замер поперечника прежнего моста на высоте борта (луч поперёк, x = 4,
  y = 0.65): попадания на z = -0.150 / 0.150 / 0.205 и 3.850 / 4.150 / 4.205.
  Оба борта на месте, но рельеф кладки fill=3 у ОБОИХ вынесен в +Z: у
  северного он смотрит на проезжую часть, у южного — за борт, в реку. Обе
  цепочки построены с одинаковым порядком вершин, а лицо цепочки задаёт
  именно он. С настила один борт читается кладкой, другой — гладкой плитой;
  это и есть «парапет один». Здесь борта — КОНТУРЫ в плоскости z, у которых
  лица два по построению: замер того же поперечника даёт 0.000/0.300 и
  3.700/4.000, симметрично до миллиметра.
  СВОДА У ПРЕЖНЕГО МОСТА НЕТ ВОВСЕ: луч снизу вверх по оси (x = 4, z = 2) даёт
  ровно два попадания — 0.050 и 0.350, то есть плиту 0.30 и ничего больше.
  У арки тот же луч даёт четыре: 0.543 (интрадос свода), 0.843 (экстрадос),
  0.863 (исподняя грань настила), 1.164 (верх настила).
  ГОРБ И УКЛОН НЕ УМЕЩАЛИСЬ В ПРЕЖНЮЮ ДЛИНУ, и это арифметика: от торца
  аппарели (-0.40) до верха настила (0.35) уже 0.75 м, горб 0.80 сверх настила
  даёт полный подъём 1.55, а 15 % отдают 6.67 м на метр подъёма — значит скату
  нужно 10.3 м даже при ОСТРОМ гребне, мосту 20.7 м. В прежних 12.88 м горб
  больше 0.21 м невозможен. Мост удлинён до 25.20 м — это следствие двух
  заданных чисел, а не запас.
  ПРОФИЛЬ ПРЯМАЯ-ДУГА-ПРЯМАЯ И ГЛАДОК ПО ПЕРВОЙ ПРОИЗВОДНОЙ: аппарели лежат на
  КАСАТЕЛЬНЫХ к дуге гребня (R 30.40, от x -0.510 до 8.510 — ровно прежний
  настил -0.6..8.6), поэтому в точке касания уклон не прыгает. Замер 102 проб
  вдоль оси: наибольший уклон грани 15.0 %, разрывов 0.
  ЗАМЕР ПРОТИВ ПАСПОРТА, и расхождения названы. Верх настила в замке 1.1635
  при константе 1.150: ось — ОПИСАННАЯ ломаная, и она лежит выше дуги на
  R*(sec δ − 1) = 0.0135. Верх у торца -0.398 при константе -0.400: толщина
  плиты откладывается по НОРМАЛИ наклонной плоскости, и по вертикали это
  0.15/cos(8.53°) = 0.1517, то есть +0.0017. Оба расхождения на порядок ниже
  порога ступени 0.20 м, но раскладчик обязан знать число, а не узнать его
  прибором.
  БЫКИ ПРЕЖНИЕ ЧИСЛО В ЧИСЛО: цепочки на x 1.2 и 6.8, от y -2.2 высотой 2.4,
  толщина 0.8, поперёк z 0.4..3.6 — замер city-bridge.dfh.
  СВОД ДЕРЖИТ НАСТИЛ, А НЕ ВИСИТ ПОД НИМ: интрадос — сегментная дуга между
  ЛИЦАМИ быков (просвет 4.80), пята 0.0, подъём 0.55, R 5.511; кольцо 0.30,
  значит экстрадос замка 0.85 — исподняя грань настила в замке. Арка СКВОЗНАЯ:
  луч поперёк моста на y = 0.20 проходит все 4.2 м ширины без единого
  попадания.
  ПЯТА НЕ СРЕЗАНА ПО ДИАГОНАЛИ: узлы лицевой стены поставлены НА пяты (x 1.6 и
  6.4), и перепад низа с -0.60 на экстрадос приходится на кусок нулевой
  ширины. При равномерной резке граница попадала в середину куска, и низ ехал
  наискось через всю пяту.
  ГАБАРИТ (замер меша): x -8.623..16.623 (25.246), y -2.200..1.797 (3.997),
  z -0.110..4.110 (4.220). Проезжая часть между бортами 3.40 м.
- 23:08:2026 - 23:40:00: ФУНДАМЕНТ И КРЫЛЬЦО — два семейства под критический
  заказ владельца 23.08 («дома почти все на треть первого этажа под землей —
  это крит!… каждому дому небольшой фундамент… а к двери идёт
  лестница-ступеньки»). Прежние 110 .dfh БИТ В БИТ (shasum до и после).
  city-foot2-h02..h40 — 20 отрезков цоколя 2.0 x 0.60, шаг высот 0.2 (тот же
  и по той же причине, что у city-kerb2: промах — половина шага, и при 0.4 он
  встал бы ровно на порог 0.20). Отрезками, а не замкнутой коробкой: пятна
  города — от 4.4x4.4 до 25.0x13.2, замкнутый рецепт пришлось бы печь на
  каждое; лишнее уходит в нахлёст, а нахлёст двух каменных тел с ОДНИМ верхом
  невидим. Толщина 0.60 против стены 0.35-0.50 — цоколь обязан ЧИТАТЬСЯ
  выступом; ось отодвинута на полтолщины, значит origin лежит на НАРУЖНОЙ
  плоскости. Без копинга, в отличие от подпорной кромки: там напуск — то, чем
  глаз узнаёт стенку, здесь поверху стоит ДОМ, и напуск стал бы полкой.
  city-stoop16-h02..h20 — 19 маршей шириной 1.6 (норматив заказа >= 1.1;
  1.6 оставляет капсуле R=0.35 по полметра с боков), шаг подъёма 0.1 — ВДВОЕ
  мельче ленты, и это не симметрия: промах ленты прячется в грунте, промах
  крыльца стоит НА ВХОДЕ В ДОМ.
  ВЫЛЕТ ВЫВЕДЕН ЧЕРЕЗ ЧИСЛО ПОДСТУПЕНКОВ ДВИЖКА, А НЕ ЗАДАН. Дробление не в
  рецепте: HouseStairs.cpp берёт steps = round(подъём / 0.175). Заказ просит
  проступь >= 0.28 — значит вылет = steps * 0.30, и проступь выходит РОВНО
  0.30 у всего ряда, а подступенок 0.150..0.200 сам ложится в заказанные
  0.15..0.25. Коэффициент «вылет = 1.7 * подъём» выглядел бы так же и дал бы
  у h03 проступь 0.255 — под порогом заказа, и никто бы не заметил.
  ХОДИБЕЛЬНАЯ ПОВЕРХНОСТЬ ВЫВЕДЕНА ИЗ КОДА, А НЕ ЗАМЕРЕНА ПОШТУЧНО, и это
  можно было сделать ровно потому, что предшественник замерил два марша и
  честно записал, что поправки У КАЖДОГО СВОИ (+0.176/+0.151 у h12r2 против
  +0.180/+0.155 у h14r2). Разбор HouseStairs.cpp объясняет обе пары: пандус
  collider_only идёт от a.y + rise - 0.18 и экструдируется вверх на 0.16,
  значит поверхность = ЯКОРЬ + подступенок - 0.02 на ОБОИХ концах. У h12r2
  подступенок 0.1714 -> +0.151 наверху (сходится в мм), у h14r2 0.175 ->
  +0.155 (тоже). Расхождение в 4 мм — это разница подступенков, а не разница
  марша, и раскладчику больше не нужна таблица: он считает поправку из
  собственного подъёма. Замеренные числа предшественника при этом остаются
  контролем формулы, а не заменяются ею.
- 24:08:2026 - 03:10:00: КРОВЛЯ ПЕРЕСТАЁТ БЫТЬ ЧЁРНЫМ ПРЯМОУГОЛЬНИКОМ (заказ
  владельца, круг 6: «город с воздуха — двадцать чёрных прямоугольников»).
  СНАЧАЛА ЗАМЕР, ПОТОМ ПРАВКА, и замер сказал не то, что ждали: тон атласа
  тут вообще ни при чём. Две руки ИЗ ОДНОГО БИНАРНИКА через дверь DFN_HOUSE_AO
  (одна поза) дали кровле 15/15/13 против 47/42/35 — ровно 3.07 раза, а
  1/3.07 = 0.326 при поле запечённой небесной видимости 0.30
  (HouseMesh.cpp): вершины кровли приходят к отрисовке с НУЛЁМ открытых лучей.
  Печка живёт в engine/world, это зона core, и правка её заказана
  архитектором отдельно; здесь — вторая половина, которая от печки не зависит.
  (1) МАТЕРИАЛ. Все городские скаты стояли в колонке SawnBoard (тёплое
  коричневое дерево), семнадцать из них тоном Dark {0.28,0.24,0.20}. Кровля
  ОБЯЗАНА быть самой светлой крупной поверхностью города, а была самой
  тёмной: люма 42 против газона 124, город читался негативом. Разложено по
  достатку: жильё и хозпостройки — СОЛОМА (mat 6, Thatch/Light 0.66/0.56/0.33
  и Thatch/Mid), дома побогаче и общественные — дранка ТОЛЬКО светлая
  (SawnBoard/Light 0.58/0.50/0.38), храм — черепица FiredClay/Light акцентом
  (второй акцент, таверна, уже её носил). tone=2 на кровлях ВЫЖЖЕН полностью.
  Замер по кадру, одна поза, одна сборка, четыре точки: кровли 55.9 -> 119.7,
  53.5 -> 124.6, 53.2 -> 112.6 люмы при газоне 96.5 ДО И ПОСЛЕ (контрольная
  величина не двинулась ни на десятую — значит меряли правку, а не погоду).
  Порог приёмки «люма кровли > люма газона» выполнен у соломы; у дранки
  79.4 против 96.5 — не выполнен, и это остаток печки, а не тона.
  (2) СОЛОМА — ЭТО ТОЛЩИНА. Настил 0.40 вместо 0.15, свес 0.55 вместо 0.35,
  конёк — ВАЛИК своего материала d=0.44 вместо бревна d=0.28 (gable_roof и
  gable_roof_z, признак is_thatch по mat). Плоский многоугольник нулевой
  толщины с воздуха читается крышкой от коробки.
  (3) И ГЛАВНОЕ ПОБОЧНОЕ: у соломы НЕТ рядов (fill не ставится), а с ними
  уходят шестьдесят дранок на скат, лежащих В ОДНОЙ ПЛОСКОСТИ и пронизывающих
  друг друга на 0.17 м (HouseRoof.cpp считает подъём куска БЕЗ номера ряда,
  хотя комментарий рядом обещает обратное). Именно они глушили касательные
  лучи печки и подставляли под тот же пиксель свою нижнюю, чёрную грань —
  отсечение задних граней в этой стадии выключено. Замер прибором по .dfh
  (HouseGraph -> build_house_mesh -> bake_house_sky_visibility): верхняя грань
  соломенного настила city-house-s даёт 196.5 из 255 при 150 у прежнего
  ГОЛОГО настила под дранкой — и без 4944 соплоскостных граней на скат.
  (4) Уклон разведён по рецептам: домик 1.5 -> 2.2 м конька (24 -> 33 гр.),
  усадьба 2.7 -> 2.4 (35 -> 32); труба домика поднята за коньком тем же
  числом. Перепечены 16 .dfh из 149, остальные 133 БИТ В БИТ (shasum).
- 23:08:2026 - 03:05:00: НАСТИЛ ПОД ДРАНКОЙ ПЕРЕСТАЁТ ЧЕРНИТЬ СКАТ (короткий
  хвост закрытой волны кровель). Прошлый заход выжег tone=2 у СКАТОВ, но
  настилы ПОД рядами дранки остались Dark, и это было незаметно ровно до тех
  пор, пока ряды лежали в одной плоскости: настила не было видно вообще.
  После подъёма рядов по номеру (HouseRoof.cpp, волна 23.08) настил
  проглядывает МЕЖДУ рядами и полосой у карниза — и скат снова читался чёрным
  пятном. Семь настилов (навесы city-shop/-old, city-house-l/-old,
  city-manor/-old и портик city-temple — ВСЕ поверхности fill=7 с tone=2 в
  дереве, у city-keep настил и так был Mid) переведены в tone=1.
  ЗАМЕР: одна поза, ОДИН бинарник, один кадр до и после — портик храма и газон
  перед ним в одном кадре (DFN_TIME=0.45,
  DFN_EDITOR_CAM_REL="89.0,6.5,121.5,0.0,-0.40", houses/whiterun). Коробки —
  ГЕОМЕТРИЯ, одни и те же пиксели в обеих руках (правило measure_aerial):
  скат 950,630 700x70; газон 800,1130 800x160. ДРАНКА 65.47 -> 111.30 люмы
  при ГАЗОНЕ 89.13 -> 89.13 — контрольная величина не двинулась ни на сотую,
  значит меряли правку, а не погоду. Порог приёмки «люма кровли > люма газона»
  у дранки ВЫПОЛНЕН (111.3 > 89.1); прошлый замер 79.4 против 96.5 его не
  проходил и был снят ДО движковых правок печки (47942d6). Перепечено 7 .dfh
  из 149, остальные 142 БИТ В БИТ (shasum). Полный записанный прогон
  генератора: assets/scenes/whiterun.scene вышла БАЙТ В БАЙТ прежней — тон
  настила не двигает ни одной посадки.
- 23:08:2026 - 18:45:00: СВЯЗНОСТЬ ПОСТРОЙКИ — судья в кузне и шесть починок по его
  находкам (заказ владельца 23.08: «башни кривые, их верхние яруса не
  присоединены к башне; ты проверял алгоритмами связности?»).
  СУДЬЯ. Forge::save зовёт house_connect_refusal ДО записи: постройка,
  распавшаяся на острова, НЕ ПИШЕТСЯ, имена копятся в g_refused, main
  возвращает 3. Отказ не останавливает прогон на первом рецепте нарочно —
  кузница перепекает всю полку разом, и остановка показала бы ровно один
  дефект за прогон. Вторая рука того же бинарника: `dfn_houses --check`
  судит ГОТОВЫЕ .dfh (149 из 149 OK, допуск 0.020 м).
  ЧТО ОН НАШЁЛ — 24 постройки, и главная причина ОДНА: контур растёт от своей
  высоты В ОБЕ СТОРОНЫ, значит верх плиты на y — это y + толщина/2, а рецепты
  ставили следующий ярус на y + толщина. Башня вывешивала парапет с шатром на
  0.125 (0.25/2), донжон — двенадцать зубцов на 0.150 (0.3/2), башенки замка —
  шатрики на 0.047 по нормали ската (0.2/2 по вертикали). Починено отсчётом
  P = H + полтолщины; заодно ось зубца отодвинута внутрь на полтолщины (лицо
  зубца встаёт заподлицо со стволом, а не свисает половиной наружу), а шатёр
  башни сел на ЧЕТЫРЕ УГЛОВЫЕ СТОЙКИ с площадки — прежде вся кровля висела на
  волосяном касании со срезом зубца (пересчёт: 4.5 мм).
  БОЕВОЙ ХОД НЕ КАСАЛСЯ СТЕНЫ НИЧЕМ. Полка начиналась в 0.55 м от лица
  (crenellated_run и crenellated_arc — семнадцать прогонов стены и ворота): по
  такому «настилу» нельзя идти, между ним и стеной полметра пустоты. Ближняя
  кромка уведена НА ОСЬ стены (правило «всё крепится к осям»), дальняя осталась
  на месте — ходибельная полка стала 1.65 вместо 1.10 и НАЧИНАЕТСЯ ОТ СТЕНЫ;
  под ней встали кронштейны шагом ~2.4 м.
  КОЛОДЕЦ: столбы кончались на 2.2, а кровля начиналась с 2.5 — крыша висела
  отдельным островом. Столбы доведены до 2.50 (это (карниз + подъём) − 0.30,
  куда gable_roof сажает головы стропил) и связаны ПРОГОНОМ по той же оси.
  РИГЕЛЬ ЛЕСТНИЧНОГО ПРОЁМА у city-house-l — вторая половина заказа (§12
  HOUSES.md): балки перекрытия, дошедшие до кромки проёма, обрывались в воздух.
  ПЕРЕПЕЧЕНО 23 .dfh из 149, остальные 126 БИТ В БИТ (shasum до и после).
  Сцена города НЕ перегенерировалась: посадки от геометрии домов не зависят, и
  контрольный прогон gen_whiterun.py это подтвердил (check_floors 26 из 26,
  мебель 205 из 205).
- 23:08:2026 - 21:40:00: РЕЕСТР РЕЦЕПТОВ И КЛЮЧ --only (волна И3 эпохи «12
  городов»). В main() стоял прямой список вызовов с СОРОКА ЧЕТЫРЬМЯ ЛИТЕРАЛАМИ
  ПУТЕЙ, и это была единственная точка схватки двенадцати городов, которые
  пекут на одну полку: два города, добавивших по рецепту, правили одну функцию.
  Теперь рецепт — строка таблицы (имя -> пакет -> рука), пакет — своя секция
  (pack_demo, pack_city, дальше pack_<город>), а ПУТЬ ВЫВОДИТСЯ ИЗ ИМЕНИ и в
  коде не повторяется: двадцать одна рука, писавшая свой литерал внутри,
  принимает файл входом. --only <маска> печёт совпавшие и НЕ ТРОГАЕТ остальные
  файлы; --list перечисляет реестр; --check по-прежнему судит всю полку.
  ВСЕ 149 .dfh ПОСЛЕ РЕФАКТОРА БАЙТ-В-БАЙТ (shasum до и после: 0 расхождений;
  git status по assets/houses чист, кроме нового INDEX.txt).
- 23:08:2026 - 21:40:00: МАНИФЕСТ ПОЛКИ (волна И2): после выпечки кузница зовёт
  write_house_manifest и пишет assets/houses/INDEX.txt — замер КАЖДОГО файла
  полки, а не только испечённого этим прогоном (потому и после --only манифест
  остаётся полным). Габарит графа, габарит построенного меша, верх тела, пятно
  стен, дверь, балки, пол. Это единственный источник габаритов для генератора
  города: python-разборщика .dfh (шесть функций в gen_whiterun.py) больше нет,
  и второго разборщика формата не заводить — они разойдутся молча.
- 23:08:2026 - 23:39:48: ПАКЕТ КОРНХОЛЛА (этап 2 пилота эпохи) — ДВЕ СТРОКИ
  ЗДЕСЬ И ВЕСЬ ТЕКСТ В tools/pack_cornhall.h: включение ниже кухни и вызов
  pack_cornhall(b) в house_recipes. Шестнадцать рецептов cornhall-*; общий
  city-* и pack_demo не тронуты, все 149 прежних .dfh байт-в-байт (shasum до
  и после — 0 расхождений). Почему заголовок, а не вторая единица трансляции:
  рука кузницы (Forge, gable_roof, keep_tier, Aging) сидит в анонимном
  пространстве имён этого файла, вынести её — правка под всеми городами
  сразу, скопировать — правило 39; а расти этому файлу дальше нельзя, он и
  так втрое над потолком правила 21.
- 24:08:2026 - 01:20:00: ПОДПОРНАЯ СТЕНКА ТЕРРАСЫ И КОНТРФОРС —
  city-revet2-h04..h40 (19 ростовок) и city-butt-h08..h40 (9). Заказ
  владельца 24.08 дословно: «донжон — переделай; сейчас он стоит на четырёх
  венцах и читается башней на скальном быке, а по смыслу там должна быть
  ПОДПОРНАЯ СТЕНКА ТЕРРАСЫ (архитектурная, с контрфорсами/откосом)».
  ПОЧЕМУ НОВАЯ ДЕТАЛЬ, А НЕ НОВОЕ ЧИСЛО У city-foot2. Пояс держит ДОМ:
  тонкий (0.60), вертикальный, без копинга, по замыслу почти весь в грунте —
  снаружи ладонь цоколя. Стенка держит ЗЕМЛЮ: толстая (0.90), с ОТКОСОМ
  венцами 1:20 и с КОПИНГОМ, и по замыслу видна вся. Тот же пояс, поднятый на
  два с половиной метра, перестаёт быть цоколем и ничем другим стать не может
  — это и увидел владелец. Довод у откоса и у копинга общий с city-kerb2:
  подпорная стенка без напуска поверху читается обрезанной.
  ОТКОС СОБИРАЕТСЯ ВЕНЦАМИ, А НЕ НАКЛОННОЙ ГРАНЬЮ: стена в графе — цепочка
  постоянной толщины, выдавленная вверх; наклонное лицо пришлось бы строить
  контуром, то есть плитой без объёма кладки. Горизонтальный шов между
  венцами при этом не потеря, а то, чем бутовая подпорная стенка и читается.
  Ряд кончается на 4.0 (не на десяти): раскладчик собирает грань венцами
  ровно как ленту, и потолок ряда — не потолок стенки.
  Прежние 163 .dfh байт-в-байт (shasum до и после — 0 расхождений); в
  INDEX.txt прибавилась колонка wpoly, довод — в tools/house_manifest.h.
- 27:08:2026 - 11:20:22: координатор: известная находка судьи лестниц у frame-replica названа в руке (демо-стенд, стропило режет марш; не чинится сознательно — боевые рецепты чисты).
- 27:08:2026 - 23:30:00: ВНУТРИДОМОВОЙ МАРШ — ОДНОЙ РУКОЙ НА ОБА ГОРОДА
  (house_flight_ax / house_flight_z / house_landing, крит владельца по кадрам
  аудита 28.08: «пожарный трап — ни подступёнков, ни перил, ни площадок»).
  До правки марш внутри дома был ГОЛЫМ контуром fill=6: у Вайтрана выписанным
  руками пять раз и БЕЗ ЕДИНОЙ БАЛЯСИНЫ, у Житнова — своей рукой с перилами;
  аудит снял с обоих один и тот же кадр. Рука кладёт ступени (riser=1 —
  подступёнок-щит, HouseMesh.h), тетиву, балясины шагом 0.8 (было «ровно три
  штуки при любой длине»), поручень 0.95, слегу 0.48 и тумбы на концах.
  ПЛОЩАДКА У ЗАМКА: подъём 6.31 это 23 ступени подряд при жилом потолке 16-18 —
  разделён пополам, между маршами площадка 1.4 (диаметр капсулы плюс радиус с
  каждой стороны). НАСТИЛ МЕЛЬНИЦЫ: у city-mill не было пола рабочего яруса
  ВОВСЕ — два кольца стен и дыра 7x7; все ШЕСТЬ находок судьи локаций по
  Вайтрану были об этом. КРЫЛЬЦА мельницы и усадьбы получили nosing=1: уклон у
  них законный (крыльцо — не марш), а пандус при nosing=0 приходил на 0.155
  выше и земли, и порога — буквально «перепрыгивать у двери».
- 28:08:2026 - 19:55:00: ПАКЕТ КУРГАНЛЫ ПОДКЛЮЧЁН ДВУМЯ СТРОКАМИ — включение
  tools/pack_kurganly.h ниже житновского и вызов pack_kurganly(b) в
  house_recipes(). Двенадцать рецептов kurganly-* (степная школа города №11:
  саманный дом-куб, войлочное жилище в трёх состояниях, нора, дромос, баба,
  варница, заслон, раскол-воронка, коновязь). Общий city-*, pack_demo и
  pack_cornhall не тронуты, все 225 прежних .dfh байт-в-байт (shasum до и
  после); манифест 225 -> 237 строк. Ровно то место и ровно тот объём правки,
  который шапка pack_cornhall.h и заведён был обещать: город добавляется своей
  секцией, а не дописыванием в чужой пакет.
*/

#include "engine/world/sources/HouseFile.h"
#include "engine/world/sources/HouseGraph.h"
#include "engine/world/sources/HouseMesh.h"
#include "tools/recipe_book.h"

#include <algorithm>
#include <cmath>
#include <cstdint>
#include <cstdio>
#include <cstdlib>
#include <functional>

#include <glm/geometric.hpp>
#include <glm/vec2.hpp>
#include <deque>
#include <filesystem>
#include <fstream>
#include <iterator>
#include <map>
#include <initializer_list>
#include <string>
#include <tuple>
#include <utility>
#include <vector>

namespace {

using dfn::world::Anchoring;
using dfn::world::ElementId;
using dfn::world::ElementKind;
using dfn::world::HouseGraph;
using dfn::world::VertexId;

using Params = std::initializer_list<std::pair<const char*, const char*>>;

/// Рецепты, которым ОТКАЗАНО судьёй связности. Список, а не флаг: оператору
/// нужны имена всех, а не факт, что где-то плохо.
std::vector<std::string> g_refused;

/// Рука над графом: рецепты читаются как список деталей, а не как API-вязь.
struct Forge {
    HouseGraph g;
    /// ОДНА ВЕРШИНА НА ТОЧКУ: рецепты называют углы повторно (столб и стена в
    /// одном углу), а граф не дедуплицирует — после распаковки пользователь
    /// тянул угол, и стена уезжала БЕЗ столба (аудит #3, находка 10).
    std::map<std::tuple<int, int, int>, VertexId> known;

    VertexId v(float x, float y, float z) {
        const auto key = std::make_tuple(static_cast<int>(std::lround(x * 1000.0f)),
                                         static_cast<int>(std::lround(y * 1000.0f)),
                                         static_cast<int>(std::lround(z * 1000.0f)));
        if (const auto it = known.find(key); it != known.end()) {
            return it->second;
        }
        const VertexId id = g.add_vertex(Anchoring::Free, {x, y, z});
        known.emplace(key, id);
        return id;
    }

    ElementId element(ElementKind kind, std::vector<VertexId> refs, bool closed,
                      Params params) {
        ElementId id = dfn::world::NO_ELEMENT;
        const auto r = g.add_element(kind, std::move(refs), "", id);
        if (!r.ok) {
            std::fprintf(stderr, "forge: add_element: %s\n", r.why.c_str());
            std::exit(2);
        }
        if (closed) {
            (void)g.set_closed(id, true);
        }
        for (const auto& kv : params) {
            (void)g.set_param(id, kv.first, kv.second);
        }
        return id;
    }

    /// Стена-цепочка между двумя углами (низ), выдавленная вверх.
    ElementId wall(VertexId a, VertexId b, Params params) {
        return element(ElementKind::Surface, {a, b}, false, params);
    }

    ElementId contour(std::vector<VertexId> refs, Params params) {
        return element(ElementKind::Surface, std::move(refs), true, params);
    }

    ElementId beam(VertexId a, VertexId b, Params params) {
        return element(ElementKind::Line, {a, b}, false, params);
    }

    /// ДВЕРЬ-СТВОРКА в проёме южной стены: цепочка шириной проёма со свойством
    /// door=1 — рендер качает её вокруг петли, коллайдер её не берёт.
    void door_leaf(float cx, float y0, float z, Params extra = {}) {
        const float w = 1.0f;  // HOUSE_DOOR_W_DEFAULT
        const VertexId a = v(cx - w * 0.5f, y0, z);
        const VertexId b = v(cx + w * 0.5f, y0, z);
        const ElementId leaf = wall(a, b,
                                    {{"height", "2.05"},
                                     {"thickness", "0.05"},
                                     {"mat", "1"},
                                     {"tone", "2"},
                                     {"door", "1"},
                                     {"hinge", "0"}});
        for (const auto& kv : extra) {
            (void)g.set_param(leaf, kv.first, kv.second);
        }
    }

    /// Створка в проёме стены, идущей вдоль Z (для дверей во двор и боковых).
    void door_leaf_x(float x, float y0, float cz) {
        const float w = 1.0f;
        const VertexId a = v(x, y0, cz - w * 0.5f);
        const VertexId b = v(x, y0, cz + w * 0.5f);
        (void)wall(a, b,
                   {{"height", "2.05"},
                    {"thickness", "0.05"},
                    {"mat", "1"},
                    {"tone", "2"},
                    {"door", "1"},
                    {"hinge", "0"}});
    }

    /// Четыре угловых столба и обвязка поверху — каркас, к которому дом
    /// читается «собранным», как у демки из деталей.
    void frame_posts(float x0, float z0, float x1, float z1, float y0, float y1,
                     const char* mat) {
        const float xs[2] = {x0, x1};
        const float zs[2] = {z0, z1};
        VertexId top[4];
        int k = 0;
        for (const float* px : {&xs[0], &xs[1]}) {
            for (const float* pz : {&zs[0], &zs[1]}) {
                const VertexId lo = v(*px, y0, *pz);
                const VertexId hi = v(*px, y1, *pz);
                (void)beam(lo, hi, {{"radius", "0.12"}, {"mat", mat}, {"tone", "2"}});
                top[k++] = hi;
            }
        }
        // Обвязка: 0-1 (вдоль z), 1-3 (вдоль x), 3-2, 2-0.
        const int ring[4][2] = {{0, 1}, {1, 3}, {3, 2}, {2, 0}};
        for (const auto& e : ring) {
            (void)beam(top[e[0]], top[e[1]],
                       {{"radius", "0.10"}, {"form", "square"}, {"mat", mat},
                        {"tone", "2"}});
        }
    }

    /// КАНТ КАРНИЗА (EXTERIOR_CATALOG.md: торец ската — всегда двойной кант,
    /// тёсовая доска + бревно-слега; без него кровля «картонно-тонкая»,
    /// приёмка №3). Кладётся вдоль нижней кромки ската.
    void eaves_trim(glm::vec3 lo_a, glm::vec3 lo_b) {
        // Доска-кант чуть ниже кромки, плашмя по скату.
        (void)beam(v(lo_a.x, lo_a.y - 0.06f, lo_a.z), v(lo_b.x, lo_b.y - 0.06f, lo_b.z),
                   {{"radius", "0.12"}, {"form", "plank"}, {"mat", "1"},
                    {"tone", "1"}});
        // Слега-бревно под самым краем.
        (void)beam(v(lo_a.x, lo_a.y - 0.16f, lo_a.z), v(lo_b.x, lo_b.y - 0.16f, lo_b.z),
                   {{"radius", "0.09"}, {"mat", "0"}, {"tone", "2"}});
    }

    /// СОЛОМА — ЭТО ТОЛЩИНА, А НЕ ПЛОСКОСТЬ (правило 52 «у предмета мира нет
    /// плоских частей», применённое к кровле; заказ архитектора К3 после
    /// кадра «город с воздуха — двадцать чёрных прямоугольников»).
    /// Настоящая соломенная кровля несёт 30-45 см материала, и с воздуха
    /// многоугольник нулевой толщины читается крышкой от коробки, а не
    /// крышей. Числа: настил 0.40 (вместо 0.15 у гонта), валик конька
    /// диаметром 0.44 (вместо 0.28), кант карниза толще на ту же долю.
    /// ПОБОЧНЫЙ ЭФФЕКТ, КОТОРЫЙ ДОРОЖЕ ЗАКАЗАННОГО: у соломы НЕТ рядов
    /// (fill не ставится), а значит нет и шестидесяти взаимно пронизывающих
    /// друг друга призм дранки в одной плоскости — а именно они глушили
    /// касательные лучи запечённой небесной видимости и подставляли под тот
    /// же пиксель свою НИЖНЮЮ, чёрную грань (отсечение задних граней в этой
    /// стадии выключено). Замер прибором по .dfh — в отчёте волны.
    static bool is_thatch(const char* mat) { return mat != nullptr && mat[0] == '6'; }

    /// Двускат над прямоугольником: конёк вдоль X посередине глубины.
    /// Два наклонных контура + фронтоны + КОНЁК-БРЕВНО и торчащие КОНЦЫ
    /// СТРОПИЛ (правка 20.08 «к скайримскому виду»: крыша собрана, а не
    /// накрыта). roof_fill: "" — гладкий настил (солома), "7" — дранка
    /// рядами, "8" — черепица рядами; wear наследуется скатами.
    void gable_roof(float x0, float z0, float x1, float z1, float eaves,
                    float ridge_h, const char* mat, const char* tone,
                    const char* roof_fill = "", const char* wear = "") {
        const bool thatch = is_thatch(mat);
        const float zm = (z0 + z1) * 0.5f;
        const float o = thatch ? 0.55f : 0.35f; // свес; у соломы он длиннее
        const float yr = eaves + ridge_h;
        const auto slab = [&](VertexId a, VertexId b, VertexId c, VertexId d) {
            const ElementId id = contour(
                {a, b, c, d},
                {{"thickness", thatch ? "0.40" : "0.15"},
                 {"mat", mat}, {"tone", tone}});
            if (roof_fill[0] != 0) {
                (void)g.set_param(id, "fill", roof_fill);
            }
            if (wear[0] != 0) {
                (void)g.set_param(id, "wear", wear);
            }
        };
        // Северный скат (от конька к z0), лицо вверх; южный — зеркально.
        slab(v(x0 - o, yr, zm), v(x1 + o, yr, zm),
             v(x1 + o, eaves - 0.1f, z0 - o), v(x0 - o, eaves - 0.1f, z0 - o));
        slab(v(x1 + o, yr, zm), v(x0 - o, yr, zm),
             v(x0 - o, eaves - 0.1f, z1 + o), v(x1 + o, eaves - 0.1f, z1 + o));
        eaves_trim({x0 - o, eaves - 0.1f, z0 - o}, {x1 + o, eaves - 0.1f, z0 - o});
        eaves_trim({x0 - o, eaves - 0.1f, z1 + o}, {x1 + o, eaves - 0.1f, z1 + o});
        // Фронтоны — треугольники в плоскостях x0 и x1.
        for (const float gx : {x0, x1}) {
            const VertexId a = v(gx, eaves, z0);
            const VertexId b = v(gx, eaves, z1);
            const VertexId c = v(gx, yr, zm);
            (void)contour({a, b, c},
                          {{"thickness", "0.12"}, {"mat", "0"}, {"tone", "1"}});
        }
        // КОНЁК: у гонта бревно-прогон, у соломы ВАЛИК своего материала —
        // он и лежит выше, потому что настил под ним толще.
        (void)beam(v(x0 - 0.4f, yr + (thatch ? 0.20f : 0.02f), zm),
                   v(x1 + 0.4f, yr + (thatch ? 0.20f : 0.02f), zm),
                   {{"radius", thatch ? "0.22" : "0.14"},
                    {"mat", thatch ? mat : "0"},
                    {"tone", thatch ? tone : "2"}});
        // СТРОПИЛА: пары от конька к карнизам с шагом ~1.5 м, концы торчат
        // за свес на 0.25 — те самые «торцы», которые читаются с земли.
        const int bays = std::max(1, static_cast<int>((x1 - x0) / 1.5f));
        for (int k = 0; k <= bays; ++k) {
            const float x = x0 + (x1 - x0) * static_cast<float>(k)
                          / static_cast<float>(bays);
            for (const float gz : {z0, z1}) {
                // ПОД настилом, торец выглядывает из-под свеса (приёмка №3:
                // стропило выше кромки лежало ПОВЕРХ гонта). Верх — под
                // коньком с запасом на толщину кровли, низ — под карнизной
                // кромкой, выпуск 0.22 за свес.
                const float out_z = gz + (gz < zm ? -(o + 0.22f) : (o + 0.22f));
                (void)beam(v(x, yr - 0.30f, zm), v(x, eaves - 0.34f, out_z),
                           {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
            }
        }
    }


    /// ДВУСКАТ С КОНЬКОМ ВДОЛЬ Z — близнец gable_roof для крыла, повёрнутого
    /// на 90°. Г-образной усадьбе нужны ПЕРПЕНДИКУЛЯРНЫЕ коньки: только они
    /// сходятся ендовой, параллельные дают два отдельных сарая. Отдельный
    /// метод, а не флаг у gable_roof: кузница переписывает ВСЕ .dfh каждым
    /// прогоном, и правка общего тела погнала бы пять принятых домов на
    /// переприёмку ради чужой крыши.
    /// trim_z0 — с какого z начинать кант и стропила: у крыла, чей конёк
    /// заведён ПОД главную кровлю, северный кусок лежит в чужом чердаке, и
    /// стропила оттуда торчали бы сквозь фасад главного корпуса.
    void gable_roof_z(float x0, float z0, float x1, float z1, float eaves,
                      float ridge_h, const char* mat, const char* tone,
                      const char* roof_fill = "", const char* wear = "",
                      float trim_z0 = -1.0e9f) {
        const bool thatch = is_thatch(mat);
        const float xm = (x0 + x1) * 0.5f;
        const float o = thatch ? 0.55f : 0.35f; // свес
        const float yr = eaves + ridge_h;
        const auto slab = [&](VertexId a, VertexId b, VertexId c, VertexId d) {
            const ElementId id = contour(
                {a, b, c, d},
                {{"thickness", thatch ? "0.40" : "0.15"},
                 {"mat", mat}, {"tone", tone}});
            if (roof_fill[0] != 0) {
                (void)g.set_param(id, "fill", roof_fill);
            }
            if (wear[0] != 0) {
                (void)g.set_param(id, "wear", wear);
            }
        };
        // ПОРЯДОК ОБХОДА ЗЕРКАЛЕН двускату вдоль X: смена оси конька — это
        // отражение, и прежний порядок положил бы настил лицом в чердак.
        // Западный скат (от конька к x0), лицо вверх; восточный — зеркально.
        slab(v(xm, yr, z1 + o), v(xm, yr, z0 - o),
             v(x0 - o, eaves - 0.1f, z0 - o), v(x0 - o, eaves - 0.1f, z1 + o));
        slab(v(xm, yr, z0 - o), v(xm, yr, z1 + o),
             v(x1 + o, eaves - 0.1f, z1 + o), v(x1 + o, eaves - 0.1f, z0 - o));
        const float tz = std::max(trim_z0, z0 - o);
        eaves_trim({x0 - o, eaves - 0.1f, tz}, {x0 - o, eaves - 0.1f, z1 + o});
        eaves_trim({x1 + o, eaves - 0.1f, tz}, {x1 + o, eaves - 0.1f, z1 + o});
        // Фронтоны — треугольники в плоскостях z0 и z1.
        for (const float gz : {z0, z1}) {
            const VertexId a = v(x0, eaves, gz);
            const VertexId b = v(x1, eaves, gz);
            const VertexId c = v(xm, yr, gz);
            (void)contour({a, b, c},
                          {{"thickness", "0.12"}, {"mat", "0"}, {"tone", "1"}});
        }
        // КОНЁК: см. gable_roof — у соломы валик своего материала.
        (void)beam(v(xm, yr + (thatch ? 0.20f : 0.02f), z0 - 0.4f),
                   v(xm, yr + (thatch ? 0.20f : 0.02f), z1 + 0.4f),
                   {{"radius", thatch ? "0.22" : "0.14"},
                    {"mat", thatch ? mat : "0"},
                    {"tone", thatch ? tone : "2"}});
        // СТРОПИЛА: ПОД настилом, торцы выглядывают из-под свеса.
        const float zs = std::max(trim_z0, z0);
        const int bays = std::max(1, static_cast<int>((z1 - zs) / 1.5f));
        for (int k = 0; k <= bays; ++k) {
            const float z = zs + (z1 - zs) * static_cast<float>(k)
                          / static_cast<float>(bays);
            for (const float gx : {x0, x1}) {
                const float out_x = gx + (gx < xm ? -(o + 0.22f) : (o + 0.22f));
                (void)beam(v(xm, yr - 0.30f, z), v(out_x, eaves - 0.34f, z),
                           {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
            }
        }
    }

    /// ЧИСЛО КАК ПАРАМЕТР. Params — это initializer_list УКАЗАТЕЛЕЙ, и строка
    /// обязана пережить вызов; deque не двигает уже отданное (та же причина,
    /// по которой она стоит у Aging).
    std::deque<std::string> said;
    const char* num(float value) {
        char buf[32];
        std::snprintf(buf, sizeof(buf), "%g", static_cast<double>(value));
        said.emplace_back(buf);
        return said.back().c_str();
    }

    /// ВЫСОТА ДВУСКАТА НАД ТОЧКОЙ z — где труба пробивает настил. Обратная
    /// gable_roof: тот кладёт скаты от (eaves-0.1) на кромке z0-o до конька на
    /// zm, и труба обязана считать по ТОЙ ЖЕ прямой, иначе её низ повиснет над
    /// кровлей или уйдёт под неё.
    static float roof_y_at(float z, float z0, float z1, float eaves, float ridge_h) {
        const float o = 0.35f;
        const float zm = (z0 + z1) * 0.5f;
        const float run = (zm - z0) + o;
        const float t = std::clamp((std::min(z - z0, z1 - z) + o) / std::max(run, 0.1f),
                                   0.0f, 1.0f);
        return (eaves - 0.1f) + (ridge_h + 0.1f) * t;
    }

    /// ТРУБА НАД ОЧАГОМ (претензия критика [13]: ноль труб на город; гайд §8 —
    /// вид держит гребёнка на линии крыш).
    ///
    /// ТРУБА ИДЁТ СКВОЗЬ СКАТ НАД ОЧАГОМ, А НЕ ГДЕ КРАСИВО. Очаг ставит
    /// расстановка (furnish), и если труба встанет мимо, снаружи дым идёт из
    /// одного угла, а внутри огонь горит в другом — это читается багом, а не
    /// вольностью. Поэтому (cx, cz) сюда приходят ВХОДОМ от раскладчика.
    ///
    /// Стояк — Line с квадратным профилем: у графа нет тела «труба», а
    /// каменный брус ею и является. Кровлю рецепт НЕ ТРОГАЕТ: труба
    /// добавляется поверх, принятая геометрия остаётся байт-в-байт.
    /// capped — напуск-плита поверху (достаток); без неё выходит простая
    /// обкладка дымового отверстия.
    void chimney(float cx, float cz, float y_lo, float y_top, float r, bool capped,
                 const char* mat, const char* tone, const char* wear) {
        (void)beam(v(cx, y_lo, cz), v(cx, y_top, cz),
                   {{"radius", num(r)}, {"form", "square"}, {"mat", mat},
                    {"tone", tone}, {"wear", wear}});
        if (capped) {
            // Напуск: плита шире ствола на ладонь — по ней вода уходит мимо
            // кладки, и с земли труба читается завершённой, а не обрубленной.
            (void)beam(v(cx, y_top, cz), v(cx, y_top + 0.18f, cz),
                       {{"radius", num(r + 0.12f)}, {"form", "square"},
                        {"mat", mat}, {"tone", tone}, {"wear", wear}});
        }
    }

    /// Марш на четырёх точках (fill=6): низ — пара (z_lo), верх — пара (z_hi).
    void stairs_z(float x0, float x1, float z_lo, float z_hi, float y0, float y1) {
        const VertexId a = v(x0, y0, z_lo);
        const VertexId b = v(x1, y0, z_lo);
        const VertexId c = v(x1, y1, z_hi);
        const VertexId d = v(x0, y1, z_hi);
        (void)contour({a, b, c, d},
                      {{"thickness", "0.1"}, {"fill", "6"}, {"mat", "1"},
                       {"tone", "1"}});
    }

    /// СВЯЗНОСТЬ ПРОВЕРЯЕТСЯ ДО ЗАПИСИ, И ОТКАЗ НЕ ПИШЕТ ФАЙЛ. Постройка,
    /// распавшаяся на острова, — это ярус, висящий в воздухе; записать его
    /// значило бы разослать по городу копиями (то же соображение, что у
    /// dfn_assemble с --require-solid, HOUSES.md §1).
    ///
    /// ПОЧЕМУ НЕ exit(3) НА ПЕРВОМ ЖЕ: кузница перепекает всю полку одним
    /// прогоном, и остановка на первом плохом рецепте показала бы ровно один
    /// дефект за прогон. Плохие файлы пропускаются, счёт копится, main
    /// возвращает 3 — оператор видит ВСЕ находки сразу.
    void save(const std::string& path) {
        std::string why;
        if (dfn::world::house_connect_refusal(g, why)) {
            std::fprintf(stderr, "forge: ОТКАЗ %s — %s\n", path.c_str(), why.c_str());
            g_refused.push_back(path);
            return;
        }
        const std::string text = dfn::world::write_house(g);
        std::ofstream f(path, std::ios::binary | std::ios::trunc);
        if (!f) {
            std::fprintf(stderr, "forge: не открылся %s\n", path.c_str());
            std::exit(2);
        }
        f << text;
        std::fprintf(stderr, "forge: %s — вершин %zu, элементов %zu\n", path.c_str(),
                     g.vertex_count(), g.element_count());
    }
};

// ---------------------------------------------------------------------------
// ВНУТРИДОМОВОЙ МАРШ — ОДНА РУКА НА ОБА ГОРОДА
//
// ЗАЧЕМ РУКА, КОГДА ЕСТЬ КОНТУР. До 28.08 марш внутри дома был ГОЛЫМ контуром
// fill=6: у Вайтрана — пять раз выписанный руками, у Житнова — через
// cornhall_flight_ax. Аудит 28.08 снял с этого кадры и назвал вслух то, чего
// не мерила ни одна из пяти рук судьи: «проступи — тонкие доски с просветами,
// подступёнков нет, ПЕРИЛ НЕТ, площадки нет; это пожарный трап, а не лестница
// жилого дома». У Вайтрана перил не было вовсе — ни одной балясины на пять
// маршей.
//
// ЧТО ИМЕННО КЛАДЁТ РУКА, И ПОЧЕМУ ИМЕННО ОНА:
//   * СТУПЕНИ — контур fill=6, open=1, rise=0.28, nosing=1, RISER=1. Последнее
//     свойство новое (HouseMesh.h): щит между проступями. Три первых уже были
//     у житновской руки, и рецепты Вайтрана выписывали их четвёркой строк
//     каждый — то есть четыре копии одного правила в одном файле;
//   * ТЕТИВА — наклонная доска по ограждаемой кромке: то, чем перила держатся;
//   * БАЛЯСИНЫ ШАГОМ ~0.8 м, а не «три штуки». Прежняя житновская рука ставила
//     ровно три (низ, середина, верх) при ЛЮБОЙ длине: на марше замка в 6.3 м
//     это стойка через каждые 3.15 м — обозначение перил, а не перила;
//   * ДВЕ ЛИНИИ — поручень 0.95 и промежуточная слега 0.48. Высоты не выбраны:
//     это ровно высоты балюстрады галереи (cornhall_deck), и ярус с маршем
//     читаются одной рукой потому, что это одни и те же два числа;
//   * ТУМБЫ (newel) в начале и в конце — толще балясин и выше поручня: конец
//     перил обязан читаться концом, иначе поручень висит обрубком.
//
// СТОРОНА ПАДЕНИЯ, А НЕ «ОБЕ СТОРОНЫ»: у стены перила не нужны — там стена, и
// вторая балюстрада была бы решёткой между маршем и кладкой. rail_side +1/-1
// называет ограждаемую грань, 0 — марш посреди зала, ограждаются обе.
// ---------------------------------------------------------------------------

/// ПОДСТУПЁНОК ВНУТРИДОМОВОГО МАРША. Число одно на движок и на кузницу
/// (HouseMesh.h): движок ДЕЛИТ марш на ступени, рецепт это деление
/// ПРЕДСКАЗЫВАЕТ, и разойтись им негде.
inline constexpr float HOUSE_FLIGHT_RISE = dfn::world::HOUSE_STAIR_RISE_STEEP_M;

/// ВЫСОТЫ ОГРАЖДЕНИЯ — те же, что у балюстрады галереи.
inline constexpr float HOUSE_RAIL_TOP = 0.95f;
inline constexpr float HOUSE_RAIL_MID = 0.48f;
/// Шаг балясин по ходу марша, м.
inline constexpr float HOUSE_BALUSTER_PITCH = 0.8f;

/// МАРШ С ЛЮБОЙ ОСЬЮ. (fx,fz)->(tx,tz) — линия хода в плане, (sx,sz) — её
/// поперечный единичный вектор, half — полуширина. rail_side: +1 ограждает
/// грань со стороны +s, -1 — со стороны -s, 0 — обе.
inline void house_flight_ax(Forge& f, float fx, float fz, float tx, float tz, float half,
                            float sx, float sz, float y_lo, float y_top, int rail_side,
                            float th, const char* wear) {
    (void)f.contour({f.v(fx + sx * half, y_lo, fz + sz * half),
                     f.v(fx - sx * half, y_lo, fz - sz * half),
                     f.v(tx - sx * half, y_top, tz - sz * half),
                     f.v(tx + sx * half, y_top, tz + sz * half)},
                    {{"thickness", f.num(th)}, {"fill", "6"}, {"open", "1"},
                     {"rise", f.num(HOUSE_FLIGHT_RISE)}, {"nosing", "1"},
                     {"riser", "1"}, {"mat", "1"}, {"tone", "1"}, {"wear", wear}});

    const float run = std::sqrt((tx - fx) * (tx - fx) + (tz - fz) * (tz - fz));
    const float slope = std::sqrt(run * run + (y_top - y_lo) * (y_top - y_lo));
    const int nb = std::max(2, static_cast<int>(std::round(slope / HOUSE_BALUSTER_PITCH)));

    const auto side_rail = [&](float s_sign) {
        const float rs = half * s_sign;
        const float rfx = fx + sx * rs;
        const float rfz = fz + sz * rs;
        const float rtx = tx + sx * rs;
        const float rtz = tz + sz * rs;
        // ТЕТИВА — наклонная доска по кромке марша; она же то, чем перила
        // держатся, и то, чем открытый марш перестаёт читаться стопкой досок.
        (void)f.beam(f.v(rfx, y_lo + 0.12f, rfz), f.v(rtx, y_top + 0.12f, rtz),
                     {{"radius", "0.13"}, {"form", "plank"}, {"mat", "1"},
                      {"tone", "1"}, {"wear", wear}});
        for (int k = 0; k <= nb; ++k) {
            const float s = static_cast<float>(k) / static_cast<float>(nb);
            const bool newel = k == 0 || k == nb;
            const float px = rfx + (rtx - rfx) * s;
            const float pz = rfz + (rtz - rfz) * s;
            const float py = y_lo + (y_top - y_lo) * s;
            (void)f.beam(f.v(px, py, pz),
                         f.v(px, py + (newel ? HOUSE_RAIL_TOP + 0.12f : HOUSE_RAIL_TOP + 0.05f),
                             pz),
                         {{"radius", newel ? "0.075" : "0.05"}, {"form", "square"},
                          {"mat", "1"}, {"tone", "1"}, {"wear", wear}});
        }
        for (const float hy : {HOUSE_RAIL_MID, HOUSE_RAIL_TOP}) {
            (void)f.beam(f.v(rfx, y_lo + hy, rfz), f.v(rtx, y_top + hy, rtz),
                         {{"radius", hy > 0.9f ? "0.06" : "0.045"}, {"form", "plank"},
                          {"mat", "1"}, {"tone", "1"}, {"wear", wear}});
        }
    };
    if (rail_side >= 0) {
        side_rail(+1.0f);
    }
    if (rail_side <= 0) {
        side_rail(-1.0f);
    }
}

/// МАРШ ВДОЛЬ Z. xa..xb — ширина, rail_x — та грань, что смотрит В КОМНАТУ
/// (сторона падения); если она совпала с серединой, ограждаются обе.
inline void house_flight_z(Forge& f, float xa, float xb, float z_foot, float z_top,
                           float y_lo, float y_top, float rail_x, const char* wear,
                           float th = 0.12f) {
    const float cx = (xa + xb) * 0.5f;
    const int side = std::abs(rail_x - cx) < 1e-3f ? 0 : (rail_x < cx ? +1 : -1);
    house_flight_ax(f, cx, z_foot, cx, z_top, (xb - xa) * 0.5f, -1.0f, 0.0f, y_lo, y_top,
                    side, th, wear);
}

/// ПРОМЕЖУТОЧНАЯ ПЛОЩАДКА: плита, на которую приходит один марш и с которой
/// уходит следующий. Толщина и посадка те же, что у настила яруса: верх плиты
/// — то, по чему ходят, и оба марша садятся концами ИМЕННО на него.
inline void house_landing(Forge& f, float x0, float z0, float x1, float z1, float y_top,
                          float th, const char* wear) {
    (void)f.contour({f.v(x0, y_top - th * 0.5f, z0), f.v(x0, y_top - th * 0.5f, z1),
                     f.v(x1, y_top - th * 0.5f, z1), f.v(x1, y_top - th * 0.5f, z0)},
                    {{"thickness", f.num(th)}, {"mat", "1"}, {"tone", "1"},
                     {"beams", "1"}, {"wear", wear}});
}

/// ДОКАЗАТЕЛЬСТВО АДДИТИВНОСТИ ТРУБ, А НЕ ОБЕЩАНИЕ. DFN_FORGE_NO_VENTS=1 печёт
/// библиотеку БЕЗ единой трубы; хеши обязаны сойтись с теми, что были до правки
/// 22.08. «Вроде ничего больше не задел» — это не приёмка; проверяемое
/// свойство остаётся в кузнице навсегда, а не живёт один вечер в чужой голове.
bool vents_on() {
    static const bool on = std::getenv("DFN_FORGE_NO_VENTS") == nullptr;
    return on;
}

/// Пол: замкнутый контур ПРОТИВ часовой сверху (лицо вверх), паркет.
void parquet_floor(Forge& f, float x0, float z0, float x1, float z1, float y) {
    const VertexId a = f.v(x0, y, z0);
    const VertexId b = f.v(x0, y, z1);
    const VertexId c = f.v(x1, y, z1);
    const VertexId d = f.v(x1, y, z0);
    (void)f.contour({a, b, c, d},
                    {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                     {"tone", "1"}});
}

/// СТЕПЕНЬ ЗАПУЩЕННОСТИ — ПАРАМЕТР РЕЦЕПТА, А НЕ ВТОРОЙ РЕЦЕПТ (заказ 21.08:
/// «у зданий города должны быть РАЗЛИЧНЫЕ эффекты износа»). Копия тела ради
/// одного числа расходится с оригиналом на первой же правке формы — свежий и
/// ветхий дом обязаны печататься ОДНИМ телом.
struct Aging {
    /// Куда печь: имя файла — тоже вариация, а не свойство рецепта.
    const char* file = "";
    /// Прибавка к СОБСТВЕННОМУ износу детали. Рельеф рецепта (цоколь трёт
    /// сильнее карниза, южный фасад сильнее северного) обязан пережить
    /// старение: равномерно серый дом читается плохой текстурой, а не
    /// возрастом. Полоса lo..hi держит прибавку в заказанном диапазоне.
    float shift = 0.0f;
    float lo = 0.0f;
    float hi = 1.0f;
    /// ЗА ДОМОМ СЛЕДЯТ: ставни на петлях, завалинка-цоколь подсыпана. У
    /// ветхого — нет; это единственные два признака ухода, которые
    /// ElementParams умеет выключить (шаг обшивки живёт в WallStyle по имени
    /// стиля, из .dfh его не достать).
    bool kept = true;

    /// СТРОКИ ЖИВУТ ДО КОНЦА РЕЦЕПТА. Params — это initializer_list
    /// УКАЗАТЕЛЕЙ: кольцевой буфер затёр бы строку, на которую ссылается
    /// Params, объявленный в начале функции и применённый в конце (у усадьбы
    /// между объявлением s2 и последней стеной — четыре десятка вызовов).
    /// deque не двигает уже отданное.
    std::deque<std::string> said;

    /// Износ детали с её собственным числом на входе. Свежий дом (shift 0,
    /// полоса 0..1) обязан дать ТУ ЖЕ строку, что стояла литералом, — иначе
    /// вариации перепекли бы принятые файлы.
    const char* w(float base) {
        const float v = std::clamp(base + shift, lo, hi);
        char buf[32];
        std::snprintf(buf, sizeof(buf), "%g", static_cast<double>(v));
        said.emplace_back(buf);
        return said.back().c_str();
    }
    const char* shutters() const { return kept ? "1" : "0"; }
    const char* plinth() const { return kept ? "1" : "0"; }
};

// ---------------------------------------------------------------------------
// ПОВТОР 1: одноэтажный сруб 6x4 под соломой (демка: log).
// ---------------------------------------------------------------------------
void forge_log(const char* file) {
    Forge f;
    const float W = 6.0f;
    const float D = 4.0f;
    const float H = 3.25f; // этаж демки, 13u
    // Пол СТАРЫЙ: неровные доски, щели, обломки (wear в паркете).
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, D);
        const VertexId c = f.v(W, 0.06f, D);
        const VertexId d = f.v(W, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "3"}, {"wear", "0.7"}});
    }
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, H, "0");
    // Стены — тёсаный брус (сруб читается материалом). Южная — со сквозным
    // дверным проёмом; окон нет, как у оригинала (одно жильё, один свет).
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params logwall = {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "0"},
                      {"tone", "1"}, {"fill", "4"}, {"logends", "1"},
                      {"wear", "0.55"}};
    (void)f.wall(ne, nw, logwall); // север, лицо на север
    (void)f.wall(se, ne, logwall); // восток, лицо на восток
    (void)f.wall(nw, sw, logwall); // запад, лицо на запад
    (void)f.wall(sw, se,
                 {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "0"},
                  {"tone", "1"}, {"fill", "4"}, {"doors", "1"}, {"logends", "1"},
                  {"porch", "1"}, {"wear", "0.35"}});
    f.door_leaf(W * 0.5f, 0.0f, D);
    f.gable_roof(0.0f, 0.0f, W, D, H, 1.4f, "6", "1", "", "0.35"); // солома
    f.save(file);
}

// ---------------------------------------------------------------------------
// ПОВТОР 2: полутораэтажный фахверк 9x4 (демка: frame) — марш на антресоль.
// ---------------------------------------------------------------------------
void forge_frame(const char* file) {
    Forge f;
    const float W = 9.0f;
    const float D = 4.0f;
    const float H = 3.25f;
    parquet_floor(f, 0.0f, 0.0f, W, D, 0.06f);
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, H, "0");
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params clad = {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                   {"tone", "0"}, {"clad", "1"}, {"windows", "1"},
                   {"shutters", "1"}, {"plinth", "1"}, {"wear", "0.25"}};
    (void)f.wall(ne, nw,
                 {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                  {"tone", "0"}, {"clad", "1"}, {"windows", "2"},
                  {"shutters", "1"}, {"plinth", "1"}, {"wear", "0.25"}});
    (void)f.wall(se, ne, clad);
    (void)f.wall(nw, sw, clad);
    // ЮЖНЫЙ ФАСАД ИЗ ТРЁХ СТЕН: раскладка умеет один вид проёма на стену
    // (дверь берёт верх) — окна получают СВОИ пролёты по бокам двери.
    {
        const VertexId s1 = f.v(3.2f, 0.0f, D);
        const VertexId s2 = f.v(5.8f, 0.0f, D);
        (void)f.wall(sw, s1,
                     {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                      {"tone", "0"}, {"clad", "1"}, {"windows", "1"},
                      {"shutters", "1"}, {"plinth", "1"}, {"wear", "0.25"}});
        (void)f.wall(s1, s2,
                     {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                      {"tone", "0"}, {"clad", "1"}, {"doors", "1"}, {"porch", "1"},
                      {"wear", "0.25"}});
        (void)f.wall(s2, se,
                     {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                      {"tone", "0"}, {"clad", "1"}, {"windows", "1"},
                      {"shutters", "1"}, {"plinth", "1"}, {"wear", "0.25"}});
    }
    f.door_leaf(W * 0.5f, 0.0f, D);
    // АНТРЕСОЛЬ над западной половиной + крутой марш вдоль северной стены,
    // как у демки (b0ce19e: этаж за собственную длину). Этаж 3.25 не кратен
    // подъёму 0.175 в целых — марш считает сам, шаг остаётся ровным.
    {
        const VertexId a = f.v(0.0f, H, 0.0f);
        const VertexId b = f.v(0.0f, H, D);
        const VertexId c = f.v(4.0f, H, D);
        const VertexId d = f.v(4.0f, H, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"}});
    }
    // Марш: низ у (7.6, 0), верх у (4.2, 3.25) — движение на запад вдоль
    // северной стены, ширина 1.2.
    //
    // ИЗВЕСТНАЯ НАХОДКА СУДЬИ ЛЕСТНИЦ (27.08, не чинится сознательно): просвет
    // 0.584 м над 18-й ступенью — стропильная балка кровли (x=4.5) режет марш
    // по всей его длине, диагональ идёт вдоль z и уходит от любого сдвига
    // створа. Это ДЕМО-СТЕНД каркаса (полка demo, ни в одном городе не стоит,
    // карта houses/demo снята из меню 25.08); переставлять антресоль ради
    // стенда значит переделывать демонстрацию, которую он показывает. У
    // БОЕВЫХ рецептов находок судьи ноль — приёмка меряется по ним.
    {
        const VertexId a = f.v(7.6f, 0.0f, 0.3f);
        const VertexId b = f.v(7.6f, 0.0f, 1.5f);
        const VertexId c = f.v(4.0f, H, 1.5f);
        const VertexId d = f.v(4.0f, H, 0.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"fill", "6"}, {"mat", "1"},
                         {"tone", "1"}});
    }
    f.gable_roof(0.0f, 0.0f, W, D, H, 1.6f, "1", "1", "7", "0.25"); // дранка рядами
    f.save(file);
}

// ---------------------------------------------------------------------------
// ПОВТОР 3: двухэтажный камень+фахверк 9x4 под черепицей (демка: stone).
// ---------------------------------------------------------------------------
void forge_stone(const char* file) {
    Forge f;
    const float W = 9.0f;
    const float D = 4.0f;
    const float H = 3.25f;
    parquet_floor(f, 0.0f, 0.0f, W, D, 0.06f);
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, 2.0f * H, "3");
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    // НИЗ — каменные блоки (fill=3), окна ставит кладка.
    Params stone = {{"height", "3.25"}, {"thickness", "0.3"}, {"mat", "3"},
                    {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                    {"wear", "0.5"}, {"plinth", "1"}};
    (void)f.wall(ne, nw, stone);
    (void)f.wall(se, ne, stone);
    (void)f.wall(nw, sw, stone);
    {
        const VertexId s1 = f.v(3.2f, 0.0f, D);
        const VertexId s2 = f.v(5.8f, 0.0f, D);
        (void)f.wall(sw, s1,
                     {{"height", "3.25"}, {"thickness", "0.3"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                      {"wear", "0.7"}, {"plinth", "1"}});
        (void)f.wall(s1, s2,
                     {{"height", "3.25"}, {"thickness", "0.3"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"doors", "1"},
                      {"wear", "0.7"}, {"porch", "1"}});
        (void)f.wall(s2, se,
                     {{"height", "3.25"}, {"thickness", "0.3"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                      {"wear", "0.7"}, {"plinth", "1"}});
    }
    f.door_leaf(W * 0.5f, 0.0f, D);
    // ВЕРХ — фахверк по штукатурке, окна чаще.
    const VertexId nw2 = f.v(0.0f, H, 0.0f);
    const VertexId ne2 = f.v(W, H, 0.0f);
    const VertexId se2 = f.v(W, H, D);
    const VertexId sw2 = f.v(0.0f, H, D);
    Params upper = {{"height", "3.25"}, {"thickness", "0.25"}, {"mat", "5"},
                    {"tone", "0"}, {"clad", "1"}, {"windows", "2"},
                    {"shutters", "1"}, {"wear", "0.3"}};
    (void)f.wall(ne2, nw2, upper);
    (void)f.wall(se2, ne2, upper);
    (void)f.wall(nw2, sw2, upper);
    (void)f.wall(sw2, se2, upper);
    // ЭТАЖНЫЙ ПОЛ с проёмом над маршем: настил из двух кусков (запад 0..6.8
    // без полосы марша, восток за верхом марша).
    {
        const VertexId a = f.v(0.0f, H, 0.0f);
        const VertexId b = f.v(0.0f, H, D);
        const VertexId c = f.v(4.4f, H, D);
        const VertexId d = f.v(4.4f, H, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"}});
    }
    {
        const VertexId a = f.v(4.4f, H, 1.8f);
        const VertexId b = f.v(4.4f, H, D);
        const VertexId c = f.v(9.0f, H, D);
        const VertexId d = f.v(9.0f, H, 1.8f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"}});
    }
    // Марш вдоль северной стены на восток: низ (4.6), верх (8.2).
    {
        const VertexId a = f.v(4.6f, 0.0f, 0.3f);
        const VertexId b = f.v(4.6f, 0.0f, 1.5f);
        const VertexId c = f.v(8.2f, H, 1.5f);
        const VertexId d = f.v(8.2f, H, 0.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"fill", "6"}, {"mat", "1"},
                         {"tone", "1"}});
    }
    f.gable_roof(0.0f, 0.0f, W, D, 2.0f * H, 1.6f, "4", "1", "8", "0.4"); // черепица рядами
    f.save(file);
}

// ---------------------------------------------------------------------------
// НОВЫЙ 1: Г-образный одноэтажный. Западное крыло 4x8 + северный бар 8x4,
// две комнаты, межкомнатная дверь, вход с юга, два ската по крыльям.
// ---------------------------------------------------------------------------
void forge_l_house(const char* file) {
    Forge f;
    const float H = 2.8f; // 16 ступеней по 0.175 ровно — и человеку не давит
    // ПОЛ — Г-контур (против часовой сверху), паркет по всей площади.
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, 8.0f);
        const VertexId c = f.v(4.0f, 0.06f, 8.0f);
        const VertexId d = f.v(4.0f, 0.06f, 4.0f);
        const VertexId e = f.v(8.0f, 0.06f, 4.0f);
        const VertexId g2 = f.v(8.0f, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d, e, g2},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}});
    }
    f.frame_posts(0.0f, 0.0f, 8.0f, 4.0f, 0.0f, H, "0");
    // Наружные стены — фахверк с окнами; южная стена западного крыла — вход.
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(8.0f, 0.0f, 0.0f);
    const VertexId e_out = f.v(8.0f, 0.0f, 4.0f);
    const VertexId inner = f.v(4.0f, 0.0f, 4.0f);
    const VertexId c_s = f.v(4.0f, 0.0f, 8.0f);
    const VertexId sw_s = f.v(0.0f, 0.0f, 8.0f);
    Params clad = {{"height", "2.8"}, {"thickness", "0.25"}, {"mat", "5"},
                   {"tone", "0"}, {"clad", "1"}, {"windows", "1"},
                   {"shutters", "1"}, {"plinth", "1"}, {"wear", "0.3"}};
    (void)f.wall(ne, nw, clad);           // север, лицо на север
    (void)f.wall(e_out, ne, clad);        // восточный торец бара, лицо на восток
    (void)f.wall(inner, e_out, clad);     // южная стена бара, лицо в угол Г
    (void)f.wall(c_s, inner, clad);       // восточная стена крыла, лицо на восток
    (void)f.wall(nw, sw_s, clad);         // западная стена крыла, лицо на запад
    (void)f.wall(sw_s, c_s,
                 {{"height", "2.8"}, {"thickness", "0.25"}, {"mat", "5"},
                  {"tone", "0"}, {"clad", "1"}, {"doors", "1"}, {"porch", "1"},
                  {"plinth", "1"}, {"wear", "0.3"}});
    f.door_leaf(2.0f, 0.0f, 8.0f);
    // МЕЖКОМНАТНАЯ стена с рабочей дверью: z=4, x 0..4 (крыло от бара).
    {
        const VertexId a = f.v(0.0f, 0.0f, 4.0f);
        (void)f.wall(a, inner,
                     {{"height", "2.8"}, {"thickness", "0.15"}, {"mat", "5"},
                      {"tone", "0"}, {"doors", "1"}});
        f.door_leaf(2.0f, 0.0f, 4.0f);
    }
    // КРЫША ИЗ ДВУХ СКАТОВ-НАВЕСОВ по крыльям: над крылом скат на юг, над
    // баром — на восток; выше в углу Г они перекрываются. Нижняя кромка
    // ЛЕЖИТ на стене (2.75 при стене 2.8 — заходит), высокая сторона
    // зашита клином-фронтоном: приёмка кадров 20.08 увидела над стенами
    // сквозное небо.
    {
        const VertexId a = f.v(-0.3f, H + 0.9f, -0.3f);
        const VertexId b = f.v(4.3f, H + 0.9f, -0.3f);
        const VertexId c = f.v(4.3f, H - 0.05f, 8.3f);
        const VertexId d = f.v(-0.3f, H - 0.05f, 8.3f);
        {
            const ElementId r1 = f.contour(
                {a, b, c, d},
                {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "2"}});
            (void)f.g.set_param(r1, "fill", "7");
            (void)f.g.set_param(r1, "wear", "0.3");
        }
    }
    {
        const VertexId a = f.v(3.7f, H + 0.9f, -0.3f);
        const VertexId b = f.v(3.7f, H + 0.9f, 4.3f);
        const VertexId c = f.v(8.3f, H - 0.05f, 4.3f);
        const VertexId d = f.v(8.3f, H - 0.05f, -0.3f);
        {
            const ElementId r2 = f.contour(
                {a, b, c, d},
                {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "2"}});
            (void)f.g.set_param(r2, "fill", "7");
            (void)f.g.set_param(r2, "wear", "0.3");
        }
    }
    // КЛИНЬЯ-ФРОНТОНЫ под высокими кромками скатов: север крыла (обе
    // стороны z=0) и запад бара (x=0), плюс восточный торец крыла над баром.
    const auto wedge = [&](float x0, float z0, float x1, float z1, float y_lo,
                           float y_hi_a, float y_hi_b) {
        const VertexId a = f.v(x0, y_lo, z0);
        const VertexId b = f.v(x1, y_lo, z1);
        const VertexId c = f.v(x1, y_hi_b, z1);
        const VertexId d = f.v(x0, y_hi_a, z0);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "5"}, {"tone", "0"},
                         {"clad", "1"}});
    };
    wedge(0.0f, 0.0f, 4.0f, 0.0f, H - 0.05f, H + 0.85f, H + 0.85f); // север крыла
    wedge(0.0f, 0.0f, 0.0f, 4.0f, H - 0.05f, H + 0.85f, H + 0.1f);  // запад бара
    wedge(4.0f, 0.0f, 4.0f, 4.0f, H - 0.05f, H + 0.85f, H + 0.1f);  // угол Г изнутри
    f.save(file);
}

// ---------------------------------------------------------------------------
// НОВЫЙ 2: П-образный двухэтажный. Крылья 4x10 запад/восток, бар 14x4 на
// севере, двор 6x6 открыт на юг. Два марша (по крылу), раздельные комнаты,
// двери между всеми, дверь из бара во двор, крыша из ЧЕТЫРЁХ частей-навесов.
// ---------------------------------------------------------------------------
void forge_u_house(const char* file) {
    Forge f;
    const float H = 2.8f;
    const float H2 = 5.6f;
    // ПОЛ первого этажа — П-контур против часовой сверху.
    {
        const VertexId p1 = f.v(0.0f, 0.06f, 0.0f);
        const VertexId p2 = f.v(0.0f, 0.06f, 10.0f);
        const VertexId p3 = f.v(4.0f, 0.06f, 10.0f);
        const VertexId p4 = f.v(4.0f, 0.06f, 4.0f);
        const VertexId p5 = f.v(10.0f, 0.06f, 4.0f);
        const VertexId p6 = f.v(10.0f, 0.06f, 10.0f);
        const VertexId p7 = f.v(14.0f, 0.06f, 10.0f);
        const VertexId p8 = f.v(14.0f, 0.06f, 0.0f);
        (void)f.contour({p1, p2, p3, p4, p5, p6, p7, p8},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}});
    }
    f.frame_posts(0.0f, 0.0f, 14.0f, 10.0f, 0.0f, H2, "3");
    // ---------- наружные стены, низ камень / верх фахверк ----------
    const auto two_storey_run = [&](float ax, float az, float bx, float bz,
                                    bool low_door, bool up_windows) {
        const VertexId a0 = f.v(ax, 0.0f, az);
        const VertexId b0 = f.v(bx, 0.0f, bz);
        if (low_door) {
            (void)f.wall(a0, b0,
                         {{"height", "2.8"}, {"thickness", "0.3"}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"doors", "1"},
                          {"porch", "1"}, {"wear", "0.4"}});
        } else {
            (void)f.wall(a0, b0,
                         {{"height", "2.8"}, {"thickness", "0.3"}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                          {"plinth", "1"}, {"wear", "0.4"}});
        }
        const VertexId a1 = f.v(ax, H, az);
        const VertexId b1 = f.v(bx, H, bz);
        (void)f.wall(a1, b1,
                     up_windows
                         ? Params{{"height", "2.8"}, {"thickness", "0.25"},
                                  {"mat", "5"}, {"tone", "0"}, {"clad", "1"},
                                  {"windows", "2"}, {"shutters", "1"},
                                  {"wear", "0.3"}}
                         : Params{{"height", "2.8"}, {"thickness", "0.25"},
                                  {"mat", "5"}, {"tone", "0"}, {"clad", "1"},
                                  {"wear", "0.3"}});
    };
    two_storey_run(14.0f, 0.0f, 0.0f, 0.0f, false, true);   // север, лицо на север
    two_storey_run(0.0f, 0.0f, 0.0f, 10.0f, false, true);   // запад, лицо на запад
    two_storey_run(14.0f, 10.0f, 14.0f, 0.0f, false, true); // восток, лицо на восток
    // Южные торцы крыльев: западный — ГЛАВНЫЙ ВХОД, восточный — второй выход.
    two_storey_run(0.0f, 10.0f, 4.0f, 10.0f, true, false);
    f.door_leaf(2.0f, 0.0f, 10.0f);
    two_storey_run(10.0f, 10.0f, 14.0f, 10.0f, true, false);
    f.door_leaf(12.0f, 0.0f, 10.0f);
    // Дворовые фасады крыльев (x=4 и x=10, z 4..10) — окна на двор,
    // лицо В ДВОР (запад крыла смотрит +X, восток -X).
    two_storey_run(4.0f, 10.0f, 4.0f, 4.0f, false, true);
    two_storey_run(10.0f, 4.0f, 10.0f, 10.0f, false, true);
    // Южная стена бара (фасад во двор): ДВЕРЬ ВО ДВОР на первом этаже.
    two_storey_run(4.0f, 4.0f, 10.0f, 4.0f, true, true);
    f.door_leaf(7.0f, 0.0f, 4.0f);
    // ---------- перегородки крыло-бар с рабочими дверями, оба этажа ----------
    const auto partition = [&](float x0, float x1, float y) {
        const VertexId a = f.v(x0, y, 4.0f);
        const VertexId b = f.v(x1, y, 4.0f);
        (void)f.wall(a, b,
                     {{"height", "2.8"}, {"thickness", "0.15"}, {"mat", "5"},
                      {"tone", "0"}, {"doors", "1"}});
        f.door_leaf((x0 + x1) * 0.5f, y, 4.0f);
    };
    partition(0.0f, 4.0f, 0.0f);   // запад, 1 этаж
    partition(10.0f, 14.0f, 0.0f); // восток, 1 этаж
    partition(0.0f, 4.0f, H);      // запад, 2 этаж
    partition(10.0f, 14.0f, H);    // восток, 2 этаж
    // ---------- полы второго этажа: бар + балконы крыльев + площадки ----------
    const auto deck = [&](float x0, float z0, float x1, float z1) {
        const VertexId a = f.v(x0, H, z0);
        const VertexId b = f.v(x0, H, z1);
        const VertexId c = f.v(x1, H, z1);
        const VertexId d = f.v(x1, H, z0);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"}});
    };
    // Бар: потолок первого этажа несёт БАЛКИ (каталог интерьеров, топ-1).
    {
        const VertexId a = f.v(0.0f, H, 0.0f);
        const VertexId b = f.v(0.0f, H, 4.0f);
        const VertexId c = f.v(14.0f, H, 4.0f);
        const VertexId d = f.v(14.0f, H, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    deck(2.0f, 4.0f, 4.0f, 10.0f);  // комната-балкон западного крыла
    deck(10.0f, 4.0f, 12.0f, 10.0f); // комната-балкон восточного крыла
    deck(0.0f, 4.0f, 2.0f, 5.0f);   // площадка над западным маршем
    deck(12.0f, 4.0f, 14.0f, 5.0f); // площадка над восточным маршем
    // ---------- два марша вдоль наружных стен крыльев ----------
    // Запад: низ у z=9.6, верх у z=5.0 (подъём на север), ширина 1.4.
    {
        const VertexId a = f.v(0.3f, 0.0f, 9.6f);
        const VertexId b = f.v(1.7f, 0.0f, 9.6f);
        const VertexId c = f.v(1.7f, H, 5.0f);
        const VertexId d = f.v(0.3f, H, 5.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"fill", "6"}, {"mat", "1"},
                         {"tone", "1"}, {"open", "1"}, {"wear", "0.3"}});
    }
    // Восток: зеркально.
    {
        const VertexId a = f.v(12.3f, 0.0f, 9.6f);
        const VertexId b = f.v(13.7f, 0.0f, 9.6f);
        const VertexId c = f.v(13.7f, H, 5.0f);
        const VertexId d = f.v(12.3f, H, 5.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"fill", "6"}, {"mat", "1"},
                         {"tone", "1"}, {"open", "1"}, {"wear", "0.3"}});
    }
    // ---------- КРЫША ИЗ ЧЕТЫРЁХ ЧАСТЕЙ-НАВЕСОВ (заказ дословно) ----------
    // Крылья — скаты на юг; бар — двускат из двух половин (на север и во
    // двор). Черепица.
    const char* tile = "4";
    {
        const VertexId a = f.v(-0.3f, H2 + 1.0f, 3.7f);
        const VertexId b = f.v(4.3f, H2 + 1.0f, 3.7f);
        const VertexId c = f.v(4.3f, H2 - 0.1f, 10.3f);
        const VertexId d = f.v(-0.3f, H2 - 0.1f, 10.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", tile}, {"tone", "1"}, {"fill", "8"}, {"wear", "0.4"}});
    }
    {
        const VertexId a = f.v(9.7f, H2 + 1.0f, 3.7f);
        const VertexId b = f.v(14.3f, H2 + 1.0f, 3.7f);
        const VertexId c = f.v(14.3f, H2 - 0.1f, 10.3f);
        const VertexId d = f.v(9.7f, H2 - 0.1f, 10.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", tile}, {"tone", "1"}, {"fill", "8"}, {"wear", "0.4"}});
    }
    {
        const VertexId a = f.v(-0.3f, H2 + 1.0f, 2.0f);
        const VertexId b = f.v(14.3f, H2 + 1.0f, 2.0f);
        const VertexId c = f.v(14.3f, H2 - 0.1f, -0.3f);
        const VertexId d = f.v(-0.3f, H2 - 0.1f, -0.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", tile}, {"tone", "1"}, {"fill", "8"}, {"wear", "0.4"}});
    }
    {
        const VertexId a = f.v(14.3f, H2 + 1.0f, 2.0f);
        const VertexId b = f.v(-0.3f, H2 + 1.0f, 2.0f);
        const VertexId c = f.v(-0.3f, H2 - 0.1f, 4.3f);
        const VertexId d = f.v(14.3f, H2 - 0.1f, 4.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", tile}, {"tone", "1"}, {"fill", "8"}, {"wear", "0.4"}});
    }
    // КОНЁК-БРЕВНО двускату бара и СТРОПИЛЬНЫЕ ТОРЦЫ по карнизам крыльев —
    // приёмка кадров: «ни одного стропильного торца, конька нет».
    (void)f.beam(f.v(-0.5f, H2 + 1.02f, 2.0f), f.v(14.5f, H2 + 1.02f, 2.0f),
                 {{"radius", "0.14"}, {"mat", "0"}, {"tone", "2"}});
    // Торцы — из-под НИЗКИХ (южных) кромок скатов крыльев, наружу и чуть
    // вниз; прежние палки у верхних кромок торчали В НЕБО (приёмка кадров).
    for (int k = 0; k <= 3; ++k) {
        const float x_w = 0.4f + 3.2f * static_cast<float>(k) / 3.0f;
        (void)f.beam(f.v(x_w, H2 + 0.05f, 9.6f), f.v(x_w, H2 - 0.35f, 10.75f),
                     {{"radius", "0.07"}, {"mat", "0"}, {"tone", "2"}});
        const float x_e = 10.4f + 3.2f * static_cast<float>(k) / 3.0f;
        (void)f.beam(f.v(x_e, H2 + 0.05f, 9.6f), f.v(x_e, H2 - 0.35f, 10.75f),
                     {{"radius", "0.07"}, {"mat", "0"}, {"tone", "2"}});
    }
    f.save(file);
}


// ===========================================================================
// ГОРОДСКОЙ НАБОР (Вайтран, 21.08): стены, башни, ворота, замок, залы,
// уличный слой. Числа — docs/WHITERUN_RESEARCH.md (оценки помечены там).
// ===========================================================================

/// Простая каменная стена-цепочка с зубцами и боевым ходом.
static void crenellated_run(Forge& f, glm::vec3 a, glm::vec3 b, float h,
                            float th, const char* wear) {
    const VertexId va = f.v(a.x, a.y, a.z);
    const VertexId vb = f.v(b.x, b.y, b.z);
    char hbuf[16];
    std::snprintf(hbuf, sizeof(hbuf), "%.2f", h);
    char tbuf[16];
    std::snprintf(tbuf, sizeof(tbuf), "%.2f", th);
    (void)f.wall(va, vb,
                 {{"height", hbuf}, {"thickness", tbuf}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"wear", wear},
                  {"unsupported", "1"}});
    // Боевой ход: полка изнутри на 2/3 высоты (лицо стены — наружу, изнанка
    // внутрь города; полка кладётся к изнанке).
    //
    // ПОЛКА НАЧИНАЛАСЬ В 0.55 М ОТ ЛИЦА СТЕНЫ — ТО ЕСТЬ В ВОЗДУХЕ. Судья
    // связности нашёл её отдельным островом на ВСЕХ семнадцати прогонах стены и
    // на воротах: настил боевого хода не касался стены ничем, и по нему нельзя
    // было идти — между стеной и доской полметра пустоты, ровно в рост
    // проваливающейся капсулы. Ближний край уведён НА ОСЬ стены (правило
    // HouseMesh.h: «всё крепится к осям, стена входит в тело»), дальний остался
    // там же — ходибельная полка стала 1.65 вместо 1.10 и НАЧИНАЕТСЯ ОТ СТЕНЫ.
    const glm::vec3 d = glm::normalize(b - a);
    const glm::vec3 inw = glm::normalize(glm::cross(glm::vec3{0, 1, 0}, d));
    const float walk_out = th * 0.5f + 1.65f;  // дальний край, прежний
    const glm::vec3 wa = a + glm::vec3{0, h - 1.7f, 0};
    const glm::vec3 wb = b + glm::vec3{0, h - 1.7f, 0};
    {
        const VertexId p1 = f.v(wa.x, wa.y, wa.z);
        const VertexId p2 = f.v(wa.x + inw.x * walk_out, wa.y, wa.z + inw.z * walk_out);
        const VertexId p3 = f.v(wb.x + inw.x * walk_out, wb.y, wb.z + inw.z * walk_out);
        const VertexId p4 = f.v(wb.x, wb.y, wb.z);
        (void)f.contour({p1, p2, p3, p4},
                        {{"thickness", "0.2"}, {"mat", "1"}, {"tone", "2"},
                         {"unsupported", "1"}});
    }
    // КРОНШТЕЙНЫ ПОД ПОЛКОЙ. Полка вылетает из стены на 1.65 м, и «держится
    // врезкой в стену» — это ответ судьи, а не глаза: консоль такой длины без
    // подкосов читается доской, приклеенной к камню. Подкос идёт ОТ ОСИ стены
    // (та же врезка) под изнанку дальнего края, шаг ~2.4 м.
    {
        const float run = glm::length(b - a);
        const int brackets = std::max(2, static_cast<int>(run / 2.4f));
        for (int k = 0; k <= brackets; ++k) {
            const float u = run * static_cast<float>(k) / static_cast<float>(brackets);
            const glm::vec3 root = a + d * u + glm::vec3{0, h - 1.7f - 0.75f, 0};
            const glm::vec3 tip = a + d * u + inw * (walk_out - 0.25f)
                                + glm::vec3{0, h - 1.7f - 0.14f, 0};
            (void)f.beam(f.v(root.x, root.y, root.z), f.v(tip.x, tip.y, tip.z),
                         {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                          {"tone", "2"}, {"wear", wear}});
        }
    }
    // Зубцы: короткие стенки поверх кромки с шагом ~2 м.
    const float len = glm::length(b - a);
    const int teeth = std::max(2, static_cast<int>(len / 2.2f));
    for (int t = 0; t < teeth; ++t) {
        const float u0 = (len / teeth) * (static_cast<float>(t) + 0.2f);
        const float u1 = u0 + 0.9f;
        const glm::vec3 ta = a + d * u0 + glm::vec3{0, h, 0};
        const glm::vec3 tb = a + d * std::min(u1, len) + glm::vec3{0, h, 0};
        const VertexId q1 = f.v(ta.x, ta.y, ta.z);
        const VertexId q2 = f.v(tb.x, tb.y, tb.z);
        (void)f.wall(q1, q2,
                     {{"height", "0.8"}, {"thickness", "0.5"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", wear},
                      {"unsupported", "1"}});
    }
}

/// БАШНЯ смотровая: квадратный ствол, площадка, парапет с зубцами, шатёр.
static void forge_tower(const char* file) {
    Forge f;
    const float S = 3.6f;
    const float H = 9.0f;
    const auto wall_q = [&](float x0, float z0, float x1, float z1) {
        const VertexId a = f.v(x0, 0.0f, z0);
        const VertexId b = f.v(x1, 0.0f, z1);
        (void)f.wall(a, b,
                     {{"height", "9"}, {"thickness", "0.6"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"}});
    };
    wall_q(S, 0.0f, 0.0f, 0.0f);  // север, лицо наружу
    wall_q(S, S, S, 0.0f);        // восток
    wall_q(0.0f, 0.0f, 0.0f, S);  // запад
    wall_q(0.0f, S, S, S);        // юг
    // Площадка и парапет.
    //
    // ВЕРХ ПЛОЩАДКИ — H + ПОЛТОЛЩИНЫ, А НЕ H + ТОЛЩИНА (заказ владельца 23.08:
    // «башни кривые — их верхние яруса не присоединены к башне»). Контур растёт
    // от своей высоты В ОБЕ СТОРОНЫ (HousePlate.cpp: «толщина симметрична»),
    // поэтому плита на H толщиной 0.25 кончается на H + 0.125, а парапет
    // ставился на H + 0.25 — ровно полтолщины воздуха под каждым зубцом. Судья
    // связности мерил 0.125 м, и это ровно 0.25/2.
    const float PLATE_T = 0.25f;
    {
        const VertexId a = f.v(-0.3f, H, -0.3f);
        const VertexId b = f.v(-0.3f, H, S + 0.3f);
        const VertexId c = f.v(S + 0.3f, H, S + 0.3f);
        const VertexId d = f.v(S + 0.3f, H, -0.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.25"}, {"mat", "3"}, {"tone", "1"},
                         {"unsupported", "1"}});
    }
    const float P = H + PLATE_T * 0.5f;
    // ЗУБЕЦ СТОИТ НА ПЛИТЕ ЦЕЛИКОМ. Ось стены — середина её толщины, значит
    // ось на кромке плиты вывешивала наружу половину зубца (0.175 из 0.35).
    // Ось отодвинута внутрь на полтолщины: наружное лицо зубца встаёт заподлицо
    // с лицом ствола, как ему и положено у крепости.
    const float MERLON_T = 0.35f;
    const float mi = MERLON_T * 0.5f;
    for (int side = 0; side < 4; ++side) {
        for (int t = 0; t < 3; ++t) {
            const float u0 = -0.3f + (S + 0.6f) * (0.08f + 0.3f * t);
            const float u1 = u0 + 0.75f;
            glm::vec3 pa, pb;
            if (side == 0) { pa = {u1, P, -0.3f + mi}; pb = {u0, P, -0.3f + mi}; }
            else if (side == 1) { pa = {S + 0.3f - mi, P, u1}; pb = {S + 0.3f - mi, P, u0}; }
            else if (side == 2) { pa = {u0, P, S + 0.3f - mi}; pb = {u1, P, S + 0.3f - mi}; }
            else { pa = {-0.3f + mi, P, u0}; pb = {-0.3f + mi, P, u1}; }
            const VertexId q1 = f.v(pa.x, pa.y, pa.z);
            const VertexId q2 = f.v(pb.x, pb.y, pb.z);
            (void)f.wall(q1, q2,
                         {{"height", "1.0"}, {"thickness", "0.35"}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"},
                          {"unsupported", "1"}});
        }
    }
    // Шатёр: четыре треугольных ската к центру, дранка.
    //
    // ШАТЁР ДЕРЖАТ ЧЕТЫРЕ СТОЙКИ С ПЛОЩАДКИ, А НЕ ЗУБЦЫ. Прежде вся кровля
    // висела на касании со срезом зубца — а зубцы отстоят от карниза шатра, и
    // касание выходило волосяным (пересчёт: 4.5 мм на скате 39°). Стойка в
    // каждом углу площадки — это и есть то, чем ярус «садится на ствол».
    const float R = P + 0.95f;
    const float apex = R + 1.8f;
    const glm::vec3 cx{S * 0.5f, apex, S * 0.5f};
    const glm::vec3 corners[4] = {{-0.4f, R, -0.4f}, {S + 0.4f, R, -0.4f},
                                  {S + 0.4f, R, S + 0.4f}, {-0.4f, R, S + 0.4f}};
    for (int k = 0; k < 4; ++k) {
        const glm::vec3 c1 = corners[k];
        const glm::vec3 c2 = corners[(k + 1) % 4];
        const VertexId a = f.v(c1.x, c1.y, c1.z);
        const VertexId b = f.v(c2.x, c2.y, c2.z);
        const VertexId t = f.v(cx.x, cx.y, cx.z);
        const ElementId s = f.contour({a, b, t},
                                      {{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}, {"unsupported", "1"}});
        (void)f.g.set_param(s, "roof", "1");
    }
    // ВЫСОТА ШАТРА В ТОЧКЕ ВЫВЕДЕНА, А НЕ ПОДОБРАНА: у пирамиды с квадратным
    // основанием полупролёта B и подъёмом rise поверхность идёт
    // y = R + rise * (1 − max(|dx|,|dz|) / B). Стойка кончается на 0.05 ВЫШЕ
    // этой поверхности — тогда она входит в настил телом, а не касается его
    // краем, и «ярус садится», а не «ярус рядом».
    const float B = S * 0.5f + 0.4f;
    for (const float px : {0.5f, S - 0.5f}) {
        for (const float pz : {0.5f, S - 0.5f}) {
            const float m = std::max(std::abs(px - S * 0.5f), std::abs(pz - S * 0.5f));
            const float top = R + 1.8f * (1.0f - m / B) + 0.05f;
            (void)f.beam(f.v(px, P, pz), f.v(px, top, pz),
                         {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                          {"tone", "2"}});
        }
    }
    // Дверь внизу южной стены.
    f.door_leaf(S * 0.5f, 0.0f, S);
    f.save(file);
}

/// ДОНЖОН: та же башня, но 14 м — замковая вертикаль (приёмка №2:
/// «замок не доминирует, сарай»). Отдельный рецепт, а не параметр: полка
/// построек живёт файлами.
static void forge_tower_tall(const char* file) {
    Forge f;
    const float S = 4.4f;
    const float H = 14.0f;
    const auto wall_q = [&](float x0, float z0, float x1, float z1) {
        const VertexId a = f.v(x0, 0.0f, z0);
        const VertexId b = f.v(x1, 0.0f, z1);
        (void)f.wall(a, b,
                     {{"height", "14"}, {"thickness", "0.7"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                      {"wear", "0.4"}});
    };
    wall_q(S, 0.0f, 0.0f, 0.0f);
    wall_q(S, S, S, 0.0f);
    wall_q(0.0f, 0.0f, 0.0f, S);
    wall_q(0.0f, S, S, S);
    {
        const VertexId a = f.v(-0.35f, H, -0.35f);
        const VertexId b = f.v(-0.35f, H, S + 0.35f);
        const VertexId c = f.v(S + 0.35f, H, S + 0.35f);
        const VertexId d = f.v(S + 0.35f, H, -0.35f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.3"}, {"mat", "3"}, {"tone", "1"},
                         {"unsupported", "1"}});
    }
    // ТА ЖЕ ПОПРАВКА, ЧТО У БАШНИ: верх плиты — H + полтолщины (контур растёт в
    // обе стороны), а не H + толщина. Здесь висело 0.15 м, и каждый из
    // двенадцати зубцов был СВОИМ островом — судья связности насчитал 13.
    const float P = H + 0.15f;
    const float mi = 0.2f;  // полтолщины зубца: ось внутрь, лицо заподлицо
    for (int side = 0; side < 4; ++side) {
        for (int t = 0; t < 3; ++t) {
            const float u0 = -0.35f + (S + 0.7f) * (0.08f + 0.3f * t);
            const float u1 = u0 + 0.9f;
            glm::vec3 pa, pb;
            if (side == 0) { pa = {u1, P, -0.35f + mi}; pb = {u0, P, -0.35f + mi}; }
            else if (side == 1) { pa = {S + 0.35f - mi, P, u1}; pb = {S + 0.35f - mi, P, u0}; }
            else if (side == 2) { pa = {u0, P, S + 0.35f - mi}; pb = {u1, P, S + 0.35f - mi}; }
            else { pa = {-0.35f + mi, P, u0}; pb = {-0.35f + mi, P, u1}; }
            const VertexId q1 = f.v(pa.x, pa.y, pa.z);
            const VertexId q2 = f.v(pb.x, pb.y, pb.z);
            (void)f.wall(q1, q2,
                         {{"height", "1.1"}, {"thickness", "0.4"}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"wear", "0.4"},
                          {"unsupported", "1"}});
        }
    }
    f.door_leaf(S * 0.5f, 0.0f, S);
    f.save(file);
}

/// КОЛЬЦО ГИЛДЕРГРИНА: приподнятый восьмигранный бортик вокруг ствола.
static void forge_tree_ring(const char* file) {
    Forge f;
    const float R = 2.6f;
    const int N = 8;
    for (int k = 0; k < N; ++k) {
        const float a0 = 2.0f * 3.14159265f * static_cast<float>(k) / N;
        const float a1 = 2.0f * 3.14159265f * static_cast<float>(k + 1) / N;
        const VertexId p = f.v(R + R * std::cos(a0), 0.0f, R + R * std::sin(a0));
        const VertexId q = f.v(R + R * std::cos(a1), 0.0f, R + R * std::sin(a1));
        (void)f.wall(p, q,
                     {{"height", "0.5"}, {"thickness", "0.35"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"}});
    }
    f.save(file);
}

/// СЕГМЕНТ СТЕНЫ 12 м с зубцами и боевым ходом.
static void forge_wall12(const char* file) {
    Forge f;
    crenellated_run(f, {0.0f, 0.0f, 0.0f}, {12.0f, 0.0f, 0.0f}, 6.0f, 1.2f, "0.55");
    f.save(file);
}

// ---------------------------------------------------------------------------
// ГНУТЫЙ ПРОГОН СТЕНЫ (замечание владельца №6: «стены расставлены криво, видны
// стыки, а должно гладко идти»).
//
// ДИАГНОЗ ЗАМЕРЕН, А НЕ ПРЕДПОЛОЖЕН. Кольцо чертежа — ломаная из 14 вершин
// (docs/WHITERUN_PLAN.json, периметр 316.2 м), повороты на изломах 13.9..49.2
// градуса. Отрезок цепочки — САМОСТОЯТЕЛЬНОЕ ТЕЛО (HouseWalls.cpp: «в углу
// стоит столб, стены упираются в его ось»), поэтому на изломе с поворотом θ по
// ЛИЦУ стены раскрывается клин шириной 2 * (th/2) * tan(θ/2): при th = 1.2 это
// 0.21 м на самом пологом изломе и 0.55 м на самом крутом. Четырнадцать
// вырезов от 21 до 55 см — это и есть «видны стыки»; вторая половина —
// нахлёст жёстких двенадцатиметровых кусков на гранях длиной 15-28 м.
//
// ЧТО ДЕЛАЕТ ЭТОТ РЕЦЕПТ. Полотно идёт ДУГОЙ: ось разбита на подхорды по
// ~2 м, каждая подхорда — свой отрезок стены, а клин между соседними ЗАКРЫТ
// НАХЛЁСТОМ (каждый внутренний торец выпущен на (th/2) * tan(δ) вдоль своего
// направления — ровно столько, чтобы наружные кромки соседей сошлись в одну
// точку). Остаточный вырез по лицу — НОЛЬ по построению.
//
// ТОРЦЫ — КАСАТЕЛЬНЫЕ, И ЭТО НЕ ОТДЕЛКА, А УСЛОВИЕ СМЫКАНИЯ. Ломаная взята
// ОПИСАННОЙ, а не вписанной: её звенья лежат на касательных к дуге, точки
// касания — концы поддуг. Крайние звенья поэтому выходят из САМИХ концов дуги
// вдоль касательной, и торец прогона строго перпендикулярен ей. Два прогона,
// поставленные с непрерывной касательной, смыкаются без уступа и без клина.
// Вписанная ломаная дала бы торец, повёрнутый на δ, и уступ на каждом стыке.
//
// СТРЕЛА — НАРУЖУ. Лицо стены смотрит в +Z (crenellated_run: изнанка и боевой
// ход — на inw = cross(Y, d) = -Z), город — в -Z. У выпуклого кольца наружная
// поверхность выпукла, значит центр кривизны лежит в городе, а середина дуги
// отходит от хорды НАРУЖУ, в +Z. Проверено на чертеже: во всех 14 изломах
// направление поворачивает в сторону inw, то есть знак один на всё кольцо —
// одного изгиба хватает, зеркальное семейство кольцу не нужно.
// ---------------------------------------------------------------------------

/// Наибольшая подхорда, метры. Больше — и отход ломаной от дуги растёт
/// квадратично; меньше — растёт число швов раскладки кладки (см. ниже).
static constexpr float WALL_ARC_SEG_MAX = 2.0f;
/// Наибольший отход ломаной от дуги, метры. Выведен, а не назначен: у самой
/// крутой дуги семейства (хорда 8, стрела 2, радиус 5.0) подхорда в 2 м даёт
/// отход 0.10 м — половину швов кладки; 0.05 держит подхорду в полосе
/// 1.4..2.0 м на всём семействе.
static constexpr float WALL_ARC_DEV_MAX = 0.05f;

/// ГЕОМЕТРИЯ ДУГИ ПРОГОНА. Хорда L идёт вдоль локального +X от (0,0,0) до
/// (L,0,0), стрела s отводит середину в +Z. s = 0 — прямая.
struct WallArcGeom {
    float L = 0.0f;
    float s = 0.0f;
    float R = 0.0f;        ///< радиус оси; 0 у прямой
    float alpha = 0.0f;    ///< половина центрального угла, радианы
    float arc_len = 0.0f;  ///< длина оси
    float cz = 0.0f;       ///< z центра кривизны (центр в (L/2, 0, cz))
    int n = 1;             ///< поддуг
    float delta = 0.0f;    ///< половина подугла = alpha / n
    float seg = 0.0f;      ///< длина средней подхорды

    /// Точка ОСИ на расстоянии u от начала (по дуге).
    [[nodiscard]] glm::vec3 at(float u) const {
        if (R <= 0.0f) {
            return {u, 0.0f, 0.0f};
        }
        const float t = -alpha + u / R;
        return {L * 0.5f + R * std::sin(t), 0.0f, cz + R * std::cos(t)};
    }
    /// Точка ОСИ по углу от середины дуги.
    [[nodiscard]] glm::vec3 at_angle(float t) const {
        if (R <= 0.0f) {
            return {L * 0.5f, 0.0f, 0.0f};
        }
        return {L * 0.5f + R * std::sin(t), 0.0f, cz + R * std::cos(t)};
    }
    /// Точка на смещённой внутрь (к центру) дуге радиуса R - d.
    [[nodiscard]] glm::vec3 inset(float t, float d) const {
        const float r = R - d;
        return {L * 0.5f + r * std::sin(t), 0.0f, cz + r * std::cos(t)};
    }
};

static WallArcGeom wall_arc_geom(float L, float s) {
    WallArcGeom a;
    a.L = L;
    a.s = s;
    if (s <= 1e-4f) {
        a.arc_len = L;
        a.n = 1;
        a.seg = L;
        return a;
    }
    a.R = (L * L * 0.25f + s * s) / (2.0f * s);
    a.alpha = std::asin(std::min(1.0f, (L * 0.5f) / a.R));
    a.cz = s - a.R;
    a.arc_len = 2.0f * a.alpha * a.R;
    // Подугол: не длиннее WALL_ARC_SEG_MAX по хорде и не дальше
    // WALL_ARC_DEV_MAX от дуги (описанная ломаная отходит на R*(sec d - 1)).
    const float d_len = std::atan(WALL_ARC_SEG_MAX / (2.0f * a.R));
    const float d_dev = std::acos(std::min(1.0f, a.R / (a.R + WALL_ARC_DEV_MAX)));
    const float d_max = std::min(d_len, d_dev);
    a.n = std::max(1, static_cast<int>(std::ceil(a.alpha / d_max - 1e-6f)));
    a.delta = a.alpha / static_cast<float>(a.n);
    a.seg = 2.0f * a.R * std::tan(a.delta);
    return a;
}

/// ПРОГОН СТЕНЫ ПО ДУГЕ: тот же профиль, что у crenellated_run (полотно с
/// зубцами и боевым ходом), но ось гнутая. Отдельная рука, а не флаг у
/// crenellated_run: та печёт принятые city-wall12 и city-gate, и правка её
/// тела погнала бы их на переприёмку ради чужой кривизны.
static void crenellated_arc(Forge& f, const WallArcGeom& a, float h, float th,
                            const char* wear) {
    const float half = th * 0.5f;
    char hbuf[16];
    std::snprintf(hbuf, sizeof(hbuf), "%.2f", h);
    char tbuf[16];
    std::snprintf(tbuf, sizeof(tbuf), "%.2f", th);

    // 1. ОСЬ — ОПИСАННАЯ ЛОМАНАЯ. Точки касания на углах t_k = -alpha + 2k*delta,
    // вершины между ними — на пересечении соседних касательных (угол
    // -alpha + (2k-1)*delta, радиус R / cos delta). Концы — САМИ концы дуги.
    std::vector<glm::vec3> axis;
    if (a.R <= 0.0f) {
        axis = {{0.0f, 0.0f, 0.0f}, {a.L, 0.0f, 0.0f}};
    } else {
        const float r_out = a.R / std::cos(a.delta);
        axis.push_back(a.at_angle(-a.alpha));
        for (int k = 1; k <= a.n; ++k) {
            const float t = -a.alpha + (2.0f * static_cast<float>(k) - 1.0f) * a.delta;
            axis.push_back({a.L * 0.5f + r_out * std::sin(t), 0.0f,
                            a.cz + r_out * std::cos(t)});
        }
        axis.push_back(a.at_angle(a.alpha));
    }

    // 2. ПОЛОТНО: каждое звено — свой отрезок стены. ВНУТРЕННИЕ торцы выпущены
    // на half*tan(delta) вдоль своего направления — на столько, чтобы наружные
    // кромки соседей сошлись в точку и клин закрылся нахлёстом (§7.0: нахлёст
    // в углу — норма). Крайние торцы НЕ выпускаются: ими прогон смыкается с
    // соседним прогоном, и лишний сантиметр там — уступ.
    // ДВЕРЬ ДОЗЫ: DFN_FORGE_ARC_NO_LAP=1 печёт те же дуги БЕЗ выпуска торцов.
    // Клин на каждом внутреннем стыке тогда открыт, и приёмка меряет ровно
    // ту величину, ради которой выпуск заведён, а не верит на слово.
    static const bool lap_on = std::getenv("DFN_FORGE_ARC_NO_LAP") == nullptr;
    const float grow = (a.R > 0.0f && lap_on) ? half * std::tan(a.delta) : 0.0f;
    for (std::size_t i = 0; i + 1 < axis.size(); ++i) {
        glm::vec3 p = axis[i];
        glm::vec3 q = axis[i + 1];
        const glm::vec3 d = glm::normalize(q - p);
        if (i > 0) { p -= d * grow; }
        if (i + 2 < axis.size()) { q += d * grow; }
        (void)f.wall(f.v(p.x, p.y, p.z), f.v(q.x, q.y, q.z),
                     {{"height", hbuf}, {"thickness", tbuf}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", wear},
                      {"unsupported", "1"}});
    }

    // 3. БОЕВОЙ ХОД: полка изнутри на h - 1.7 — числа crenellated_run. У дуги
    // это плоское кольцевое полотно: обход тот же (дальняя кромка от начала к
    // концу, ближняя обратно), иначе полка ляжет лицом вниз.
    //
    // БЛИЖНЯЯ КРОМКА — НА ОСИ, а не в 0.55 м от лица (та же поправка, что у
    // прямого прогона: судья связности назвал полку отдельным островом на всех
    // пятнадцати дугах). Дальняя кромка осталась ровно там же.
    {
        const float y = h - 1.7f;
        const float d_near = 0.0f;
        const float d_far = half + 1.65f;
        std::vector<VertexId> ring;
        if (a.R <= 0.0f) {
            ring = {f.v(0.0f, y, -d_far), f.v(a.L, y, -d_far),
                    f.v(a.L, y, -d_near), f.v(0.0f, y, -d_near)};
        } else {
            for (int k = 0; k <= a.n; ++k) {
                const float t = -a.alpha + 2.0f * static_cast<float>(k) * a.delta;
                const glm::vec3 p = a.inset(t, d_far);
                ring.push_back(f.v(p.x, y, p.z));
            }
            for (int k = a.n; k >= 0; --k) {
                const float t = -a.alpha + 2.0f * static_cast<float>(k) * a.delta;
                const glm::vec3 p = a.inset(t, d_near);
                ring.push_back(f.v(p.x, y, p.z));
            }
        }
        (void)f.contour(ring, {{"thickness", "0.2"}, {"mat", "1"}, {"tone", "2"},
                               {"unsupported", "1"}});
        // КРОНШТЕЙНЫ — как у прямого прогона: подкос от оси полотна под
        // изнанку дальнего края, шаг ~2.4 м по дуге. Консоль в 1.65 м без них
        // читается доской, приклеенной к камню.
        const int brackets = std::max(2, static_cast<int>(a.arc_len / 2.4f));
        for (int k = 0; k <= brackets; ++k) {
            const float u = static_cast<float>(k) / static_cast<float>(brackets);
            glm::vec3 root, tip;
            if (a.R <= 0.0f) {
                root = {a.L * u, y - 0.75f, 0.0f};
                tip = {a.L * u, y - 0.14f, -(d_far - 0.25f)};
            } else {
                const float t = -a.alpha + 2.0f * a.alpha * u;
                root = a.inset(t, 0.0f);
                tip = a.inset(t, d_far - 0.25f);
                root.y = y - 0.75f;
                tip.y = y - 0.14f;
            }
            (void)f.beam(f.v(root.x, root.y, root.z), f.v(tip.x, tip.y, tip.z),
                         {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                          {"tone", "2"}, {"wear", wear}});
        }
    }

    // 4. ЗУБЦЫ: шаг и ширина — те же, что у прямого прогона (len/2.2, зуб 0.9,
    // начало на 0.2 шага), только отмеряются ПО ДУГЕ.
    {
        const int teeth = std::max(2, static_cast<int>(a.arc_len / 2.2f));
        for (int t = 0; t < teeth; ++t) {
            const float u0 = (a.arc_len / static_cast<float>(teeth))
                           * (static_cast<float>(t) + 0.2f);
            const float u1 = std::min(u0 + 0.9f, a.arc_len);
            const glm::vec3 ta = a.at(u0) + glm::vec3{0.0f, h, 0.0f};
            const glm::vec3 tb = a.at(u1) + glm::vec3{0.0f, h, 0.0f};
            (void)f.wall(f.v(ta.x, ta.y, ta.z), f.v(tb.x, tb.y, tb.z),
                         {{"height", "0.8"}, {"thickness", "0.5"}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"wear", wear},
                          {"unsupported", "1"}});
        }
    }
}

/// СЕМЕЙСТВО ГНУТЫХ ПРОГОНОВ: хорды 8/12/16, стрелы 0/0.25/0.5/1/2.
/// Профиль тот же, что у city-wall12 (высота 6.0, толщина 1.2, износ 0.55).
static void forge_wall_arc(const char* file, float L, float s) {
    Forge f;
    const WallArcGeom a = wall_arc_geom(L, s);
    crenellated_arc(f, a, 6.0f, 1.2f, "0.55");
    // ПАСПОРТ ПРОГОНА В ЖУРНАЛ ВЫПЕЧКИ: раскладчику нужен поворот касательной
    // (2*alpha) и длина оси, а не только имя файла.
    std::fprintf(stderr,
                 "forge: дуга %s — R %.3f, поворот %.2f гр., ось %.3f м, "
                 "звеньев %d по %.3f м, касательная на торце %.2f гр.\n",
                 file, static_cast<double>(a.R),
                 static_cast<double>(2.0f * a.alpha * 57.29578f),
                 static_cast<double>(a.arc_len),
                 (a.R > 0.0f ? a.n + 1 : 1),
                 static_cast<double>(a.seg),
                 static_cast<double>(a.alpha * 57.29578f));
    f.save(file);
}

/// ВОРОТА: две щеки, надвратная перемычка, проём 4 м.
static void forge_gate(const char* file) {
    Forge f;
    crenellated_run(f, {0.0f, 0.0f, 0.0f}, {4.0f, 0.0f, 0.0f}, 7.0f, 1.4f, "0.5");
    crenellated_run(f, {8.0f, 0.0f, 0.0f}, {12.0f, 0.0f, 0.0f}, 7.0f, 1.4f, "0.5");
    // Перемычка над проёмом: стена на высоких якорях.
    {
        const VertexId a = f.v(4.0f, 4.6f, 0.0f);
        const VertexId b = f.v(8.0f, 4.6f, 0.0f);
        (void)f.wall(a, b,
                     {{"height", "2.4"}, {"thickness", "1.4"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"},
                      {"unsupported", "1"}});
    }
    f.save(file);
}

/// УЛИЧНАЯ ЛЕСТНИЦА-6: один марш на ПОЛНЫЙ подъём террасы (стык двух
/// маршей дважды ловил бота в щель — класс проблемы снят одним телом).
static void forge_street_stairs6(const char* file) {
    Forge f;
    const VertexId a = f.v(0.0f, 0.0f, 12.0f);
    const VertexId b = f.v(4.0f, 0.0f, 12.0f);
    const VertexId c = f.v(4.0f, 6.0f, 0.0f);
    const VertexId d = f.v(0.0f, 6.0f, 0.0f);
    (void)f.contour({a, b, c, d},
                    {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                     {"mat", "3"}, {"tone", "1"}, {"wear", "0.45"}});
    f.save(file);
}

/// УЛИЧНАЯ ЛЕСТНИЦА: каменный марш 4 м шириной, подъём 3 м, блоками.
static void forge_street_stairs(const char* file) {
    Forge f;
    const VertexId a = f.v(0.0f, 0.0f, 6.0f);
    const VertexId b = f.v(4.0f, 0.0f, 6.0f);
    const VertexId c = f.v(4.0f, 3.0f, 0.0f);
    const VertexId d = f.v(0.0f, 3.0f, 0.0f);
    (void)f.contour({a, b, c, d},
                    {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                     {"mat", "3"}, {"tone", "1"}, {"wear", "0.45"}});
    f.save(file);
}

/// ПЛАЗА: мощёная плита с бордюром-секциями.
static void forge_plaza(const char* file, float w, float d) {
    Forge f;
    const VertexId a = f.v(0.0f, 0.02f, 0.0f);
    const VertexId b = f.v(0.0f, 0.02f, d);
    const VertexId c = f.v(w, 0.02f, d);
    const VertexId e = f.v(w, 0.02f, 0.0f);
    (void)f.contour({a, b, c, e},
                    {{"thickness", "0.16"}, {"mat", "3"}, {"tone", "1"},
                     {"wear", "0.4"}});
    // Бордюр по периметру: низкие каменные ленты (сами секциями).
    const glm::vec3 pts[5] = {{0, 0, 0}, {w, 0, 0}, {w, 0, d}, {0, 0, d}, {0, 0, 0}};
    for (int k = 0; k < 4; ++k) {
        const VertexId p = f.v(pts[k].x, 0.0f, pts[k].z);
        const VertexId q = f.v(pts[k + 1].x, 0.0f, pts[k + 1].z);
        (void)f.wall(p, q,
                     {{"height", "0.22"}, {"thickness", "0.25"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"}});
    }
    f.save(file);
}

/// МОЩЕНИЕ УЛИЦЫ — семейство city-cobble. Претензия тяжести 3: у города ноль
/// метров уличного покрытия, а плаза под улицу не годится по одной причине —
/// её бордюр — это СТЕНА 0.22 м по всему периметру, и капсула персонажа на неё
/// взбирается (бот уже застревал на кромке плиты). Улицу переходят поперёк в
/// любой точке, значит кромка обязана быть ПРОХОДИМОЙ.
///
/// Отсюда вся форма:
/// - ФАСКА ВМЕСТО БОРТА. По периметру — не стенка, а наклонный поясок шириной
///   0.30 м: сверху он на уровне мостовой, снаружи ровно на нуле земли. Уклон
///   15°, персонаж на него ВХОДИТ, а не влезает.
/// - ФАСКА ВНУТРЬ, а не наружу. Габарит объекта ровно w x d, и две плиты,
///   поставленные встык, не наезжают поясками друг на друга (z-fighting) и не
///   вылезают за клетку раскладки. Шов между ними читается стыком мощения.
/// - КАМЕНЬ ОТДЕЛЬНЫМИ ПЛИТАМИ поверх основания. Ровный серый прямоугольник —
///   это не мостовая, это пол; шов и разнобой высот по 8 мм и есть то, из чего
///   глаз собирает тёсаный камень. Клетки атласа НОВЫЕ НЕ ЗАВОДЯТСЯ (mat=3,
///   Stone — та, что уже носит плаза): PARTS_ATLAS_REVISION входит в ключ кэша.
/// - ВЕРХ НИЗКО: 0.08 м над землёй. Мостовая — покрытие, а не подиум.
///
/// stone_m — целевая сторона плиты камня; сетка подгоняется под размер участка.
/// slope — ПРОДОЛЬНЫЙ УКЛОН вдоль локального +X, подъём на метр длины (0.08 =
/// 8%). Заведён по замеру раскладчика: подъём к замку идёт 8.3%, а плоская
/// жёсткая плита 6 м на таком уклоне разбегается с землёй на 0.48 м — либо
/// висит краем, либо даёт соседке ступеньку в полметра. Плоскими кусками
/// непрерывно замостить склон НЕЛЬЗЯ, это геометрия, а не огрех раскладки.
/// Наклонный кусок снимает и то и другое: верх следует за подъёмом улицы, а
/// стык двух одинаково наклонных совпадает ПО ПОСТРОЕНИЮ.
static void forge_cobble(const char* file, float w, float d, float stone_m,
                         const char* tone, float wear, float slope = 0.0f) {
    Forge f;
    // Высота точки с учётом уклона. slope = 0 даёт y + 0*x, то есть РОВНО y:
    // прежние семь плит перепекаются бит в бит, и это проверено хешами.
    const auto Y = [slope](float y, float x) { return y + slope * x; };
    std::deque<std::string> said;
    const auto num = [&said](float value) {
        char buf[32];
        std::snprintf(buf, sizeof(buf), "%g", static_cast<double>(value));
        said.emplace_back(buf);
        return said.back().c_str();
    };
    // ДЕТЕРМИНИРОВАННАЯ ДРОЖЬ: две выпечки обязаны дать один .dfh (артефакты
    // лежат в git), поэтому не rand(), а хэш от координат клетки.
    const auto jit = [](int i, int j) {
        std::uint32_t h = static_cast<std::uint32_t>(i) * 73856093u
                        ^ static_cast<std::uint32_t>(j) * 19349663u;
        h ^= h >> 13;
        h *= 0x5bd1e995u;
        h ^= h >> 15;
        return static_cast<float>(h & 0xFFFFu) / 65535.0f;
    };

    const float chamfer = 0.30f; ///< ширина фаски по периметру
    const float top = 0.08f;     ///< верх мостовой над землёй
    const float base_top = 0.05f; ///< верх основания: разница и есть глубина шва
    const float joint = 0.05f;   ///< шов между камнями

    const float ix0 = chamfer;
    const float iz0 = chamfer;
    const float ix1 = w - chamfer;
    const float iz1 = d - chamfer;

    // ОСНОВАНИЕ под камнями: без него швы смотрели бы в пустоту.
    {
        const float th = 0.09f;
        const float y = base_top - th * 0.5f;
        (void)f.contour({f.v(ix0, Y(y, ix0), iz0), f.v(ix0, Y(y, ix0), iz1),
                         f.v(ix1, Y(y, ix1), iz1), f.v(ix1, Y(y, ix1), iz0)},
                        {{"thickness", "0.09"}, {"mat", "3"}, {"tone", tone},
                         {"wear", num(std::min(wear + 0.15f, 0.85f))}});
    }

    // КАМНИ: сетка подогнана под участок, чтобы крайние ряды были целыми.
    const int nx = std::max(1, static_cast<int>(std::lround((ix1 - ix0) / stone_m)));
    const int nz = std::max(1, static_cast<int>(std::lround((iz1 - iz0) / stone_m)));
    const float sx = (ix1 - ix0) / static_cast<float>(nx);
    const float sz = (iz1 - iz0) / static_cast<float>(nz);
    const float st = 0.05f; ///< толщина камня
    for (int i = 0; i < nx; ++i) {
        for (int j = 0; j < nz; ++j) {
            const float x0 = ix0 + sx * static_cast<float>(i) + joint * 0.5f;
            const float x1 = ix0 + sx * static_cast<float>(i + 1) - joint * 0.5f;
            const float z0 = iz0 + sz * static_cast<float>(j) + joint * 0.5f;
            const float z1 = iz0 + sz * static_cast<float>(j + 1) - joint * 0.5f;
            // Разнобой высот — ВОСЕМЬ МИЛЛИМЕТРОВ. Больше — и капсула начнёт
            // спотыкаться о собственную мостовую; меньше — швы не читаются.
            const float y = top - st * 0.5f + (jit(i, j) - 0.5f) * 0.016f;
            // Поворот текстуры покамённо: у соседних плит разное направление
            // зерна, иначе весь участок читается одним растянутым куском.
            const float deg = 15.0f * static_cast<float>(
                static_cast<int>(jit(i + 97, j + 31) * 5.99f));
            (void)f.contour({f.v(x0, Y(y, x0), z0), f.v(x0, Y(y, x0), z1),
                             f.v(x1, Y(y, x1), z1), f.v(x1, Y(y, x1), z0)},
                            {{"thickness", "0.05"}, {"mat", "3"}, {"tone", tone},
                             {"tex_deg", num(deg)},
                             {"wear", num(std::min(wear + jit(j, i) * 0.2f, 0.85f))}});
        }
    }

    // ФАСКА: четыре трапеции в ус, лицом вверх. Верх внутренней кромки совпадает
    // с верхом мостовой, верх внешней — ровно земля, поэтому вход на мостовую
    // не имеет ступени НИ С ОДНОЙ стороны.
    {
        const float sth = 0.06f;
        const float cosf_ = chamfer / std::sqrt(chamfer * chamfer + top * top);
        const float dy = sth * 0.5f * cosf_;
        const float yi = top - dy;   ///< якорь внутренней кромки
        const float yo = -dy;        ///< якорь внешней кромки
        const Params skirt = {{"thickness", "0.06"}, {"mat", "3"}, {"tone", tone},
                              {"wear", num(std::min(wear + 0.2f, 0.85f))}};
        // юг (z=0)
        (void)f.contour({f.v(0.0f, Y(yo, 0.0f), 0.0f), f.v(ix0, Y(yi, ix0), iz0),
                         f.v(ix1, Y(yi, ix1), iz0), f.v(w, Y(yo, w), 0.0f)}, skirt);
        // север (z=d)
        (void)f.contour({f.v(0.0f, Y(yo, 0.0f), d), f.v(w, Y(yo, w), d),
                         f.v(ix1, Y(yi, ix1), iz1), f.v(ix0, Y(yi, ix0), iz1)}, skirt);
        // запад (x=0)
        (void)f.contour({f.v(0.0f, Y(yo, 0.0f), 0.0f), f.v(0.0f, Y(yo, 0.0f), d),
                         f.v(ix0, Y(yi, ix0), iz1), f.v(ix0, Y(yi, ix0), iz0)}, skirt);
        // восток (x=w)
        (void)f.contour({f.v(w, Y(yo, w), d), f.v(w, Y(yo, w), 0.0f),
                         f.v(ix1, Y(yi, ix1), iz0), f.v(ix1, Y(yi, ix1), iz1)}, skirt);
    }
    f.save(file);
}

/// МАРШ В СТВОР УЛИЦЫ — подъём на подпорную кромку (заказ раскладчика по
/// замеру южной грани рынка). Отличается от city-stairs двумя вещами, и обе
/// продиктованы посадкой ЦЕПЬЮ, а не вкусом:
/// - ШИРИНА 6.0 м, в ширину мостовой: марш обязан читаться продолжением
///   улицы, а не калиткой в ограде;
/// - ПОДЪЁМ ВДОЛЬ ЛОКАЛЬНОГО +X, как у city-cobble-sNN, и origin у НИЖНЕЙ
///   ступени. Тогда раскладчик сажает марш той же арифметикой, что и
///   наклонные плиты: origin соседа = origin + подъём, и цепь отметок вдоль
///   ломаной не разъезжается.
/// Подъём берётся РОВНО ПО ПАСПОРТУ (1.200, а не 1.19 из замера земли):
/// цепь ведётся по объявленным числам, иначе накопится дрейф.
/// Ступени — fill=6: ПАНДУС ПОД НИМИ СТРОИТ ДВИЖОК (HouseStairs.cpp, ходьба
/// идёт по 26-градусному пандусу, картинка остаётся честными ступенями).
/// Своего пандуса рецепт не кладёт — второй под первым дал бы спор коллайдеров.
static void forge_street_steps(const char* file, float rise, float run) {
    Forge f;
    const float W = 6.0f;
    (void)f.contour({f.v(0.0f, 0.0f, 0.0f), f.v(0.0f, 0.0f, W),
                     f.v(run, rise, W), f.v(run, rise, 0.0f)},
                    {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                     {"mat", "3"}, {"tone", "1"}, {"wear", "0.4"}});
    f.save(file);
}

/// ПОДПОРНАЯ КРОМКА — отрезок стенки, из которых раскладчик собирает КЛИН.
/// Южная грань рынка падает с 1.30 м на западном конце до нуля у x=122, и
/// одной высотой её не закрыть; отрезками — закрыть, и лицо получается
/// ступенчатым, как у настоящей подпорной стенки.
/// ORIGIN В НИЖНЕМ УГЛУ ЛИЦЕВОЙ ГРАНИ (заказ раскладчика): тело стены сидит
/// симметрично вокруг своей оси, поэтому ось отодвинута на полтолщины вглубь —
/// иначе лицо оказалось бы в полушаге перед объявленной точкой.
static void forge_kerb(const char* file, float h) {
    Forge f;
    const float L = 2.0f;
    const float T = 0.35f;
    char hs[16];
    std::snprintf(hs, sizeof(hs), "%g", static_cast<double>(h));
    (void)f.wall(f.v(0.0f, 0.0f, T * 0.5f), f.v(L, 0.0f, T * 0.5f),
                 {{"height", hs}, {"thickness", "0.35"}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"},
                  {"unsupported", "1"}});
    // КОПИНГ: плита поверху с напуском на ладонь. Подпорная стенка без копинга
    // читается обрезанной, а напуск — то, по чему глаз узнаёт её как стенку, а
    // не как забытый блок. Тот же довод, что у напуска трубы.
    (void)f.beam(f.v(0.0f, h, T * 0.5f), f.v(L, h, T * 0.5f),
                 {{"radius", "0.21"}, {"form", "plank"}, {"mat", "3"},
                  {"tone", "1"}, {"wear", "0.45"}});
    f.save(file);
}

/// ФУНДАМЕНТНЫЙ ПОЯС — отрезок цоколя, из которых раскладчик собирает ЛЕНТУ по
/// периметру пятна дома (заказ владельца 23.08: «земля не ровная, дома ровные;
/// каждому дому небольшой фундамент, который может уходить под землю, но сам
/// дом на нём стоит как на ровной земле»).
///
/// ПОЧЕМУ ОТРЕЗКАМИ, А НЕ ЗАМКНУТОЙ КОРОБКОЙ. Пятна домов города — 4.4x4.4,
/// 4.5x6.0, 6x8, 10x10.35, 16x8, 25x13.2: замкнутый рецепт пришлось бы печь на
/// каждое, то есть двадцать шесть штучных .dfh на двадцать шесть домов, и
/// каждый следующий чертёж просил бы новых. Отрезок — один рецепт на любую
/// сторону; лишнее уходит в НАХЛЁСТ, а нахлёст двух каменных тел с ОДНИМ
/// верхом невидим (в отличие от нахлёста мостовых плит, где спорит верхняя
/// грань).
///
/// ВЫСОТА — ОДНА НА ВЕСЬ ПЕРИМЕТР ДОМА, и это не упрощение, а само правило:
/// лента висит от ОТМЕТКИ ПОЛА вниз до минимума земли под пятном с запасом.
/// На склоне верх ленты горизонтален (на нём стоит дом), а низ уходит в грунт
/// тем глубже, чем выше склон, — ровно так стоит настоящий бутовый фундамент.
///
/// ТОЛЩИНА 0.60 ПРОТИВ СТЕНЫ 0.35-0.50: цоколь обязан ЧИТАТЬСЯ, то есть
/// выступать за лицо стены. Ось отодвинута на полтолщины вглубь (как у
/// city-kerb2), поэтому origin лежит на НАРУЖНОЙ плоскости пояса, и
/// раскладчику остаётся поставить эту плоскость на 0.15 наружу от лица стены.
/// БЕЗ КОПИНГА, в отличие от подпорной кромки: у кромки напуск поверху — то,
/// чем глаз узнаёт стенку; у фундамента поверху стоит ДОМ, и напуск оказался
/// бы полкой под стеной, которой у бутового цоколя не бывает.
/// unsupported=1 — судья не спрашивает у ленты, на чём она держится: она и
/// есть то, на чём держится всё остальное.
static void forge_footing(const char* file, float h) {
    Forge f;
    const float L = 2.0f;
    const float T = 0.60f;
    char hs[16];
    std::snprintf(hs, sizeof(hs), "%g", static_cast<double>(h));
    (void)f.wall(f.v(0.0f, 0.0f, T * 0.5f), f.v(L, 0.0f, T * 0.5f),
                 {{"height", hs}, {"thickness", "0.6"}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"wear", "0.55"},
                  {"unsupported", "1"}});
    f.save(file);
}

/// ПОДПОРНАЯ СТЕНКА ТЕРРАСЫ — отрезок 2.0 м, из которых раскладчик собирает
/// подпорную грань под постройкой, стоящей НА КРОМКЕ (заказ владельца 24.08:
/// «донжон — переделай; по смыслу там должна быть подпорная стенка террасы,
/// архитектурная, с контрфорсами/откосом»).
///
/// ЧЕМ ОНА ОТЛИЧАЕТСЯ ОТ ФУНДАМЕНТНОГО ПОЯСА, И ПОЧЕМУ ЭТО ДРУГАЯ ДЕТАЛЬ, А НЕ
/// ДРУГОЕ ЧИСЛО. Пояс city-foot2 держит ДОМ: он тонкий (0.60), вертикальный,
/// без копинга, и по замыслу почти весь в грунте — снаружи от него видно
/// ладонь цоколя. Подпорная стенка держит ЗЕМЛЮ: она толстая (0.90), с
/// ОТКОСОМ (лицо отклонено внутрь, низ шире верха) и с КОПИНГОМ поверху, и она
/// по замыслу видна вся, от подошвы до напуска. Донжон на четырёх венцах пояса
/// читался «башней на скальном быке» ровно потому, что бутовый цоколь, поднятый
/// на четыре метра, перестаёт быть цоколем и ничем другим стать не может.
///
/// ОТКОС СОБИРАЕТСЯ ВЕНЦАМИ, А НЕ НАКЛОННОЙ ГРАНЬЮ. Стена в графе — это
/// выдавленная вверх цепочка постоянной толщины; наклонное лицо пришлось бы
/// строить контуром, то есть плитой, у которой нет объёма кладки. Венцы по
/// метру со сдвигом лица на 0.05 дают уклон 1:20 (канон для бутовой подпорной
/// стенки — от 1:12 до 1:24) И горизонтальный шов между венцами, которым
/// настоящая подпорная стенка и читается.
///
/// ORIGIN — В НИЖНЕМ УГЛУ ЛИЦЕВОЙ ГРАНИ ВЕРХНЕГО ВЕНЦА (та же конвенция, что у
/// city-kerb2 и city-foot2): раскладчику надо поставить одну плоскость, а
/// куда от неё уходит откос — забота рецепта. Лицо верхнего венца лежит на
/// z == 0, нижние венцы выступают в -z.
static void forge_revet(const char* file, float h) {
    Forge f;
    const float L = 2.0f;
    const float T = 0.90f;
    const float BATTER = 0.05f;   // отступ лица на венец
    const int n = std::max(1, static_cast<int>(std::ceil(h / 1.0f - 1e-4f)));
    const float hc = h / static_cast<float>(n);
    for (int j = 0; j < n; ++j) {
        // j == 0 — ВЕРХНИЙ венец (лицо на нуле), ниже лицо уходит наружу
        const float out = BATTER * static_cast<float>(j);
        const float t = T + out;
        const float y0 = h - hc * static_cast<float>(j + 1);
        char hs[16];
        char ts[16];
        std::snprintf(hs, sizeof(hs), "%g", static_cast<double>(hc));
        std::snprintf(ts, sizeof(ts), "%g", static_cast<double>(t));
        (void)f.wall(f.v(0.0f, y0, t * 0.5f - out), f.v(L, y0, t * 0.5f - out),
                     {{"height", hs}, {"thickness", ts}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"},
                      {"unsupported", "1"}});
    }
    // КОПИНГ с напуском на ладонь в обе стороны — тот же довод, что у
    // city-kerb2: подпорная стенка без копинга читается обрезанной.
    (void)f.beam(f.v(0.0f, h, T * 0.5f), f.v(L, h, T * 0.5f),
                 {{"radius", "0.51"}, {"form", "plank"}, {"mat", "3"},
                  {"tone", "1"}, {"wear", "0.45"}});
    f.save(file);
}

/// КОНТРФОРС — пилон, приставленный к лицу подпорной стенки снаружи. Без него
/// стенка выше двух метров читается плоской доской: контрфорсы дают ей ритм и
/// объявляют вслух, что она ДЕРЖИТ, а не просто закрывает.
/// Ширина 0.9 вдоль лица, вылет 0.60 у подошвы, сходящий на 0.15 к верху —
/// венцами по той же причине, что и откос стенки. Верх контрфорса на 0.35
/// НИЖЕ верха стенки: пилон, доведённый до копинга, спорил бы с его напуском.
/// Локаль: лицо стенки — плоскость z == 0, контрфорс уходит в -z.
static void forge_buttress(const char* file, float h) {
    Forge f;
    const float W = 0.9f;
    const float P0 = 0.60f;
    const float P1 = 0.15f;
    const int n = std::max(2, static_cast<int>(std::ceil(h / 0.9f - 1e-4f)));
    const float hc = h / static_cast<float>(n);
    for (int j = 0; j < n; ++j) {
        const float k = static_cast<float>(j) / static_cast<float>(n - 1);
        const float p = P0 + (P1 - P0) * k;
        char hs[16];
        char ps[16];
        std::snprintf(hs, sizeof(hs), "%g", static_cast<double>(hc));
        std::snprintf(ps, sizeof(ps), "%g", static_cast<double>(p));
        (void)f.wall(f.v(0.0f, hc * static_cast<float>(j), -p * 0.5f),
                     f.v(W, hc * static_cast<float>(j), -p * 0.5f),
                     {{"height", hs}, {"thickness", ps}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.5"},
                      {"unsupported", "1"}});
    }
    f.save(file);
}

/// КРЫЛЬЦО — марш от земли к порогу дома, вторая половина того же заказа: «а к
/// двери в дом идёт лестница-ступеньки». Ширина 1.6 м (норматив заказа >= 1.1,
/// и 1.6 даёт капсуле R=0.35 по полметра с каждой стороны), подъём — параметр,
/// ВЫЛЕТ ВЫВОДИТСЯ ИЗ НЕГО.
///
/// ВЫЛЕТ СЧИТАЕТСЯ ЧЕРЕЗ ЧИСЛО ПОДСТУПЕНКОВ ДВИЖКА, А НЕ ЗАДАЁТСЯ. Дробление
/// марша не в рецепте: HouseStairs.cpp берёт steps = round(подъём / 0.175), и
/// проступь выходит вылет/steps. Заказ требует проступь >= 0.28 — значит вылет
/// обязан быть steps * 0.30, и тогда проступь ровно 0.30 у ВСЕГО ряда, а
/// подступенок = подъём/steps сам ложится в заказанные 0.15..0.25 (крайние
/// случаи ряда: 0.200 у h02, 0.150 у h03, 0.182 у h20).
/// Считать вылет как «подъём * коэффициент» было бы третьим числом там, где
/// хватает двух: у h03 (0.3 м) round(0.3/0.175)=2, и любой коэффициент около
/// 1.7 дал бы проступь 0.255 — под порогом заказа, и никто бы не заметил.
///
/// fill=6 + open=2 — каменные блоки с зазорами И НЕВИДИМЫЙ ПАНДУС под ними
/// (HouseStairs.cpp: открытая лестница капсуле непроходима в принципе, ходьба
/// идёт по 26-градусной наклонной плоскости collider_only). Своего пандуса не
/// кладу — два под одним маршем спорили бы коллайдерами.
/// ХОДИБЕЛЬНАЯ ПОВЕРХНОСТЬ = ЯКОРЬ + подступенок - 0.02 НА ОБОИХ КОНЦАХ (низ
/// пандуса a.y + rise - 0.18, экструзия вверх на 0.16). Значит ходибельный
/// подъём марша РАВЕН паспортному, а посадка сдвигается на одну и ту же
/// поправку сверху и снизу — раскладчик считает её из подъёма, не из таблицы.
static void forge_stoop(const char* file, float rise) {
    Forge f;
    const float W = 1.6f;
    const int steps = std::max(1, static_cast<int>(std::lround(rise / 0.175f)));
    const float run = static_cast<float>(steps) * 0.30f;
    (void)f.contour({f.v(0.0f, 0.0f, 0.0f), f.v(0.0f, 0.0f, W),
                     f.v(run, rise, W), f.v(run, rise, 0.0f)},
                    {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                     {"mat", "3"}, {"tone", "1"}, {"wear", "0.45"}});
    f.save(file);
}

/// МОСТ через городской рукав: плита с бордюрами на двух опорах.
static void forge_bridge(const char* file) {
    Forge f;
    // Опоры в русле.
    for (const float x : {1.2f, 6.8f}) {
        const VertexId a = f.v(x, -2.2f, 0.4f);
        const VertexId b = f.v(x, -2.2f, 3.6f);
        (void)f.wall(a, b,
                     {{"height", "2.4"}, {"thickness", "0.8"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.6"},
                      {"unsupported", "1"}});
    }
    const VertexId a = f.v(-0.6f, 0.2f, 0.0f);
    const VertexId b = f.v(-0.6f, 0.2f, 4.0f);
    const VertexId c = f.v(8.6f, 0.2f, 4.0f);
    const VertexId e = f.v(8.6f, 0.2f, 0.0f);
    (void)f.contour({a, b, c, e},
                    {{"thickness", "0.3"}, {"mat", "3"}, {"tone", "1"},
                     {"wear", "0.5"}, {"unsupported", "1"}});
    // ЗАЕЗДЫ-АППАРЕЛИ по торцам: настил выше берега, и без пандуса ни бот,
    // ни игрок ступень настила не берут.
    for (const float xr : {-2.4f, 8.6f}) {
        const VertexId r1 = f.v(xr, -0.5f, 0.0f);
        const VertexId r2 = f.v(xr, -0.5f, 4.0f);
        const VertexId r3 = f.v(xr < 0.0f ? -0.6f : 10.4f, 0.32f, 4.0f);
        const VertexId r4 = f.v(xr < 0.0f ? -0.6f : 10.4f, 0.32f, 0.0f);
        if (xr < 0.0f) {
            (void)f.contour({r1, r2, r3, r4},
                            {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                             {"wear", "0.5"}, {"unsupported", "1"}});
        } else {
            const VertexId q3 = f.v(8.6f, 0.32f, 4.0f);
            const VertexId q4 = f.v(8.6f, 0.32f, 0.0f);
            const VertexId q1 = f.v(10.4f, -0.5f, 0.0f);
            const VertexId q2 = f.v(10.4f, -0.5f, 4.0f);
            (void)f.contour({q1, q2, q3, q4},
                            {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                             {"wear", "0.5"}, {"unsupported", "1"}});
        }
    }
    for (const float z : {0.0f, 4.0f}) {
        const VertexId p = f.v(-0.6f, 0.35f, z);
        const VertexId q = f.v(8.6f, 0.35f, z);
        (void)f.wall(p, q,
                     {{"height", "0.6"}, {"thickness", "0.3"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.6"},
                      {"unsupported", "1"}});
    }
    f.save(file);
}

// ---------------------------------------------------------------------------
// АРКА МОСТА ([N2], тяжесть 3, четыре круга подряд). Прежний city-bridge —
// ПЛОСКАЯ ПЛИТА на двух опорах: настил горизонтален от торца до торца, свода
// под ним нет вовсе, а два парапета построены ЦЕПОЧКАМИ с одинаковым порядком
// вершин, то есть лицо кладки у них смотрит в одну сторону — наружу у одного и
// внутрь у другого. Здесь оба борта — ОДНА И ТА ЖЕ деталь-контур, у которой
// лица два по построению.
//
// ЧИСЛА ПРОЛЁТА — ЗАМЕР ПРЕЖНЕГО РЕЦЕПТА, А НЕ ПАСПОРТ. Меш city-bridge.dfh:
// габарит x -2.441..10.441, y -2.200..0.950, z -0.150..4.244; ходибельная
// поверхность настила 0.350 на всём протяжении, торцы аппарелей -0.400 (это и
// есть BRIDGE_TIP генератора), быки — цепочки на x = 1.2 и 6.8, от y = -2.2
// высотой 2.4 (верх 0.2), толщиной 0.8, поперёк z 0.4..3.6. Быки здесь те же
// до сантиметра.
//
// ГОРБ И УКЛОН НЕ УМЕЩАЮТСЯ В ПРЕЖНЮЮ ДЛИНУ, и это арифметика, а не мнение.
// От торца аппарели (-0.40) до верха настила (0.35) уже 0.75 м; горб 0.80 м
// сверх настила даёт полный подъём 1.55 м. Уклон 15 % отдаёт 6.67 м длины на
// каждый метр подъёма, значит одному скату нужно не меньше 10.3 м, а мосту —
// не меньше 20.7 м даже при ОСТРОМ гребне. Прежние 12.88 м не дают горба
// больше 0.21 м. Поэтому мост удлинён до 25.2 м, и это не «на всякий случай»,
// а ровно то, что следует из двух заданных чисел.
//
// ПРОФИЛЬ — ПРЯМАЯ, ДУГА, ПРЯМАЯ, И ОН ГЛАДКИЙ В МАТЕМАТИЧЕСКОМ СМЫСЛЕ:
// аппарели лежат на КАСАТЕЛЬНЫХ к дуге гребня, поэтому в точке касания уклон
// не прыгает (непрерывность по первой производной). Уклон равен ровно 15 % на
// аппарелях и падает до нуля в замке; больше 15 % нет нигде.
// ---------------------------------------------------------------------------

/// ВЕРХ НАСТИЛА В ЗАМКЕ, локально. Прецедент BRIDGE_DECK/BRIDGE_TIP: числа,
/// по которым генератор ищет поверхность, живут константами, а не в теле.
static constexpr float BRIDGE_ARC_CROWN = 1.15f;
/// ВЕРХ АППАРЕЛИ У ТОРЦА, локально. То же число, что BRIDGE_TIP у прежнего
/// моста: земля подходов доводится генератором именно к этой отметке, и менять
/// её значило бы переделывать grade_bridge ради косметики.
static constexpr float BRIDGE_ARC_TIP = -0.40f;
/// Замок стоит над серединой пролёта быков (1.2 и 6.8 -> 4.0).
static constexpr float BRIDGE_ARC_CX = 4.0f;
/// Наибольший уклон ходибельной поверхности (заказ: не круче 15 %).
static constexpr float BRIDGE_ARC_GRADE = 0.15f;
/// Радиус вертикальной кривой гребня. ВЫБРАН ТАК, ЧТОБЫ ДУГА НАКРЫЛА РОВНО
/// ПРЕЖНИЙ НАСТИЛ: полуширина кривой = R*sin(atan(0.15)) = 4.507, то есть
/// дуга идёт от x = -0.507 до 8.507 при прежнем настиле -0.6..8.6.
static constexpr float BRIDGE_ARC_RV = 30.40f;
/// Ширина настила и толщина плиты — у прежнего рецепта.
static constexpr float BRIDGE_ARC_W = 4.0f;
static constexpr float BRIDGE_ARC_TH = 0.30f;
/// Ось борта: 0.15 от кромки, чтобы тело 0.30 легло В габарит настила.
/// У прежнего моста борт стоял на самой кромке и свисал наружу на 0.15.
static constexpr float BRIDGE_ARC_KERB = 0.15f;
/// Парапет над настилом — высота прежнего борта.
static constexpr float BRIDGE_ARC_RAIL = 0.60f;

/// ПРОФИЛЬ ХОДИБЕЛЬНОЙ ПОВЕРХНОСТИ. Одна формула на весь рецепт: настил,
/// парапет, лицевые стены и замер отчёта берут высоту ОТСЮДА.
struct BridgeArc {
    float alpha = std::atan(BRIDGE_ARC_GRADE);  ///< угол касания
    float curve_half = 0.0f;   ///< полуширина дуги гребня по x
    float y_tangent = 0.0f;    ///< верх настила в точке касания
    float ramp_run = 0.0f;     ///< длина аппарели по x
    float half = 0.0f;         ///< полудлина моста по x
    float x0 = 0.0f, x1 = 0.0f;
    float cy = 0.0f;           ///< y центра вертикальной кривой

    BridgeArc() {
        curve_half = BRIDGE_ARC_RV * std::sin(alpha);
        y_tangent = BRIDGE_ARC_CROWN - BRIDGE_ARC_RV * (1.0f - std::cos(alpha));
        ramp_run = (y_tangent - BRIDGE_ARC_TIP) / BRIDGE_ARC_GRADE;
        half = curve_half + ramp_run;
        x0 = BRIDGE_ARC_CX - half;
        x1 = BRIDGE_ARC_CX + half;
        cy = BRIDGE_ARC_CROWN - BRIDGE_ARC_RV;
    }
    /// Верх настила над точкой x.
    [[nodiscard]] float top(float x) const {
        const float a = std::fabs(x - BRIDGE_ARC_CX);
        if (a <= curve_half) {
            return cy + std::sqrt(std::max(0.0f, BRIDGE_ARC_RV * BRIDGE_ARC_RV - a * a));
        }
        return y_tangent - BRIDGE_ARC_GRADE * (a - curve_half);
    }
};

static void forge_bridge_arc(const char* file) {
    Forge f;
    const BridgeArc B;

    // 1. БЫКИ — ПРЕЖНИЕ, ЧИСЛО В ЧИСЛО (замер city-bridge.dfh).
    for (const float x : {1.2f, 6.8f}) {
        (void)f.wall(f.v(x, -2.2f, 0.4f), f.v(x, -2.2f, 3.6f),
                     {{"height", "2.4"}, {"thickness", "0.8"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.6"},
                      {"unsupported", "1"}});
    }

    // 2. ОСЬ НАСТИЛА — ЛОМАНАЯ ПО ОПИСАННОЙ, как у гнутой стены: звенья лежат
    // на касательных к дуге гребня, поэтому крайние звенья ПРОДОЛЖАЮТ аппарели
    // без излома, а не пристыковываются к ним.
    std::vector<glm::vec2> spine;  // (x, верх настила)
    {
        const float d_len = std::atan(1.0f / BRIDGE_ARC_RV);           // хорда <= 2 м
        const float d_dev = std::acos(std::min(1.0f, BRIDGE_ARC_RV
                                                     / (BRIDGE_ARC_RV + 0.02f)));
        const float d_max = std::min(d_len, d_dev);
        const int n = std::max(1, static_cast<int>(std::ceil(B.alpha / d_max - 1e-6f)));
        const float d = B.alpha / static_cast<float>(n);
        const float r_out = BRIDGE_ARC_RV / std::cos(d);
        spine.emplace_back(B.x0, BRIDGE_ARC_TIP);
        for (int k = 1; k <= n; ++k) {
            const float t = -B.alpha + (2.0f * static_cast<float>(k) - 1.0f) * d;
            spine.emplace_back(BRIDGE_ARC_CX + r_out * std::sin(t),
                               B.cy + r_out * std::cos(t));
        }
        spine.emplace_back(B.x1, BRIDGE_ARC_TIP);
    }

    // 3. НАСТИЛ: по плите на звено. Плита СИММЕТРИЧНА срединной плоскости, а
    // человек называет ВЕРХ, поэтому ось кладётся на th/2 ниже. Внутренние
    // торцы выпущены на (th/2)*tan(излом/2): выпуклый гребень раскрывал бы на
    // каждом стыке клин глубиной в полтолщины — та же болезнь, что у стены.
    const float dth = BRIDGE_ARC_TH * 0.5f;
    for (std::size_t i = 0; i + 1 < spine.size(); ++i) {
        glm::vec2 p = spine[i];
        glm::vec2 q = spine[i + 1];
        const glm::vec2 dv = glm::normalize(q - p);
        if (i > 0 || i + 2 < spine.size()) {
            // Излом между этим звеном и соседним.
            const auto kink = [&](std::size_t a, std::size_t b, std::size_t c) {
                const glm::vec2 u = glm::normalize(spine[b] - spine[a]);
                const glm::vec2 w = glm::normalize(spine[c] - spine[b]);
                return std::acos(std::clamp(glm::dot(u, w), -1.0f, 1.0f));
            };
            if (i > 0) {
                p -= dv * (dth * std::tan(kink(i - 1, i, i + 1) * 0.5f));
            }
            if (i + 2 < spine.size()) {
                q += dv * (dth * std::tan(kink(i, i + 1, i + 2) * 0.5f));
            }
        }
        (void)f.contour({f.v(p.x, p.y - dth, 0.0f), f.v(p.x, p.y - dth, BRIDGE_ARC_W),
                         f.v(q.x, q.y - dth, BRIDGE_ARC_W), f.v(q.x, q.y - dth, 0.0f)},
                        {{"thickness", "0.30"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.5"}, {"unsupported", "1"}});
    }

    // 4. СВОД. Интрадос — сегментная дуга между ЛИЦАМИ быков (1.6 и 6.4,
    // просвет 4.8): пята на 0.0, подъём 0.55, значит замок свода 0.55, а с
    // кольцом 0.30 экстрадос замка 0.85 — ровно исподняя грань настила в
    // замке (1.15 - 0.30). Свод ДЕРЖИТ настил, а не висит под ним.
    const float SPR_X0 = 1.6f;
    const float SPR_X1 = 6.4f;
    const float VAULT_RISE = 0.55f;
    const float VAULT_RING = 0.30f;
    const float v_half = (SPR_X1 - SPR_X0) * 0.5f;
    const float v_R = (VAULT_RISE * VAULT_RISE + v_half * v_half) / (2.0f * VAULT_RISE);
    const float v_cy = VAULT_RISE - v_R;              // центр дуги, y
    const float v_beta = std::asin(v_half / v_R);     // полуугол
    const auto vault_y = [&](float x, float dr) {
        const float a = std::fabs(x - BRIDGE_ARC_CX);
        const float r = v_R + dr;
        const float rr = r * r - a * a;
        return v_cy + (rr > 0.0f ? std::sqrt(rr) : 0.0f);
    };
    {
        const int nv = 9;  // клинья свода
        for (int k = 0; k < nv; ++k) {
            const float t0 = -v_beta + 2.0f * v_beta * static_cast<float>(k)
                           / static_cast<float>(nv);
            const float t1 = -v_beta + 2.0f * v_beta * static_cast<float>(k + 1)
                           / static_cast<float>(nv);
            const float rm = v_R + VAULT_RING * 0.5f;  // срединная поверхность кольца
            const float ax = BRIDGE_ARC_CX + rm * std::sin(t0);
            const float ay = v_cy + rm * std::cos(t0);
            const float bx = BRIDGE_ARC_CX + rm * std::sin(t1);
            const float by = v_cy + rm * std::cos(t1);
            (void)f.contour({f.v(ax, ay, 0.4f), f.v(ax, ay, 3.6f),
                             f.v(bx, by, 3.6f), f.v(bx, by, 0.4f)},
                            {{"thickness", "0.30"}, {"mat", "3"}, {"tone", "1"},
                             {"wear", "0.6"}, {"unsupported", "1"}});
        }
        // ПОДПРУЖНЫЕ КАМНИ (архивольт) на обоих лицах: без кольца свод читается
        // выемкой в стене, а не аркой. Брус квадратного сечения по срединной
        // поверхности, вынесенный на 0.10 за лицо стены.
        for (const float z : {0.06f, BRIDGE_ARC_W - 0.06f}) {
            for (int k = 0; k < nv; ++k) {
                const float t0 = -v_beta + 2.0f * v_beta * static_cast<float>(k)
                               / static_cast<float>(nv);
                const float t1 = -v_beta + 2.0f * v_beta * static_cast<float>(k + 1)
                               / static_cast<float>(nv);
                const float rm = v_R + VAULT_RING * 0.5f;
                (void)f.beam(f.v(BRIDGE_ARC_CX + rm * std::sin(t0), v_cy + rm * std::cos(t0), z),
                             f.v(BRIDGE_ARC_CX + rm * std::sin(t1), v_cy + rm * std::cos(t1), z),
                             {{"radius", "0.17"}, {"form", "square"}, {"mat", "3"},
                              {"tone", "1"}, {"wear", "0.5"}});
            }
        }
    }

    // 5. ЛИЦЕВАЯ СТЕНА — ОДНА ДЕТАЛЬ ОТ ПОДОШВЫ ДО ВЕРХА ПАРАПЕТА, и это ответ
    // на «парапет один»: борт и пазуха собраны одним контуром в плоскости z,
    // у которого лица два по построению, а не цепочкой, у которой лицо одно и
    // повёрнуто порядком вершин. Низ идёт по экстрадосу свода в пролёте и по
    // подошве устоя за ним — пазуха закрыта, арка открыта.
    // РАЗБИЕНИЕ ЛИЦА ИДЁТ ПО ПЯТАМ СВОДА, А НЕ ПО ЗВЕНЬЯМ НАСТИЛА. Если резать
    // лицо равномерно, граница «устой / пазуха» попадает в середину куска, и
    // низ куска едет наискось от -0.60 до экстрадоса — пята свода получается
    // срезанной по диагонали. Здесь x = 1.6 и 6.4 стоят В СПИСКЕ узлов, и
    // перепад низа приходится ровно на пяту, шириной в ноль.
    const float FACE_FOOT = -0.60f;
    std::vector<std::pair<float, float>> face;  // (x, низ лица)
    {
        const auto push = [&](float x, float lo) { face.emplace_back(x, lo); };
        const int n_ab = 6;   // устой: от торца до пяты
        const int n_vt = 12;  // пазуха: по экстрадосу свода
        for (int k = 0; k <= n_ab; ++k) {
            push(B.x0 + (SPR_X0 - B.x0) * static_cast<float>(k) / n_ab, FACE_FOOT);
        }
        for (int k = 0; k <= n_vt; ++k) {
            const float x = SPR_X0 + (SPR_X1 - SPR_X0) * static_cast<float>(k) / n_vt;
            push(x, vault_y(x, VAULT_RING));
        }
        for (int k = 0; k <= n_ab; ++k) {
            push(SPR_X1 + (B.x1 - SPR_X1) * static_cast<float>(k) / n_ab, FACE_FOOT);
        }
    }
    for (const float z : {BRIDGE_ARC_KERB, BRIDGE_ARC_W - BRIDGE_ARC_KERB}) {
        const bool north = z < BRIDGE_ARC_W * 0.5f;
        for (std::size_t i = 0; i + 1 < face.size(); ++i) {
            const float xa = face[i].first;
            const float xb = face[i + 1].first;
            if (xb - xa < 1e-4f) {
                continue;  // перепад низа на пяте: ширина ноль, куска нет
            }
            const VertexId p1 = f.v(xa, face[i].second, z);
            const VertexId p2 = f.v(xb, face[i + 1].second, z);
            const VertexId p3 = f.v(xb, B.top(xb) + BRIDGE_ARC_RAIL, z);
            const VertexId p4 = f.v(xa, B.top(xa) + BRIDGE_ARC_RAIL, z);
            const std::vector<VertexId> ring =
                north ? std::vector<VertexId>{p4, p3, p2, p1}
                      : std::vector<VertexId>{p1, p2, p3, p4};
            (void)f.contour(ring,
                            {{"thickness", "0.30"}, {"mat", "3"}, {"tone", "1"},
                             {"fill", "3"}, {"wear", "0.55"},
                             {"unsupported", "1"}});
        }
        // КОПИНГ поверх парапета и ПОЯСОК на уровне настила: без напуска борт
        // читается обрубленным (тот же довод, что у копинга подпорной стенки),
        // а поясок прячет торец плиты настила и делит парапет и пазуху.
        for (std::size_t i = 0; i + 1 < spine.size(); ++i) {
            const float xa = spine[i].x;
            const float xb = spine[i + 1].x;
            (void)f.beam(f.v(xa, B.top(xa) + BRIDGE_ARC_RAIL, z),
                         f.v(xb, B.top(xb) + BRIDGE_ARC_RAIL, z),
                         {{"radius", "0.19"}, {"form", "plank"}, {"mat", "3"},
                          {"tone", "1"}, {"wear", "0.45"}});
            (void)f.beam(f.v(xa, B.top(xa) - 0.14f, z),
                         f.v(xb, B.top(xb) - 0.14f, z),
                         {{"radius", "0.11"}, {"form", "plank"}, {"mat", "3"},
                          {"tone", "1"}, {"wear", "0.5"}});
        }
    }

    std::fprintf(stderr,
                 "forge: арка моста — торцы x %.3f..%.3f (%.3f м), замок %.3f, "
                 "торец %.3f, горб над прежним настилом %.3f, уклон аппарели "
                 "%.1f%%, дуга гребня R %.2f от %.3f до %.3f, свод: просвет "
                 "%.2f, подъём %.2f, R %.3f\n",
                 static_cast<double>(B.x0), static_cast<double>(B.x1),
                 static_cast<double>(B.x1 - B.x0),
                 static_cast<double>(BRIDGE_ARC_CROWN),
                 static_cast<double>(BRIDGE_ARC_TIP),
                 static_cast<double>(BRIDGE_ARC_CROWN - 0.35f),
                 static_cast<double>(BRIDGE_ARC_GRADE * 100.0f),
                 static_cast<double>(BRIDGE_ARC_RV),
                 static_cast<double>(BRIDGE_ARC_CX - B.curve_half),
                 static_cast<double>(BRIDGE_ARC_CX + B.curve_half),
                 static_cast<double>(SPR_X1 - SPR_X0),
                 static_cast<double>(VAULT_RISE), static_cast<double>(v_R));
    f.save(file);
}

/// МАЛЫЙ ДОМ 4.5x6: фахверк под дранкой, полный набор деталей.
static void forge_small_house(Aging age) {
    Forge f;
    const float W = 4.5f;
    const float D = 6.0f;
    const float H = 2.8f;
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, D);
        const VertexId c = f.v(W, 0.06f, D);
        const VertexId d = f.v(W, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}, {"wear", age.w(0.5f)}});
    }
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, H, "0");
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params clad = {{"height", "2.8"}, {"thickness", "0.25"}, {"mat", "5"},
                   {"tone", "0"}, {"clad", "1"}, {"windows", "1"},
                   {"shutters", age.shutters()}, {"plinth", age.plinth()},
                   {"wear", age.w(0.45f)}};
    (void)f.wall(ne, nw, clad);
    (void)f.wall(se, ne, clad);
    (void)f.wall(nw, sw, clad);
    (void)f.wall(sw, se,
                 {{"height", "2.8"}, {"thickness", "0.25"}, {"mat", "5"},
                  {"tone", "0"}, {"clad", "1"}, {"doors", "1"}, {"porch", "1"},
                  {"plinth", age.plinth()}, {"wear", age.w(0.45f)}});
    f.door_leaf(W * 0.5f, 0.0f, D);
    f.gable_roof(0.0f, 0.0f, W, D, H, 2.2f, "6", "0", "", age.w(0.35f));
    // ТРУБА — МАРКЕР ДОСТАТКА, И ПОТОМУ У ДВУХ ВЕРСИЙ РАЗНАЯ. Ухоженный домик
    // держит каменную трубу с напуском выше конька; у ветхого её нет —
    // осталась низкая обкладка дымового отверстия по скату. Асимметрия
    // бесплатно даёт разноту силуэта и градиент достатка: бьёт разом по
    // претензии [13] (ноль труб) и [14] (один силуэт на всю застройку).
    // Точка — от расстановки (очаг у глухой стены z=0, центр 4.5 м стены).
    if (vents_on()) {
        const float ridge_y = H + 2.2f;
        const float cz = 1.05f;
        if (age.kept) {
            f.chimney(2.25f, cz, H - 0.4f, ridge_y + 0.55f, 0.30f, true, "3", "1",
                      age.w(0.5f));
        } else {
            // ВЫСОТА ОБКЛАДКИ МЕРИТСЯ ПО ВЕРХНЕЙ ПО СКАТУ КРОМКЕ, А НЕ ПО ОСИ.
            // Скат под обкладкой поднимается на её же ширину, и отсчёт 0.35 от
            // ОСИ оставлял на верхней кромке 10 см — обкладка наполовину тонула
            // в кровле (мерено: верх 3.72 против ската 3.62 в 26 см выше по
            // скату). Отсчёт от cz + r это чинит: стало 3.84, выступ 0.22 сверху
            // и 0.46 снизу по скату.
            // ЗАПАС НЕ РАВЕН ЗАКАЗАННЫМ 0.35, И ЭТО НЕ ПРОМАХ: roof_y_at даёт
            // СРЕДИННУЮ плоскость ската, а видимый верх выше неё на полтолщины
            // настила плюс ряд дранки — мерено 0.13. Обкладке этого хватает:
            // 0.22 м камня над кровлей — это обкладка отверстия, а 0.35 читались
            // бы уже обрубком трубы, то есть не тем, что заказано.
            const float r = 0.26f;
            f.chimney(2.25f, cz, H - 0.4f,
                      Forge::roof_y_at(cz + r, 0.0f, D, H, 2.2f) + 0.45f, r, false,
                      "3", "1", age.w(0.5f));
        }
    }
    f.save(age.file);
}

/// ЛАВКА 6x6 с верандой-навесом на столбах.
static void forge_shop(Aging age) {
    Forge f;
    const float W = 6.0f;
    const float D = 6.0f;
    const float H = 2.9f;
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, D);
        const VertexId c = f.v(W, 0.06f, D);
        const VertexId d = f.v(W, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}, {"wear", age.w(0.35f)}});
    }
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, H, "0");
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params low = {{"height", "2.9"}, {"thickness", "0.28"}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                  {"plinth", age.plinth()}, {"wear", age.w(0.5f)}};
    (void)f.wall(ne, nw, low);
    (void)f.wall(se, ne, low);
    (void)f.wall(nw, sw, low);
    (void)f.wall(sw, se,
                 {{"height", "2.9"}, {"thickness", "0.28"}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"doors", "1"}, {"porch", "1"},
                  {"wear", age.w(0.5f)}});
    f.door_leaf(W * 0.5f, 0.0f, D);
    // Веранда: столбы + прогон + наклонный навес перед входом.
    for (const float x : {0.4f, W - 0.4f}) {
        const VertexId lo = f.v(x, 0.0f, D + 2.0f);
        const VertexId hi = f.v(x, 2.4f, D + 2.0f);
        (void)f.beam(lo, hi, {{"radius", "0.11"}, {"mat", "0"}, {"tone", "2"}});
    }
    {
        // Прогон по верху столбов: скату видно, НА ЧЁМ он лежит («навес
        // держится на одной тумбе» — глаза, кадр houses 21.08).
        const VertexId pa = f.v(0.3f, 2.42f, D + 2.0f);
        const VertexId pb = f.v(W - 0.3f, 2.42f, D + 2.0f);
        (void)f.beam(pa, pb, {{"radius", "0.09"}, {"mat", "0"}, {"tone", "2"}});
    }
    {
        // Верх навеса заведён ГЛУБЖЕ под свес и выше: тёмная щель между
        // скатом дома и навесом закрыта нахлёстом (глаза, тот же кадр).
        const VertexId a = f.v(-0.3f, H + 0.62f, D - 0.5f);
        const VertexId b = f.v(W + 0.3f, H + 0.62f, D - 0.5f);
        const VertexId c = f.v(W + 0.3f, 2.35f, D + 2.4f);
        const VertexId d = f.v(-0.3f, 2.35f, D + 2.4f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}, {"fill", "7"},
                                       {"wear", age.w(0.4f)}});
        (void)f.g.set_param(s, "roof", "1");
        (void)f.g.set_param(s, "unsupported", "1");
    }
    f.gable_roof(0.0f, 0.0f, W, D, H, 1.7f, "1", "0", "7", age.w(0.35f));
    // ТРУБА: обе версии носят её — лавка топит и ветхой, а у запущенной это
    // КУЗНИЦА, где горн стоит по середине глухой стены (x 3.0), тогда как у
    // ухоженной очаг сдвинут к западу (x 1.15): восток занят прилавком и
    // стеллажами. Точки — вход от расстановки, не мой выбор.
    if (vents_on()) {
        f.chimney(age.kept ? 1.15f : 3.0f, 1.10f, H - 0.4f, H + 1.7f + 0.6f, 0.32f,
                  true, "3", "1", age.w(0.5f));
    }
    f.save(age.file);
}

/// ПРИЛАВОК РЫНКА: стойка + столбы + тент-навес.
static void forge_stall(const char* file) {
    Forge f;
    {
        const VertexId a = f.v(0.0f, 0.85f, 0.0f);
        const VertexId b = f.v(0.0f, 0.85f, 0.9f);
        const VertexId c = f.v(2.2f, 0.85f, 0.9f);
        const VertexId d = f.v(2.2f, 0.85f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.08"}, {"mat", "1"}, {"tone", "1"},
                         {"unsupported", "1"}});
    }
    // Столбы потолще прежних 0.07 — «плоские карточки без опор» (глаза,
    // кадр roofs 21.08): тонкий брус на общем плане исчезал.
    for (const float x : {0.1f, 2.1f}) {
        const VertexId lo = f.v(x, 0.0f, 0.1f);
        const VertexId hi = f.v(x, 2.2f, 0.1f);
        (void)f.beam(lo, hi, {{"radius", "0.10"}, {"mat", "0"}, {"tone", "2"}});
        const VertexId lo2 = f.v(x, 0.0f, 0.8f);
        const VertexId hi2 = f.v(x, 1.9f, 0.8f);
        (void)f.beam(lo2, hi2, {{"radius", "0.10"}, {"mat", "0"}, {"tone", "2"}});
    }
    // Юбка прилавка: дощатая панель от земли до столешницы, лицом к покупателю.
    {
        const VertexId a = f.v(0.05f, 0.04f, 0.06f);
        const VertexId b = f.v(0.05f, 0.80f, 0.06f);
        const VertexId c = f.v(2.15f, 0.80f, 0.06f);
        const VertexId d = f.v(2.15f, 0.04f, 0.06f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.05"}, {"mat", "1"}, {"tone", "2"},
                         {"unsupported", "1"}});
    }
    {
        const VertexId a = f.v(-0.25f, 2.25f, -0.35f);
        const VertexId b = f.v(2.45f, 2.25f, -0.35f);
        const VertexId c = f.v(2.45f, 1.85f, 1.25f);
        const VertexId d = f.v(-0.25f, 1.85f, 1.25f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.10"}, {"mat", "5"},
                                       {"tone", "0"}, {"unsupported", "1"}});
        (void)f.g.set_param(s, "paint", "3"); // тент — красная фалу
        (void)f.g.set_param(s, "roof", "1");
    }
    f.save(file);
}

/// КОЛОДЕЦ: каменное кольцо, столбы, мини-двускат.
static void forge_well(const char* file) {
    Forge f;
    const float S = 1.7f;
    const glm::vec3 pts[5] = {{0, 0, 0}, {S, 0, 0}, {S, 0, S}, {0, 0, S}, {0, 0, 0}};
    for (int k = 0; k < 4; ++k) {
        const VertexId a = f.v(pts[k].x, 0.0f, pts[k].z);
        const VertexId b = f.v(pts[k + 1].x, 0.0f, pts[k + 1].z);
        (void)f.wall(a, b,
                     {{"height", "0.9"}, {"thickness", "0.28"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.55"}});
    }
    // СТОЙКИ ДОРАСТАЮТ ДО ПРОГОНА, А ПРОГОН ДЕРЖИТ СТРОПИЛА. Прежде столбы
    // кончались на 2.2, а вся кровля начиналась с 2.5 (головы стропил) — крыша
    // колодца висела в воздухе отдельным островом, ближайшее её тело в 0.21 м
    // от макушки столба. Число 2.5 не выбрано: gable_roof ставит головы стропил
    // ровно на (карниз + подъём) − 0.30 = 2.1 + 0.7 − 0.30, и прогон кладётся
    // на ту же ось, поэтому касается и стропил, и столбов телом.
    const float PURLIN_Y = 2.1f + 0.7f - 0.30f;
    for (const float x : {0.2f, S - 0.2f}) {
        const VertexId lo = f.v(x, 0.0f, S * 0.5f);
        const VertexId hi = f.v(x, PURLIN_Y, S * 0.5f);
        (void)f.beam(lo, hi, {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
    }
    (void)f.beam(f.v(-0.15f, PURLIN_Y, S * 0.5f), f.v(S + 0.15f, PURLIN_Y, S * 0.5f),
                 {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}});
    f.gable_roof(-0.15f, S * 0.5f - 0.8f, S + 0.15f, S * 0.5f + 0.8f, 2.1f, 0.7f,
                 "1", "2", "7", "0.5");
    f.save(file);
}

/// ДЛИННЫЙ ЗАЛ (Йоррваскр): 16x8, торцевой вход, огромные свесы.
static void forge_longhall(const char* file) {
    Forge f;
    const float W = 16.0f;
    const float D = 8.0f;
    const float H = 3.6f;
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, D);
        const VertexId c = f.v(W, 0.06f, D);
        const VertexId d = f.v(W, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.14"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "2"}, {"wear", "0.5"}});
    }
    f.frame_posts(0.0f, 0.0f, W, D, 0.0f, H, "0");
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params logw = {{"height", "3.6"}, {"thickness", "0.3"}, {"mat", "0"},
                   {"tone", "2"}, {"fill", "4"}, {"logends", "1"},
                   {"wear", "0.5"}};
    (void)f.wall(ne, nw, logw);
    (void)f.wall(nw, sw, logw);
    (void)f.wall(sw, se, logw);
    // Восточный торец — вход.
    (void)f.wall(se, ne,
                 {{"height", "3.6"}, {"thickness", "0.3"}, {"mat", "0"},
                  {"tone", "2"}, {"fill", "4"}, {"logends", "1"},
                  {"doors", "1"}, {"porch", "1"}, {"wear", "0.5"}});
    {
        // Дверь на восточной стене (вдоль Z).
        f.door_leaf_x(W, 0.0f, D * 0.5f);
    }
    // Потолок с балками.
    {
        const VertexId a = f.v(0.0f, H, 0.0f);
        const VertexId b = f.v(0.0f, H, D);
        const VertexId c = f.v(W, H, D);
        const VertexId d = f.v(W, H, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "2"},
                         {"beams", "1"}});
    }
    f.gable_roof(0.0f, 0.0f, W, D, H, 2.6f, "6", "0", "", "0.4");
    // ДЫМНИК, А НЕ ТРУБА. У Йоррваскра очаг стоит ПОСРЕДИ ЗАЛА (8.0, 4.0), то
    // есть ровно под коньком, и каменный стояк там пришлось бы вести сквозь
    // конёк-бревно. У длинного зала с очагом в полу дым и в жизни уходит в
    // отверстие на коньке под деревянным колпаком — его и ставим: тёмный
    // тёсаный брус (mat 0) с широким напуском, а не камень.
    if (vents_on()) {
        const float ridge_y = H + 2.6f;
        f.chimney(8.0f, 4.0f, ridge_y - 0.45f, ridge_y + 0.85f, 0.50f, true, "0",
                  "2", "0.5");
    }
    f.save(file);
}

/// ХРАМ: каменный зал с портиком.
static void forge_temple(const char* file) {
    Forge f;
    const float W = 12.0f;
    const float D = 8.0f;
    const float H = 4.6f;
    {
        const VertexId a = f.v(0.0f, 0.05f, 0.0f);
        const VertexId b = f.v(0.0f, 0.05f, D);
        const VertexId c = f.v(W, 0.05f, D);
        const VertexId d = f.v(W, 0.05f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.35"}});
    }
    const VertexId nw = f.v(0.0f, 0.0f, 0.0f);
    const VertexId ne = f.v(W, 0.0f, 0.0f);
    const VertexId se = f.v(W, 0.0f, D);
    const VertexId sw = f.v(0.0f, 0.0f, D);
    Params st = {{"height", "4.6"}, {"thickness", "0.4"}, {"mat", "3"},
                 {"tone", "1"}, {"fill", "3"}, {"windows", "2"},
                 {"plinth", "1"}, {"wear", "0.4"}};
    (void)f.wall(ne, nw, st);
    (void)f.wall(se, ne, st);
    (void)f.wall(nw, sw, st);
    (void)f.wall(sw, se,
                 {{"height", "4.6"}, {"thickness", "0.4"}, {"mat", "3"},
                  {"tone", "1"}, {"fill", "3"}, {"doors", "1"}, {"porch", "1"},
                  {"wear", "0.4"}});
    f.door_leaf(W * 0.5f, 0.0f, D);
    // Портик: 4 колонны + плита-навес.
    for (int k = 0; k < 4; ++k) {
        const float x = W * 0.5f - 2.7f + 1.8f * static_cast<float>(k);
        const VertexId lo = f.v(x, 0.0f, D + 2.2f);
        const VertexId hi = f.v(x, 3.4f, D + 2.2f);
        (void)f.beam(lo, hi,
                     {{"radius", "0.22"}, {"sides", "8"}, {"mat", "3"},
                      {"tone", "1"}});
    }
    {
        const VertexId a = f.v(W * 0.5f - 3.3f, H - 0.2f, D - 0.2f);
        const VertexId b = f.v(W * 0.5f + 3.3f, H - 0.2f, D - 0.2f);
        const VertexId c = f.v(W * 0.5f + 3.3f, 3.35f, D + 2.6f);
        const VertexId d = f.v(W * 0.5f - 3.3f, 3.35f, D + 2.6f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.15"}, {"mat", "1"},
                                       {"tone", "1"}, {"fill", "7"},
                                       {"wear", "0.35"}});
        (void)f.g.set_param(s, "roof", "1");
        (void)f.g.set_param(s, "unsupported", "1");
    }
    // НОРДСКАЯ ПАЛИТРА (приёмка №2: красная черепица — «чужеродно сильнее
    // всего»): тёмный гонт, как у всего города.
    f.gable_roof(0.0f, 0.0f, W, D, H, 2.2f, "4", "0", "8", "0.35");
    f.save(file);
}

/// ЯРУС ЧЕТЫРЁХ СТЕН — ОБЩАЯ РУКА (была лямбдой внутри forge_keep; вынесена
/// 22.08, когда за ярусами пришли ещё два рецепта — компактный замок и
/// мельница). Длинные и короткие стены просят РАЗНОЕ число проёмов: общий
/// ring() старого замка ставил всем поровну, и торцы шли решетом.
/// Порядок постановки стен (север, восток, запад, юг) — часть контракта: по
/// нему нумеруются вершины в .dfh, и перестановка перепекла бы принятый
/// city-keep.dfh.
/// door — сторона с проёмом и крыльцом: 0 (нет), 'S' (юг) или 'W' (запад,
/// мельница смотрит дверью на дорогу вдоль реки); остальные стены получают
/// свои окна.
static void keep_tier(Forge& f, float y, float h, float x0, float z0, float x1,
                      float z1, const char* th, const char* mat,
                      const char* tone, const char* fill, const char* win_long,
                      const char* win_short, const char* wear, bool clad,
                      bool shutters, bool plinth, char door) {
    char hb[16];
    std::snprintf(hb, sizeof(hb), "%.2f", h);
    const VertexId nw = f.v(x0, y, z0);
    const VertexId ne = f.v(x1, y, z0);
    const VertexId se = f.v(x1, y, z1);
    const VertexId sw = f.v(x0, y, z1);
    const auto put = [&](VertexId a, VertexId b, const char* win, bool has_door) {
        const ElementId id =
            f.wall(a, b,
                   {{"height", hb}, {"thickness", th}, {"mat", mat},
                    {"tone", tone}, {"wear", wear}});
        if (fill[0] != 0) {
            (void)f.g.set_param(id, "fill", fill);
        }
        if (clad) {
            (void)f.g.set_param(id, "clad", "1");
        }
        if (shutters) {
            (void)f.g.set_param(id, "shutters", "1");
        }
        if (plinth) {
            (void)f.g.set_param(id, "plinth", "1");
        }
        if (has_door) {
            (void)f.g.set_param(id, "doors", "1");
            (void)f.g.set_param(id, "porch", "1");
        } else if (win[0] != 0) {
            (void)f.g.set_param(id, "windows", win);
        }
    };
    put(ne, nw, win_long, door == 'N');   // север, лицо на север
    put(se, ne, win_short, door == 'E');  // восток
    put(nw, sw, win_short, door == 'W');  // запад
    put(sw, se, win_long, door == 'S');
}

/// ЗАМОК-ДОМИНАНТА (Драконий Предел; заказ 21.08 «замок сделать более
/// солидным»: зал 20x12 с коньком 11.6 читался сараем рядом с донжоном 14).
/// ЯРУСНЫЙ ОБЪЁМ, как у ставкирки: зал 26x14 на каменном стилобате 2.9, два
/// фахверковых яруса до нижнего карниза 9.8, широкие нижние скаты уходят к
/// узкому ФОНАРЮ (глубина 7 вместо 14) со своей двускатной кровлей — конёк
/// 18.8. Четыре угловые каменные башенки-контрфорса до нижнего карниза с
/// шатриками, парадный портал с террасой, широким маршем и двумя башенками.
/// ПОЧЕМУ ЯРУСЫ, А НЕ ПРОСТО ВЫШЕ КОНЁК: один скат от 9.8 до 18.8 дал бы
/// 60-градусную стену гонта в пол-силуэта — доминанта читается ступенями,
/// каждая уже нижней (CITY_DESIGN_GUIDE.md §8).
static void forge_keep(const char* file) {
    Forge f;
    const float W = 26.0f;    // зал по фасаду
    const float D = 14.0f;    // глубина зала
    const float YS = 2.9f;    // верх каменного стилобата
    const float Y2 = 6.35f;   // пол верхнего зала
    const float E1 = 9.8f;    // нижний карниз (и верх угловых башенок)
    const float LZ0 = 3.5f;   // фонарь: северная стена
    const float LZ1 = 10.5f;  // фонарь: южная стена
    const float LTOP = 12.4f; // где нижний скат встречает фонарь
    const float E2 = 14.2f;   // карниз фонаря
    const float SO = 0.45f;   // свес нижних скатов
    const float RIDGE_H = 4.6f; // конёк 18.8

    // Ярус четырёх стен — общая рука keep_tier() выше (вынесена 22.08 под
    // компактный замок и мельницу).
    const auto tier = [&](float y, float h, float x0, float z0, float x1, float z1,
                          const char* th, const char* mat, const char* tone,
                          const char* fill, const char* win_long,
                          const char* win_short, const char* wear, bool clad,
                          bool shutters, bool plinth, bool door_south) {
        keep_tier(f, y, h, x0, z0, x1, z1, th, mat, tone, fill, win_long,
                  win_short, wear, clad, shutters, plinth,
                  door_south ? 'S' : '\0');
    };

    // ---------------- каменная башенка с шатриком (контрфорс/фланкер) -----
    const auto turret = [&](float x0, float z0, float s, float h, float rise,
                            const char* th, const char* wear) {
        const float x1 = x0 + s;
        const float z1 = z0 + s;
        char hb[16];
        std::snprintf(hb, sizeof(hb), "%.2f", h);
        const auto q = [&](float ax, float az, float bx, float bz) {
            (void)f.wall(f.v(ax, 0.0f, az), f.v(bx, 0.0f, bz),
                         {{"height", hb}, {"thickness", th}, {"mat", "3"},
                          {"tone", "1"}, {"fill", "3"}, {"wear", wear}});
        };
        q(x1, z0, x0, z0);  // север
        q(x1, z1, x1, z0);  // восток
        q(x0, z0, x0, z1);  // запад
        q(x0, z1, x1, z1);  // юг
        const float o = 0.22f;
        {
            const VertexId a = f.v(x0 - o, h, z0 - o);
            const VertexId b = f.v(x0 - o, h, z1 + o);
            const VertexId c = f.v(x1 + o, h, z1 + o);
            const VertexId d = f.v(x1 + o, h, z0 - o);
            (void)f.contour({a, b, c, d},
                            {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                             {"wear", wear}, {"unsupported", "1"}});
        }
        // Шатрик: четыре треугольника к вершине (рука башни/донжона).
        // БАЗА — h + ПОЛТОЛЩИНЫ плиты (0.2/2), а не h + толщина: контур растёт
        // в обе стороны от своей высоты, и прежние h + 0.2 вешали шатрик над
        // площадкой. Судья связности мерил 0.047 м по нормали ската — это те
        // же 0.1 по вертикали, посчитанные вдоль наклонной плоскости.
        const float base = h + 0.1f;
        const glm::vec3 corners[4] = {{x0 - o, base, z0 - o},
                                      {x1 + o, base, z0 - o},
                                      {x1 + o, base, z1 + o},
                                      {x0 - o, base, z1 + o}};
        const glm::vec3 apex{(x0 + x1) * 0.5f, base + rise, (z0 + z1) * 0.5f};
        for (int k = 0; k < 4; ++k) {
            const glm::vec3 c1 = corners[k];
            const glm::vec3 c2 = corners[(k + 1) % 4];
            const ElementId t = f.contour({f.v(c1.x, c1.y, c1.z),
                                           f.v(c2.x, c2.y, c2.z),
                                           f.v(apex.x, apex.y, apex.z)},
                                          {{"thickness", "0.12"}, {"mat", "1"},
                                           {"tone", "1"}, {"wear", "0.4"},
                                           {"unsupported", "1"}});
            (void)f.g.set_param(t, "roof", "1");
        }
    };

    // ---------------- подошва-стилобат и парадный двор --------------------
    // Плита на 1.6 шире плана: замок СТОИТ НА ПЛИТЕ, а не воткнут в землю.
    // Плита ЛЕЖИТ НА ЗЕМЛЕ, а не ступенью: контур выдавливается ОТ СВОЕЙ
    // высоты в обе стороны, и 0.02 при толщине 0.2 даёт кромку 0.12 —
    // проверенный подиум прежнего замка. Толще — и парадный двор обзаводится
    // порогом на входе.
    {
        const VertexId a = f.v(-1.6f, 0.02f, -1.6f);
        const VertexId b = f.v(-1.6f, 0.02f, D + 1.6f);
        const VertexId c = f.v(W + 1.6f, 0.02f, D + 1.6f);
        const VertexId d = f.v(W + 1.6f, 0.02f, -1.6f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.4"}});
    }
    // Терраса перед порталом (стык с подошвой в z = D+1.6, без нахлёста:
    // две компланарные плиты на одной высоте дерутся за пиксель).
    {
        const VertexId a = f.v(5.0f, 0.02f, D + 1.6f);
        const VertexId b = f.v(5.0f, 0.02f, D + 7.0f);
        const VertexId c = f.v(W - 5.0f, 0.02f, D + 7.0f);
        const VertexId d = f.v(W - 5.0f, 0.02f, D + 1.6f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.4"}});
    }
    // Бортики террасы по бокам (марш посередине остаётся свободным).
    for (const float px : {5.0f, W - 5.0f}) {
        (void)f.wall(f.v(px, 0.0f, D + 1.6f), f.v(px, 0.0f, D + 7.0f),
                     {{"height", "0.7"}, {"thickness", "0.4"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.45"}});
    }
    // ОДИН ШИРОКИЙ МАРШ 12 м (open=2): стык двух маршей дважды ловил бота в
    // щель, и парадному входу пара узких лесенок всё равно не по чину.
    {
        const VertexId a = f.v(7.0f, -3.0f, D + 13.0f);
        const VertexId b = f.v(W - 7.0f, -3.0f, D + 13.0f);
        const VertexId c = f.v(W - 7.0f, 0.0f, D + 7.0f);
        const VertexId d = f.v(7.0f, 0.0f, D + 7.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                         {"mat", "3"}, {"tone", "1"}, {"wear", "0.4"},
                         {"unsupported", "1"}});
    }

    // ---------------- три яруса стен зала ---------------------------------
    // Стилобат: камень 0.9 толщиной — в полтора раза толще прежнего низа,
    // и уступ на 2.9 читается тенью по всему периметру.
    tier(0.0f, YS, 0.0f, 0.0f, W, D, "0.9", "3", "1", "3", "3", "2", "0.42",
         false, false, true, true);
    tier(YS, Y2 - YS, 0.0f, 0.0f, W, D, "0.4", "5", "0", "", "4", "2", "0.38",
         true, true, false, false);
    tier(Y2, E1 - Y2, 0.0f, 0.0f, W, D, "0.4", "5", "0", "", "4", "2", "0.35",
         true, true, false, false);
    // Фонарь: тот же фасад по X, но вдвое меньше глубина — ярус сужается.
    tier(E1, E2 - E1, 0.0f, LZ0, W, LZ1, "0.35", "5", "0", "", "5", "1", "0.35",
         true, false, false, false);
    f.door_leaf(W * 0.5f, 0.0f, D);
    // Каркас фахверка: угловые стойки от стилобата до карниза + обвязка-карниз.
    f.frame_posts(0.0f, 0.0f, W, D, YS, E1, "0");
    // Междуэтажный поясок — тёсовая слега по всем четырём фасадам: без него
    // 6.9 м фахверка над стилобатом стоят одной пустой доской.
    {
        const glm::vec3 p[5] = {{0.0f, Y2, 0.0f}, {W, Y2, 0.0f}, {W, Y2, D},
                                {0.0f, Y2, D},    {0.0f, Y2, 0.0f}};
        for (int k = 0; k < 4; ++k) {
            (void)f.beam(f.v(p[k].x, p[k].y, p[k].z),
                         f.v(p[k + 1].x, p[k + 1].y, p[k + 1].z),
                         {{"radius", "0.11"}, {"form", "plank"}, {"mat", "0"},
                          {"tone", "2"}});
        }
    }

    // ---------------- полы, лестница --------------------------------------
    // Пол верхнего зала — ДВА прямоугольника вокруг лестничного колодца:
    // невыпуклый контур пришлось бы триангулировать веером, а колодец нужен —
    // марш упирался в сплошную плиту.
    {
        const VertexId a = f.v(4.7f, Y2, 0.0f);
        const VertexId b = f.v(4.7f, Y2, D);
        const VertexId c = f.v(W, Y2, D);
        const VertexId d = f.v(W, Y2, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    // МАРШ РАЗВЁРНУТ И СТАЛ 45-ГРАДУСНЫМ, А КРОМКА ВЕРХНЕГО ПОЛА ЕДЕТ ЗА НИМ
    // (правка 27.08, и первым это нашёл судья, а не чтение кода).
    //
    // ЧТО СТОЯЛО: марш шёл С ЮГА (z 13.4) НА СЕВЕР и кончался на z 0.7, а
    // кромка верхнего пола лежала на z 5.5, то есть ПОЗАДИ поднявшегося и в
    // четырёх с лишним метрах от него. Марш выводил В ПУСТОТУ: наверху не на
    // что было ступить. Приёмка этого не видела, потому что мерила просвет над
    // ступенями (2.218, честные) и не спрашивала, ЕСТЬ ЛИ площадка.
    // ЧТО СТОИТ: подножие у северной стены, ход на юг, и верх приходит ровно
    // на кромку пола — она считается ОТ НЕГО (§13: марш и проём кладёт одна
    // рука). Ход 6.31 вместо 12.70: колодец перестал занимать зал по диагонали.
    // МАРШ ЛОМАЕТСЯ ПЛОЩАДКОЙ (правка 28.08 по кадрам аудита). ЧИСЛО, ИЗ
    // КОТОРОГО ЭТО СЛЕДУЕТ, — ЧИСЛО СТУПЕНЕЙ: подъём 6.31 при подступёнке 0.28
    // это 23 ступени ПОДРЯД, без единой остановки. Ни один жилой норматив
    // столько не разрешает (обычный потолок — 16..18), и глаз читает такое как
    // приставную стремянку, а не как парадный подъём замка. Делится ПОПОЛАМ:
    // два марша по 3.15 (11 и 12 ступеней) и площадка 1.4 м между ними —
    // ровно диаметр капсулы плюс её радиус с каждой стороны, то есть
    // остановившийся помещается на площадке ЦЕЛИКОМ и успевает повернуть.
    // КРОМКА ВЕРХНЕГО ПОЛА СЧИТАЕТСЯ ОТ ВЕРХА ВТОРОГО МАРША, как и была (§13).
    const float KS_LO = 0.12f;          // верх плиты стилобата (плоскость 0.02)
    const float KS_HI = Y2 + 0.075f;    // верх настила верхнего зала
    const float KS_FOOT = 0.9f;         // от внутренней грани северной стены
    const float KS_LAND_W = 1.4f;                     // длина площадки
    const float KS_MID = (KS_LO + KS_HI) * 0.5f;      // отметка площадки
    const float KS_Z1 = KS_FOOT + (KS_MID - KS_LO);   // верх нижнего марша
    const float KS_Z2 = KS_Z1 + KS_LAND_W;            // подножие верхнего
    const float KS_TOP = KS_Z2 + (KS_HI - KS_MID);
    {
        const VertexId a = f.v(0.0f, Y2, KS_TOP);
        const VertexId b = f.v(0.0f, Y2, D);
        const VertexId c = f.v(4.7f, Y2, D);
        const VertexId d = f.v(4.7f, Y2, KS_TOP);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    // Марш посреди зала: ограждаются ОБЕ грани (rail_x = середина).
    house_flight_z(f, 1.0f, 4.2f, KS_FOOT, KS_Z1, KS_LO, KS_MID, 2.6f, "0.35");
    house_landing(f, 1.0f, KS_Z1, 4.2f, KS_Z2, KS_MID, 0.12f, "0.35");
    house_flight_z(f, 1.0f, 4.2f, KS_Z2, KS_TOP, KS_MID, KS_HI, 2.6f, "0.35");

    // ---------------- нижние скаты к фонарю -------------------------------
    // Не двускат: широкая пола от карниза 9.8 вверх к стене фонаря 12.4.
    const auto shed = [&](VertexId a, VertexId b, VertexId c, VertexId d) {
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.16"}, {"mat", "1"}, {"tone", "1"},
                         {"fill", "7"}, {"wear", "0.4"}});
    };
    shed(f.v(-SO, LTOP, LZ0), f.v(W + SO, LTOP, LZ0),
         f.v(W + SO, E1 - 0.1f, -SO), f.v(-SO, E1 - 0.1f, -SO));
    shed(f.v(W + SO, LTOP, LZ1), f.v(-SO, LTOP, LZ1),
         f.v(-SO, E1 - 0.1f, D + SO), f.v(W + SO, E1 - 0.1f, D + SO));
    f.eaves_trim({-SO, E1 - 0.1f, -SO}, {W + SO, E1 - 0.1f, -SO});
    f.eaves_trim({-SO, E1 - 0.1f, D + SO}, {W + SO, E1 - 0.1f, D + SO});
    // Торцы скатов: треугольники в плоскостях x=0 и x=W по бокам от фонаря —
    // ими же держится верхняя кромка полы (правило опоры крыш).
    for (const float gx : {0.0f, W}) {
        (void)f.contour({f.v(gx, E1, -SO), f.v(gx, E1, LZ0), f.v(gx, LTOP, LZ0)},
                        {{"thickness", "0.14"}, {"mat", "5"}, {"tone", "0"},
                         {"clad", "1"}, {"wear", "0.35"}});
        (void)f.contour({f.v(gx, E1, LZ1), f.v(gx, E1, D + SO), f.v(gx, LTOP, LZ1)},
                        {{"thickness", "0.14"}, {"mat", "5"}, {"tone", "0"},
                         {"clad", "1"}, {"wear", "0.35"}});
    }
    // Стропила нижних пол: под настилом, торцы выглядывают из-под свеса.
    {
        const int bays = 13;
        for (int k = 0; k <= bays; ++k) {
            const float x = W * static_cast<float>(k) / static_cast<float>(bays);
            (void)f.beam(f.v(x, LTOP - 0.3f, LZ0), f.v(x, E1 - 0.34f, -SO - 0.22f),
                         {{"radius", "0.09"}, {"mat", "0"}, {"tone", "2"}});
            (void)f.beam(f.v(x, LTOP - 0.3f, LZ1),
                         f.v(x, E1 - 0.34f, D + SO + 0.22f),
                         {{"radius", "0.09"}, {"mat", "0"}, {"tone", "2"}});
        }
    }

    // ---------------- кровля фонаря и резной конёк ------------------------
    f.gable_roof(0.0f, LZ0, W, LZ1, E2, RIDGE_H, "1", "0", "7", "0.35");
    const float YR = E2 + RIDGE_H;
    // ОХЛУПЕНЬ поверх конька с выпуском 1.1 за щипцы и резными подвесами на
    // торцах — «торцы коньковых брёвен выступают» из заказа.
    (void)f.beam(f.v(-1.1f, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                 f.v(W + 1.1f, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                 {{"radius", "0.13"}, {"mat", "0"}, {"tone", "2"}});
    for (const float ex : {-1.1f, W + 1.1f}) {
        (void)f.beam(f.v(ex, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                     f.v(ex, YR - 0.75f, (LZ0 + LZ1) * 0.5f),
                     {{"radius", "0.1"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
    }
    // ПЕРЕКРЕЩЁННЫЕ ДОСКИ на щипцах: сходятся под коньком и расходятся выше
    // него рогами — нордский знак дома, читается с площади силуэтом.
    for (const float gx : {-0.12f, W + 0.12f}) {
        (void)f.beam(f.v(gx, E2 + 0.2f, LZ0 + 0.4f), f.v(gx, YR + 0.7f, 7.75f),
                     {{"radius", "0.1"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
        (void)f.beam(f.v(gx, E2 + 0.2f, LZ1 - 0.4f), f.v(gx, YR + 0.7f, 6.25f),
                     {{"radius", "0.1"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
    }

    // ---------------- четыре угловые башенки-контрфорса -------------------
    // Заходят на угол зала на 0.2: тангенциальная башенка отрывается от
    // фасада щелью, а контрфорс обязан читаться сросшимся с ним.
    for (const float tx : {-2.3f, W - 0.2f}) {
        for (const float tz : {-2.3f, D - 0.2f}) {
            turret(tx, tz, 2.5f, E1, 2.3f, "0.5", "0.45");
        }
    }

    // ---------------- парадный портал -------------------------------------
    // Две башенки-фланкера у входа + две колонны держат навес: проём между
    // ними 4 м — ширина парадного хода, а не двери.
    turret(8.6f, D + 0.6f, 2.4f, 6.6f, 1.9f, "0.45", "0.45");
    turret(W - 11.0f, D + 0.6f, 2.4f, 6.6f, 1.9f, "0.45", "0.45");
    for (const float cx : {11.35f, W - 11.35f}) {
        (void)f.beam(f.v(cx, 0.0f, D + 4.0f), f.v(cx, 5.3f, D + 4.0f),
                     {{"radius", "0.28"}, {"sides", "8"}, {"mat", "3"},
                      {"tone", "1"}});
    }
    {
        const VertexId a = f.v(10.9f, 6.3f, D - 0.1f);
        const VertexId b = f.v(W - 10.9f, 6.3f, D - 0.1f);
        const VertexId c = f.v(W - 10.9f, 5.3f, D + 4.1f);
        const VertexId d = f.v(10.9f, 5.3f, D + 4.1f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.15"}, {"mat", "1"},
                                       {"tone", "1"}, {"fill", "7"},
                                       {"wear", "0.4"}});
        (void)f.g.set_param(s, "roof", "1");
        (void)f.g.set_param(s, "unsupported", "1");
    }

    // ---------------- крылья ----------------------------------------------
    // Ниже нижнего карниза вдвое: крыло — подножие доминанты, а не сосед.
    for (const float wx : {-8.4f, W + 0.4f}) {
        tier(0.0f, 4.6f, wx, 2.0f, wx + 8.0f, 12.0f, "0.45", "3", "1", "3", "2",
             "1", "0.45", false, false, true, false);
        f.gable_roof(wx, 2.0f, wx + 8.0f, 12.0f, 4.6f, 2.2f, "1", "0", "7",
                     "0.45");
    }
    f.save(file);
}

/// ВОДЯНАЯ МЕЛЬНИЦА 7x7 (docs/WHITERUN_PLAN.json, houses[kind=mill]: 7x7 у
/// излучины, дверь на запад к дороге вдоль реки). Корпус — камень до 1.2 и
/// фахверк выше под дранкой; на ВОСТОЧНОЙ стене — наливное колесо, которое и
/// делает сарай мельницей.
/// ПОЧЕМУ КОЛЕСО ОПУЩЕНО НИЖЕ ПОЛА: постройка садится на берег, а колесо
/// обязано доставать до воды — центр на 0.7, радиус 2.2, значит низ обода на
/// -1.5 и уходит под срез берега. Колесо в плане идёт ВОСЬМИГРАННИКОМ (рука
/// forge_well/tree_ring): круг из прямых брусьев — то же решение, что у
/// колодезного кольца, а гнутого обода в графе нет.
static void forge_mill(const char* file) {
    Forge f;
    const float W = 7.0f;
    const float D = 7.0f;
    const float YS = 1.2f;  // верх каменного низа = пол рабочего яруса
    const float H = 3.6f;   // карниз
    // Пол ПОСТАВА — над каменным подклетом: в подклете сидит вал колеса, и
    // рабочий ярус мельницы обязан быть выше воды.
    parquet_floor(f, 0.0f, 0.0f, W, D, YS + 0.06f);
    // НИЗ — ГЛУХАЯ КЛАДКА: 1.2 м не берут ни окно 2.1, ни дверь 2.05
    // (build_house_mesh: «проём просит 2.10 м, стена 1.20»), и подклет мельницы
    // и не должен светиться проёмами. Проёмы живут в фахверке над ним — тот же
    // порядок, что у city-house-l с его цоколем 0.525.
    keep_tier(f, 0.0f, YS, 0.0f, 0.0f, W, D, "0.4", "3", "1", "3", "", "",
              "0.5", false, false, true, '\0');
    keep_tier(f, YS, H - YS, 0.0f, 0.0f, W, D, "0.3", "5", "0", "", "3", "3",
              "0.4", true, true, false, 'W');
    // НАСТИЛ РАБОЧЕГО ЯРУСА — ЕГО НЕ БЫЛО ВОВСЕ (правка 28.08). У мельницы
    // стояли два кольца стен и НИ ОДНОЙ горизонтальной плиты внутри: каменный
    // низ — это ЛЕНТА кладки по периметру, а не короб, и над ней зияла дыра во
    // всю комнату 7 x 7. Снаружи это не видно (стены целы, дверь на месте), и
    // ни один прибор постройки этого не ловил: связность считает КАСАНИЯ тел,
    // а недостающее тело касаний не рвёт.
    //
    // НАШЁЛ СУДЬЯ ЛОКАЦИЙ, И ЭТО ЕГО ЛУЧШАЯ НАХОДКА ЗА ВОЛНУ: все ШЕСТЬ находок
    // Вайтрана в аудите 28.08 — одна и та же локация x132z182, «до стола, трёх
    // бочек, полки и лавки от [spawn] не дойти». Карта проходимости объясняет
    // почему: свободна полоса 1.2 м у самой двери, а дальше на восток у судьи
    // нет ПОЛА — то есть шесть предметов мельницы стояли над пустотой, и
    // «путь перекрыт» здесь значило «идти не по чему».
    //
    // ОТМЕТКА ВЫВОДИТСЯ, А НЕ НАЗНАЧАЕТСЯ: плита растёт в обе стороны от своей
    // плоскости, значит плоскость YS - 0.06 при толщине 0.12 даёт ВЕРХ ровно
    // на YS — на той же отметке, на которую садятся порог двери и верх
    // западного крыльца. Манифест полки после этого меряет пол мельницы по
    // самой широкой плите комнаты (1.20) вместо прежних 1.32, и убранство
    // садится на настил, а не висит на 12 см над бывшей дырой.
    parquet_floor(f, 0.0f, 0.0f, W, D, YS - 0.06f);
    f.door_leaf_x(0.0f, YS, D * 0.5f);
    // КРЫЛЬЦО НА ЗАПАД (схема: дверь мельницы смотрит на дорогу): площадка на
    // уровне порога и марш 1.2 на вынос 2.4 — уклон 26.6 гр., по нему бот
    // ходит. Обход контура «+Z, потом +X» — при нём лицо смотрит вверх.
    {
        const VertexId a = f.v(-1.4f, YS - 0.06f, 2.6f);
        const VertexId b = f.v(-1.4f, YS - 0.06f, 4.4f);
        const VertexId c = f.v(0.0f, YS - 0.06f, 4.4f);
        const VertexId d = f.v(0.0f, YS - 0.06f, 2.6f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}, {"wear", "0.45"}});
    }
    {
        const VertexId a = f.v(-3.8f, 0.0f, 2.9f);
        const VertexId b = f.v(-3.8f, 0.0f, 4.1f);
        const VertexId c = f.v(-1.4f, YS, 4.1f);
        const VertexId d = f.v(-1.4f, YS, 2.9f);
        // nosing=1 И БОЛЬШЕ НИЧЕГО (правка 28.08). УКЛОН ОСТАЁТСЯ 26.6:
        // это НАРУЖНОЕ КРЫЛЬЦО, а не внутридомовой марш, и норма «круче
        // 45 гр.» к нему не относится — по крыльцу входят, а не взбираются.
        // А ВОТ ФАНТОМНАЯ СТУПЕНЬКА У ДВЕРИ БЫЛА НАСТОЯЩЕЙ: при nosing=0
        // невидимый пандус (HouseStairs.cpp) идёт по ЗАДНИМ углам ступеней и
        // приходит на 0.155 ВЫШЕ и земли внизу, и площадки наверху. Уличным
        // маршам эту поправку снимает посадка генератора города — но своё
        // крыльцо мельница носит В СЕБЕ, генератор его не сажает, и таблица
        // поправок (gen_city.py, только city-stoop*/city-steps*) о нём не
        // знает. Отсюда буквально то, что владелец назвал «перепрыгивать».
        // ЩИТА (riser) ЗДЕСЬ НЕТ НАРОЧНО: «доска на тетивах, под ней воздух» —
        // это заказанный вид деревенского крыльца, а не дефект.
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "6"}, {"open", "1"},
                         {"nosing", "1"}, {"mat", "1"}, {"tone", "1"},
                         {"wear", "0.4"}});
    }
    f.frame_posts(0.0f, 0.0f, W, D, YS, H, "0");
    f.gable_roof(0.0f, 0.0f, W, D, H, 1.9f, "6", "1", "", "0.4");

    // ---------------- водяное колесо на восточной стене -------------------
    const float R = 2.2f;          // радиус обода
    const float CZ = D * 0.5f;     // ось колеса по глубине
    const float CY = 0.7f;         // центр: низ обода уходит на -1.5
    const float X0 = W + 0.4f;     // ближний обод
    const float X1 = W + 1.2f;     // дальний обод — выступ за стену ровно 1.2
    const auto ring = [&](float r, int k) {
        const float t = 0.7853981634f * static_cast<float>(k);
        return glm::vec3{0.0f, CY + r * std::sin(t), CZ + r * std::cos(t)};
    };
    // ОСЬ-БАЛКА СКВОЗЬ СТЕНУ: от подклета (x = W - 0.8) наружу за дальний
    // обод. Без неё колесо висит в воздухе отдельной деталью.
    (void)f.beam(f.v(W - 0.8f, CY, CZ), f.v(X1 + 0.25f, CY, CZ),
                 {{"radius", "0.16"}, {"mat", "0"}, {"tone", "2"}});
    // Два обода-восьмигранника и спицы к ступице в плоскости каждого обода.
    for (const float x : {X0, X1}) {
        for (int k = 0; k < 8; ++k) {
            const glm::vec3 p = ring(R, k);
            const glm::vec3 q = ring(R, k + 1);
            (void)f.beam(f.v(x, p.y, p.z), f.v(x, q.y, q.z),
                         {{"radius", "0.13"}, {"form", "square"}, {"mat", "0"},
                          {"tone", "2"}, {"wear", "0.55"}});
            (void)f.beam(f.v(x, CY, CZ), f.v(x, p.y, p.z),
                         {{"radius", "0.07"}, {"form", "square"}, {"mat", "0"},
                          {"tone", "2"}, {"wear", "0.5"}});
        }
    }
    // ЛОПАСТИ: доска между ободами на каждом углу восьмигранника, от 1.55 до
    // 2.35 по радиусу — она и черпает воду, и не даёт кольцу читаться рамой.
    for (int k = 0; k < 8; ++k) {
        const glm::vec3 in = ring(1.55f, k);
        const glm::vec3 out = ring(2.35f, k);
        const ElementId s = f.contour({f.v(X0, in.y, in.z), f.v(X1, in.y, in.z),
                                       f.v(X1, out.y, out.z),
                                       f.v(X0, out.y, out.z)},
                                      {{"thickness", "0.07"}, {"mat", "1"},
                                       {"tone", "2"}, {"wear", "0.55"}});
        (void)f.g.set_param(s, "unsupported", "1");
    }
    // Подкосы-подшипник: от восточной стены к наружному концу оси.
    for (const float dz : {-1.1f, 1.1f}) {
        (void)f.beam(f.v(W - 0.05f, 2.6f, CZ + dz), f.v(X1 + 0.2f, CY + 0.05f, CZ),
                     {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                      {"tone", "2"}});
    }
    f.save(file);
}

/// КОМПАКТНЫЙ ЗАМОК ПО СХЕМЕ (docs/WHITERUN_PLAN.json: houses[kind=keep] —
/// корпус 15x8, дверь на юг; два крыла 5x6 по бокам). Прежний city-keep 26x14
/// с коньком 18.8 в утверждённую схему не влезает, но и переделывать его
/// нельзя — принятая доминанта живёт своим файлом; здесь ОТДЕЛЬНЫЙ рецепт на
/// ОБЩИХ РУКАХ (keep_tier, gable_roof, frame_posts), а не копия тела.
/// ЯРУСЫ те же, что у большого: стилобат-камень 1.6, два фахверковых яруса до
/// карниза 7.4, широкие нижние полы к ФОНАРЮ глубиной 4 (из 8) и его двускат
/// с коньком 13.8 — доминанта читается ступенями, каждая уже нижней
/// (CITY_DESIGN_GUIDE.md §8). Крылья вдвое ниже корпуса: крыло — подножие,
/// а не сосед.
static void forge_keep_small(const char* file) {
    Forge f;
    const float W = 15.0f;     // корпус по фасаду
    const float D = 8.0f;      // глубина корпуса
    const float WW = 5.0f;     // крыло по фасаду
    const float WZ0 = 1.0f;    // крыло: северная стена (глубина 6 из 8)
    const float WZ1 = 7.0f;
    const float YS = 1.6f;     // верх каменного стилобата = пол зала
    const float Y2 = 4.6f;     // пол верхнего зала
    const float E1 = 7.4f;     // нижний карниз
    const float LZ0 = 2.0f;    // фонарь: северная стена
    const float LZ1 = 6.0f;    // фонарь: южная стена
    const float LTOP = 9.2f;   // где нижняя пола встречает фонарь
    const float E2 = 10.6f;    // карниз фонаря
    const float SO = 0.4f;     // свес нижних пол
    const float RIDGE_H = 3.2f; // конёк 13.8
    const float WE = 4.2f;     // карниз крыльев (конёк 6.0)

    // ---------------- подошва и парадное крыльцо --------------------------
    // Плита ЛЕЖИТ НА ЗЕМЛЕ, а не ступенью: контур растёт в обе стороны от
    // своей высоты, и 0.02 при толщине 0.2 даёт кромку 0.12 — проверенный
    // подиум большого замка. Толще — и у входа заводится порог.
    {
        const VertexId a = f.v(-WW - 0.8f, 0.02f, -0.8f);
        const VertexId b = f.v(-WW - 0.8f, 0.02f, D + 0.8f);
        const VertexId c = f.v(W + WW + 0.8f, 0.02f, D + 0.8f);
        const VertexId d = f.v(W + WW + 0.8f, 0.02f, -0.8f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.4"}});
    }
    // Площадка крыльца на уровне пола: верх плиты = YS (якорь на YS - th/2).
    {
        const VertexId a = f.v(5.5f, YS - 0.1f, D);
        const VertexId b = f.v(5.5f, YS - 0.1f, D + 2.0f);
        const VertexId c = f.v(9.5f, YS - 0.1f, D + 2.0f);
        const VertexId d = f.v(9.5f, YS - 0.1f, D);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.2"}, {"mat", "3"}, {"tone", "1"},
                         {"wear", "0.4"}, {"unsupported", "1"}});
    }
    // Бортики площадки по бокам — марш между ними остаётся свободным.
    for (const float px : {5.5f, 9.5f}) {
        (void)f.wall(f.v(px, YS, D), f.v(px, YS, D + 2.0f),
                     {{"height", "0.7"}, {"thickness", "0.3"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", "0.45"},
                      {"unsupported", "1"}});
    }
    // КОРОТКИЙ МАРШ НА 1.6 (open=2): подъём 1.6 на вынос 3.2 — тот же уклон
    // 26.6 гр., что у принятой уличной лестницы-6, по ней бот ходит. Один
    // марш, а не два: стык двух маршей дважды ловил бота в щель.
    {
        const VertexId a = f.v(6.3f, 0.0f, D + 5.2f);
        const VertexId b = f.v(8.7f, 0.0f, D + 5.2f);
        const VertexId c = f.v(8.7f, YS, D + 2.0f);
        const VertexId d = f.v(6.3f, YS, D + 2.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"fill", "6"}, {"open", "2"},
                         {"mat", "3"}, {"tone", "1"}, {"wear", "0.4"},
                         {"unsupported", "1"}});
    }

    // ---------------- ярусы корпуса ---------------------------------------
    // СТИЛОБАТ БЕЗ ПРОЁМОВ: 1.6 м не берут окно 2.1 (build_house_mesh: «проём
    // просит 2.10 м, стена 1.60»), да и парадный цоколь замка светиться
    // окошками не должен — глухая кладка, вход по крыльцу на ярус выше.
    keep_tier(f, 0.0f, YS, 0.0f, 0.0f, W, D, "0.7", "3", "1", "3", "", "",
              "0.42", false, false, true, '\0');
    keep_tier(f, YS, Y2 - YS, 0.0f, 0.0f, W, D, "0.35", "5", "0", "", "3", "2",
              "0.38", true, true, false, 'S');
    keep_tier(f, Y2, E1 - Y2, 0.0f, 0.0f, W, D, "0.35", "5", "0", "", "3", "2",
              "0.35", true, true, false, '\0');
    // Фонарь: тот же фасад по X, вдвое меньше глубина — ярус сужается.
    keep_tier(f, E1, E2 - E1, 0.0f, LZ0, W, LZ1, "0.3", "5", "0", "", "4", "1",
              "0.35", true, false, false, '\0');
    // Створка — НА УРОВНЕ ПОЛА (YS), а не земли: вход на стилобате, к нему и
    // ведёт крыльцо.
    f.door_leaf(W * 0.5f, YS, D);
    f.frame_posts(0.0f, 0.0f, W, D, YS, E1, "0");
    // Междуэтажный поясок: без него 5.8 м фахверка над стилобатом стоят одной
    // пустой доской.
    {
        const glm::vec3 p[5] = {{0.0f, Y2, 0.0f}, {W, Y2, 0.0f}, {W, Y2, D},
                                {0.0f, Y2, D},    {0.0f, Y2, 0.0f}};
        for (int k = 0; k < 4; ++k) {
            (void)f.beam(f.v(p[k].x, p[k].y, p[k].z),
                         f.v(p[k + 1].x, p[k + 1].y, p[k + 1].z),
                         {{"radius", "0.1"}, {"form", "plank"}, {"mat", "0"},
                          {"tone", "2"}});
        }
    }

    // ---------------- полы и внутренний марш ------------------------------
    parquet_floor(f, 0.0f, 0.0f, W, D, YS);
    // Пол верхнего зала — ДВА прямоугольника вокруг лестничного колодца:
    // невыпуклый контур пришлось бы триангулировать веером.
    {
        const VertexId a = f.v(4.0f, Y2, 0.0f);
        const VertexId b = f.v(4.0f, Y2, D);
        const VertexId c = f.v(W, Y2, D);
        const VertexId d = f.v(W, Y2, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    // ТА ЖЕ ПРАВКА, ЧТО У БОЛЬШОГО ЗАМКА (27.08): марш разворачивается
    // подножием к северной стене, идёт на юг под 45 гр. и приходит РОВНО на
    // кромку верхнего пола, которая считается от него. Прежде он кончался на
    // z 1.2, а пол начинался на 7.2 — поднявшийся выходил в пустоту.
    const float KS_LO = YS + 0.06f;     // верх паркета (плоскость YS)
    const float KS_HI = Y2 + 0.075f;    // верх настила верхнего зала
    const float KS_FOOT = 1.2f;
    const float KS_TOP = KS_FOOT + (KS_HI - KS_LO);
    {
        const VertexId a = f.v(0.0f, Y2, KS_TOP);
        const VertexId b = f.v(0.0f, Y2, D);
        const VertexId c = f.v(4.0f, Y2, D);
        const VertexId d = f.v(4.0f, Y2, KS_TOP);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    // Марш посреди зала: ограждаются обе грани (rail_x = середина).
    house_flight_z(f, 1.0f, 3.4f, KS_FOOT, KS_TOP, KS_LO, KS_HI, 2.2f, "0.35");

    // ---------------- нижние полы к фонарю --------------------------------
    const auto shed = [&](VertexId a, VertexId b, VertexId c, VertexId d) {
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "1"}, {"tone", "1"},
                         {"fill", "7"}, {"wear", "0.4"}});
    };
    shed(f.v(-SO, LTOP, LZ0), f.v(W + SO, LTOP, LZ0),
         f.v(W + SO, E1 - 0.1f, -SO), f.v(-SO, E1 - 0.1f, -SO));
    shed(f.v(W + SO, LTOP, LZ1), f.v(-SO, LTOP, LZ1),
         f.v(-SO, E1 - 0.1f, D + SO), f.v(W + SO, E1 - 0.1f, D + SO));
    f.eaves_trim({-SO, E1 - 0.1f, -SO}, {W + SO, E1 - 0.1f, -SO});
    f.eaves_trim({-SO, E1 - 0.1f, D + SO}, {W + SO, E1 - 0.1f, D + SO});
    // Торцы пол: треугольники в плоскостях x=0 и x=W — ими же держится
    // верхняя кромка полы (правило опоры крыш, check_roof_support).
    for (const float gx : {0.0f, W}) {
        (void)f.contour({f.v(gx, E1, -SO), f.v(gx, E1, LZ0), f.v(gx, LTOP, LZ0)},
                        {{"thickness", "0.14"}, {"mat", "5"}, {"tone", "0"},
                         {"clad", "1"}, {"wear", "0.35"}});
        (void)f.contour({f.v(gx, E1, LZ1), f.v(gx, E1, D + SO), f.v(gx, LTOP, LZ1)},
                        {{"thickness", "0.14"}, {"mat", "5"}, {"tone", "0"},
                         {"clad", "1"}, {"wear", "0.35"}});
    }
    // Стропила нижних пол: ПОД настилом, торцы выглядывают из-под свеса.
    {
        const int bays = 8;
        for (int k = 0; k <= bays; ++k) {
            const float x = W * static_cast<float>(k) / static_cast<float>(bays);
            (void)f.beam(f.v(x, LTOP - 0.3f, LZ0), f.v(x, E1 - 0.34f, -SO - 0.22f),
                         {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
            (void)f.beam(f.v(x, LTOP - 0.3f, LZ1),
                         f.v(x, E1 - 0.34f, D + SO + 0.22f),
                         {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
        }
    }

    // ---------------- кровля фонаря и резной конёк ------------------------
    f.gable_roof(0.0f, LZ0, W, LZ1, E2, RIDGE_H, "1", "0", "7", "0.35");
    const float YR = E2 + RIDGE_H;
    // Охлупень с выпуском за щипцы и подвесами на торцах.
    (void)f.beam(f.v(-0.9f, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                 f.v(W + 0.9f, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                 {{"radius", "0.12"}, {"mat", "0"}, {"tone", "2"}});
    for (const float ex : {-0.9f, W + 0.9f}) {
        (void)f.beam(f.v(ex, YR + 0.2f, (LZ0 + LZ1) * 0.5f),
                     f.v(ex, YR - 0.65f, (LZ0 + LZ1) * 0.5f),
                     {{"radius", "0.09"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
    }
    // Перекрещённые доски на щипцах — нордский знак дома, читается силуэтом.
    for (const float gx : {-0.12f, W + 0.12f}) {
        (void)f.beam(f.v(gx, E2 + 0.2f, LZ0 + 0.4f), f.v(gx, YR + 0.6f, 4.75f),
                     {{"radius", "0.09"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
        (void)f.beam(f.v(gx, E2 + 0.2f, LZ1 - 0.4f), f.v(gx, YR + 0.6f, 3.25f),
                     {{"radius", "0.09"}, {"form", "plank"}, {"mat", "0"},
                      {"tone", "2"}});
    }

    // ---------------- два крыла 5x6 ---------------------------------------
    // Примыкают к ТОРЦАМ корпуса (x = -WW..0 и W..W+WW): общий габарит 25x8.
    // Конёк 6.0 — на 1.4 ниже нижнего карниза корпуса и на 7.8 ниже его конька.
    // КАРНИЗ КРЫЛА 4.2, А НЕ 3.4: фахверк над общим стилобатом 1.6 обязан быть
    // не ниже 2.5, иначе окно 2.1 в него не влезает; чтобы конёк остался на 6,
    // подъём ската уменьшен с 2.6 до 1.8.
    for (const float wx : {-WW, W}) {
        keep_tier(f, 0.0f, YS, wx, WZ0, wx + WW, WZ1, "0.5", "3", "1", "3", "",
                  "", "0.45", false, false, true, '\0');
        keep_tier(f, YS, WE - YS, wx, WZ0, wx + WW, WZ1, "0.35", "5", "0", "",
                  "1", "2", "0.4", true, true, false, '\0');
        parquet_floor(f, wx, WZ0, wx + WW, WZ1, YS);
        f.gable_roof(wx, WZ0, wx + WW, WZ1, WE, 1.8f, "1", "0", "7", "0.35");
    }
    f.save(file);
}

/// БОЛЬШОЙ ЖИЛОЙ ДОМ 10x8, два этажа: каменный цоколь и первый этаж, фахверк
/// сверху, окна на обоих этажах, крыльцо-ступени на цоколь, двускат-гонт.
/// ЦОКОЛЬ — не украшение: WHITERUN_RESEARCH.md §2 («цоколь дикий камень, у
/// каждого дома своей высоты; крыльцо в 3-7 ступеней ПОТОМУ ЧТО цоколь
/// высокий»). 0.525 — ровно три ступени по 0.175, и марш выходит целым.
static void forge_house_large(Aging age) {
    Forge f;
    const float W = 10.0f;
    const float D = 8.0f;
    const float PL = 0.525f;        // цоколь = 3 ступени
    const float H1 = 2.9f;          // первый этаж
    const float H2 = 2.6f;          // второй ниже первого, как у прототипов
    const float Y1 = PL;            // пол первого этажа
    const float Y2 = PL + H1;       // пол второго
    const float EAVES = PL + H1 + H2;
    // ---------- цоколь: сплошная каменная лента под всеми стенами ----------
    const auto plinth_run = [&](float ax, float az, float bx, float bz) {
        (void)f.wall(f.v(ax, 0.0f, az), f.v(bx, 0.0f, bz),
                     {{"height", "0.525"}, {"thickness", "0.4"}, {"mat", "3"},
                      {"tone", "1"}, {"fill", "3"}, {"wear", age.w(0.6f)}});
    };
    plinth_run(W, 0.0f, 0.0f, 0.0f);  // север, лицо на север
    plinth_run(W, D, W, 0.0f);        // восток
    plinth_run(0.0f, 0.0f, 0.0f, D);  // запад
    plinth_run(0.0f, D, W, D);        // юг
    parquet_floor(f, 0.0f, 0.0f, W, D, Y1 + 0.06f);
    f.frame_posts(0.0f, 0.0f, W, D, Y1, EAVES, "0");
    // ---------- первый этаж: кладка с окнами, дверь посередине юга ----------
    const auto low = [&](float ax, float az, float bx, float bz, Params p) {
        (void)f.wall(f.v(ax, Y1, az), f.v(bx, Y1, bz), p);
    };
    Params stone2 = {{"height", "2.9"}, {"thickness", "0.35"}, {"mat", "3"},
                     {"tone", "1"}, {"fill", "3"}, {"windows", "2"},
                     {"plinth", age.plinth()}, {"wear", age.w(0.45f)}};
    low(W, 0.0f, 0.0f, 0.0f,
        {{"height", "2.9"}, {"thickness", "0.35"}, {"mat", "3"}, {"tone", "1"},
         {"fill", "3"}, {"windows", "3"}, {"plinth", age.plinth()},
         {"wear", age.w(0.45f)}});
    low(W, D, W, 0.0f, stone2);
    low(0.0f, 0.0f, 0.0f, D, stone2);
    // ЮЖНЫЙ ФАСАД ИЗ ТРЁХ ПРОЛЁТОВ: раскладка знает один вид проёма на стену
    // (дверь берёт верх над окнами) — окна получают свои пролёты по бокам.
    low(0.0f, D, 3.5f, D,
        {{"height", "2.9"}, {"thickness", "0.35"}, {"mat", "3"}, {"tone", "1"},
         {"fill", "3"}, {"windows", "1"}, {"plinth", age.plinth()},
         {"wear", age.w(0.5f)}});
    low(3.5f, D, 6.5f, D,
        {{"height", "2.9"}, {"thickness", "0.35"}, {"mat", "3"}, {"tone", "1"},
         {"fill", "3"}, {"doors", "1"}, {"wear", age.w(0.5f)}});
    low(6.5f, D, W, D,
        {{"height", "2.9"}, {"thickness", "0.35"}, {"mat", "3"}, {"tone", "1"},
         {"fill", "3"}, {"windows", "1"}, {"plinth", age.plinth()},
         {"wear", age.w(0.5f)}});
    f.door_leaf(W * 0.5f, Y1, D);
    // ---------- второй этаж: фахверк по штукатурке, окна чаще ----------
    const auto up = [&](float ax, float az, float bx, float bz, Params p) {
        (void)f.wall(f.v(ax, Y2, az), f.v(bx, Y2, bz), p);
    };
    Params clad2 = {{"height", "2.6"}, {"thickness", "0.28"}, {"mat", "5"},
                    {"tone", "0"}, {"clad", "1"}, {"windows", "2"},
                    {"shutters", age.shutters()}, {"wear", age.w(0.3f)}};
    up(W, 0.0f, 0.0f, 0.0f,
       {{"height", "2.6"}, {"thickness", "0.28"}, {"mat", "5"}, {"tone", "0"},
        {"clad", "1"}, {"windows", "3"}, {"shutters", age.shutters()},
        {"wear", age.w(0.3f)}});
    up(W, D, W, 0.0f, clad2);
    up(0.0f, 0.0f, 0.0f, D, clad2);
    up(0.0f, D, W, D,
       {{"height", "2.6"}, {"thickness", "0.28"}, {"mat", "5"}, {"tone", "0"},
        {"clad", "1"}, {"windows", "3"}, {"shutters", age.shutters()},
        {"wear", age.w(0.3f)}});
    // ---------- междуэтажный настил с проёмом над маршем ----------
    // КРОМКА ПРОЁМА СЧИТАЕТСЯ ОТ ВЕРХА МАРША (правка 27.08): марш стал
    // 45-градусным, ход укоротился с 4.40 до 2.96, и верх ушёл с z 3.20 на
    // 4.76. Кромка, оставленная числом 3.0, оставила бы поднявшегося В
    // ВОЗДУХЕ в 1.76 м от настила — судья это и назвал.
    const float ST_LO = Y1 + 0.12f;     // верх паркета (плоскость Y1+0.06)
    const float ST_HI = Y2 + 0.06f;     // верх настила (плоскость Y2)
    const float ST_FOOT = 7.6f;
    const float ST_TOP = ST_FOOT - (ST_HI - ST_LO);
    {
        const VertexId a = f.v(0.0f, Y2, 0.0f);
        const VertexId b = f.v(0.0f, Y2, D);
        const VertexId c = f.v(8.0f, Y2, D);
        const VertexId d = f.v(8.0f, Y2, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    {
        const VertexId a = f.v(8.0f, Y2, 0.0f);
        const VertexId b = f.v(8.0f, Y2, ST_TOP);
        const VertexId c = f.v(W, Y2, ST_TOP);
        const VertexId d = f.v(W, Y2, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"mat", "1"}, {"tone", "1"},
                         {"beams", "1"}});
    }
    // РИГЕЛЬ ЛЕСТНИЧНОГО ПРОЁМА. Балки перекрытия идут вдоль X и на кромке
    // проёма (x = 8, от z = 3 до южной стены) обрываются в воздух: у настоящего
    // наката там стоит ригель, в который они врубаются, и без него четыре
    // балки читаются обломанными. Сечение и высота — ровно балочные
    // (Y2 − 0.185 ± 0.125), поэтому ригель ловит их торцы телом, а верхом
    // упирается в изнанку настила.
    (void)f.beam(f.v(8.0f, Y2 - 0.185f, ST_TOP), f.v(8.0f, Y2 - 0.185f, D),
                 {{"radius", "0.125"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}, {"wear", age.w(0.3f)}});
    // МАРШ ВДОЛЬ ВОСТОЧНОЙ СТЕНЫ НА СЕВЕР, 45 ГР. (правка 27.08, крит владельца
    // «слишком длинные они, должны быть круче»). Прежде: ход 4.4 на подъём 2.9,
    // то есть 33 гр.; теперь ход РАВЕН подъёму, и марш короче на 1.44 м.
    //
    // ОБА КОНЦА САДЯТСЯ НА ТО, ПО ЧЕМУ ХОДЯТ, А НЕ НА ПЛОСКОСТИ ПЛИТ. Плита
    // растёт в обе стороны от своей плоскости: пол лежит плоскостью на Y1+0.06
    // и верхом на Y1+0.12, настил — плоскостью на Y2 и верхом на Y2+0.06.
    // Прежние якоря стояли на Y1 и Y2, и судья мерил ступеньку +0.151 на входе
    // и −0.091 на выходе — по 12 и 6 см в никуда.
    // Ограждается ЗАПАДНАЯ грань (8.3) — та, что смотрит в зал и совпадает с
    // кромкой проёма; восточная стоит в 0.3 от стены, и перила там были бы
    // решёткой между маршем и кладкой.
    house_flight_z(f, 8.3f, 9.7f, ST_FOOT, ST_TOP, ST_LO, ST_HI, 8.3f, age.w(0.3f),
                   0.1f);
    // ---------- КРЫЛЬЦО: площадка на уровне порога + марш на землю ----------
    {
        const VertexId a = f.v(3.0f, Y1, D);
        const VertexId b = f.v(3.0f, Y1, D + 1.3f);
        const VertexId c = f.v(7.0f, Y1, D + 1.3f);
        const VertexId d = f.v(7.0f, Y1, D);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}, {"wear", age.w(0.4f)}});
    }
    for (const float px : {3.2f, 6.8f}) {
        (void)f.beam(f.v(px, 0.0f, D + 1.15f), f.v(px, Y1, D + 1.15f),
                     {{"radius", "0.1"}, {"mat", "0"}, {"tone", "2"}});
    }
    {
        // Три открытые ступени (open=1): доска на тетивах, под ней воздух —
        // ровно то крыльцо, которое рисуют кадры-референсы. Уклон 26.6 гр.
        // остаётся: крыльцо — не марш, норма «круче» к нему не относится.
        // nosing=1 (28.08) снимает фантомную ступеньку 0.155 у самой двери —
        // довод целиком у крыльца мельницы выше в этом же файле.
        const VertexId a = f.v(3.9f, 0.0f, D + 2.35f);
        const VertexId b = f.v(6.1f, 0.0f, D + 2.35f);
        const VertexId c = f.v(6.1f, Y1, D + 1.3f);
        const VertexId d = f.v(3.9f, Y1, D + 1.3f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"fill", "6"}, {"open", "1"},
                         {"nosing", "1"}, {"mat", "1"}, {"tone", "1"},
                         {"wear", age.w(0.35f)}});
    }
    // Навес крыльца на двух столбах, заведён под свес второго этажа.
    for (const float px : {3.3f, 6.7f}) {
        (void)f.beam(f.v(px, Y1, D + 1.15f), f.v(px, Y1 + 2.4f, D + 1.15f),
                     {{"radius", "0.12"}, {"mat", "0"}, {"tone", "2"}});
    }
    (void)f.beam(f.v(3.2f, Y1 + 2.42f, D + 1.15f), f.v(6.8f, Y1 + 2.42f, D + 1.15f),
                 {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}});
    {
        const VertexId a = f.v(7.3f, Y2 + 0.12f, D - 0.3f);
        const VertexId b = f.v(2.7f, Y2 + 0.12f, D - 0.3f);
        const VertexId c = f.v(2.7f, Y1 + 2.35f, D + 1.6f);
        const VertexId d = f.v(7.3f, Y1 + 2.35f, D + 1.6f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}, {"fill", "7"},
                                       {"wear", age.w(0.4f)}});
        (void)f.g.set_param(s, "roof", "1");
        (void)f.g.set_param(s, "unsupported", "1");
    }
    f.gable_roof(0.0f, 0.0f, W, D, EAVES, 3.0f, "6", "1", "", age.w(0.35f));
    // ТРУБА обеих версий: очаг по середине глухой стены 10 м (5.0, 1.10).
    // Ствол толще домика — труба высокого дома и в жизни толще, а с земли она
    // читается по силуэту, а не по сечению.
    if (vents_on()) {
        f.chimney(5.0f, 1.10f, EAVES - 0.4f, EAVES + 3.0f + 0.7f, 0.34f, true, "3",
                  "1", age.w(0.5f));
    }
    f.save(age.file);
}

/// УСАДЬБА Г-ОБРАЗНАЯ: главный корпус 14x7 на севере, крыло 7x8 на западе,
/// внутренний уголок-дворик на юго-востоке, вход В УГЛУ под навесом.
/// ДВЕ КРОВЛИ СХОДЯТСЯ ЕНДОВОЙ: конёк крыла заведён ПОД главный (до его
/// конька, z=3.5) — иначе фронтон крыла торчал бы треугольной стеной из
/// южного ската главного корпуса, а не растворялся в ендове.
static void forge_manor(Aging age) {
    Forge f;
    const float AX = 14.0f;   // главный корпус: x 0..14, z 0..7
    const float AZ = 7.0f;
    const float BX = 7.0f;    // западное крыло: x 0..7, z 7..15
    const float BZ = 15.0f;
    const float H1 = 3.0f;
    const float H2 = 2.8f;
    const float EAVES = H1 + H2;
    // ---------- пол Г-контуром (против часовой сверху) ----------
    {
        const VertexId a = f.v(0.0f, 0.06f, 0.0f);
        const VertexId b = f.v(0.0f, 0.06f, BZ);
        const VertexId c = f.v(BX, 0.06f, BZ);
        const VertexId d = f.v(BX, 0.06f, AZ);
        const VertexId e = f.v(AX, 0.06f, AZ);
        const VertexId g2 = f.v(AX, 0.06f, 0.0f);
        (void)f.contour({a, b, c, d, e, g2},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "1"}, {"wear", age.w(0.3f)}});
    }
    f.frame_posts(0.0f, 0.0f, AX, AZ, 0.0f, EAVES, "3");
    f.frame_posts(0.0f, AZ, BX, BZ, 0.0f, EAVES, "3");
    // ---------- обход контура: лицо стены = направление, повёрнутое на
    // 90° по часовой сверху, поэтому порядок углов задаёт наружность ----------
    const auto low = [&](float ax, float az, float bx, float bz, Params p) {
        (void)f.wall(f.v(ax, 0.0f, az), f.v(bx, 0.0f, bz), p);
    };
    const auto up = [&](float ax, float az, float bx, float bz, Params p) {
        (void)f.wall(f.v(ax, H1, az), f.v(bx, H1, bz), p);
    };
    Params s2 = {{"height", "3"}, {"thickness", "0.35"}, {"mat", "3"},
                 {"tone", "1"}, {"fill", "3"}, {"windows", "2"},
                 {"plinth", age.plinth()}, {"wear", age.w(0.4f)}};
    Params s1 = {{"height", "3"}, {"thickness", "0.35"}, {"mat", "3"},
                 {"tone", "1"}, {"fill", "3"}, {"windows", "1"},
                 {"plinth", age.plinth()}, {"wear", age.w(0.4f)}};
    Params sdoor = {{"height", "3"}, {"thickness", "0.35"}, {"mat", "3"},
                    {"tone", "1"}, {"fill", "3"}, {"doors", "1"},
                    {"porch", "1"}, {"wear", age.w(0.4f)}};
    low(0.0f, 0.0f, 0.0f, AZ, s2);            // запад главного, лицо на запад
    low(0.0f, AZ, 0.0f, BZ, s2);              // запад крыла
    low(0.0f, BZ, BX, BZ, s2);                // южный торец крыла
    low(BX, BZ, BX, 10.0f, s1);               // восток крыла, лицо во дворик
    low(BX, 10.0f, BX, AZ, sdoor);            // чёрный ход во дворик
    low(BX, AZ, 10.0f, AZ, sdoor);            // ГЛАВНЫЙ ВХОД — в углу Г
    low(10.0f, AZ, AX, AZ, s2);               // южный фасад главного
    low(AX, AZ, AX, 0.0f, s2);                // восток главного
    low(AX, 0.0f, 0.0f, 0.0f,
        {{"height", "3"}, {"thickness", "0.35"}, {"mat", "3"}, {"tone", "1"},
         {"fill", "3"}, {"windows", "4"}, {"plinth", age.plinth()},
         {"wear", age.w(0.4f)}});
    f.door_leaf(8.5f, 0.0f, AZ);       // главный, в стене вдоль X
    f.door_leaf_x(BX, 0.0f, 8.5f);     // чёрный, в стене вдоль Z
    Params c2 = {{"height", "2.8"}, {"thickness", "0.28"}, {"mat", "5"},
                 {"tone", "0"}, {"clad", "1"}, {"windows", "2"},
                 {"shutters", age.shutters()}, {"wear", age.w(0.3f)}};
    Params c3 = {{"height", "2.8"}, {"thickness", "0.28"}, {"mat", "5"},
                 {"tone", "0"}, {"clad", "1"}, {"windows", "3"},
                 {"shutters", age.shutters()}, {"wear", age.w(0.3f)}};
    up(0.0f, 0.0f, 0.0f, AZ, c2);
    up(0.0f, AZ, 0.0f, BZ, c2);
    up(0.0f, BZ, BX, BZ, c2);
    up(BX, BZ, BX, AZ, c2);
    up(BX, AZ, AX, AZ, c3);
    up(AX, AZ, AX, 0.0f, c2);
    up(AX, 0.0f, 0.0f, 0.0f,
       {{"height", "2.8"}, {"thickness", "0.28"}, {"mat", "5"}, {"tone", "0"},
        {"clad", "1"}, {"windows", "4"}, {"shutters", age.shutters()},
        {"wear", age.w(0.3f)}});
    // ---------- настил второго этажа, проём над маршем в крыле ----------
    const auto deck = [&](float x0, float z0, float x1, float z1, bool beams) {
        const VertexId a = f.v(x0, H1, z0);
        const VertexId b = f.v(x0, H1, z1);
        const VertexId c = f.v(x1, H1, z1);
        const VertexId d = f.v(x1, H1, z0);
        (void)f.contour({a, b, c, d},
                        beams ? Params{{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}, {"beams", "1"}}
                              : Params{{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}});
    };
    // МАРШ КРЫЛА — 45 ГР., И ПЛОЩАДКА ЕДЕТ ЗА НИМ (правка 27.08). Прежде ход
    // 4.6 на подъём 3.0 (33 гр.), верх на z 9.4; теперь ход равен подъёму, и
    // верх приходит на 11.06. КРОМКА ПЛОЩАДКИ СЧИТАЕТСЯ ОТ ВЕРХА МАРША, а не
    // стоит числом: разъехавшись, они дают либо дыру под ступившим, либо
    // потолок, в который упирается поднимающийся (HOUSES.md §13 — марш и проём
    // кладёт одна рука).
    const float ST_LO = 0.12f;          // верх половой плиты (плоскость 0.06)
    const float ST_HI = H1 + 0.06f;     // верх настила (плоскость H1)
    const float ST_RUN = ST_HI - ST_LO; // 45 гр.: ход равен подъёму
    const float ST_TOP = 14.0f - ST_RUN;
    deck(0.0f, 0.0f, AX, AZ, true);       // весь главный корпус
    deck(2.0f, AZ, BX, BZ, true);         // крыло, восточная полоса
    deck(0.0f, AZ, 2.0f, ST_TOP, false);  // площадка над верхом марша
    deck(0.0f, 14.2f, 2.0f, BZ, false);   // южный кусок за маршем
    // Ограждается ВОСТОЧНАЯ грань (1.75) — она же кромка проёма над маршем;
    // западная стоит в 0.35 от стены.
    house_flight_z(f, 0.35f, 1.75f, 14.0f, ST_TOP, ST_LO, ST_HI, 1.75f, age.w(0.3f),
                   0.1f);
    // ---------- НАВЕС НАД УГЛОВЫМ ВХОДОМ ----------
    for (const float px : {8.0f, 10.6f}) {
        (void)f.beam(f.v(px, 0.0f, 9.2f), f.v(px, 2.75f, 9.2f),
                     {{"radius", "0.12"}, {"mat", "0"}, {"tone", "2"}});
    }
    (void)f.beam(f.v(7.9f, 2.77f, 9.2f), f.v(10.7f, 2.77f, 9.2f),
                 {{"radius", "0.09"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}});
    {
        const VertexId a = f.v(11.0f, 3.35f, AZ);
        const VertexId b = f.v(7.6f, 3.35f, AZ);
        const VertexId c = f.v(7.6f, 2.65f, 9.6f);
        const VertexId d = f.v(11.0f, 2.65f, 9.6f);
        const ElementId s = f.contour({a, b, c, d},
                                      {{"thickness", "0.12"}, {"mat", "1"},
                                       {"tone", "1"}, {"fill", "7"},
                                       {"wear", age.w(0.35f)}});
        (void)f.g.set_param(s, "roof", "1");
        (void)f.g.set_param(s, "unsupported", "1");
    }
    // ---------- ДВОРИК: низкая ограда замыкает угол Г, в ней проезд ----------
    Params yard = {{"height", "1.2"}, {"thickness", "0.3"}, {"mat", "3"},
                   {"tone", "1"}, {"fill", "3"}, {"wear", age.w(0.55f)}};
    low(AX, BZ, AX, AZ, yard);        // восточная сторона дворика
    low(BX, BZ, 9.5f, BZ, yard);      // южная сторона, до проезда
    low(11.5f, BZ, AX, BZ, yard);     // южная сторона, после проезда
    for (const float px : {9.5f, 11.5f}) {
        (void)f.beam(f.v(px, 0.0f, BZ), f.v(px, 2.4f, BZ),
                     {{"radius", "0.16"}, {"mat", "0"}, {"tone", "2"}});
    }
    // ---------- ДВЕ КРОВЛИ ----------
    f.gable_roof(0.0f, 0.0f, AX, AZ, EAVES, 2.4f, "1", "0", "7", age.w(0.35f));
    // ТРУБА обеих версий, на ГЛАВНОМ корпусе (x 0..14, z 0..7): очаг в его
    // восточной половине (11.0, 1.10) — запад занят крылом x 0..7, z 7..15.
    // Самая толстая труба города: у усадьбы она и есть верхняя точка достатка.
    if (vents_on()) {
        f.chimney(11.0f, 1.10f, EAVES - 0.4f, EAVES + 2.7f + 0.75f, 0.36f, true,
                  "3", "1", age.w(0.45f));
    }
    // Крыло: z0 = 3.85, чтобы кромка скатов со свесом 0.35 села РОВНО на
    // конёк главного (z=3.5) — дальше на север скат вылез бы из чужой кровли.
    f.gable_roof_z(0.0f, 3.85f, BX, BZ, EAVES, 2.4f, "1", "0", "7",
                   age.w(0.35f), 7.2f);
    f.save(age.file);
}

/// АМБАР-ХЛЕВ 12x9: широкие ворота без створки, сеновал-надстройка,
/// АСИММЕТРИЧНЫЙ двускат — конёк сдвинут к югу, северный скат уходит почти
/// до земли (0.475 м) и накрывает стойла, как навес.
/// Боковые стены — не цепочки, а КОНТУРЫ-ПРОФИЛИ: у цепочки одна высота на
/// всю длину, а здесь верх стены обязан повторять ломаную кровли.
static void forge_barn(Aging age) {
    Forge f;
    const float W = 12.0f;
    const float D = 9.0f;
    const float ZR = 6.0f;      // конёк сдвинут к югу
    const float YR = 5.6f;
    const float ZN = -1.5f;     // северный карниз — за стеной, у самой земли
    const float YN = 0.475f;
    const float ZS = 9.8f;      // южный карниз
    const float YS = 3.32f;
    const float HN = 1.5f;      // северная стена под низким скатом
    const float HS = 3.8f;      // южная стена: ворота и сеновал
    // Земляной пол, битый и вытоптанный.
    {
        const VertexId a = f.v(0.0f, 0.05f, 0.0f);
        const VertexId b = f.v(0.0f, 0.05f, D);
        const VertexId c = f.v(W, 0.05f, D);
        const VertexId d = f.v(W, 0.05f, 0.0f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.1"}, {"mat", "3"}, {"tone", "2"},
                         {"wear", age.w(0.7f)}});
    }
    // ---------- боковые стены-профили: низ ровный, верх по кровле ----------
    for (const float gx : {0.0f, W}) {
        const VertexId a = f.v(gx, 0.0f, 0.0f);
        const VertexId b = f.v(gx, 0.0f, D);
        const VertexId c = f.v(gx, HS, D);
        const VertexId d = f.v(gx, YR, ZR);
        const VertexId e = f.v(gx, HN, 0.0f);
        (void)f.contour({a, b, c, d, e},
                        {{"thickness", "0.25"}, {"mat", "1"}, {"tone", "2"},
                         {"wear", age.w(0.55f)}});
    }
    // ---------- север: низкая доска под самым скатом ----------
    (void)f.wall(f.v(W, 0.0f, 0.0f), f.v(0.0f, 0.0f, 0.0f),
                 {{"height", "1.5"}, {"thickness", "0.22"}, {"mat", "1"},
                  {"tone", "2"}, {"wear", age.w(0.55f)}});
    // ---------- юг: ворота 3.2 м БЕЗ СТВОРКИ ----------
    // Проём набран стенами, а не doors=1: раскладка знает только створку
    // шириной HOUSE_DOOR_W_DEFAULT=1.0, а въезд с возом её втрое шире.
    Params board = {{"height", "3.8"}, {"thickness", "0.22"}, {"mat", "1"},
                    {"tone", "2"}, {"wear", age.w(0.55f)}};
    (void)f.wall(f.v(0.0f, 0.0f, D), f.v(4.4f, 0.0f, D), board);
    (void)f.wall(f.v(7.6f, 0.0f, D), f.v(W, 0.0f, D), board);
    (void)f.wall(f.v(4.4f, 2.9f, D), f.v(7.6f, 2.9f, D),
                 {{"height", "0.9"}, {"thickness", "0.22"}, {"mat", "1"},
                  {"tone", "2"}, {"wear", age.w(0.55f)}, {"unsupported", "1"}});
    for (const float px : {4.4f, 7.6f}) {
        (void)f.beam(f.v(px, 0.0f, D), f.v(px, 2.95f, D),
                     {{"radius", "0.15"}, {"mat", "0"}, {"tone", "2"}});
    }
    (void)f.beam(f.v(4.2f, 2.9f, D), f.v(7.8f, 2.9f, D),
                 {{"radius", "0.13"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}});
    // БАЛКА-ВЫЛЕТ ПОДЪЁМНИКА из сеновала над воротами (под южным карнизом).
    (void)f.beam(f.v(6.0f, 3.2f, 7.6f), f.v(6.0f, 3.2f, 10.6f),
                 {{"radius", "0.09"}, {"mat", "0"}, {"tone", "2"}});
    // ---------- СЕНОВАЛ: настил под коньком, площадка и марш ----------
    {
        const VertexId a = f.v(0.0f, 2.6f, 3.4f);
        const VertexId b = f.v(0.0f, 2.6f, 6.4f);
        const VertexId c = f.v(9.2f, 2.6f, 6.4f);
        const VertexId d = f.v(9.2f, 2.6f, 3.4f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "2"}, {"beams", "1"}, {"wear", age.w(0.5f)}});
    }
    // ПЛОЩАДКА ВОСТОЧНОГО КРАЯ СЕНОВАЛА — ЕЁ ЮЖНАЯ КРОМКА СЧИТАЕТСЯ ОТ ВЕРХА
    // МАРША (правка 27.08). Марш стал 45-градусным, ход укоротился с 3.7 до
    // 2.56, и верх ушёл с z 4.2 на 5.34; кромка, оставленная числом, дала бы
    // ступившему дыру под ногой. Заодно ВЫИГРАН ПРОСВЕТ: конёк стоит на z 6.0,
    // северный скат падает к земле, и каждый метр к югу поднимает кровлю над
    // головой на 0.68 м — судья мерил над прежним верхом 1.386 при норме 1.9,
    // над новым 2.4.
    const float LOFT = 2.6f;              // плоскость настила сеновала
    const float LOFT_TOP = LOFT + 0.06f;  // по чему по нему ходят
    const float LOFT_LO = 0.10f;          // верх земляного пола (плоскость 0.05)
    const float LOFT_RUN = LOFT_TOP - LOFT_LO;
    const float LOFT_Z = 7.9f - LOFT_RUN;
    {
        const VertexId a = f.v(9.2f, LOFT, 3.4f);
        const VertexId b = f.v(9.2f, LOFT, LOFT_Z);
        const VertexId c = f.v(11.4f, LOFT, LOFT_Z);
        const VertexId d = f.v(11.4f, LOFT, 3.4f);
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.12"}, {"fill", "5"}, {"mat", "1"},
                         {"tone", "2"}, {"wear", age.w(0.5f)}});
    }
    for (const float px : {1.0f, 4.6f, 8.6f}) {
        (void)f.beam(f.v(px, 0.0f, 6.4f), f.v(px, 2.6f, 6.4f),
                     {{"radius", "0.13"}, {"mat", "0"}, {"tone", "2"}});
    }
    (void)f.beam(f.v(0.0f, 2.6f, 6.4f), f.v(9.2f, 2.6f, 6.4f),
                 {{"radius", "0.11"}, {"form", "square"}, {"mat", "0"},
                  {"tone", "2"}});
    // Марш на сеновал: 45 гр., ход равен подъёму. Оба конца — на том, по чему
    // ходят: низ на верху земляного пола, верх на верху настила. Ограждается
    // западная грань (9.6) — та, что смотрит в сарай.
    house_flight_z(f, 9.6f, 11.0f, 7.9f, LOFT_Z, LOFT_LO, LOFT_TOP, 9.6f, age.w(0.4f),
                   0.1f);
    // ---------- СТОЙЛА под низким скатом ----------
    for (const float px : {2.4f, 4.8f, 7.2f}) {
        (void)f.wall(f.v(px, 0.0f, 0.5f), f.v(px, 0.0f, 2.9f),
                     {{"height", "1.1"}, {"thickness", "0.12"}, {"mat", "1"},
                      {"tone", "2"}, {"wear", age.w(0.6f)}});
    }
    // ---------- КРОВЛЯ: два ската разной длины ----------
    const float o = 0.4f;
    const auto slab = [&](VertexId a, VertexId b, VertexId c, VertexId d) {
        (void)f.contour({a, b, c, d},
                        {{"thickness", "0.15"}, {"mat", "6"}, {"tone", "1"},
                         {"wear", age.w(0.40f)}});
    };
    slab(f.v(-o, YR, ZR), f.v(W + o, YR, ZR), f.v(W + o, YN, ZN), f.v(-o, YN, ZN));
    slab(f.v(W + o, YR, ZR), f.v(-o, YR, ZR), f.v(-o, YS, ZS), f.v(W + o, YS, ZS));
    f.eaves_trim({-o, YN, ZN}, {W + o, YN, ZN});
    f.eaves_trim({-o, YS, ZS}, {W + o, YS, ZS});
    (void)f.beam(f.v(-0.5f, YR + 0.02f, ZR), f.v(W + 0.5f, YR + 0.02f, ZR),
                 {{"radius", "0.15"}, {"mat", "0"}, {"tone", "2"}});
    // Стропила ПОД настилом, торцы из-под обоих свесов.
    for (int k = 0; k <= 8; ++k) {
        const float x = W * static_cast<float>(k) / 8.0f;
        (void)f.beam(f.v(x, YR - 0.32f, ZR), f.v(x, YN - 0.1f, ZN - 0.25f),
                     {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
        (void)f.beam(f.v(x, YR - 0.32f, ZR), f.v(x, YS - 0.28f, ZS + 0.25f),
                     {{"radius", "0.08"}, {"mat", "0"}, {"tone", "2"}});
    }
    // ПОДПОРКИ СЕВЕРНОГО СКАТА: он уходит НИЖЕ стены, и держать его нечему —
    // короткие стойки по карнизу, они же столбы навеса над стойлами.
    for (const float px : {0.0f, 4.0f, 8.0f, W}) {
        (void)f.beam(f.v(px, 0.0f, ZN), f.v(px, YN, ZN),
                     {{"radius", "0.12"}, {"mat", "0"}, {"tone", "2"}});
    }
    f.save(age.file);
}

/// ПРОГОН СУДЬИ ПО ВСЕЙ ПОЛКЕ (dfn_houses --check). Читает готовые .dfh — те
/// самые файлы, что лежат в git и грузятся картой, — а не пересобирает рецепты:
/// правило обязано судить АРТЕФАКТ, иначе оно проверяет только те постройки,
/// которые печёт эта кузница, и утварь с чужими файлами остаётся без присмотра.
int check_library(const char* dir) {
    std::vector<std::string> files;
    for (const auto& it : std::filesystem::directory_iterator(dir)) {
        if (it.path().extension() == ".dfh") {
            files.push_back(it.path().string());
        }
    }
    std::sort(files.begin(), files.end());
    int ok = 0;
    for (const std::string& path : files) {
        std::ifstream in(path, std::ios::binary);
        const std::string text((std::istreambuf_iterator<char>(in)),
                               std::istreambuf_iterator<char>());
        dfn::world::HouseGraph g;
        const auto r = dfn::world::read_house(text, g);
        if (!r.ok) {
            std::printf("СВЯЗНОСТЬ: %s — НЕ ПРОЧЁЛСЯ: %s (строка %d)\n", path.c_str(),
                        r.why.c_str(), r.line);
            continue;
        }
        const dfn::world::HouseMesh mesh = dfn::world::build_house_mesh(g);
        const dfn::world::HouseConnectivity c = dfn::world::check_house_connectivity(mesh);
        if (c.ok()) {
            ++ok;
            continue;
        }
        std::printf("СВЯЗНОСТЬ: %s — ОСТРОВОВ %zu (тел %zu)\n", path.c_str(),
                    c.islands.size(), c.bodies);
        for (const dfn::world::HouseIsland& is : c.islands) {
            std::printf("    %-9s %-40s тел %4zu  y %8.3f..%8.3f",
                        is.grounded ? "земля" : "ВИСИТ",
                        dfn::world::island_name(is).c_str(), is.bodies,
                        static_cast<double>(is.y_lo), static_cast<double>(is.y_hi));
            if (!is.grounded && is.nearest_element != dfn::world::NO_ELEMENT) {
                std::printf("  зазор %.3f м до e%u", static_cast<double>(is.nearest_gap),
                            is.nearest_element);
            }
            std::printf("\n");
        }
    }
    std::printf("СВЯЗНОСТЬ: %d/%zu OK (допуск %.3f м)\n", ok, files.size(),
                static_cast<double>(dfn::world::HOUSE_CONTACT_TOL_M));
    return ok == static_cast<int>(files.size()) ? 0 : 3;
}

// ---------------------------------------------------------------------------
// РЕЕСТР РЕЦЕПТОВ (волна И3 эпохи «12 городов»)
//
// ЗДЕСЬ БЫЛ ПРЯМОЙ СПИСОК ВЫЗОВОВ с сорока четырьмя литералами путей — и это
// была ЕДИНСТВЕННАЯ ТОЧКА СХВАТКИ двенадцати городов, которые пекут на одну
// полку: два города, добавивших по рецепту, правили одну и ту же функцию.
// Теперь рецепт — СТРОКА ТАБЛИЦЫ (имя -> пакет -> рука), пакет — своя секция,
// а путь выводится из имени и в коде больше не повторяется.
// ---------------------------------------------------------------------------

using dfn::forge::add_recipe;
using dfn::forge::Recipe;

/// ПАКЕТ demo — пять повторов домиков демки (заказ 20.08): сруб, фахверк,
/// камень, Г-образный и П-образный. Городам они не нужны, но полка их держит:
/// на них стоят сцена build.scene и тесты проходимости.
void pack_demo(std::vector<Recipe>& b) {
    add_recipe(b, "demo", "log-replica", [](const char* f) { forge_log(f); });
    add_recipe(b, "demo", "frame-replica", [](const char* f) { forge_frame(f); });
    add_recipe(b, "demo", "stone-replica", [](const char* f) { forge_stone(f); });
    add_recipe(b, "demo", "l-house", [](const char* f) { forge_l_house(f); });
    add_recipe(b, "demo", "u-house", [](const char* f) { forge_u_house(f); });
}

/// ПАКЕТ city — ОБЩИЕ городские рецепты: стены, ворота, лестницы, мощение,
/// фундаменты, мосты и жилые тела. Ими пекутся ВСЕ двенадцать городов, поэтому
/// правка здесь — правка у всех сразу; собственные постройки города заводят
/// своей секцией (см. house_recipes ниже), а не дописыванием в этот пакет.
void pack_city(std::vector<Recipe>& b) {
    add_recipe(b, "city", "city-tower", [](const char* f) { forge_tower(f); });
    add_recipe(b, "city", "city-donjon", [](const char* f) { forge_tower_tall(f); });
    add_recipe(b, "city", "city-treering", [](const char* f) { forge_tree_ring(f); });
    add_recipe(b, "city", "city-wall12", [](const char* f) { forge_wall12(f); });
    // ГНУТЫЕ ПРОГОНЫ. Хорды 8/12/16 — те же, которыми раскладчик уже режет
    // грани кольца; стрелы 0 (прямой с касательными торцами), 0.25, 0.5, 1, 2.
    // 0.25 — НЕ ДЛЯ СИММЕТРИИ, А ПО ЗАМЕРУ: кольцо чертежа поворачивает на
    // 360 гр. за 316.2 м, то есть 1.139 гр./м, и двенадцатиметровому куску
    // средней кривизны положено 13.7 гр. — стрела 0.36. Стрела 0.5 даёт 19.1
    // гр. на те же 12 м и замкнула бы кольцо за 226 м вместо 316.
    for (const float chord : {8.0f, 12.0f, 16.0f}) {
        for (const float sag : {0.0f, 0.25f, 0.5f, 1.0f, 2.0f}) {
            char name[64];
            std::snprintf(name, sizeof(name), "city-wall-arc%g-s%03d",
                          static_cast<double>(chord),
                          static_cast<int>(std::lround(sag * 100.0f)));
            add_recipe(b, "city", name, [chord, sag](const char* f) {
                forge_wall_arc(f, chord, sag);
            });
        }
    }
    add_recipe(b, "city", "city-gate", [](const char* f) { forge_gate(f); });
    add_recipe(b, "city", "city-stairs6",
               [](const char* f) { forge_street_stairs6(f); });
    add_recipe(b, "city", "city-stairs",
               [](const char* f) { forge_street_stairs(f); });
    add_recipe(b, "city", "city-plaza12",
               [](const char* f) { forge_plaza(f, 12.0f, 12.0f); });
    add_recipe(b, "city", "city-plaza20",
               [](const char* f) { forge_plaza(f, 20.0f, 20.0f); });
    // МОЩЕНИЕ УЛИЦ. Ширина 6 м у главной — норматив улицы города; 4 м —
    // второстепенный проход. Длина 24 м — участок, которым мостят прогон;
    // 8x6 — короткая вставка на поворот и стык с площадью.
    //
    // КОРОТКИЕ УЧАСТКИ — не роскошь, а требование трассы: главная улица города
    // ломаная, между изломами 10-14 м, и плита 24 м на изломе даёт веер с
    // клиньями травы между соседками. 12 ложится по прямым, 8 и 6 — на изломы
    // и на подход к воротам (запрос раскладчика, замерено по чертежу).
    for (const auto& c : {std::pair<const char*, float>{"city-cobble24x6", 24.0f},
                          {"city-cobble12x6", 12.0f},
                          {"city-cobble8x6", 8.0f},
                          {"city-cobble6x6", 6.0f}}) {
        const float w = c.second;
        add_recipe(b, "city", c.first,
                   [w](const char* f) { forge_cobble(f, w, 6.0f, 2.0f, "1", 0.35f); });
    }
    // НАКЛОННЫЕ КУСКИ ПОД ПОДЪЁМ К ЗАМКУ (замер раскладчика: 8.3% на 108 м).
    // Шаг 2% выбран по остатку: раскладка берёт ближайший уклон, промах не
    // больше 1%, и на шестиметровом куске это 6 см — вчетверо ниже её же
    // порога ступени 0.20 м. Восьмиметровые для пологих участков: там промах
    // 8 см, всё ещё под порогом.
    // ПОТОЛОК 10% ПРОБИТ ЗАМЕРОМ РАСКЛАДЧИКА, а не запрошен «на всякий случай»:
    // 8.3% оказались СРЕДНИМ по всему подъёму, а по окну 6 м тридцать метров
    // идут 11.5-12.6%, и у самой кромки рынка — 21%. С потолком 10% промах на
    // 12.6% дал бы 0.16 м, а на 21% — 0.66 м, то есть разрыв мощения ровно у
    // выхода с рынка. Шестиметровые до 20%, восьмиметровые до 14%: на длинном
    // куске крутой уклон означает подъём под метр с лишним, а такому месту
    // место короткому.
    for (const auto& s : {std::pair<const char*, float>{"04", 0.04f},
                          {"06", 0.06f}, {"08", 0.08f}, {"10", 0.10f},
                          {"12", 0.12f}, {"14", 0.14f}, {"16", 0.16f},
                          {"18", 0.18f}, {"20", 0.20f}}) {
        const float slope = s.second;
        add_recipe(b, "city", std::string("city-cobble6x6-s") + s.first,
                   [slope](const char* f) {
                       forge_cobble(f, 6.0f, 6.0f, 2.0f, "1", 0.35f, slope);
                   });
        if (slope <= 0.145f) {
            add_recipe(b, "city", std::string("city-cobble8x6-s") + s.first,
                       [slope](const char* f) {
                           forge_cobble(f, 8.0f, 6.0f, 2.0f, "1", 0.35f, slope);
                       });
        }
    }
    // ВТОРОСТЕПЕННЫЙ ПРОХОД — тот же приём, другая одежда: камень МЕЛЬЧЕ
    // (1.7 против 2.0), тон ТЁМНЫЙ и износ выше порога 0.7, на котором
    // отрисовка уводит деталь в выветренный ряд атласа. Разница читается
    // фактурой и тоном, а не вторым рецептом.
    add_recipe(b, "city", "city-cobble24x4",
               [](const char* f) { forge_cobble(f, 24.0f, 4.0f, 1.7f, "2", 0.62f); });
    // РЫНОЧНЫЙ КВАДРАТ: plaza12 мала под ряды и носит тот самый бордюр-стену,
    // на который взбирается капсула. 16x16 с проходимой фаской — и 14x11 ПО
    // ФАКТИЧЕСКОМУ РЫНКУ чертежа: жёсткую плиту раскладкой не режут, лишние
    // два на пять метров ушли бы под соседние постройки.
    add_recipe(b, "city", "city-cobble16",
               [](const char* f) { forge_cobble(f, 16.0f, 16.0f, 2.0f, "1", 0.4f); });
    add_recipe(b, "city", "city-cobble14x11",
               [](const char* f) { forge_cobble(f, 14.0f, 11.0f, 2.0f, "1", 0.4f); });
    // МАРШ И ПОДПОРНАЯ КРОМКА под южную грань рынка (замер раскладчика: клин
    // 1.30 м на западном конце до нуля у x=122, тринадцать метров).
    // Подступенок 0.20, проступь 0.35 — отсюда вылет 2.10 при 1.20 и 1.40 при 0.80.
    add_recipe(b, "city", "city-steps6-h12",
               [](const char* f) { forge_street_steps(f, 1.20f, 2.10f); });
    add_recipe(b, "city", "city-steps6-h08",
               [](const char* f) { forge_street_steps(f, 0.80f, 1.40f); });
    // ВЫЛЕТ МАРША — ЦЕЛОЕ ЧИСЛО МЕТРОВ (правило раскладчика по замеру). Ниша
    // под маршем режется в поле высот, а поле — сетка с шагом 1 м. Кромка,
    // попавшая МЕЖДУ строк, представлена быть не может: билинейная выборка
    // тянет соседнюю строку, и на подошве 57% каждая десятая доля строки стоит
    // 5.7 см. У h12 вылет 2.100 — верхняя кромка садится на z=143.9, отход
    // верхних углов -0.051 при заданных -0.020. Замер раскладчика сошёлся с
    // расчётом (доля_строки * уклон * шаг = 0.9*0.571*1.0) до миллиметра.
    // 2.000 кладёт обе кромки на целые строки, квантование исчезает.
    add_recipe(b, "city", "city-steps6-h12r2",
               [](const char* f) { forge_street_steps(f, 1.20f, 2.00f); });
    // h14r2 — под ВЫРОСШИЙ перепад кромки террасы (1.23 -> 1.35: мостовую рынка
    // посадили заподлицо, +0.147 -> +0.030, а улица осталась). h12r2 давал бы
    // остаток 0.175, то есть по 0.087 под ногой на входе и выходе — под порогом
    // 0.20, но девять сантиметров ступени там, где её не задумано.
    // Подъём 1.400 при вылете 2.000 попадает в сетку ЛУЧШЕ прочих: 1.400/0.175
    // ровно 8, значит подступенок выходит канонический 0.175 без остатка, а
    // проступь 0.250 даёт 2R+T = 0.600 — низ удобной полосы.
    add_recipe(b, "city", "city-steps6-h14r2",
               [](const char* f) { forge_street_steps(f, 1.40f, 2.00f); });
    // ШАГ ВЫСОТ 0.2, А НЕ 0.4. Раскладчик берёт ближайшую высоту, значит промах
    // — половина шага. При шаге 0.4 это 0.20 ровно, то есть ТОЧНО порог ступени,
    // по которому он же и бракует; запас должен быть под порогом, а не на нём.
    // 0.2 даёт промах 0.10 — вдвое под порогом, и уступы копинга в 20 см
    // читаются кладкой, ради чего клин и собирается отрезками.
    for (const auto& k : {std::pair<const char*, float>{"02", 0.2f},
                          {"04", 0.4f}, {"06", 0.6f}, {"08", 0.8f},
                          {"10", 1.0f}, {"12", 1.2f}}) {
        const float h = k.second;
        add_recipe(b, "city", std::string("city-kerb2-h") + k.first,
                   [h](const char* f) { forge_kerb(f, h); });
    }
    // ФУНДАМЕНТ И КРЫЛЬЦО КАЖДОМУ ДОМУ (заказ владельца 23.08).
    // ШАГ ВЫСОТ ЛЕНТЫ 0.2, тот же и по той же причине, что у city-kerb2:
    // раскладчик берёт БЛИЖАЙШУЮ СВЕРХУ (лента обязана дотянуться до грунта, а
    // не повиснуть), промах уходит ВНИЗ, в землю, где его никто не увидит.
    // ПОТОЛОК 4.0 М взят по замеру, а не на глаз: самый крутой перепад под
    // пятном города — донжон на кромке замкового плато, 4.24 м земли над полом
    // в метре от стены; ленте нужен этот перепад плюс запас 0.3.
    for (int dm = 2; dm <= 40; dm += 2) {
        char name[64];
        std::snprintf(name, sizeof(name), "city-foot2-h%02d", dm);
        const float h = static_cast<float>(dm) * 0.1f;
        add_recipe(b, "city", name, [h](const char* f) { forge_footing(f, h); });
    }
    // ПОДПОРНАЯ СТЕНКА ТЕРРАСЫ И КОНТРФОРС (заказ владельца 24.08 по донжону).
    // РЯД ДО 4.0, А НЕ ДО ДЕСЯТИ: раскладчик собирает грань ВЕНЦАМИ ровно как
    // ленту, и потолок ряда — не потолок стенки. Шаг 0.2 — тот же и по той же
    // причине, что у city-kerb2: промах 0.10, вдвое под порогом ступени.
    for (int dm = 4; dm <= 40; dm += 2) {
        char name[64];
        std::snprintf(name, sizeof(name), "city-revet2-h%02d", dm);
        const float h = static_cast<float>(dm) * 0.1f;
        add_recipe(b, "city", name, [h](const char* f) { forge_revet(f, h); });
    }
    // ШАГ КОНТРФОРСА 0.4, ВДВОЕ КРУПНЕЕ СТЕНКИ: пилон не обязан кончаться
    // ровно под копингом — он и не должен (см. forge_buttress), а промах 0.2
    // на его верхнем венце неотличим от разницы кладки.
    for (int dm = 8; dm <= 40; dm += 4) {
        char name[64];
        std::snprintf(name, sizeof(name), "city-butt-h%02d", dm);
        const float h = static_cast<float>(dm) * 0.1f;
        add_recipe(b, "city", name, [h](const char* f) { forge_buttress(f, h); });
    }
    // ШАГ ВЫСОТ КРЫЛЬЦА 0.1, ВДВОЕ МЕЛЬЧЕ ЛЕНТЫ. Промах ленты прячется в
    // грунте, промах крыльца стоит НА ВХОДЕ В ДОМ: половина шага 0.2 — это
    // 0.10 м под ногой на пороге, половина 0.1 — 0.05, вчетверо под порогом
    // ступени 0.20 и втрое ниже подступенка, то есть неразличимо на ходу.
    for (int dm = 2; dm <= 20; ++dm) {
        char name[64];
        std::snprintf(name, sizeof(name), "city-stoop16-h%02d", dm);
        const float rise = static_cast<float>(dm) * 0.1f;
        add_recipe(b, "city", name, [rise](const char* f) { forge_stoop(f, rise); });
    }
    add_recipe(b, "city", "city-bridge", [](const char* f) { forge_bridge(f); });
    add_recipe(b, "city", "city-bridge-arc",
               [](const char* f) { forge_bridge_arc(f); });
    add_recipe(b, "city", "city-stall", [](const char* f) { forge_stall(f); });
    add_recipe(b, "city", "city-well", [](const char* f) { forge_well(f); });
    add_recipe(b, "city", "city-longhall", [](const char* f) { forge_longhall(f); });
    add_recipe(b, "city", "city-temple", [](const char* f) { forge_temple(f); });
    add_recipe(b, "city", "city-keep", [](const char* f) { forge_keep(f); });
    add_recipe(b, "city", "city-keep-s", [](const char* f) { forge_keep_small(f); });
    add_recipe(b, "city", "city-mill", [](const char* f) { forge_mill(f); });
    // ЖИЛЫЕ ПОСТРОЙКИ ПЕЧАТАЮТСЯ ДВАЖДЫ — ухоженной и запущенной. Улица из
    // одинаково потрёпанных домов читается одной текстурой; разброс износа
    // между соседями и есть то, из чего глаз собирает возраст города.
    //
    // ВЕТХИЙ: +0.28 к собственному износу каждой детали, зажатые в полосу
    // заказа. Порог 0.7 не косметический — на нём отрисовка уводит деталь в
    // ВЫВЕТРЕННЫЙ ряд атласа (серость и лишайник там нарисованы), поэтому
    // нижняя граница взята выше него у всего, что не цоколь.
    // Амбар и в уходе стоит грязнее жилья — у ветхого пол полосы выше.
    struct Dwelling {
        const char* name;
        void (*forge)(Aging);
        float derelict_lo;
    };
    for (const Dwelling& d : {Dwelling{"city-house-s", &forge_small_house, 0.65f},
                              Dwelling{"city-shop", &forge_shop, 0.65f},
                              Dwelling{"city-house-l", &forge_house_large, 0.65f},
                              Dwelling{"city-manor", &forge_manor, 0.65f},
                              Dwelling{"city-barn", &forge_barn, 0.72f}}) {
        auto forge = d.forge;
        const float lo = d.derelict_lo;
        add_recipe(b, "city", d.name, [forge](const char* f) {
            Aging a;
            a.file = f;
            forge(std::move(a));
        });
        add_recipe(b, "city", std::string(d.name) + "-old", [forge, lo](const char* f) {
            Aging a;
            a.file = f;
            a.shift = 0.28f;
            a.lo = lo;
            a.hi = 0.8f;
            a.kept = false;
            forge(std::move(a));
        });
    }
}

// ПАКЕТ КОРНХОЛЛА — СВОИМ ФАЙЛОМ, НО В ЭТОЙ ЖЕ ЕДИНИЦЕ ТРАНСЛЯЦИИ. Рука
// кузницы (Forge, gable_roof, keep_tier, Aging) живёт в анонимном пространстве
// имён выше и другой единице недоступна; вынести её в общий заголовок значило
// бы править этот файл под всеми городами сразу, а завести копию — теневая
// цепочка по правилу 39. Поэтому у пакета свой файл (правило 21: этот уже
// 4318 строк), а здесь от него ровно две строки — включение и вызов ниже.
// Довод целиком — в шапке tools/pack_cornhall.h.
#include "tools/pack_cornhall.h"
// ПАКЕТ КУРГАНЛЫ (город №11, степь/юг) — та же граница, тот же довод.
#include "tools/pack_kurganly.h"

/// ВЕСЬ РЕЕСТР. ГОРОДСКОЙ ПАКЕТ БУДУЩЕГО ГОРОДА ДОБАВЛЯЮТ СЮДА СВОЕЙ СТРОКОЙ:
/// пишут pack_<город>(std::vector<Recipe>&) со своими add_recipe(b, "<город>",
/// "<город>-<рецепт>", ...) и зовут её здесь. Имена файлов получат префикс
/// города, манифест — семейство с его именем (префикс имени до дефиса), а
/// ключ --only '<город>-*' испечёт ровно его пакет, не тронув ничего чужого.
std::vector<Recipe> house_recipes() {
    std::vector<Recipe> b;
    pack_demo(b);
    pack_city(b);
    pack_cornhall(b);
    pack_kurganly(b);
    // pack_solitude(b);
    // pack_markarth(b);
    return b;
}

} // namespace

int main(int argc, char** argv) {
    // ДВЕ РУКИ ОДНОГО БИНАРНИКА: печь и судить. Судья отдельным исполняемым
    // файлом стоил бы второй цели сборки ради двухсот строк, а рецепты и
    // правило всё равно живут вместе.
    return dfn::forge::run_forge(
        argc, argv, "houses", house_recipes(), g_refused,
        [](const char* dir) { return check_library(dir); });
}
