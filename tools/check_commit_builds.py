#!/usr/bin/env python3
#
# Created: 27:08:2026 - 22:17:42
# Last updated: 27:08:2026 - 23:00:37
# File: tools/check_commit_builds.py
#
# Responsibility:
# - Answer ONE question about a commit: СОБЕРЁТСЯ ЛИ ОН У ТОГО, КТО ЕГО НЕ
#   ПИСАЛ. Not "does the working tree build" — that question is answered by
#   `cmake --build` and it is a different question on a SHARED tree, where the
#   working copy holds several waves at once and every one of them is missing
#   from HEAD until its author commits.
#
# КАК ЗАПУСКАТЬ:
#   python3 tools/check_commit_builds.py                 # HEAD
#   python3 tools/check_commit_builds.py --sha ee031b0   # любой коммит
#   python3 tools/check_commit_builds.py --build-dir build_music
#   python3 tools/check_commit_builds.py --range 9c8cafb..HEAD   # цепочка целиком
#   python3 tools/check_commit_builds.py --self-test     # три руки, см. ниже
# Exit code 0 — чисто; 1 — не собирается; 2 — проверить не удалось.
#
# ЧТО ОН ДЕЛАЕТ, В ОДНОЙ СТРОКЕ: разворачивает ДЕРЕВО КОММИТА в отдельный
# git worktree и гоняет по нему `clang++ -fsyntax-only` на тех TU, до которых
# коммит дотянулся. Команду компиляции берёт готовую, из
# build*/compile_commands.json, и переставляет в ней ТОЛЬКО `-I<корень репо>` на
# дерево коммита: третьи стороны и генерённые каталоги от коммита не зависят и
# остаются общими — поэтому bgfx не пересобирается и минут здесь нет.
#
# ЦЕНА, ЗАМЕРЕНА: 0.33 с на лёгком TU (MenuArt.cpp) и 0.53–2.06 с на тяжёлом
# (Menu.cpp — три прогона рукава подряд дали 2.06, 1.08 и 0.53), Homebrew
# clang 22, RelWithDebInfo. Холодный и тёплый прогон неразличимы (1.02 против
# 1.06) — упирается в разбор заголовков, не в диск; вчетверо разошлись прогоны
# при РАЗНОЙ ЗАГРУЗКЕ общей машины, где рядом собираются чужие зоны.
# Разворот worktree — ещё несколько секунд на 1843 файла. Итого рукав целиком,
# с тремя развёртываниями, 12 с.
#
# И ЧИСЛО В ЭТОЙ СТРОКЕ УЖЕ ОДИН РАЗ ПРОТУХЛО, ПОКА ПРИБОР ПИСАЛСЯ. Первый
# замер дал 0.53–0.56 с на том же Menu.cpp — но это был Menu.cpp ДО того, как
# в него легли две волны сразу (поле искр и группы настроек), то есть на четыреста
# строк короче. Замер не был неверным, он состарился за час; поймано тем, что
# рукав печатает своё время рядом, и оно не сошлось с шапкой вчетверо. Мораль
# ровно та же, что у правила 16 про метки: число, переписанное из памяти, а не
# перемеренное, врёт тихо. Здесь оно врало в БЕЗОПАСНУЮ сторону, что хуже.
#
# ЧТО ИЗ ЭТОГО СЛЕДУЕТ И ЧТО НЕТ. Следует: застава может быть НЕ выборочной —
# секунда на TU не жалко платить на каждом коммите, трогающем .h/.cpp. Не
# следует: «ровно полсекунды». Величина растёт с весом TU и скачет от загрузки
# общей машины (в одном прогоне рукава тот же файл дал 2.06 с, пока рядом
# собиралась чужая зона). Порядок величины — СЕКУНДЫ, а не минуты, — вот что
# здесь несущее.
#
# Dependencies:
# - Uses: Python stdlib, git, готовый build*/compile_commands.json.
# - Used by: человек и агент перед `git commit --only` в общем дереве; CI.
#
# AI Agents Notice:
# - Follow docs/ARCHITECTURE.md strictly.
#
# ПОЧЕМУ ЭТОТ ПРИБОР ВООБЩЕ ЕСТЬ — ДВА СЛУЧАЯ ОДНОГО ВЕЧЕРА (27.08.2026).
# Правило, которое он стережёт: КОММИТ ОБЯЗАН СОБИРАТЬСЯ У ТОГО, КТО ЕГО НЕ
# ПИСАЛ. Разрез волны по файлам между агентами законен ровно до тех пор, пока
# каждая половина собирается сама по себе; «мои строки, его строки» — это про
# АВТОРСТВО, и собираемость из него не следует.
#
#   1. ВЫПУЩЕН. Волну «настройки по группам» разрезали по файлам между двумя
#      агентами: один унёс Menu.cpp, у второго остался Menu.h с новыми
#      перечислениями. Между ee031b0 и aae1bc9 дерево не собиралось —
#      девятнадцать обращений к именам, которых в заголовке того коммита ещё
#      нет. Находка и замер — bigworld-menufx.
#   2. ПОЙМАН. Часом позже App.h обзавёлся `#include "DoorAim.h"` от третьей
#      волны, а сам DoorAim.{h,cpp} лежал незакоммиченным. Готовые App.cpp и
#      App.h не легли: коммит собрался бы у автора и не собрался бы ни у кого
#      другого. Дождались чужого коммита и легли следом.
#
# И ГЛАВНОЕ, ЧЕМУ НАУЧИЛИ ЭТИ ДВА СЛУЧАЯ ВМЕСТЕ. Первой заставой предлагался
# grep по включениям («есть ли такой файл в дереве коммита»). Он ловит случай 2
# и СЛЕП К СЛУЧАЮ 1: Menu.h в том коммите ЕСТЬ, он лишь старый, а дефект был не
# «файла нет», а «имени нет в файле». Обратная проверка (`git ls-files`) слепа
# симметрично. Правило, продаваемое вместе с проверкой, которая его не ловит,
# хуже правила без проверки — поэтому здесь не grep, а компилятор: общее у двух
# случаев не имя и не файл, а СОБИРАЕМОСТЬ, и её не выражает никакой поиск по
# тексту. Признание про слепую проверку — bigworld-menufx; проверка компилятором
# и три руки ниже — bigworld-music.
#
# ЧЕГО ЭТОТ ПРИБОР НЕ ЛОВИТ, СКАЗАНО ЗАРАНЕЕ (урок tools/header_check.py: у
# прибора, чьи дыры не названы, зелёный отчёт принимают за доказательство).
#
# НО СПИСОК НИЖЕ — НЕ ИНДУЛЬГЕНЦИЯ, И ЭТО ГЛАВНОЕ, ЧТО НАДО ЗНАТЬ ПРО НЕГО.
# Формулировка bigworld-menufx, которой он поправил сам себя: НАЗВАННАЯ ДЫРА
# ПЕРЕСТАЁТ БЫТЬ ЛОВУШКОЙ ТОЛЬКО ДЛЯ ТОГО, КТО ЕЁ НАЗВАЛ. Тот, кто придёт
# через месяц, прочтёт список ровно один раз — при первом знакомстве, — и
# именно в день, когда дыра выстрелит, перечитывать его не пойдёт: она же
# названа, значит на вид безобидна. Поэтому две из перечисленных ниже (запуск
# из чужого worktree, слияния) НЕ остались в списке предупреждениями, а
# ПОЧИНЕНЫ — вторая до того, как в дереве появился хоть один предмет. Пока
# предмета нет, починка стоит часа и ничего не ломает; когда он появится,
# цена другая и платит её не автор.
#
# Итак, что остаётся НЕ пойманным:
#   * ЛИНКОВКУ. `-fsyntax-only` не зовёт компоновщик, поэтому объявленная и
#     нигде не определённая функция пройдёт. Ловится сборкой, и это осознанный
#     размен: полсекунды против минут.
#   * TU, КОТОРЫХ НЕТ В compile_commands.json — новый файл, ещё не попавший в
#     CMake. Такие НЕ пропускаются молча: они перечисляются в отчёте и делают
#     ответ «проверить не удалось» (код 2), а не «всё хорошо».
#   * ВКЛЮЧЕНИЯ ПОД #if И СОБРАННЫЕ МАКРОСОМ — замыкание строится текстовым
#     разбором `#include "..."`. В этом дереве таких нет; появятся — прибор
#     недосчитает TU и об этом никто не узнает.
#   * ЧУЖИЕ ЗОНЫ СБОРКИ: берётся ОДИН набор флагов (одна конфигурация). Коммит,
#     ломающий только Apple clang или только Debug, отсюда не виден. И ЧЕЙ
#     ИМЕННО НАБОР — РЕШАЕТ СЛУЧАЙ: без --build-dir каталог выбирается по
#     СВЕЖЕСТИ, а каталогов сборки в этом дереве девятнадцать (по зоне на
#     агента), так что победитель — тот, кто компилировал последним, и это
#     почти наверняка ЧУЖАЯ зона (замер 28.08: авто-выбор дал build_look при
#     запуске из зоны музыки). Для синтаксической проверки флаги зон
#     равноценны, поэтому ответ верен — но имя выбранного каталога печатается
#     и основным ходом, и рукавом, чтобы «проверено» никогда не значило
#     «проверено неизвестно чем». Это тот же класс, что подмена файла в общем
#     скретчпаде по короткому имени (находка bigworld-menufx, 28.08): общее
#     пространство имён + выбор по случайному признаку = тихая подстановка.
#   * ЗАПУСК ИЗ ЧУЖОГО git worktree — БЫЛА ЗЕЛЁНАЯ ДЫРА, ПОЧИНЕНА (найдена
#     bigworld-menufx). Корень для подстановки -I брался из
#     `git rev-parse --show-toplevel`, то есть у ЗАПУСКАЮЩЕГО, и сравнивался с
#     путём из compile_commands.json ТОЧНО. Из другого рабочего дерева того же
#     репозитория подстановка не срабатывала ни разу, включения разрешались
#     против ГЛАВНОГО рабочего каталога — с чужими незакоммиченными файлами, —
#     и прибор отвечал «чисто» на коммите, который из главного дерева называл
#     несобираемым. Ни предупреждения, ни кода 2: зелёный отчёт, то есть
#     враньё в безопасную сторону, которое эта же шапка называет худшим.
#     Воспроизведено им на одном sha и одном --build-dir и подтверждено
#     числами: VideoRow в Menu.h дерева ee031b0 — 0 вхождений, в главном
#     каталоге — 2. Не экзотика: агентов здесь запускают с isolation worktree.
#     Починено ДВАЖДЫ, потому что одной починки мало: корень читается из самой
#     базы (cc_repo_root), И подстановки считаются — ноль подстановок даёт код
#     2 «проверить не удалось», а не зелёный. Держат руки 5 и 5b.
#   * СЛИЯНИЯ — БЫЛА БЫ ТРЕТЬЯ ЗЕЛЁНАЯ ДЫРА, ЗАКРЫТА ЗАРАНЕЕ (указана
#     bigworld-menufx как БУДУЩАЯ). `git show --name-only` на слиянии печатает
#     ПУСТО, поэтому прибор сказал бы «проверять нечего» и вернул ноль. Теперь
#     у коммита считаются родители, и у слияния изменённое читается против
#     ПЕРВОГО. Слияний в этом дереве нет ни одного (`git log --merges` пуст) —
#     чинилось до появления предмета, потому что предмет появится не когда о
#     нём вспомнят, а когда кто-нибудь заведёт ветвление. Держат руки 6a/6b, и
#     пробное слияние они лепят сами через `git commit-tree`: объект висячий,
#     ни ветки, ни тега в общем репозитории не заводится. ПРОВЕРЕНО ЧУЖОЙ
#     РУКОЙ, и проверено то, чего автор не обещал: после прогона рукава
#     `git branch -a` даёт только main, `git worktree list` — только главное
#     дерево, а на пробное слияние не ссылается ни один ref
#     (`git for-each-ref --contains` пуст). Замер bigworld-menufx.
#
# И ПРАВИЛО ДЛЯ РУКАВОВ ВООБЩЕ, СОБРАННОЕ ИЗ ДВУХ ПОЛОВИН. Первая выведена
# bigworld-menufx из провала руки 5b: РУКА ОБЯЗАНА ПЕЧАТАТЬ ЧИСЛА, ПО КОТОРЫМ
# СУДИТ, а не только свой приговор — ошибка в МЕХАНИЗМЕ видна по нелепому
# результату, а ошибка в ПРИГОВОРЕ при верных числах нелепости не производит, и
# заметить её можно только тем, что числа стоят рядом. Вторая половина нашлась
# в этом же приборе через час: он печатал ВЫХОД и молчал про ВХОД, то есть
# девять зелёных утверждений не говорили, на чьей конфигурации получены.
#
# Целиком: РУКА НАЗЫВАЕТ НЕ ТОЛЬКО ТО, ЧТО НАМЕРИЛА, НО И НА ЧЁМ. Здесь числа
# печатают все руки, включая зелёные, а рукав первой строкой называет каталог
# сборки и корень.
#
# АВТОРСТВО НЕ СХЛОПЫВАЮ, хотя менуфикс предлагал забрать правило целиком без
# ссылки на него («половина правила не стоит второй строки авторства»). Стоит:
# вторая половина не нашлась бы без первой — я искал вход ровно потому, что он
# заставил меня печатать выход, — и правило, у которого стёрта история, читается
# как чьё-то мнение, а не как вывод из двух измеренных провалов.
#   * УНАСЛЕДОВАННУЮ ПОЛОМКУ — и это САМАЯ ВАЖНАЯ ИЗ ДЫР, потому что её легко
#     принять за зелёный ответ. Проверка отвечает «не сломал ли ЭТОТ коммит то,
#     что тронул», а НЕ «собирается ли дерево». Разница измерена (находка
#     bigworld-menufx, 27.08): то окно оказалось шириной в ТРИ коммита, и один
#     из них — ee031b0 -> 847f020 -> 9c8cafb -> закрылось на aae1bc9.
#       - 847f020 не трогал ни одного спорного файла и всё равно несобираем: он
#         унаследовал сломанный HEAD. То есть «я положил только свои файлы» не
#         защищает НИ ОТ ЧЕГО.
#       - 9c8cafb — волна мебели, к меню отношения не имеющая. Её втянуло в окно
#         тем, что она встала следом: цена ложится не на того, кто окно открыл,
#         а на случайного соседа, и это ЕГО коммит найдёт бисект.
#     Для этой дыры есть --range A..B: он берёт ОБЪЕДИНЕНИЕ изменённых файлов по
#     всей цепочке и проверяет их против дерева B. «Собрал ли я лично» и «здорово
#     ли дерево» — два разных вопроса, и второй задаётся только так.
#
# UPD:
# - 27:08:2026 - 22:22:09: Создан по решению координатора. ПРАВИЛО, которое
#   прибор стережёт («коммит обязан собираться у того, кто его не писал»), —
#   находка bigworld-menufx: он же выпустил первый случай, он же его нашёл, он
#   же измерил (девятнадцать обращений к именам, которых в заголовке того
#   коммита нет) и он же признал, что первая предложенная им застава слепа к
#   его собственному дефекту. ПРОВЕРКА КОМПИЛЯТОРОМ и три руки рукава —
#   bigworld-music; цена измерена дважды и независимо: 0.53-0.56 с у автора
#   прибора, 0.59-0.61 с у menufx своим скриптом на том же коммите.
#   ЦЕНА ПЕРЕМЕРЕНА ПЕРЕД САМОЙ УКЛАДКОЙ: первая цифра (0.53 с) состарилась за
#   час — см. блок про цену выше. Рук в рукаве стало ЧЕТЫРЕ: --range появился
#   после спецификации, в ответ на измерение menufx, а ключ без руки — это ровно
#   то, за что этот прибор и ругает предыдущую заставу.
#   ШИРИНА ОКНА (три коммита, из них один чужой и невиновный) — измерение
#   menufx уже после того, как прибор был написан; из-за него в шапке появилась
#   дыра «унаследованная поломка» и ключ --range, а вывод перестал говорить
#   «самодостаточен» и стал говорить «не ломает того, что тронул».
#   И ОТДЕЛЬНО, ПРО КОНТРОЛЬНУЮ РУКУ. За один вечер инструмент соврал своему
#   автору ТРИЖДЫ (tr, стиравший символы вместо префикса; grep, молчавший на
#   своём же дефекте; цикл, переиспользовавший один путь worktree и выдавший
#   ровный столбец «0.03 с, 2 ошибки» на всех коммитах подряд). Все три раза
#   ловила не догадка, а ФОРМА ОТВЕТА рядом с контрольной рукой. Поэтому рука 2
#   здесь не формальность: без неё две красные доказывают только, что автор
#   умеет ломать компилятор.
# - 27:08:2026 - 22:31:19: ПОЧИНЕНА ЗЕЛЁНАЯ ДЫРА, найденная bigworld-menufx
#   в первый же час жизни прибора: ответ зависел от того, из какого каталога
#   его запустили (подробности — в списке названных дыр выше). Корень для
#   флагов теперь читается из compile_commands.json, а не из cwd; подстановки
#   считаются, и ноль подстановок — это код 2, а не зелёный. Рук стало семь:
#   5 (ответ не зависит от cwd, код руки его) и 5b (сам предохранитель).
#   И РУКА 5b ПОКРАСНЕЛА НА ВЕРНОМ ПОВЕДЕНИИ — искала «не сработала» в строке,
#   где стоит «НЕ сработала». Проверка, чувствительная к регистру чужого
#   текста, проверяет текст, а не предмет; сверяется теперь по «подстановка».
#   Это четвёртое за вечер враньё инструмента своему автору и третье, которое
#   поймала форма ответа рядом (0 ошибок, 1 непроверенный — числа были верны,
#   красным был вердикт).
# - 27:08:2026 - 22:37:18: ЗАКРЫТА ТРЕТЬЯ ЗЕЛЁНАЯ ДЫРА — слияния, — и
#   закрыта ДО появления предмета: слияний в дереве нет, дыру указал
#   bigworld-menufx как будущую, механизм проверен числом (git show на слиянии
#   печатает 0 строк). Руки 6a/6b строят пробное слияние через commit-tree, не
#   заводя ни ветки, ни тега. Утверждений в рукаве стало девять. Заодно все
#   руки печатают числа, по которым судят, — правило выведено из провала руки
#   5b, и оно его же и объясняет.
# - 27:08:2026 - 22:41:13: Одна строка доводом, кода не тронуто. Список
#   названных дыр получил оговорку про самого себя: названная дыра перестаёт
#   быть ловушкой только для того, кто её назвал (формулировка
#   bigworld-menufx, которой он поправил своё же прежнее «названная дыра
#   перестаёт быть ловушкой»). Это и есть довод, по которому две дыры из
#   списка были ПОЧИНЕНЫ, а не оставлены предупреждениями. И там же — чужая
#   проверка чистоты руки 6, которой автор не обещал.
# - 27:08:2026 - 22:55:41: Рукав называет, чьими флагами проверял. Он
#   молчал об этом, пока основной ход печатал, — то есть девять зелёных
#   утверждений могли быть получены на чужой конфигурации, и никто бы не узнал,
#   на чьей. Повод — находка bigworld-menufx про общий скретчпад (его shrink.py
#   подменили чужим файлом с тем же именем); свой прибор от неё не страдает
#   (mkdtemp с уникальным префиксом, коротких имён нет — проверено), но
#   авто-выбор каталога сборки по свежести оказался тем же классом и назван
#   числом: девятнадцать каталогов, авто-выбор даёт чужой. ПОДТВЕРЖДЕНО ЧУЖОЙ
#   РУКОЙ на другой зоне: у menufx авто-выбор дал тот же build_look, а все его
#   вечерние прогоны этого прибора шли с явным --build-dir его зоны, то есть
#   половина замеров, которыми он проверял мою работу, этой дырой не отравлена.
#   Мои собственные замеры цены брались из build_music прямым чтением его
#   compile_commands.json — тем же явным путём.
# - 27:08:2026 - 23:00:37: Правило рукавов дописано второй половиной (вход,
#   а не только выход) и собрано в одну формулировку. Авторство первой половины
#   оставлено за bigworld-menufx вопреки его же предложению его стереть: правило
#   без истории читается как мнение, а не как вывод из двух измеренных провалов.

import argparse
import json
import os
import re
import shlex
import shutil
import subprocess
import sys
import tempfile
import time
from pathlib import Path

# Каталоги ЭТОГО дерева. Всё остальное (_deps, bgfx) от коммита не зависит и в
# замыкание не входит — именно поэтому проверка стоит полсекунды, а не минуты.
OUR_ROOTS = ("engine/", "games/", "tests/", "tools/")
SOURCE_SUFFIXES = (".cpp", ".h", ".hpp", ".cc", ".cxx")
INCLUDE_RE = re.compile(r'^\s*#\s*include\s+"([^"]+)"', re.M)


def run(cmd, **kw):
    return subprocess.run(cmd, capture_output=True, text=True, **kw)


def repo_root() -> Path:
    r = run(["git", "rev-parse", "--show-toplevel"])
    if r.returncode != 0:
        sys.exit("не репозиторий git")
    return Path(r.stdout.strip())


def pick_build_dir(root: Path, given: str | None) -> Path:
    """Каталог сборки, из которого берутся флаги. ЛЮБОЙ годится: нужны только
    -I и -std, а они у всех зон одинаковы. Читается ТОЛЬКО на чтение — чужой
    каталог сборки этим не портится."""
    if given:
        p = (root / given) if not os.path.isabs(given) else Path(given)
        if not (p / "compile_commands.json").is_file():
            sys.exit(f"в {p} нет compile_commands.json")
        return p
    cands = sorted(root.glob("build*/compile_commands.json"),
                   key=lambda p: p.stat().st_mtime, reverse=True)
    if not cands:
        sys.exit("не нашёл ни одного build*/compile_commands.json — "
                 "сконфигурируй любую свою сборку и повтори")
    return cands[0].parent


def cc_repo_root(cc: list[dict], fallback: Path) -> Path:
    """КОРЕНЬ РЕПОЗИТОРИЯ ПО МНЕНИЮ САМОЙ БАЗЫ, а не по текущему каталогу.
    Раньше он брался из `git rev-parse --show-toplevel`, то есть у ЗАПУСКАЮЩЕГО,
    и подстановка -I сравнивалась с ним точно: из чужого git worktree она не
    срабатывала ни разу, включения разрешались против ГЛАВНОГО рабочего каталога
    с чужими незакоммиченными файлами, и прибор отвечал ЗЕЛЁНЫМ на красном
    коммите (дыра найдена bigworld-menufx, см. шапку). База знает правду: пути
    в ней абсолютные и записаны тем, кто конфигурировал сборку."""
    counts: dict[str, int] = {}
    for e in cc:
        f = e.get("file", "")
        for r in OUR_ROOTS:
            marker = "/" + r
            if marker in f:
                counts[f.split(marker)[0]] = counts.get(f.split(marker)[0], 0) + 1
                break
    if not counts:
        print("ВНИМАНИЕ: корень репозитория не выводится из compile_commands.json, "
              f"беру текущий ({fallback})", file=sys.stderr)
        return fallback
    return Path(max(counts.items(), key=lambda kv: kv[1])[0])


def changed_files(sha: str) -> list[str]:
    """Изменённое коммитом. У СЛИЯНИЯ — против ПЕРВОГО РОДИТЕЛЯ, и это не
    придирка: `git show --name-only` на слиянии печатает ПУСТО, поэтому прибор
    сказал бы «проверять нечего» и вернул ноль — молча-зелёный ответ на
    коммите, который может не собираться. Ровно тот класс, ради которого он
    написан, и третий его случай за вечер (дыра указана bigworld-menufx как
    БУДУЩАЯ: слияний в этом дереве пока нет, `git log --merges` пуст). Чинится
    заранее, потому что предмет появится не когда о нём вспомнят, а когда
    кто-нибудь заведёт ветвление."""
    parents = run(["git", "rev-list", "--parents", "-n", "1", sha])
    if parents.returncode != 0:
        sys.exit(f"не читается коммит {sha}")
    n_parents = len(parents.stdout.split()) - 1
    if n_parents > 1:
        print(f"  {sha}: СЛИЯНИЕ ({n_parents} родителя) — читаю против первого")
        r = run(["git", "diff", "--name-only", sha + "^1", sha])
    else:
        r = run(["git", "show", "--name-only", "--pretty=format:", sha])
    if r.returncode != 0:
        sys.exit(f"не читается коммит {sha}")
    return [ln.strip() for ln in r.stdout.splitlines() if ln.strip()]


def changed_in_range(rng: str) -> list[str]:
    """ОБЪЕДИНЕНИЕ изменённого по цепочке. Отвечает на второй вопрос — «здорово
    ли дерево», — которого не видит покоммитная проверка: коммит, не тронувший
    ни одного спорного файла, наследует сломанный HEAD и несобираем вместе с
    ним."""
    r = run(["git", "diff", "--name-only", rng])
    if r.returncode != 0:
        sys.exit(f"не читается диапазон {rng}")
    return [ln.strip() for ln in r.stdout.splitlines() if ln.strip()]


def tree_files(tree: Path) -> list[str]:
    out = []
    for rootdir in OUR_ROOTS:
        base = tree / rootdir
        if not base.is_dir():
            continue
        for p in base.rglob("*"):
            if p.is_file() and p.suffix in SOURCE_SUFFIXES:
                out.append(str(p.relative_to(tree)))
    return out


def affected_tus(tree: Path, changed: list[str]) -> list[str]:
    """TU, до которых коммит дотянулся: изменённые .cpp плюс все .cpp, чьё
    ЗАМЫКАНИЕ ВКЛЮЧЕНИЙ содержит изменённый заголовок. Замыкание строится по
    нашему дереву текстом — третьи стороны наших заголовков не включают, и это
    то самое допущение, которое делает проверку дешёвой."""
    ours = [c for c in changed if c.startswith(OUR_ROOTS)
            and c.endswith(SOURCE_SUFFIXES)]
    if not ours:
        return []
    files = tree_files(tree)
    includes: dict[str, set[str]] = {}
    for f in files:
        try:
            text = (tree / f).read_text(encoding="utf-8", errors="replace")
        except OSError:
            continue
        includes[f] = {m for m in INCLUDE_RE.findall(text) if m.startswith(OUR_ROOTS)}

    tainted = set(ours)
    grew = True
    while grew:  # пока множество растёт: заголовок тянет заголовок тянет .cpp
        grew = False
        for f, incs in includes.items():
            if f not in tainted and (incs & tainted):
                tainted.add(f)
                grew = True
    return sorted(f for f in tainted if f.endswith((".cpp", ".cc", ".cxx")))


def syntax_command(cc: list[dict], root: Path, tree: Path,
                   rel: str) -> tuple[list[str], int] | None:
    """Готовая команда компиляции, переделанная в синтаксическую проверку
    ПРОТИВ ДЕРЕВА КОММИТА. Переставляется ровно один флаг — -I корня репозитория;
    всё остальное (третьи стороны, генерённые каталоги) от коммита не зависит."""
    entry = next((e for e in cc if e["file"].endswith("/" + rel)), None)
    if entry is None:
        return None
    args = shlex.split(entry.get("command") or " ".join(entry["arguments"]))
    out, skip, hits = [], False, 0
    for a in args:
        if skip:
            skip = False
            continue
        if a in ("-o", "-MF", "-MT", "-MQ"):
            skip = True
            continue
        if a == "-c" or a.startswith("@") or a.startswith("-fmodule") \
           or a.startswith("-fdeps") or a in ("-MD", "-MMD"):
            continue          # объектник, карта модулей и файлы зависимостей не нужны
        if a.endswith("/" + rel):
            continue          # исходник подставим свой, из дерева коммита
        if a == "-I" + str(root):
            a = "-I" + str(tree)
            hits += 1
        out.append(a)
    out += ["-fsyntax-only", str(tree / rel)]
    # ЧИСЛО ПОДСТАНОВОК ВОЗВРАЩАЕТСЯ НАРУЖУ, и это половина починки той самой
    # зелёной дыры. Ноль подстановок значит, что включения разрешатся не против
    # дерева коммита, а против чьего-то рабочего каталога, — то есть прибор
    # проверит НЕ ТО. Такой ответ не имеет права быть зелёным, и вызывающий
    # кладёт этот TU в «проверить не удалось» (код 2). Один раз уже стоило
    # зелёного отчёта на заведомо красном коммите.
    return out, hits


def check_tree(tree: Path, root: Path, cc: list[dict], tus: list[str],
               verbose: bool) -> tuple[list[str], list[tuple[str, str]], float]:
    """Возвращает (ошибки, непроверенные TU с причиной, секунды)."""
    errors, unchecked = [], []
    began = time.monotonic()
    for rel in tus:
        made = syntax_command(cc, root, tree, rel)
        if made is None:
            unchecked.append((rel, "нет в compile_commands.json"))
            continue
        cmd, hits = made
        if hits == 0:
            unchecked.append((rel, "подстановка -I на дерево коммита НЕ сработала "
                                   "— проверялся бы чужой рабочий каталог"))
            continue
        r = run(cmd)
        if r.returncode != 0:
            first = [ln for ln in r.stderr.splitlines() if ": error:" in ln
                     or ": fatal error:" in ln]
            errors.extend(first[:4] or [f"{rel}: компилятор вернул {r.returncode}"])
        elif verbose:
            print(f"  ok  {rel}")
    return errors, unchecked, time.monotonic() - began


class Worktree:
    """Дерево КОММИТА, развёрнутое рядом. git stash здесь запрещён: каталог
    общий, и спрятать чужие незакоммиченные файлы значит сломать чужую работу."""

    def __init__(self, root: Path, sha: str):
        self.root, self.sha = root, sha
        self.path = Path(tempfile.mkdtemp(prefix="dfn-commit-check-"))

    def __enter__(self) -> Path:
        shutil.rmtree(self.path, ignore_errors=True)
        r = run(["git", "-C", str(self.root), "worktree", "add", "--detach",
                 str(self.path), self.sha])
        if r.returncode != 0:
            sys.exit("не развернулось дерево коммита:\n" + r.stderr)
        return self.path

    def __exit__(self, *exc):
        run(["git", "-C", str(self.root), "worktree", "remove", "--force",
             str(self.path)])
        shutil.rmtree(self.path, ignore_errors=True)
        run(["git", "-C", str(self.root), "worktree", "prune"])


def check_commit(root: Path, build: Path, sha: str, verbose: bool,
                 rng: str | None = None) -> int:
    cc = json.loads((build / "compile_commands.json").read_text())
    flag_root = cc_repo_root(cc, root)   # корень ФЛАГОВ — из базы, не из cwd
    if rng:
        changed = changed_in_range(rng)
        sha = rng.split("..")[-1] or "HEAD"   # дерево КОНЦА цепочки
    else:
        changed = changed_files(sha)
    with Worktree(root, sha) as tree:
        tus = affected_tus(tree, changed)
        if not tus:
            print(f"{sha}: ни одного нашего TU не затронуто — проверять нечего")
            return 0
        print(f"{sha}: изменено файлов {len(changed)}, "
              f"затронуто TU {len(tus)}; флаги из {build.name}")
        errors, unchecked, secs = check_tree(tree, flag_root, cc, tus, verbose)
        # Пути временного дерева в отчёте — шум: читателю нужен файл и строка,
        # а не то, в какой каталог его сегодня развернули.
        errors = [e.replace(str(tree) + "/", "") for e in errors]

    if errors:
        print(f"\nКОММИТ НЕ СОБЕРЁТСЯ У ТОГО, КТО ЕГО НЕ ПИСАЛ ({secs:.1f} с):")
        for e in errors[:20]:
            print("  " + e)
        return 1
    if unchecked:
        # ГРОМКО, А НЕ МОЛЧА: это ровно та половина, из-за которой предыдущая
        # застава оказалась слепа к своему же дефекту.
        print(f"\nПРОВЕРИТЬ НЕ УДАЛОСЬ: {len(unchecked)} TU:")
        for rel, why in unchecked[:10]:
            print(f"  {rel} — {why}")
        return 2
    # ФОРМУЛИРОВКА ВЫВЕРЕНА: не «дерево собирается», а «не сломал того, что
    # тронул». Второе — правда, первое — то полуутверждение, из-за которого
    # 847f020 и был объявлен самодостаточным своим же автором.
    print(f"не ломает того, что тронул: {len(tus)} TU чисты за {secs:.1f} с "
          f"({secs / max(1, len(tus)):.2f} с на TU)")
    if not rng:
        print("  (это НЕ значит «дерево собирается» — унаследованную поломку "
              "видит только --range, см. шапку)")
    return 0


# --- РУКАВ ------------------------------------------------------------------
# Три руки, и средняя из них — КОНТРОЛЬ (правило 30). Без неё две красные
# доказывали бы только, что автор умеет ломать компилятор.
SELF_TEST_BROKEN_SHA = "ee031b0"   # тот самый несобираемый коммит, случай 1
# НЕВИНОВНЫЙ СОСЕД: волна мебели, вставшая внутрь чужого окна. Своих файлов не
# ломала, ни одного спорного не трогала — и несобираема вместе с деревом.
SELF_TEST_INHERITED_SHA = "9c8cafb"


def self_test(root: Path, build: Path) -> int:
    cc = json.loads((build / "compile_commands.json").read_text())
    flag_root = cc_repo_root(cc, root)
    # ЧЬИМИ ФЛАГАМИ ПРОВЕРЯЕМ — ВСЛУХ, И В РУКАВЕ ТОЖЕ. Основной ход это печатал,
    # рукав молчал: то есть девять зелёных утверждений могли быть получены на
    # чужой конфигурации, и никто бы не узнал, на чьей.
    print(f"рукав: флаги из {build.name}, корень {flag_root}")
    ok = True

    def say(name, passed, detail=""):
        nonlocal ok
        ok = ok and passed
        print(f"[{'ПРОШЛА' if passed else 'КРАСНАЯ'}] {name}{detail}")

    # РУКА 1: коммит, разрезанный по файлам между агентами. Красный ПО ИМЕНИ —
    # заголовок в дереве ЕСТЬ, но он старый и нужного перечисления в нём нет.
    if run(["git", "-C", str(root), "cat-file", "-e", SELF_TEST_BROKEN_SHA + "^{commit}"]).returncode:
        say("рука 1 (разрез волны, красный по имени)", False,
            f" — коммита {SELF_TEST_BROKEN_SHA} нет в этом клоне")
    else:
        with Worktree(root, SELF_TEST_BROKEN_SHA) as tree:
            errs, _, _ = check_tree(tree, flag_root, cc, ["engine/app/sources/Menu.cpp"], False)
        hit = any("VideoRow" in e or "SettingsRow" in e for e in errs)
        say("рука 1 (разрез волны, красный по ИМЕНИ)", hit,
            f" — ошибок {len(errs)}, имя найдено" if hit
            else f" — ждал имя, получил: {errs[:1]}")

    # РУКА 2, КОНТРОЛЬНАЯ: то же самое на HEAD, где дерево заведомо собирается.
    with Worktree(root, "HEAD") as tree:
        errs, unchecked, secs = check_tree(tree, flag_root, cc,
                                           ["engine/app/sources/Menu.cpp"], False)
        say("рука 2 (КОНТРОЛЬ: HEAD зелён)", not errs and not unchecked,
            f" — {secs:.2f} с на TU" if not errs else f" — {errs[:1]}")

        # РУКА 3: зависимость от файла, которого в дереве коммита НЕТ. Так
        # выглядел бы промах с DoorAim.h, если бы его закоммитили: App.h его
        # включает, а сам он лежал незакоммиченным. Восстановлен МЕХАНИЗМОМ, а
        # не историей — заголовок выбирается из живого замыкания и удаляется,
        # поэтому рука не сгниёт, когда DoorAim.h однажды переименуют.
        victim = None
        for inc in INCLUDE_RE.findall((tree / "engine/app/sources/App.h").read_text()):
            if inc.startswith(OUR_ROOTS) and (tree / inc).is_file():
                victim = inc
                break
        if victim is None:
            say("рука 3 (красный по ФАЙЛУ)", False, " — не нашёл, что удалить")
        else:
            (tree / victim).unlink()
            errs, _, _ = check_tree(tree, flag_root, cc, ["engine/app/sources/App.cpp"], False)
            hit = any("file not found" in e or "не найден" in e for e in errs)
            say("рука 3 (незакоммиченная зависимость, красный по ФАЙЛУ)", hit,
                f" — убран {victim}, ошибок {len(errs)}" if hit
                else f" — ждал 'file not found', получил: {errs[:1]}")

    # РУКА 4: УНАСЛЕДОВАННАЯ ПОЛОМКА, и она про то, чего первые три не видят.
    # 9c8cafb своих файлов не ломал; покоммитная проверка на нём молчит и права
    # — вопрос «сломал ли я» и вопрос «здорово ли дерево» разные. Рука держит
    # ОБА ответа сразу: --sha зелён, --range на той же точке красен. Без второй
    # половины это была бы проверка, доказывающая, что она умеет молчать.
    if run(["git", "-C", str(root), "cat-file", "-e",
            SELF_TEST_INHERITED_SHA + "^{commit}"]).returncode:
        say("рука 4 (унаследованная поломка)", False,
            f" — коммита {SELF_TEST_INHERITED_SHA} нет в этом клоне")
    else:
        with Worktree(root, SELF_TEST_INHERITED_SHA) as tree:
            own = affected_tus(tree, changed_files(SELF_TEST_INHERITED_SHA))
            e_own, _, _ = check_tree(tree, flag_root, cc, own, False)
            rng_files = changed_in_range(f"{SELF_TEST_BROKEN_SHA}~1..{SELF_TEST_INHERITED_SHA}")
            e_rng, _, _ = check_tree(tree, flag_root, cc, affected_tus(tree, rng_files), False)
        say("рука 4a (свои файлы у невиновного соседа чисты)", not e_own,
            "" if not e_own else f" — {e_own[:1]}")
        say("рука 4b (--range видит унаследованную поломку)", bool(e_rng),
            "" if e_rng else " — ждал красный, получил зелёный")

    # РУКА 5: ОТВЕТ НЕ ЗАВИСИТ ОТ ТОГО, ОТКУДА ПРИБОР ЗАПУЩЕН. Код руки —
    # bigworld-menufx, он же нашёл дыру. Она держит не путь, а ОТВЕТ: тот же
    # sha, тот же build-dir, другой cwd — ответ обязан совпасть с рукой 1.
    # До починки прибор, запущенный из чужого git worktree, отвечал ЗЕЛЁНЫМ на
    # заведомо красном коммите: подстановка -I сравнивалась с корнем ПО CWD, из
    # чужого дерева не срабатывала ни разу, и включения читались из главного
    # рабочего каталога — то есть из чужих незакоммиченных файлов.
    with Worktree(root, "HEAD") as alien:
        r = run([sys.executable, str(root / "tools/check_commit_builds.py"),
                 "--sha", SELF_TEST_BROKEN_SHA, "--build-dir", str(build)],
                cwd=str(alien))
        say("рука 5 (ответ не зависит от cwd)", r.returncode == 1,
            "" if r.returncode == 1 else f" — из чужого дерева вернул {r.returncode}")

        # РУКА 5b: САМ ПРЕДОХРАНИТЕЛЬ. Даже если корень однажды снова разойдётся
        # (сменится форма флагов, придёт другая система сборки), ноль подстановок
        # обязан давать «проверить не удалось», а не зелёный. Проверяется прямо:
        # заведомо неверный корень -> TU уходит в непроверенные, ошибок ноль.
        errs, unchecked, _ = check_tree(alien, Path("/nowhere-at-all"), cc,
                                        ["engine/app/sources/Menu.cpp"], False)
        # Сверяется по «подстановка», а не по всей фразе: первая редакция руки
        # искала «не сработала» в строке, где стоит «НЕ сработала», и покраснела
        # на верном поведении (0 ошибок, 1 непроверенный). Рука, чувствительная
        # к регистру чужого текста, проверяет текст, а не предмет.
        guarded = not errs and len(unchecked) == 1 and "подстановка" in unchecked[0][1]
        # ЧИСЛА ПЕЧАТАЮТСЯ И НА ЗЕЛЁНОЙ РУКЕ (наблюдение bigworld-menufx после
        # того, как эта самая рука покраснела на верном поведении): приговор,
        # рядом с которым не стоят числа, нечем перепроверить, а ошибка в
        # ПРИГОВОРЕ при верных числах не выдаёт себя нелепостью.
        say("рука 5b (ноль подстановок -> НЕ зелёный, а «не удалось»)", guarded,
            f" — ошибок {len(errs)}, непроверенных {len(unchecked)}")

    # РУКА 6: СЛИЯНИЕ. Дыра указана bigworld-menufx как БУДУЩАЯ — слияний в
    # этом дереве нет ни одного, — и потому рука строит своё: `git commit-tree`
    # лепит коммит С ДВУМЯ РОДИТЕЛЯМИ и НИ ОДНОЙ ССЫЛКОЙ. Объект висячий, сам
    # уберётся сборкой мусора, ни ветки, ни тега в общем репозитории не
    # появляется — чужую историю рука не трогает.
    #
    # Она держит ДВА утверждения, и первое объясняет, зачем второе: механизм
    # (`git show --name-only` на слиянии печатает ПУСТО — вот откуда взялся бы
    # молча-зелёный ответ) и исход (прибор всё равно красен на сломанном
    # дереве, потому что читает против первого родителя).
    merge = run(["git", "-C", str(root), "commit-tree",
                 SELF_TEST_BROKEN_SHA + "^{tree}",
                 "-p", "aae1bc9", "-p", SELF_TEST_BROKEN_SHA,
                 "-m", "рукав: слияние, ни на что не ссылается"])
    if merge.returncode != 0:
        say("рука 6 (слияние)", False, " — не собралось пробное слияние")
    else:
        msha = merge.stdout.strip()
        naive = run(["git", "-C", str(root), "show", "--name-only",
                     "--pretty=format:", msha]).stdout.strip()
        say("рука 6a (механизм: git show на слиянии молчит)", naive == "",
            f" — строк {len(naive.splitlines())}")
        files = changed_files(msha)
        with Worktree(root, msha) as tree:
            errs, _, _ = check_tree(tree, flag_root, cc, affected_tus(tree, files), False)
        say("рука 6b (слияние со сломанным деревом — КРАСНОЕ)", bool(errs),
            f" — файлов {len(files)}, ошибок {len(errs)}")

    print("\nрукав пройден" if ok else "\nРУКАВ КРАСНЫЙ")
    return 0 if ok else 1


def main() -> int:
    ap = argparse.ArgumentParser(description=__doc__)
    ap.add_argument("--sha", default="HEAD")
    ap.add_argument("--range", dest="rng", default=None,
                    help="A..B — объединение изменённого по цепочке против дерева B")
    ap.add_argument("--build-dir", default=None)
    ap.add_argument("--self-test", action="store_true")
    ap.add_argument("-v", "--verbose", action="store_true")
    a = ap.parse_args()
    root = repo_root()
    build = pick_build_dir(root, a.build_dir)
    if a.self_test:
        return self_test(root, build)
    return check_commit(root, build, a.sha, a.verbose, a.rng)


if __name__ == "__main__":
    sys.exit(main())
