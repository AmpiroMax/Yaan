#!/usr/bin/env python3
# Module: tools
# File: tools/gen_whiterun.py
#
# Responsibility:
# - ОБЁРТКА РАДИ СТАРЫХ КОМАНД И ДОМ ДЛЯ ИСТОРИИ. Код генератора переехал
#   (волна И1 эпохи «12 городов») в два файла:
#       tools/gen_city.py        — МЕХАНИЗМЫ, общие на все двенадцать городов;
#       tools/cities/whiterun.py — ПАСПОРТ Вайтрана, только данные.
#   Собирать город надо так:  python3 tools/gen_city.py whiterun
#   Этот файл делает ровно то же самое и существует по двум причинам.
#   ПЕРВАЯ: команда `python3 tools/gen_whiterun.py` записана в двух десятках
#   мест — в записанном полном прогоне, в отчётах критиков, в docs/HOUSES.md и
#   в памяти всех, кто ходил вокруг этого города неделю. Ломать её ради
#   переименования — плата без покупки.
#   ВТОРАЯ, и она важнее: ЗДЕСЬ ЖИВЁТ UPD ЭТОГО КОДА. Правило 17 говорит, что
#   записи UPD не удаляются никогда, и перенос трёхсот записей в новый файл был
#   бы их удалением из этого — то есть ровно тем, что правило запрещает.
#   Поэтому история осталась там, где её оставила каждая волна, а gen_city.py
#   начинает свою с переезда и ссылается сюда. Читать про то, ПОЧЕМУ в
#   генераторе стоит то или иное число, надо ниже.
"""Обёртка: `python3 tools/gen_whiterun.py` == `python3 tools/gen_city.py whiterun`."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

import gen_city

if __name__ == "__main__":
    # ЧЕРЕЗ cli(), А НЕ main(): ЭТО ПОЛОВИНА ИНЦИДЕНТА 28.08. Обёртка звала
    # main() напрямую, а код возврата жил в cli() — и прогон, потерявший
    # натуральную землю, отдавал НОЛЬ при выпуске без единой строки дельт.
    # «Обёртка» обязана быть обёрткой целиком, вместе с разбором доводов
    # (--check) и кодом возврата, иначе это вторая, урезанная дверь.
    sys.exit(gen_city.cli([sys.argv[0], "whiterun", *sys.argv[1:]]))
