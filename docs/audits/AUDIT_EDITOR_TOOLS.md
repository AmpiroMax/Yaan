<!--
File: docs/audits/AUDIT_EDITOR_TOOLS.md
-->

# Аудит кода инструментов редактора

Заказан пользователем 18.08.2026 дословно: «у меня серьезная настроенность в том,
что текущий код инструментов стал спагетти кодом. У тебя нет отдельного класса
под инструменты, нет четких интерфейсов взаимодействия и прочее. Если бы они
были, нельзя бы было поймать ошибки, что я сразу два инструмента в руке держу и
прочее.»

**Претензия верна целиком.** Ниже — числа, потом причина, потом что с этим
делать. Ничего не сглажено: аудит, который заканчивается словами «в целом
неплохо», не стоит времени на чтение.

## Числа

| что | сколько | норма |
|---|---|---|
| `engine/app/sources/App.cpp` | **6358 строк** | правило 21: 2500 (поднято 18.08) |
| одна функция `App::run()` | **1926 строк** | — |
| мест, спрашивающих «какой сейчас инструмент» | **7** | 1 |
| меток `case EditorTool::` | **12** в 2 разных `switch` | — |
| файлов, знающих перечисление инструментов | 3 (`App.cpp`, `EditorUi.cpp`, `EditorUi.h`) | — |
| классов, представляющих инструмент | **0** | 1 на инструмент |

## Причина, а не симптом

`EditorTool` — это **ярлык**, а не объект. Он лежит в `EditorUi`, а всё
поведение инструмента разбросано по местам, которые обязаны про него помнить:

- щелчок левой кнопкой — в `run()`, строка ~4885;
- проход «что под прицелом» и призрак — в `update_build_tool()`, ~3718;
- мазок кистью — в `update_editor_tools()`, ~4262;
- подпись на экране — снова в `run()`, ~5784;
- панель настроек — в `wire_editor_panels()`, ~4093;
- дверь выбора режима — снова в `run()`, ~4741.

Шесть мест, каждое со своим `if` или `switch`. **Ни одно из них не знает про
остальные пять.** Добавляя инструмент, надо вспомнить все шесть; забыв одно,
получаешь инструмент, который наполовину работает — и именно это происходило
весь день:

- **«держу сразу два инструмента»** — щелчок взводился условием
  `режим постановки ИЛИ открыт список объектов`, а кисть висела на той же
  кнопке отдельно. Два владельца одной кнопки, и никто не спрашивал другого.
- **«деталь остаётся в руках»** — загрузка призрака стояла в конце функции с
  тремя ранними выходами, и вне режима редактора не вызывалась вовсе.
- **«покраска и высота странно взаимодействуют»** — это ОДИН объект
  `TerrainBrush` с полем `mode`, показанный как ДВА инструмента на полосе.
  Полоса и переключатель в панели задают одно состояние из двух мест.

- **«что за порода выбирается, когда я открываю меню кисти? она ни на что не
  влияет»** — и она действительно ни на что не влияет. Список пород живёт в
  панели КИСТИ, а посадка вызывается ровно из одного места: `plant_dab_here` на
  Shift+ЛКМ **внутри обработчика инструмента ПОСТАНОВКИ** (`App.cpp:4887`).
  То есть галочки стоят у одного инструмента, а читает их другой — и только
  если человек догадается переключиться и держать Shift. «Посадки» нет среди
  пяти инструментов вовсе: её органы управления у кисти, её действие у
  постройки, а хозяина нет ни у чего.

Все четыре — не разные ошибки. Это один и тот же дефект: **у инструмента нет
единственного владельца своего поведения**, поэтому исключительность
инструментов не следует из устройства, а держится на договорённости, которую
надо соблюсти в шести местах.

## Что предлагается

Интерфейс `IEditorTool` — по классу на инструмент, и один список активных.
Контракт минимальный, но ПОЛНЫЙ: если инструмент не отвечает на вопрос, значит
он этого не делает — а не «где-то ещё в App это дописано».

```
class IEditorTool {
    // ЧТО ЭТО. Имя для полосы, значок, подсказка внизу кадра.
    virtual ToolIdentity identity() const = 0;

    // ЧТО ОН ДЕЛАЕТ ПО ЩЕЛЧКУ И ПО ПРОТЯЖКЕ. Один владелец кнопки.
    virtual void on_press(const ToolAim& aim, ToolWorld& world) = 0;
    virtual void on_drag(const ToolAim& aim, float dt, ToolWorld& world) = 0;
    virtual void on_release(ToolWorld& world) = 0;

    // ЧТО ОН РИСУЕТ В МИРЕ, пока выбран: призрак детали, кольца кисти.
    virtual void preview(const ToolAim& aim, ToolPreview& out) = 0;

    // ЕГО СОБСТВЕННЫЕ НАСТРОЙКИ — и ничьи больше.
    virtual void draw_settings() = 0;

    // ЕГО ДАЛЬНОБОЙНОСТЬ, ограниченная общим потолком.
    virtual float max_reach_m() const = 0;

    // ВЫБРАЛИ / ОТПУСТИЛИ. Здесь инструмент обязан всё за собой убрать —
    // именно это место отсутствовало, когда деталь оставалась в руках.
    virtual void on_selected(ToolWorld& world) = 0;
    virtual void on_deselected(ToolWorld& world) = 0;
};
```

Три свойства, которые из этого следуют **по построению**, а не по внимательности:

1. **Два инструмента одновременно невозможны.** Активный указатель один;
   щелчок уходит ему и только ему. Нет места, где второй мог бы подписаться.
2. **Инструмент убирает за собой.** `on_deselected` вызывается ровно один раз
   и ровно в одном месте — там, где меняется активный указатель.
3. **Настройки не разъезжаются с выбором.** Панель рисует активный инструмент
   сам; переключателя, задающего то же состояние из второго места, нет.

## Что это НЕ чинит

Аудит обязан говорить и это. Перенос в интерфейс не исправит:

- `App::run()` в 1926 строк — она уменьшится, но останется слишком большой;
- хранение земли сеткой 2 м (отдельный заказ пользователя, зона world);
- то, что `App.cpp` в восемь раз длиннее собственного правила 21.

Разбор `run()` — отдельная работа, и делать её одним заходом с интерфейсом
инструментов нельзя: это две правки в одном доказательстве.

# Что сделано по этому аудиту (18.08.2026, 12:20)

Аудит исполнен целиком; ниже — числа и то, чего в нём НЕ сделано.

## Числа после правки

| что | было | стало |
|---|---|---|
| мест, спрашивающих «какой сейчас инструмент» | 7 | **0** (спрашивают ответ: `preview()`, `status()`, `update()`, `active_reach_m()`) |
| мест, называющих КОНКРЕТНЫЙ инструмент по имени | — | 3, и все три — двери и старые клавиши (`index_of("height")`, `index_of("place")` ×2) |
| меток `case EditorTool::` | 12 в 2 switch | **0**; перечисление `EditorTool` удалено |
| классов, представляющих инструмент | 0 | **6**: `IEditorTool` + пять инструментов |
| выражений вида «взведён ли щелчок» вне инструмента | 2 (`build_click_is_armed`, `build_hand_wants_aim`) | 0 — удалены вместе с ярлыком |
| проверок на ящик инструментов | 0 | 14 случаев / 124 утверждения (`app_editor_toolbox`) |

## Что именно стало невозможным по устройству

- **Два инструмента в руке.** Один `active_` в `EditorToolbox`; `update()`
  вызывает `active()` и НИ РАЗУ не обходит список. Проверено счётчиком нажатий
  у каждого инструмента: сумма после одного щелчка равна 1 (контрфакт «раздать
  всем» — 3 упавших случая из 14).
- **Уборка за инструментом.** `on_deselected` зовётся из `select()` и больше
  ниоткуда; `select()` — единственный, кто пишет `active_`.
- **Настройки ≠ выбор.** `click_settings()` физически не пишет `active_`
  (контрфакт «пусть пишет» — 3 упавших случая).

## Чего этот заход НЕ починил

- `App.cpp` — 6409 строк (было 6358; ушли два switch, пришли крючки мира и
  штабелирование). Правило 21 по-прежнему нарушено, разбор `run()` — отдельная
  работа, и делать её одним заходом с интерфейсом инструментов нельзя.
- `EditorUi.cpp` — 1173 строки (было 1238). Тоже выше 800.
- ШТАБЕЛИРОВАНИЕ БЕЗ ПОСТРОЙКИ. Инструмент теперь предлагает верх той
  расстановки, в которую упёрся прицел, но судья принимает опору на другую
  деталь ТОЛЬКО внутри одной группы (`Scene.cpp`: `if (!p.group.empty())` и
  `placements[j].group != p.group`). Значит: строишь с группой — работает;
  ставишь одиночки — судья ответит «hovers above the ground». Это правило
  ЗОНЫ world и контракт судьи, я его не менял. Обе половины зафиксированы
  рукавом `app_build_tool` («без группы судья штабель НЕ принимает — и это его
  правило, не наше»).

---

# Второй аудит: система классов через сутки жизни (19.08.2026)

Заказ дословно: «проведи детальный аудит всей системы инструментов, оцени
связность, простоту правки инструментов, добавления инструментов». Числа сняты
с дерева на эту минуту, не по памяти.

## Числа

| что | сколько | сутки назад |
|---|---|---|
| классов инструмента (`: public IEditorTool`) | **9** (высота, поверхность, выбор, постройка, посадка, тропа, якорь, прямая, полотно) | 6 |
| виртуальных методов в `IEditorTool` | **19** | 10 |
| крючков в `ToolWorld` (`std::function`) | 7 | 7 |
| мест `App`, зовущих `toolbox()` | 29 | — |
| мест `App`, зовущих `house_` напрямую | 33 | — |
| `EditorToolHouse.cpp` | 1710 строк | — |
| `EditorToolHouse.h` | 809 строк | — |
| `App.cpp` | **5009 строк** | 6358 |

## Что ХОРОШО и почему это не комплимент себе

Проверяемые свойства, каждое с прибором:

1. **Один инструмент в руке** — свойство ящика, рукав app_editor_toolbox.
2. **Показ пересобирается целиком** — три кадра дают один размер (поймало
   утечку призрака в тот же день, что она родилась).
3. **Дальность судит начало, а не ведение** — stroke_needs_reach, и обрыв
   протаскивания ушёл вместе с жалобой.
4. **Просьбы руки идут через ответы, а не ярлыки** (wants_wheel, has_draft,
   aims_without_ground): App не знает ИМЁН инструментов нигде, кроме
   регистрации. Добавление инструмента не трогает run().

## Связность: где она настоящая, а где протекает

**Настоящая.** Модель (HouseGraph/HouseMesh, world) не знает про редактор;
инструменты не знают про bgfx; App не знает про ImGui постройки. Версия
геометрии живёт в графе — три потребителя (тело, коллайдер, проволока) читают
одно число, и рассинхрон невозможен ПО УСТРОЙСТВУ, что и было заказано.

**Протекает в трёх местах, и все три — App:**

1. **33 прямых обращения App к house_.** App знает про сетку, ось, метку
   стрелок, выбранное. Каждая следующая механика постройки добавляет сюда
   2-4 обращения. Это не спагетти ещё, но это его рассада: у App появляется
   ЗНАНИЕ О ПРЕДМЕТНОЙ ОБЛАСТИ постройки, которое должно жить у сессии.
2. **Клавиши постройки размазаны**: V/X/Delete/стрелки/Enter обрабатываются в
   AppInput четырьмя разными обработчиками, каждый сам спрашивает модификаторы.
   Таблица действий это скрывает наполовину: строки есть, но УСЛОВИЯ (cmd?
   shift? есть ли черновик?) живут в коде обработчиков.
3. **Двери постройки в App.cpp** (DFN_HOUSE_*) — четыре штуки, каждая со своим
   static-флагом. Работают, но это четвёртая копия одного паттерна.

## Цена правки инструмента: НИЗКАЯ

Поменять поведение одного инструмента = править один файл (его .cpp), и рукав
на него уже есть. Проверено практикой: за сутки в инструменты постройки внесено
~15 правок, ни одна не тронула чужой инструмент. Это и есть ответ на «простоту
правки»: да.

## Цена ДОБАВЛЕНИЯ инструмента: СРЕДНЯЯ, 6 шагов

1. Класс в своём .cpp (интерфейс — 5 обязательных методов из 19).
2. Значок в ToolIcon + рисовалка в EditorToolIcons.cpp.
3. Регистрация в App::wire_editor_panels (одна на инструмент).
4. Строка Tool<N> в таблице клавиш (если нужна цифра).
5. Подписи в ru.txt.
6. Рукав.

Шаги 2 и 4 — механические, но их НЕЛЬЗЯ забыть молча: пропущенный значок валит
рукав попарной различимости, пропущенная строка — рукав «каждая строка
рисуется». Это хорошо. Плохо то, что шаг 3 просит трогать App.cpp — файл, и без
того вдвое больше лимита.

## Пять долгов, названных по именам

1. **App.cpp: 5009 строк при лимите 2500.** Кандидаты на выезд: editor_aim +
   сетка + стрелки (в EditorAim.cpp), двери (в AppDoors механизм уже есть),
   upload_house_mesh + seed_demo_house (в AppHouse.cpp). Оценка: −1200 строк.
2. **IEditorTool растолстел до 19 виртуалов.** Ещё 5-6 — и это уже «интерфейс
   всего». Лекарство известно по ToolWorld: сгруппировать редкие ответы
   (wants_wheel/on_wheel, has_draft/on_confirm/on_cancel) в опциональные
   расширения. Не срочно: 19 читается на одном экране.
3. **EditorToolHouse.h 809 строк держит ЧЕТЫРЕ класса.** Сессию стоит отделить
   от трёх инструментов (HouseSession.h): её читает и App, и SelectTool, а
   инструменты — только свои .cpp.
4. **Панель постройки дублирует поля свойств элемента** со своими ползунками
   инструмента (толщина в панели полотна И толщина выбранного элемента). Сейчас
   это два вопроса («какой будет» / «какой есть»), но на экране они выглядят
   одинаково — конфликт ID из ImGui был симптомом ровно этого. Стоит развести
   заголовками или убрать преднастройки в пользу «поставил — поправил».
5. **Правки, не доехавшие до бинарника, дважды за вечер** — процессный долг:
   команда с ними падала на разборе оболочки, а я читал вывод следующего шага.
   Теперь после каждой пачки правок обязателен grep по дереву на маркер правки
   ДО сборки; оба случая пойманы пользователем, не мной, и это самое дорогое в
   этом списке.

## Вердикт

Система классов держит нагрузку: девять инструментов, общая модель, три
потребителя геометрии без рассинхрона, правка локальна, добавление — шесть
шагов с громкими провалами. Спагетти образца 18.08 не вернулось. Главный риск
роста — не инструменты, а App.cpp как место, куда стекается «мелочь по чуть-
чуть»; долги 1-3 — это его профилактика, и делать их стоит ДО города, а не
после.
