<!--
File: docs/audits/PLAN_APP_DECOMPOSITION.md
-->

# Разбор App.cpp: план

Заказ пользователя 18.08: отрефакторить App. Основание — docs/audits/AUDIT_EDITOR_TOOLS.md:
**6358 строк** в файле при норме 2500 (правило 21), из них **1926 строк** в одной
функции `App::run()` (строки 4397–6323 по HEAD).

## Почему это не косметика

App.cpp — композиционный корень: он владеет окном, а значит **не тестируется
ни одним рукавом**. Всё, что попадает внутрь `run()`, автоматически выходит
из-под приборов. Каждый отказ, который сегодня ловил пользователь, а не
проверка, жил именно там: щелчок, взводимый двумя условиями; призрак за тремя
ранними выходами; список деталей в двух экземплярах; подсказка поперёк экрана.

Правило простое и оно же критерий приёмки: **решение, вынесенное из `run()`,
обязано получить проверку**. Вынос без рукава — перекладывание, а не разбор.

## Порядок, и почему именно такой

Разбор идёт СЛОЯМИ СНИЗУ ВВЕРХ, каждый слой — отдельная сдача с зелёным
прогоном. Слоями, а не «по файлу за раз», потому что `run()` — это один
кадровый цикл, и разрезать его поперёк значит получить два куска, которые
нельзя собрать обратно.

**Слой 0 — ждём.** Рефактор инструментов (IEditorTool) идёт прямо сейчас и
забирает из `run()` весь блок инструментов: щелчок, призрак, панели, подпись,
двери. Это самая большая доля. Начинать разбор до его посадки — значит дважды
править одни строки. Ожидаемое сокращение: 400–500 строк.

**Слой 1 — КЛАВИШИ.** Сейчас обработчики размазаны по `run()` с 4559 по 4990:
чат, ESC, полноэкранный режим, Tab, F2/F3, клавиши 1–5, каркас, снимок,
запись траектории. Каждый — `if (action_pressed(...))` вперемешку с телом
кадра. Выносится в `AppInput.cpp`: таблица «действие → что сделать», где «что
сделать» — вызов метода App, а не тело. Проверяется тем, что таблица ПОЛНАЯ и
ОДНОЗНАЧНАЯ: два действия не могут делить клавишу в одной области видимости.
Рукав уже есть (app_controls), расширяется.

**Слой 2 — ДВЕРИ.** `DFN_*` читаются в `run()` россыпью: DFN_EDITOR_TOOL,
DFN_EDITOR_PARTS, DFN_EDITOR_BRUSH, DFN_CAPTURE_AFTER, DFN_SHOT_AFTER,
DFN_THIRD_PERSON, DFN_BUILD, DFN_CAM_TRACE, DFN_HUD. Выносится в `AppDoors.cpp`
одной таблицей: имя, что делает, один раз читается или каждый кадр. Сегодня это
знание нигде не собрано, и «какие вообще есть двери» — вопрос к `grep`.
Проверка: каждая дверь в таблице ИМЕЕТ имя в документации, и наоборот.

**Слой 3 — ОВЕРЛЕИ.** Блок с 5715 по 6135 — компас, полосы, прицел, подсказка,
блок редактора, отладочный вывод. Половина уже вынесена (HudScreen, EditorHud,
DebugOverlay), но СБОРКА кадра осталась в `run()`. Выносится в `AppHud.cpp`:
одна функция, принимающая факты и холст. Проверяется раскладкой — рукав есть
(app_hud_screen), и он уже ловил наложения.

**Слой 4 — ПОСЛЕ ОТРИСОВКИ.** 6136–6323: запись траектории, телеметрия,
снимок, запись чата, стриминг с гистерезисом. Общее у них одно — они обязаны
идти ПОСЛЕ render(), и это единственная причина, по которой они там, где они
есть. Выносится в `AppAfterFrame.cpp` с этим доводом в шапке.

**Слой 5 — САМ ЦИКЛ.** Что останется: тик симуляции, интерполяция, вызовы
слоёв. Цель — не «красиво», а ЧИТАЕМО ЗА ОДИН ЭКРАН, чтобы порядок кадра был
виден целиком.

## Чего этот план НЕ обещает

- Он не сделает App.cpp тестируемым целиком: окно останется, и корень
  композиции останется корнем композиции.
- Цель первого прохода — 6358 -> около 2500. С потолком, поднятым 18.08 до
  2500, это ПОПАДАНИЕ В НОРМУ, а не недобор: пользователь снял прежние 800
  словами «нормально когда есть большой файл и он много связывает».
  ЧИСЛО ПЕРЕСТАЛО БЫТЬ ЦЕЛЬЮ, и это важнее самой цифры. Разбор делается не
  ради длины, а ради проверяемости: пока решение сидит внутри кадрового цикла,
  до него не дотягивается ни один рукав, сколько бы строк ни было в файле.
  Файл на 2400 строк с одной ответственностью — норма; 700 строк со спрятанным
  внутри непроверяемым выбором — дефект, и потолок его не ловит.
- Он не трогает зону world и зону кистей — там идут свои работы.

## Порядок сдачи

Каждый слой — своя сдача: сборка, полный ctest, коммит. Не «весь разбор одним
коммитом»: правка на 4000 строк, которую нельзя откатить по частям, — это не
рефактор, а замена файла.

# Что сделано по этому плану (18.08.2026, 19:06)

Слои 1–4 исполнены. Слой 0 (инструменты) был посажен до начала. Слой 5 (сам
цикл) НЕ делался — см. «Чего этот заход не сделал».

## Числа

| что | было | стало |
|---|---|---|
| `engine/app/sources/App.cpp` | 6696 строк | **6129** |
| `App::run()` | 1977 строк | **1390** (−30%) |
| обработчиков клавиш внутри `run()` | 18 | **0** |
| написанных от руки `!chat_typing &&` | 18 | **1** (колонка таблицы) |
| вызовов `action_pressed()` в App.cpp | 18 | **0** (один обход в AppInput.cpp) |
| `std::getenv` в зоне app вне таблицы | 75 | **0** |
| дверей, собранных в одном месте | 0 | **58**, у каждой описание |
| слагаемых в `unattended_run()` | 13, от руки | **колонка** (13 из 58 строк) |
| строк сборки оверлеев в `run()` | 238 | **0** (сбор фактов: 95) |
| строк хвоста кадра в `run()` | 165 | **1 вызов** |
| наборов ctest | 83 | **85** (+app_doors, +app_after_frame) |

## Что именно получило проверку

Критерий приёмки плана — «решение, вынесенное из run(), обязано получить
проверку». По слоям:

1. **Клавиши** (`app_controls`, +ActionRoutesTests): таблица диспетчеризации
   ПОЛНА (строка на каждое `Action`, в порядке `Action`), ОДНОЗНАЧНА (пара
   «метод + аргумент» не повторяется) и НИ ОДНА клавиша не проходит сквозь
   открытое окно чата. Плюс проверка «диспатч ровно в одном месте»: App.cpp
   больше не зовёт `action_pressed` ни разу.
2. **Двери** (`app_doors`, новый): таблица замкнута с ОБЕИХ сторон — имя,
   читаемое в зоне app, обязано иметь строку, а строка обязана где-то
   читаться; имя без строки не открывается и говорит об этом; признак
   «беспилотный прогон» проверяется по каждой двери порознь.
3. **Сборка оверлеев** (`app_hud_screen`, +HudComposeTests): подпись под
   прицелом (пустая рука, приговор судьи сильнее ключа, группа только при трёх
   условиях, перехват указателя панелью сильнее всего), чистый кадр по
   DFN_HUD=0 — НИ ОДНОГО пикселя, и главное: добавление редакторского блока НЕ
   МЕНЯЕТ НИ ОДНОГО ПИКСЕЛЯ, принадлежащего отладочному выводу.
4. **После отрисовки** (`app_after_frame`, новый): затвор тура (гистерезис
   4 кадра, потолок 600 от ПОСЛЕДНЕГО успокоения), период телеметрии по
   счётному времени, отсчёт до закрытия (второй взвод НЕ укорачивает), приговор
   восстановлению на ГОРИЗОНТАЛЬНОЙ ошибке. Числа 4/600/8 выписаны от руки
   отдельным контролем — первая редакция набора выражала гистерезис через саму
   константу и оставалась ЗЕЛЁНОЙ при её замене на 1.

## Что вынесено БЕЗ проверки, и это честный список

- **Сами обработчики клавиш** (`AppInput.cpp`). Проверена ТАБЛИЦА и то, что
  диспатч один; что именно делает `on_toggle_body`, не проверяет ничто — он
  трогает окно, курсор и мир. Разрез сделан так, чтобы решение («когда
  срабатывает») отделилось от следствия («что происходит»), но следствие
  остаётся непроверяемым.
- **Тело `after_frame()`** — запись файлов, вызовы рендерера и окна. Проверены
  четыре решения, которые оно принимает, а не эффекты.
- **Сбор фактов для оверлея** в `run()` (95 строк). Он читает мир и рендерер;
  проверить его без окна нельзя, и он оставлен там намеренно.
- **Порядок вызовов внутри кадра** — то, что стриминг идёт перед шагом, а
  `after_frame` после `render()`, — по-прежнему держится комментарием, а не
  прибором.
