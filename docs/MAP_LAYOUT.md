<!--
Created: 14:08:2026 - 16:40:00
Last updated: 17:08:2026 - 16:15:30
-->
<!--
UPD:
- 14:08:2026 - 16:40:00: Создан. Контракт раскладки карт/демок и браузера редактора,
- 14:08:2026 - 18:05:00: Пустые папки-категории убраны (решение пользователя). Каталог сканирует существующие папки, не фиксированный список; зона создаёт свою папку при первой демке и удаляет пустую. Осталось landscape+trees.
                         заложен ДО того как зоны начнут делать демки (требование
                         пользователя: «эту систему нужно заложить ДО того как агенты
                         начнут свои демки делать»). Лид-владение (правило 25, в45).
- 17:08:2026 - 16:15:30: dfn_newmap и запрет создавать папки побочным эффектом записи (заказ 17.08).
-->

# MAP_LAYOUT.md — раскладка карт и демок

Контракт, на котором держится весь конвейер инструментов. Определён ДО демок,
чтобы зоны не расходились. Меняется только лидом (правило 25).

## Дерево на диске

```
assets/maps/
  <категория>/
    <карта>.map          # манифест карты (текст, в git — лёгкий, читаемый)
    <карта>.chat.jsonl   # двусторонний чат рядом с картой (см. ниже)
    <карта>.dfw          # ПЕЧЁНАЯ карта — появится с пекарем (пока нет; НЕ в git, в кэш)
```

- **Карта = файл-манифест `.map`** внутри папки-категории. Пользователь: «папки
  разные, и в папке я уже выбирал карту» — два уровня, категория → карта.
- **Манифест `.map` — в git** (рецепт, лёгкий). **Печёный `.dfw` — НЕ в git**
  (десятки-сотни МБ, раздувают историю; в35): пересобирается при первом запуске
  из манифеста и кэшируется локально (`cache/maps/`), с загрузочным экраном
  «подготовка объектов».
- **`chat/` НЕТ отдельной папкой** — чат живёт файлом `<карта>.chat.jsonl` рядом
  с картой, чтобы «коммент в демке» сам знал свою карту и её зону-владельца.

## Категории — ПАПКИ СОЗДАЮТ ЗОНЫ, НЕ ЛИД

**Пустых папок в дереве не держим** (решение пользователя: «пустые папки убери,
пусть агенты по необходимости создают их»). Браузер сканирует то, что ЕСТЬ под
`assets/maps/`, а не фиксированный список. Зона:

- **создаёт** свою папку-категорию, когда впервые сохраняет туда демку;
- **удаляет** папку, когда та осталась пустой;
- владеет своей категорией (правило 25) и её содержимым.

Ожидаемые имена (В40, как договорённость, а не как предсоздание): `landscape`
← рельеф, `trees` ← флора, `bushes`/`fallen-tree` ← флора, `houses` ← строитель,
`light` ← свет/факела, `water` ← вода, `mountain`/`caves` ← рельеф. Имя новой
категории согласуй с лидом (формат — контракт лида), но саму папку заводит зона.

### Как именно завести папку и карту: `dfn_newmap`

```
./build_<зона>/dfn_newmap <зона>/<имя> [--stand Gallery] [--chunks 1]
```

Создаёт `assets/maps/<зона>/<имя>.map` и `assets/scenes/<имя>.scene`, печатает
что создал, и **отказывается перезаписывать** существующее. Заготовка
`.map` намеренно неполна: `name`, `zone` и `description` содержат слово
ЗАПОЛНИ, а `built_commit` пуст — заготовка, выглядящая законченной, разносит
подставные строки по списку карт.

**Папка не должна появляться как побочный эффект записи файла.** Раньше
появлялась: бот прогулок и обзорный тур создавали свой каталог при НАСТРОЙКЕ,
поэтому каждый прогон, который бота включил и ничего не написал, оставлял за
собой пустую папку — и они возвращались снова и снова. Теперь каталог делает
тот, кто уже держит в руках, что в него положить (17.08, по слову
пользователя: «есть механизм который их постоянно создает, удалить его»).
Каталог в дереве — это свидетельство, что туда что-то записали.

## Манифест `.map` (транзитный формат, до пекаря)

Текст, `ключ = значение`. Пример `assets/maps/landscape/valley.map`:

```
name = Долина у скалы-храма
zone = terrain
size_chunks = 4
# ИСТОЧНИК карты. Сейчас — генератор-стенд (мост, пока нет пекаря);
# позже source = dfw:valley.dfw (печёная карта).
source = stand:Testbed
description = Базовый ландшафт: долина, скала, деревни, вода.
built_commit =
```

Когда пекарь `.dfw` заведён — `source = stand:...` меняется на `source = dfw:<файл>`,
и браузер грузит печёное, а не генерирует. Мост нужен, чтобы браузер работал
end-to-end уже сегодня, до пекаря.

## Чат `<карта>.chat.jsonl` (двусторонний, в28/о1)

Append-лог, по строке-JSON на запись. Две роли:

- **человек → зона** (замечание на месте): `{"t":..., "who":"human", "text":"...",
  "capture":"<путь к снимку>", "trajectory":"<путь к записи>"}`
- **зона → человек** (самодокументация демки, правило о1): когда агент создаёт/меняет
  демку — пишет `{"t":..., "who":"<zone>", "text":"зачем демка, какая задача, что
  решено, как, на что смотреть чтобы убедиться"}`. Правило пользователя: демка
  сама объясняет себя.

`t` — счётчик/`game_seconds` для порядка (стенных часов в детерминированном коде
нет); реальную дату инструмент подставляет при сбросе. Ответы агента — короткие.

## Браузер редактора

Вход в «Редактор» (и в «Играть») открывает НЕ карту, а браузер:
**категории (папки) → карты (`.map` в папке) → открыть**. Прыгать сразу в карту
нельзя — это была ошибка первого каркаса, названная пользователем.
