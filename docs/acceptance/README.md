<!--
Created: 10:08:2026 - 10:35:51
Last updated: 12:08:2026 - 00:52:40
-->
<!--
UPD:
- 11:08:2026 - 15:08:47: ДИАГНОСТИЧЕСКАЯ пара R3 по СТРУКТУРЕ облачного слоя (не приёмочная — код не написан):
  жёсткая светлая полоса у горизонта опознана как собственное среднее листа, доказано
  построчным СКО РАЗНОСТНОГО кадра против руки DFN_CLOUD=0. Прибор не нуждается в маске.
- 10:08:2026 - 10:35:51: Created with the menu's first acceptance frame (Rule 27's archive clause).
- 10:08:2026 - 11:02:00: Кадры зоны character (тело от первого лица, зеркальный
  двойник, витрина клипов) + оговорка о том, как кадр приводится к 640×360.
- 10:08:2026 - 11:02:19: Кадры зоны render — облака W4 (лист, тени по долине, кучевые на
  горизонте), пара по сносу, контроль cover=0, закат и ночь.
- 10:08:2026 - 11:42:00: Поправка к рядам look-down: торс занимает 63 % кадра, а
  не 100 %, как было записано в 4a44c26 (нашёл sim при проверке моей посылки).
- 10:08:2026 - 11:12:17: Кадры облаков переотсняты после того, как ведущий увидел в них
  «шахматку» по крагу — это растворение LOD, снятое НА ЛЕТУ; добавлен раздел
  о том, сколько кадров ждать перед снимком.
- 10:08:2026 - 12:20:00: Кадры после четырёх замечаний пользователя (грудь, скругление, стойка, суставы).
- 10:08:2026 - 20:00:23: СТАРЫЕ КАДРЫ С ВЫВЕРНУТЫМИ КОЛЕНЯМИ УДАЛЕНЫ И ПЕРЕСНЯТЫ
  (пользователь: «есть старые скрины ходьбы, где колени выгибаются странно»).
  Плюс кадр витрины «привет» удалён без замены — он не мог провалиться
  (правило 27), см. строку ниже. Добавлены два кадра по первому замечанию.
- 10:08:2026 - 19:59:10: Два кадра стенда 0 от core после «рисуемая земля и
  земля размещения — одна поверхность». Один из них честно помечен как
  НЕ-доказательство: на нём ещё стоит заплатка render'а, поэтому этот ракурс
  не может провалиться, и доказательством служат числа, а не он.
- 10:08:2026 - 20:17:57: Кадры flora по правке угла карточек листвы (d06db79): пара «взгляд
  вверх» (выигрыш) и пара «горизонт на дальний лес» (цена). Оба плеча
  собраны из одного состояния дерева и сняты одним restore-файлом.
- 10:08:2026 - 20:24:42: Кадр рич-кромки после исправления единой поверхности у core: кромка
  читается на ходовой высоте. Прежняя честная запись «в архивном кадре не
  читается» закрыта переснятой уликой, а не доводом.
- 10:08:2026 - 20:30:28: Устаревший кадр обочины помечен как снятый на несуществующей земле и
  заменён переснятым. Наблюдение «обочина грунтовой дороги пуста» УСТОЯЛО
  после исправления core — значит это не был дефект двух поверхностей.
- 10:08:2026 - 20:35:52: §5.12 apron before/after из одного файла восстановления,
  плюс сам файл — чтобы плечи можно было переснять, а не поверить на слово.
- 10:08:2026 - 22:41:59: Кадры присяда (character): камера была внутри коробки туловища; два плеча из одного файла восстановления, плюс сам файл восстановления.
- 11:08:2026 - 13:44:01: R1 (воздушная перспектива) — три дальности до и после плюс контрфактическое
  плечо на более густом воздухе. Инструмент `tools/measure_aerial.py`; его первая
  версия сломалась на сильном плече и это записано в самом инструменте.
- 11:08:2026 - 14:08:27: Кадры H1/H2/низины по трём утверждениям design §10.9, с
  КОНТРОЛЕМ БЕЗ ДЫМКИ. Контроль проваливает H2 и почти проваливает H1 — то есть
  два порога из трёх сейчас недостижимы независимо от воздуха.
- 11:08:2026 - 14:23:04: Отгруженные кадры R1 (плечо C, без перекрытий) и пара R2 по
  полосе мглы с выключенным слоем как контролем.
- 11:08:2026 - 14:29:59: Пара R2 на 900 м ПЕРЕВЕДЕНА В ДИАГНОСТИЧЕСКИЕ — ведущий не различил
  на ней плечи глазом. Приёмочная для R2 — только пара на 360 м.
- 11:08:2026 - 14:39:40: Пара R3.1 по куполам на горизонте (0 → 11 колонок с дырой) и вид
  после неё.
- 11:08:2026 - 14:52:34: Высота полосы мглы ВЫВЕДЕНА; ограничения не сошлись, и виновата
  оказалась проба неба, стоявшая на 1.6 м внутри слоя, а не сама полоса.
- 12:08:2026 - 00:16:30: R5 (цвет земли) — контрфактическая пара по тайлу, которая и есть
  ДИАГНОЗ, и пара BEFORE/AFTER по тинту с нулевой дозой в качестве контроля.
  Прибор новый и отдельный (`tools/measure_ground_colour.py`), потому что R1 меряет,
  как гребень отделяется от неба, а тут вопрос о ЦВЕТНОСТИ одной поверхности —
  другой предмет, другой прибор (правило 41).
- 12:08:2026 - 00:36:10: Цена R5 по РЯБИ померена и записана в ту же строку, вместе с
  двумя отложенными наблюдениями ведущего по этим же кадрам. Порог 64 на этой
  точке провалиться не может (обе руки 0.000 %), поэтому число снято ещё на
  трёх порогах, где может; ухудшения нет ни на одном. `tools/measure_speckle.py`.
- 12:08:2026 - 00:52:40: Слой МЕЛКОЙ ТРАВЫ (`engine/render/sources/GroundTufts.*`) с парой
  плеч по `DFN_NO_TUFTS`. Цена по ряби замерена ТЕМ ЖЕ прибором и ОНА НЕ
  НУЛЕВАЯ: худший скачок 47.8 → 61.6, хвост выше порога 32 втрое. Записана в
  строку, а не в примечание.
-->

# Архив приёмочных кадров

Правило 27: **рецепт доказывает настоящее, пиксели сохраняют прошлое.** Сюда
кладутся только те кадры, на которые ССЫЛАЕТСЯ решение или приёмка, в родном
разрешении 640×360 — четырёхкратное увеличение тура добавляет пиксели, а не
сведения. Рядом с каждым кадром — рецепт воспроизведения.

| Кадр | Что доказывает | Рецепт |
|---|---|---|
| `menu_root_640x360.png` | Стартовое меню читается: заголовок, каретка выбора, кириллица, подсказка управления | `DFN_MENU=1 DFN_INTERNAL_RES=640x360 DFN_MENU_SHOT=<path> ./dfn_app` |
| `character-lookdown-66deg-torso-blocks-view-*.png` | ДЕФЕКТ: взгляд вниз (тангаж −1.15 рад) упирается в собственный торс. Измерено по кадру: торс начинается со строки 113 из 360 и занимает 63 % кадра — **не 100 %, как утверждала первая редакция (4a44c26), это поправка**. Вывод от поправки не страдает, а усиливается: ступня даже на предельном видимом выносе 0.486 м лежит на 74.0° склонения против кромки груди на 46.8°, то есть скрыта ГЕОМЕТРИЧЕСКИ, на любой фазе и при любом тангаже | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=stride DFN_BODY_PITCH=-1.15 DFN_PLAYTEST=soak DFN_PLAYTEST_SECONDS=45 ./dfn_app` |
| `character-lookdown-40deg-torso-blocks-view-*.png` | Тот же дефект на −0.70 рад: торс занимает 33 % кадра, начиная со строки 220. Контроль диапазона: пологий угол, на котором дефект обязан был бы ослабнуть — и он ослабевает ровно так, как предсказывает `atan(0.134/0.126)` = 46.8° при `CAMERA_FOV_Y` 75° | то же, `DFN_BODY_PITCH=-0.70` |
| `character-plant-phase-{0.25,0.50,0.75,1.00}-*.png` | КОНТРАКТ ШАГА в профиль: на 0.25 и 0.75 ноги максимально разведены (постановка), на 0.50 и 1.00 проходят рядом. Четыре кадра — один инструмент: два из них обязаны провалиться, если фазы сдвинуты | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=plant DFN_SHOWCASE=1 ./dfn_app`; попадание по фазе — в `probe_log.txt` рядом |
| `character-mirror-standing-*.png` | Пропорции и опора: макушка y=176, шея y=185 при расчётных 176.1 и 185.2 (Winter-доли от `PLAYER_CAPSULE_HEIGHT`); подошва y=242, трава с 243 — стоит на земле, не висит | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=mirror DFN_MIRROR=1 ./dfn_app` |
| `character-mirror-turned-0.5rad-*.png` | Поворачиваюсь налево на 0.5 рад — двойник поворачивается в зеркальную сторону (`double_yaw` в логе: π−yaw с точностью 0.005 рад) | то же |
| `character-walk-level-gaze-no-chest-*.png` | ПЕРВОЕ ЗАМЕЧАНИЕ, ПРОВЕРКА ПОСЫЛКИ: иду на 1.80 м/с, тангаж 0.00 — в кадре НЕТ ни груди, ни рук, ни ног. Отсечка груди по ключице сработала. Это точка съёмки, которая МОГЛА провалиться: до правки торс входил в кадр с 61°, а строка отладочного вывода (pitch 0.00, spd 1.80, phase 0.23) утверждает именно ту позу, о которой идёт спор | `DFN_PLAYTEST=patrol DFN_BODY_PROBE=stride DFN_BODY_PITCH=0.0 DFN_DEBUG_OVERLAY=1 ./dfn_app` (кадр 00), бинарь b134936 |
| `character-mirror-shape-*.png` | ВТОРОЕ ЗАМЕЧАНИЕ («форма персонажа странная») в стойке у зеркала: руки НЕ ЧИТАЮТСЯ в силуэте — плечевой сустав стоит ровно на полуширине торса (±0.233 м), поэтому половина толщины руки внутри коробки; и головы нет шеи. Оба — измеренные, НЕ починенные дефекты (см. specs/character.md 3b) | `DFN_PLAYTEST=patrol DFN_MIRROR=1 DFN_BODY_PROBE=mirror ./dfn_app` (кадр 00), бинарь b134936 |
| `character-profile-walk-knee-fixed-*.png`, `character-profile-run-knee-fixed-*.png` | Силуэт походки сбоку ПОСЛЕ починки колена: опорное колено прямое, пятка отрывается, стопа катится через носок. ЗАМЕНА, а не добавление: кадры `character-profile-{walk,run}-da9ff6e.png` показывали переразгиб −33.4° и были удалены — архив, показывающий починенный дефект как текущее состояние, хуже отсутствия архива | `DFN_PLAYTEST=patrol DFN_SHOWCASE=1 DFN_BODY_PROBE=profile ./dfn_app` (кадры 00 и 01), бинарь b134936 |
| (удалён) `character-showcase-wave-*.png` | УДАЛЁН БЕЗ ЗАМЕНЫ, и это правило 27, а не лень: кадр утверждал «привет читается как жест рукой», но НЕПОДВИЖНЫЙ КАДР НЕ МОЖЕТ ЭТОГО ПОКАЗАТЬ. Сломанный клип (мах сидел на ЛОКТЕ, а шарнир его удалял) давал ровно ту же поднятую руку — точка съёмки не могла провалиться. Доказательством служит ИЗМЕРЕНИЕ: путь кисти за один цикл 0.011 м до и 0.236 м после (`character_clips`, «the wave waves») | измерение, не кадр |
| `clouds-midday-sky-upwind-*.png` | ТРИ ВИДА ОБЛАКОВ в одном кадре (в10): дрейфующий лист с параллаксом двух слоёв, кучевые на горизонте против ветра (объявление погоды, W2.3), и небо с настоящим градиентом — до правки `v_dir` зенитный цвет не рисовался ни разу | `DFN_TOUR=1 DFN_CLOUD_PROBE=1 DFN_TIME=0.5 DFN_VISTIME=0 DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 ./dfn_app` (кадр 01) |
| `clouds-drift-t0-valley-*.png`, `clouds-drift-t30-valley-*.png` | ПАРА ПО СНОСУ: та же точка, 30 с игрового времени врозь. При 10 м/с это 300 м — полдлины волны: на t0 долина в тени, на t30 тень сошла и потемнел дальний берег. Тень ползёт по земле от ТОГО ЖЕ поля, что рисует лист | `... DFN_VISTIME=0` и `... DFN_VISTIME=30` (кадр 00) |
| `clouds-control-cover0-*.png` | КОНТРОЛЬ (правило 30): `DFN_CLOUD=0` убирает лист, кучевые И тени с земли ОДНИМ движением — потому что это одно поле. Тень, пережившая cover=0, была бы дефектом «двух копий» | `... DFN_CLOUD=0 DFN_VISTIME=0` (кадр 00) |
| `clouds-dusk-*.png` | Облака краснеют с небом: палитра листа берётся из того же `u_sunColor`, которым освещена земля, поэтому расходиться им не с чем | `... DFN_TIME=0.78` (кадр 01) |
| `clouds-night-stars-*.png` | Ночью облака темнеют и ЗАКРЫВАЮТ звёзды: звёзды видны в разрывах и не видны на облаках. Отдельного гейта для звёзд нет — облака просто смешиваются поверх них | `... DFN_TIME=0.0 DFN_MOON=0.5 DFN_CLOUD=0.40` (кадр 01) |

| `render-aerial-{BEFORE,AFTER}-{250,500,900}m-*.png` | **ВОЗДУШНАЯ ПЕРСПЕКТИВА (REFERENCE_FRAMES.md R1). ТРИ ДАЛЬНОСТИ — ЭТО И ЕСТЬ КОНТРОЛЬ (правило 30):** одна и та же скала L0, одна переменная — дальность, так что «стало красивее» без расхождения по дальности проваливается видимой РОВНОЙ колонкой. Число — `standout`, средний \|L−L(неба)\| по всей рамке, БЕЗ сегментации (см. предупреждение в шапке инструмента). **ДО: 19.71 → 27.19 → 30.40 — контраст РОС с дальностью на 54 %**, потому что небо к горизонту светлеет, а скала нет; кадр утверждал обратное референсу. Причина не в настройке: `dfn_fog_factor` был `smoothstep(2400, 6800, d)` при мире 1024 м поперёк, то есть РОВНО НОЛЬ везде, ни разу за всё время. **ПОСЛЕ: 18.45 → 20.67 → 20.45 — рост убит, колонка ровная.** ЧЕСТНО О ТОМ, ЧЕГО ЭТИ КАДРЫ НЕ ПОКАЗЫВАЮТ: ровно — это ещё не референс. В кадрах пользователя дальний гребень выделяется МЕНЬШЕ ближнего, а у нас пока столько же. Достижимо и измерено — см. строку STRONG600 | три прогона, различаются ТОЛЬКО `DFN_MASSIF_EYE`: `DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=<614,325 \| 397,450 \| 51,650> DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_TIME=0.5 DFN_CLOUD=0 DFN_MENU=0 ./build_render/engine/app/dfn_app`, затем `tools/archive_frame.py`. Точки — пеленг 240° от вершины (830,200) на 250/500/900 м: самый широкий разброс дальностей, при котором ВСЕ три стоят на открытой земле внутри стенда. Полдень и `DFN_CLOUD=0` — чтобы небо за гребнем было чистым градиентом и сегментация не была предметом спора. Замер: `tools/measure_aerial.py <кадр> 175,90,390,205 200,40,380,80` (250 м), `282,148,356,192 282,110,356,140` (500 м), `296,168,341,192 296,140,341,163` (900 м) — рамки ЧАСТЬ РЕЦЕПТА, оба плеча меряются одними и теми же |
| `render-aerial-vista-{BEFORE,AFTER}-*.png` | ТОТ ЖЕ РОД РАКУРСА, ЧТО У РЕФЕРЕНСОВ (широкий вид на даль, ср. кадры 02/05/12 в REFERENCE_FRAMES.md), поднятая точка над долиной. Здесь видно, ЧТО ИМЕННО поменялось в картинке: дальняя скала, дальняя кромка леса и трава у горизонта уходят к цвету неба, ближний план не тронут. **ЭТО ИЛЛЮСТРАЦИЯ, А НЕ ЗАМЕР** — ракурс не может провалиться количественно, потому что дальности в нём не названы; заверяет число тройка кадров скалы выше. Тот же кадр показывает и НЕЗАКРЫТОЕ: облака здесь по-прежнему «квадратные и плоские» (жалоба пользователя), небо по-прежнему один вертикальный градиент — это R2 и R3, они не входили в эту работу | `DFN_SKY_PROBE=1 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_TIME=0.35 DFN_MENU=0 ./build_render/engine/app/dfn_app`, затем `tools/archive_frame.py` |
| `render-aerial-STRONG600-900m-*.png` | **КОНТРФАКТИЧЕСКОЕ ПЛЕЧО, И ОНО РЕШЕНИЕ ВЕДУЩЕГО, А НЕ МОЁ.** Тот же кадр 900 м при `HAZE_SCALE_LENGTH` 600 м вместо принятых 1400: `standout` 14.57 против 18.45 на 250 м — дальний гребень наконец выделяется МЕНЬШЕ ближнего, то есть ведёт себя как во всех 16 референсах. Не поставлено, потому что 600 м ломает чужой принятый контракт: design §1.3a требует, чтобы Ravenscar на 287–717 м читался ПЛОТНЫМ, и прямо называет дымку на нём багом. 1400 м выведено ИЗ этого контракта и его не нарушает. Выбор между «слушаться §1.3a» и «слушаться кадров пользователя» — межзонный, и он оформлен кадром, а не доводом | то же, плюс `DFN_HAZE=600` |
| `render-haze-H1-360m-{Z,A,B,C}-*.png` | **H1 — СИЛУЭТ НА 360 м, КОНТРОВОЙ СВЕТ** (design §10.9; 360 м — это `d_accept`=3R по §1.6.1, а НЕ отозванные 717 м). Плечи отличаются ТОЛЬКО двумя строками воздуха: **Z — контроль без дымки** (`DFN_HAZE=100000`, правило 30), A = 1400/250 (стоит сейчас), B = 600/250 (один рычаг), C = 600/40 (два рычага). Порог — 2 шага `PALETTE_SHADE_STEP_REF` по ВСЕМУ контуру, поэтому число это МИНИМУМ по столбцам, а не среднее: среднее пропустит гору с растворившимся плечом, если вершина осталась тёмной, а это и есть форма отказа, которую даёт дымка. **Z 2.36 · A 1.84 · B 1.34 · C 1.75** (по 5-му перцентилю 2.77 / 2.25 / 1.69 / 1.96). ГЛАВНОЕ: КОНТРОЛЬ БЕЗ ДЫМКИ ДАЁТ 2.36 ПРИ ПОРОГЕ 2.00 — запас 0.36 шага, то есть H1 в нынешней формулировке запрещает почти любую воздушную перспективу на 360 м. И статистика важна: 105 столбцов кадра 640×360, жёсткий минимум — ОДИН пиксельный столбец, а порог на разрешении прибора не имеет запаса (следствие 30a) | `DFN_HAZE=<L> DFN_HAZE_H=<H> DFN_HAZE_BASE=30 DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=518,380 DFN_TIME=0.30 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_CLOUD=0 DFN_MENU=0 ./build_render/engine/app/dfn_app`, затем `tools/archive_frame.py`. Замер: `tools/measure_aerial.py contour <кадр> 245,100,350,215 <кадр Z>` — КРОМКИ БЕРУТСЯ ИЗ КОНТРОЛЯ, потому что положение силуэта это геометрия и во всех плечах одно |
| `render-haze-H2-287m-{Z,C}-*.png` | **H2 — РИТМ ПОЛОС У ПОДОЛА, 287 м, скользящий свет.** Порог 1 шаг, и меряется на САМОЙ НИЖНЕЙ паре полос, никогда по среднему склону. **Z (БЕЗ ДЫМКИ) 0.61 · A 0.45 · B 0.25 · C 0.17.** ЭТО ОТВЕТ, И ОН НЕ ПРО ВОЗДУХ: контроль без единой молекулы дымки уже проваливает H2 почти вдвое. Ритма полос у подола НЕТ до всякой атмосферы, значит H2 сейчас — дефект рельефа/сплата, и НИКАКАЯ длина шкалы его не выполнит. Дымка потом честно ухудшает, и высотный рычаг ухудшает сильнее (C хуже B), ровно как предсказала design | то же, `DFN_MASSIF_EYE=581,344 DFN_TIME=0.72`. Замер: `tools/measure_aerial.py bands <кадр> 250,143,310,190 <кадр Z>` — СТРОКИ ПОЛОС ТОЖЕ ИЗ КОНТРОЛЯ |
| `render-haze-H2-287m-DESIGN-VANTAGE-OCCLUDED-*.png` | **ДЕФЕКТ ЧУЖОЙ ТОЧКИ СЪЁМКИ, найденный по дороге.** Собственный вантаж design §7.1b (545,165) БОЛЬШЕ НЕ МОЖЕТ проверять ритм полос: сосновый массив вырос поперёк луча зрения, и от склона остались щели между стволами. Поэтому H2 снят с (581,344) — та же дальность 287 м и тот же скользящий час, но с чистым лучом. Вантаж принадлежит design, переклеймить его им | `... DFN_MASSIF_PROBE=2 DFN_TIME=0.72` |
| `render-haze-lowland-900m-{A,C}-*.png` | **НИЗИНА — ДРУГОЕ УТВЕРЖДЕНИЕ И ПОТОМУ ДРУГОЙ ПРИБОР (правило 41).** `standout` спрашивает, как гребень отделяется от неба; дно долины не стоит ни против чего. «Плоско» означает, что дальняя земля выглядит как земля под ногами, поэтому число — глубинный признак между двумя полосами ОДНОЙ поверхности. **A +10.12 лума (падение цветности 11.66) → C +19.78 (24.33): вдвое.** И отдельно: **B и C дают ЗДЕСЬ РОВНО ОДНО И ТО ЖЕ ЧИСЛО** — это и есть доказательство, что два рычага независимы, потому что дно долины лежит внутри зажатого слоя `HAZE_BASE_HEIGHT` и высотная шкала до него не дотягивается | `... DFN_MASSIF_EYE=51,650 DFN_TIME=0.5`. Замер: `tools/measure_aerial.py ground <кадр> 40,320,600,356 40,200,280,209` |
| `core-A1-lowland-900m-{BEFORE,AFTER}-4deffcd+g1.png` | **A1 (§10.8) — ОБЪЕКТНАЯ ГРАММАТИКА, ШАГ ПЕРВЫЙ.** Та же точка (51, 650), что у архивных кадров низины render'а, и это НАМЕРЕННО: их выбрала другая зона для другого вопроса, поэтому никто не компоновал ракурс под выигрышное «после». Оба плеча сняты ОДНИМ бинарником — `DFN_NO_RELIEF=1` снимает §2.7 рельеф, выходы породы и валуны разом и воспроизводит прежний мир БАЙТ В БАЙТ. ДО: непрерывный травяной скат от ног до кромки леса, камешки одного размера стоят НА поверхности. ПОСЛЕ: две каменные массы на горизонте, гребневая линия, за которой земля прячет землю, валуны разного размера ВЫХОДЯТ из грунта. Числа: σ ухабистости на этой точке 0.101 → 0.353 м (пол 0.35); силуэты среднего плана, СЧИТАННЫЕ В ГЕНЕРАТОРЕ (правило 47), 0 → 17 расставленных и читаемых, из них 8 не закрыто рельефом (пол 5). ЧЕГО НЕ ЗАВЕРЯЕТ: цвет земли (render) и одинаковые деревья (flora) на кадре по-прежнему есть; пласты у подола сделаны только геометрией. Полный разбор и открытые красные пункты — `core-object-grammar-step1.md` | `DFN_HAZE=1400 DFN_HAZE_H=250 DFN_HAZE_BASE=30 DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=51,650 DFN_TIME=0.5 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_CLOUD=0 DFN_MENU=0 ./build_core/engine/app/dfn_app`, плечо «до» — то же плюс `DFN_NO_RELIEF=1`, затем `python3 tools/archive_frame.py <src> <dst> 640` |
| `render-tufts-lowland-{BEFORE,AFTER}-5b2adcc+t1.png` | **МЕЛКАЯ ТРАВА, и требование пользователя необычное — прочитать буквально: «её должно быть НЕ МНОГО, но она должна быть ВЕЗДЕ и прикрывать плоскость».** Это не газон. Оба плеча — один бинарник, одна переменная `DFN_NO_TUFTS`. На «после» по всей ближней земле стоят редкие мелкие пучки разной формы и оттенка; земля перестала быть поверхностью, по которой ходят, и стала поверхностью, на которой что-то растёт. **ВСЕ ЧЕТЫРЕ ЧИСЛА СЛОЯ ВЫВЕДЕНЫ ИЗ УЖЕ УТВЕРЖДЁННЫХ СТРОК, ни одно не выбрано (правило 14):** плотность = `GRASS_DENSITY_MIN` 0.5/м² (ПОЛ утверждённой вилки 0.5–1.5 — это и есть «не много», выраженное существующим числом, а не новым); высота = `GRASS_HEIGHT_MAX`; уклон = `SLOPE_GRASS_MAX`, тот же порог, что у сплата, поэтому пучок не может встать на земле, которую шейдер рисует камнем; **дальность видимости СЧИТАЕТСЯ ПО ПРАВИЛУ 33** из `SILHOUETTE_MIN_PX` / `INTERNAL_RES_H` / `CAMERA_FOV_Y` и выходит ≈12 м. **ТРАВА НЕ МОЖЕТ ПОЧИНИТЬ СРЕДНИЙ ПЛАН И НЕ ПЫТАЕТСЯ:** пучок 0.4 м перестаёт быть объектом за 12 м, а рисовать его до `GRASS_VIEW_DISTANCE` 50 м значило бы поставить тысячи подпиксельных треугольников ради ничего — и произвести ровно ту рябь, с которой зона уже дважды воевала. Её полоса — ближние метры, и это законно: там сейчас пусто. Пучки посажены НА ОТРИСОВАННУЮ поверхность (треугольники воксельного меша, из которых она и построена), а не по карте высот, потому что эти две поверхности расходятся на 0.10–0.15 м — для дерева округление, для пучка половина роста. **ЦЕНА ПО РЯБИ ЗАМЕРЕНА И ОНА НЕ НУЛЕВАЯ — говорю прямо, а не прячу** (`tools/measure_speckle.py`, тот же рецепт `FLORA_SPECKLE_*`, шаг 0.05 м, `DFN_WIND_FREEZE=120`): при пороге 64 обе руки по-прежнему 0.000 %, но **худший скачок пикселя вырос 47.8 → 61.6**, а хвост поехал вверх на каждом пороге, где прибор может провалиться: 8 → 10.720 против 11.029 %, 16 → 1.098 против 1.233 %, **32 → 0.009 против 0.030 %, то есть 21 пиксель против 70 — втрое.** Это 0.03 % кадра против принятых 0.621 % у ближнего полога, то есть цена мелкая; но она есть, направление плохое, и 61.6 уже недалеко от порога 64. **ЧТО СДЕЛАНО, ЧТОБЫ БЫЛО НЕ ХУЖЕ:** лезвие — НЕПРОЗРАЧНАЯ ГЕОМЕТРИЯ, а не карточка с маской, поэтому его край лежит там, где его берёт покрытийное сглаживание, а не внутри выборки текстуры, куда MSAA не достаёт (тот самый механизм, за который зона заплатила дважды). **ЧЕГО КАДР НЕ ЗАВЕРЯЕТ:** ветер по пучкам не идёт (они не подключены к `dfn_wind_offset`), теней пучки не отбрасывают, и на равнине без деревьев проверен только один ракурс | `DFN_NO_TUFTS=<не задано \| 1>` плюс тот же рецепт низины, что у строки R5. Замер ряби — `DFN_FLORA_PROBE=2 DFN_FLORA_EYE=51,650 DFN_FLORA_YAW=1.047 DFN_FLORA_PITCH=-0.35 DFN_FLORA_STEP=<0 \| 0.05> DFN_WIND_FREEZE=120`, затем `python3 tools/measure_speckle.py <still> <stepped> - <порог>`. **Точка СВОЯ, а не канопейная: прибор, наведённый на полог, не принимает утверждение о земле (правило 41), и числа из строк `FLORA_SPECKLE_*` этим числам не порог** |
| `render-R5-tilearm-{32m,2m}-eda8599+r5.png` | **ДИАГНОЗ R5, ПОЛОВИНА «ВИДИМЫЙ ПОВТОРЯЮЩИЙСЯ УЗОР», И ЭТО КОНТРФАКТИЧЕСКАЯ ПАРА, А НЕ ЗАМЕР (правило 30).** Вопрос был ровно один и одним кадром он не решается: узор на земле — это ДИЗЕРИНГ ПАЛИТРЫ / упорядоченный дизеринг сплата / покрытийное сглаживание (всё это ЭКРАННОЕ, масштаб в пикселях) или ТАЙЛ МАТЕРИАЛА (мировой, масштаб в метрах)? Две руки отличаются ТОЛЬКО `DFN_TERRAIN_TILES`: 8 повторов на чанк = период 32 м, 128 = период 2 м, при отгруженных 32 = 8 м. **Размер пятен на земле поехал РОВНО В ТУ ЖЕ СТОРОНУ И РОВНО ВО СТОЛЬКО ЖЕ РАЗ — вчетверо крупнее и вчетверо мельче. Значит узор МИРОВОЙ, то есть материал; ни один экранный механизм так двигаться не может по построению.** Палитра в обеих руках выключена (`DFN_PALETTE=0`), так что дизеринг палитры был исключён ещё и вторым способом. Тайл — 8 м (`LOOKDEV_TERRAIN_TILES_PER_CHUNK` 32 на `CHUNK_SIZE` 256), внутри него крупнейшая гармоника травы — 2 м (`ProcTexture` GRASS, октава «patches» с периодом 4), и по правилу 33 (читаемый размер = дальность/30) кусок 8 м читается до 240 м, то есть по всей низине. ЧЕГО ПАРА НЕ ГОВОРИТ: она не измеряет, НАСКОЛЬКО он заметен — она устанавливает, ЧТО ЭТО | `DFN_TERRAIN_TILES=<8 \| 128>` плюс рецепт низины ниже. Обе руки — один бинарник, различие только в переменной; переменная печатает себя в stderr и ГРОМКО отвергает мусор, иначе неприменившаяся рука — это дубликат соседней |
| `render-R5-lowland-{BEFORE,AFTER}-eda8599+r5.png` | **R5 — ЦВЕТ ЗЕМЛИ. Жалоба пользователя: «цвет земли без повторяющихся крупных кусков, ничто не ощущается плоским».** Оба плеча — ОДИН бинарник, одна переменная: `DFN_GROUND_TINT` 0 (нулевая доза) и 1 (отгруженная). Точка — та же (51, 650), что у архивных кадров низины: её выбрала другая зона для другого вопроса, поэтому ракурс не скомпонован под выигрышное «после». **ЧТО ИЗМЕНИЛОСЬ В КАРТИНКЕ:** земля перестала быть одной зелёной заливкой — по ней идут охристо-сухие и серо-зелёные куртины разного размера, и соседние копии 8-метрового тайла больше не одинаковы. **ЧИСЛА** (`tools/measure_ground_colour.py`, рамка `0,240,640,360` — ЧАСТЬ РЕЦЕПТА, оба плеча читаются по одной): разброс ЦВЕТНОСТИ по блокам мелкий/средний/крупный **5.66 / 3.55 / 1.80 → 13.04 / 11.32 / 8.97**, при том что тем же прибором на самих референсах: кадр 01 (самый выцветший из шестнадцати) 9.31 / 7.28 / 2.34, кадр 15 9.03 / 8.49 / 6.57, кадр 02 21.45 / 17.68 / 7.45. **Разброс ЯРКОСТИ при этом не тронут: 2.71 → 2.85 единиц, то есть 0.14 линейки против 0.13–0.25 у референсов** — и это не удача, а вшитое ограничение (см. ниже). **НУЛЕВАЯ ДОЗА ВОСПРОИЗВОДИТ «ДО» В ТОЧНОСТИ** — все девять чисел совпадают до сотых (правило 48: критерий, который проходит и при нулевой дозе, меряет другую систему). **РЯБЬ ПОМЕРЕНА, И ЭТО БЫЛО НУЖНО** (замечание ведущего, и он прав): тинт действительно только красит уже нарисованные пиксели, но ВТОРАЯ выборка травы в несоизмеримом масштабе поднимает пространственную частоту земли, а это ровно тот класс, который мерцает. `tools/measure_speckle.py`, тот же рецепт, что у `FLORA_SPECKLE_*` (шаг 0.05 м = один кадр при 120/с на скорости бега, `DFN_WIND_FREEZE=120`), но со СВОЕЙ точки — открытая низина, взгляд вниз на землю, потому что прибор, наведённый на полог, не принимает утверждение о земле (правило 41). **При пороге 64 единиц яркости обе руки дают 0.000 %, и худший скачок пикселя ОДИНАКОВ: 52.2 → 52.2.** Порог 64 на этой точке провалиться не может, поэтому число снято ещё на трёх порогах, где может: 8 → 11.212 % против 10.714 %, 16 → 1.245 % против 1.103 %, 32 → 0.012 % против 0.010 %. **На каждом пороге рука с правкой НИЖЕ, а не выше** — двойная выборка слегка снижает локальный контраст, а перещёлкивает именно контраст. Ухудшения нет ни на одном пороге. **ЧЕГО ЭТИ КАДРЫ НЕ ЗАВЕРЯЮТ:** трава (пучков в кадре по-прежнему нет) и одинаковые деревья на горизонте (зона flora). **ОТЛОЖЕНО, ЗАМЕЧЕНО ВЕДУЩИМ ГЛАЗОМ НА ЭТИХ ЖЕ КАДРАХ, НЕ ЧИНИТСЯ СЕГОДНЯ:** (1) БЛИЖНЯЯ земля в нижней части кадра по-прежнему несёт блочный узор — тайл разбит на средней и дальней дистанции, а под ногами нет; правдоподобная причина в том, что вблизи одна копия тайла занимает пол-экрана, так что поле выбора 11 м не успевает сменить выборку в пределах видимого куска, и лечится это полем выбора покороче, а не большей амплитудой. (2) В средней дали новый цвет местами ложится ПОЛОСАМИ параллельно горизонту; ракурс сжимает по вертикали, поэтому изотропное поле 19 м обязано выглядеть полосатым в перспективе — но это гипотеза, а не диагноз, и различит их наклон поля относительно взгляда | `DFN_GROUND_TINT=<0 \| 1> DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=51,650 DFN_TIME=0.5 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_CLOUD=0 DFN_MENU=0 ./build_render/engine/app/dfn_app`, затем `python3 tools/archive_frame.py <src> <dst> 640`. Замер: `python3 tools/measure_ground_colour.py refs 0,240,640,360 <BEFORE> <AFTER>`; референсы — та же команда с рамками `ref_01 500,300,900,500`, `ref_02 0,390,520,506`, `ref_15 0,430,900,670` (JPEG → PNG через `sips -s format png`, это без потерь; хроматическая субдискретизация JPEG может только ЗАНИЗИТЬ цветность референса, то есть ошибается в безопасную сторону) |
| `render-aerial-SHIPPED-{360m,900m,vista}-*.png` | **ЧТО В ИТОГЕ ОТГРУЖЕНО** (`HAZE_SCALE_LENGTH` 600, `HAZE_HEIGHT_SCALE` 40, `HAZE_BASE_HEIGHT` 30), снято БЕЗ единого перекрытия среды. Сборка по умолчанию воспроизводит руку C ТОЧНО: H1 1.75 min / 1.96 p05, низина +19.78 / 24.33, расхождение по пикселям 0.050 % (дрожание стриминга, см. оговорку в `tools/pngdiff.py`). Кадр вида — тот же род ракурса, что у референсов 02/05/12. ЧЕСТНО О ТОМ, ЧЕГО НА НИХ ВСЁ ЕЩЁ НЕТ: земля — плоская зелёная плоскость с повторяющимся узором и одинаковыми камешками, частокол одинаковых деревьев на горизонте. Это R5/D1/D2 и чужая зона; воздух тут ни при чём | `DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=<518,380 \| 51,650> DFN_TIME=<0.30 \| 0.5> DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_CLOUD=0 DFN_MENU=0 ./build_render/engine/app/dfn_app`; вид — `DFN_SKY_PROBE=1 DFN_TIME=0.35` |
| `render-mist-R2-360m-{OFF,ON}-*.png` — **ПРИЁМОЧНАЯ ПАРА** | **R2 — ПОЛОСА МГЛЫ, И ЕЁ ДОКАЗАТЕЛЬСТВО ЭТО ГОРБ.** Слой мглы пересекает рельеф на середине горы и режет её на освещённую корону и замгленное основание (референсы 02, 04, 12). Мерить «стало ли мутнее» бессмысленно — это умеет и R1. Мерится РАЗНОСТЬ профиля яркости вверх по склону против плеча с выключенным слоем (правило 47): **+0.00 у подола (ряды 199/191/183 совпадают до сотой), +32.43 на ряду 159, +0.00 выше вершины.** Горб — единственное, чего НЕ МОЖЕТ дать никакая настройка строк R1: изменение дымки двигает весь склон монотонно. `DFN_MIST=0` стирает полосу и больше ничего (контроль, правило 30). Пара РАЗЛИЧИМА ГЛАЗОМ без подсказки — это и делает её приёмочной по правилу 27. ЧЕСТНО О СИЛЕ: у нас вал читается промывкой, у Skyrim в кадрах 02 и 12 это плотная масса, режущая гору пополам. Направление верное, сила пока меньше | `DFN_MIST=<0\|4> DFN_MASSIF_PROBE=1 DFN_MASSIF_EYE=518,380 DFN_TIME=0.30 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_CLOUD=0 DFN_MENU=0 ./build_render/engine/app/dfn_app`. Замер: `tools/measure_aerial.py profile <ON> 270,118,320,200 <OFF> 8` |
| `render-mist-R2-900m-{OFF,ON}-*.png` — **ДИАГНОСТИЧЕСКАЯ, НЕ ПРИЁМОЧНАЯ** | ТА ЖЕ ПРАВКА на 900 м. **ССЫЛАТЬСЯ НА ЭТУ ПАРУ КАК НА ДОКАЗАТЕЛЬСТВО R2 НЕЛЬЗЯ:** ведущий смотрел её глазами и НЕ РАЗЛИЧИЛ плечи — гора там занимает несколько десятков пикселей. Замер на ней по-прежнему честен, но правило 27 говорит, что приёмка это КАДР, а не число, поэтому пара оставлена как диагностика дальности и помечена здесь, чтобы её не процитировали вместо приёмочной | то же, `DFN_MASSIF_EYE=51,650 DFN_TIME=0.5` |
| `render-cumulus-R3-domes-{BEFORE,AFTER}-*.png` | **КУПОЛА НА ГОРИЗОНТЕ — ЭТО БЫЛ БАГ, А НЕ СТИЛЬ** (ведущий: «шляпки грибов, посаженные на линию горизонта»). Плечи собраны из ОДНОГО состояния мира и отличаются ТОЛЬКО двумя файлами шейдера (BEFORE — `git checkout HEAD` только своих `fs_sky.sc` и `dfn_env.sh`, то есть R1 и R2 уже стоят в обоих). Число: **колонок с ДЫРОЙ 0 → 11.** Ноль здесь не «мало», а СТРУКТУРНОЕ предсказание: поле читалось как функция ОДНОГО азимута, значит силуэт был однозначной функцией азимута и дыры в нём не могло быть в принципе. Любое положительное число доказывает, что силуэт перестал быть однозначным. Полоса заодно стала ШИРЕ и ТОНЬШЕ — 363 колонки с облаком против 196 при МЕНЬШЕЙ общей доле облака (13.5 % против 17.6 %). Маска — НЕЙТРАЛЬНОСТЬ (b−r ≤ 5), а не яркость: измерено, что кучевые приходят ровно на b−r = 0, а бледное небо у горизонта ЯРЧЕ затенённого основания кучевого облака, поэтому яркостный порог назвал облаком 80 % бокса в ОБОИХ плечах | `DFN_CLOUD_PROBE=1 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_TIME=0.35 DFN_MENU=0 ./build_render/engine/app/dfn_app`, кадр 01, затем `tools/archive_frame.py`. Замер: `tools/measure_aerial.py runs <кадр> 0,160,640,204` |
| `render-cumulus-R3-vista-AFTER-*.png` | Тот же ракурс вида, что у отгруженных кадров R1: куполов на горизонте больше нет, вместо них низкие неправильные массы. ЧЕСТНО: кадр молочнее прежнего, и это НЕ облака — точка съёмки стоит на 84.3 м, а полоса мглы R2 занимает 54–86 м, то есть камера ВНУТРИ слоя. Физически верно, но делает этот вантаж плохой приёмочной точкой для всего остального | `DFN_SKY_PROBE=1 DFN_TOUR=1 ... DFN_TIME=0.35` |
| `render-mist-R2-eye-{INSIDE-band-84m,ABOVE-band-106m}-*.png` | **ПОЛОСА НЕ ПОДНЯТА — ПОДНЯТА КАМЕРА, И ЭТО ВЫВОД, А НЕ УПРЯМСТВО.** Два ограничения ведущего не сходятся: если низ слоя обязан быть выше САМОЙ высокой точки обзора в мире (99.62 м у обзорных камер), а корона на 135 м, то короне остаётся 1 м — слой становится шапкой на вершине, то есть «просто туманом». Ошибочно ПЕРВОЕ ограничение: стоять внутри мглы на середине склона — верная физика, а в референсах 02/04/12 наблюдатель в чистом воздухе потому, что стоит В ДОЛИНЕ. Точки, где реально стоит игрок, кончаются на 25.44 м (`massif_verdict`), низ слоя — 54 м: 28 м чистого воздуха. Виновата была ПРОБА НЕБА: ground+70 давало 84.43 м при слое 54–86 м — **камера стояла на 1.6 м ВНУТРИ слоя**, оттого и молоко. Теперь она привязана к `MIST_BAND_HEIGHT + THICKNESS/2 + 20` = 106 м, абсолютной, а не относительной к земле. На кадре с 106 м видно ОБА требования сразу: воздух наблюдателя чист, гора разрезана — основание утоплено в слое, корона стоит тёмной над ним | `DFN_SKY_PROBE=1 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_TIME=0.35 DFN_MENU=0 ./build_render/engine/app/dfn_app` |
| `render-sky-R3-structure-CLOUD-{ON,OFF}-4deffcd.png` — **ДИАГНОСТИЧЕСКАЯ ПАРА, НЕ ПРИЁМОЧНАЯ** | **ЖЁСТКАЯ СВЕТЛАЯ ПОЛОСА У ГОРИЗОНТА — ЭТО СОБСТВЕННОЕ СРЕДНЕЕ ЛИСТА, НАРИСОВАННОЕ ТАМ, ГДЕ ПОЛЕ ЕЩЁ РАЗРЕШИМО** (R3.3, отложено — код НЕ написан, см. `docs/specs/render.md`). Пара снята одним состоянием мира, отличается ровно `DFN_CLOUD` (0 — контроль нулевой дозы, правило 48). Прибор: построчное СКО РАЗНОСТНОГО кадра (ON минус OFF попиксельно), и он не нуждается ни в яркостной, ни в цветовой маске — всё, что не облако, в обеих руках одинаково и вычитается в ноль (структурное лекарство правила 47). Замер по внутренним рядам: лист 110–133 СКО **38–51**, полоса 144–152 СКО **8.2–13.0** при СРЕДНЕМ 72–78 (самом высоком в кадре), кучевые 155–178 СКО 42–49, вторая плоская полоска 180–183 СКО **1.7–3.3**, ниже 193 — ровно 0.00. То есть полоса не тусклая и не шов градиента: это яркая полоса облачного тона, из которой вынули структуру. **Прибор по СРЕДНЕМУ ряду не увидел бы ничего** — средний профиль падает с 75 до 0 без единой ступеньки. Причина: внешняя сходимость в `dfn_cloud_alpha` срабатывает на `cells_px` 0.60, когда у базовой октавы ещё жив 21 % амплитуды | `DFN_CLOUD=<1|0> DFN_SKY_PROBE=1 DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 DFN_TIME=0.35 DFN_MENU=0 ./build_render/engine/app/dfn_app`, затем `tools/archive_frame.py <src> <dst> 640` |

## Сколько ждать перед снимком

Первая партия облачных кадров была снята через 75 и 30 кадров ожидания, и по
крагу шла регулярная ШАХМАТКА. Это не залипший fade и не «две системы рисуют
одну землю»: та же точка, снятая неподвижно через 300 кадров, приходит сплошной
— контрправило (30b) разводит эти две версии за один прогон. Счётчик
`TourStep::wait_frames` читали как «успело устояться», а он означает только
«кадров отрисовано»: дальние узлы всё ещё ПРИЕЗЖАЮТ от core на 75-м кадре, и
каждый начинает свои `LOD_FADE_SECONDS` = 0.6 с растворения тогда, когда
приехал. Время устаканивания = задержка стриминга ПЛЮС растворение, а не одно
растворение. Для дальних видов 300 кадров хватает; кадр с чужим дефектом не
может ничего заверить, поэтому все шесть кадров ниже переотсняты.

| `character-standing-front-*.png` | ПОСЛЕ четырёх замечаний пользователя: стойка сомкнута (лодыжки 0.344 → 0.117 м), сегменты — фаски вместо коробок, плечи скатом к короткой шее вместо плиты груди до подбородка. Руки стали ВИДНЫ: фаска срезала угол торса, за которым они прятались | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=mirror DFN_MIRROR=1 ./dfn_app` |
| `character-lookdown-after-*.png` | Тот же взгляд вниз, что и в кадре-дефекте выше: грудь больше не стена, в кадре рука, бедро и земля. Порог видимости стопы 0.430 → 0.294 м при вылете 0.485 | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=stride DFN_BODY_PITCH=-1.15 DFN_PLAYTEST=soak ./dfn_app` |
| `character-walk-knee-fixed-{0.25,0.50,0.75,1.00}-*.png` | КОЛЕНИ БОЛЬШЕ НЕ ВЫГИБАЮТСЯ (жалоба «крипово»): было −33.4°, стало −5.2° (физиологический допуск). Видно перекат через носок — пятка задней ноги поднята. Четыре фазы, потому что дефект жил именно в поздней опоре и на одном кадре его не поймать | `DFN_MENU=0 DFN_INTERNAL_RES=640x360 DFN_BODY_PROBE=plant DFN_SHOWCASE=1 ./dfn_app` |
| `flora-forest-floor-01-path-along-dirt-fb7e3e1.png` **УСТАРЕЛ — СНЯТ НА ЗЕМЛЕ, КОТОРОЙ БОЛЬШЕ НЕТ** (до f81690c/f84d9e6: рисуемая земля расходилась с землёй размещения на −1.50…+1.50 м). Заменён на `-cc19caf`. Оставлен как след, читать как улику НЕЛЬЗЯ. Прежняя подпись ниже. | ПОСЛЕ маршрутизации `flora_owns()`: пятнадцать кадров стенда снимаются БЕЗ единой строки `SCATTER SPECIES … HAS NO MESH` — пять видов §5.10 больше не рисуются ничем. Полотно тропы текстурировано (сплат render'а приехал), справа от тропы валун. ЧЕСТНО О ТОМ, ЧЕГО НЕ ВИДНО: богатая кромка (мох, цветы, грибы, галечная линия) на этом ракурсе НЕ ЧИТАЕТСЯ — это не заверенный кадр кромки, а заверение того, что дефект «рисуется ничем» закрыт | `DFN_STAND=forest DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 ./build_lead/engine/app/dfn_app`, затем box-average 4×4 (см. ниже) |
| `core-one-surface-01-path-along-dirt-f84d9e6.png` | ПОСЛЕ того как рисуемая земля и земля размещения стали ОДНОЙ поверхностью (f81690c + f84d9e6). Тропа читается непрерывно до горизонта. ЧЕСТНО О ТОМ, ЧЕГО ЭТОТ КАДР НЕ ДОКАЗЫВАЕТ: на нём ещё стоит временный подъём `PATH_SURFACE_LIFT_M` render'а, поэтому он НЕ отделяет исправление core от их заплатки — до её удаления этот ракурс не может провалиться (Rule 27) и служит проверкой отсутствия регрессии, а не доказательством. Доказательство — числа: худший зазор протектора над РИСУЕМОЙ поверхностью −0.663 → +0.100 м, медиана +0.004 → +0.146 м при канавке 0.15 м | `DFN_STAND=forest DFN_TOUR=1 DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 ./build_core/engine/app/dfn_app`, затем box-average 4×4 (см. ниже) |
| `core-one-surface-06-lf2-crest-f84d9e6.png` | ТОТ ЖЕ ФИКС, ракурс, который МОЖЕТ провалиться: всё, что размещено ПО ВЫСОТЕ (поваленное дерево, камни, подстилка), стоит на рисуемой земле. До фикса heightmap расходился с полем размещения на −1.50…+1.50 м — плавающее бревно было бы видно сразу. Замер того же утверждения: расхождение ±0.0031 м, один шаг квантования | то же |

| `flora-cards-lookup-before-d06db79.png` / `-after-` | ВЗГЛЯД ВВЕРХ НА ПОЛОГ (pitch 0.60 рад, φ 40-75°) — ракурс, на котором изменение ВЫИГРЫВАЕТ. Плоскости карточек листвы 63.6-80.8° → одна из трёх на 5-10°, две на 48-66°; представленная площадь дуба на 60° 229 → 413 м²/дерево. Кроны шире и мягче, неба в верхних 60 % кадра 39.96 → 39.60 %. ЧЕСТНО: доля зелёных пикселей при этом падает 17.30 → 15.84 % — прибавка полога приходит СНИЗУ и потому тёмная, как настоящий полог снизу | оба плеча собраны из ОДНОГО состояния дерева (git stash только своих двух файлов), снято по рецепту render: `DFN_MENU=0 DFN_STAND=forest DFN_RESTORE=<pose, pitch=0.60> DFN_TIME=0.350000 DFN_VISTIME=10 DFN_CAPTURE_DIR=<НОВЫЙ dir> DFN_CAPTURE_AFTER=5 ./dfn_app`, затем box-average 4×4 |
| `flora-cards-treeline-before-d06db79.png` / `-after-` | ТОТ ЖЕ ФИКС, ракурс, который МОЖЕТ провалиться (Rule 27): взгляд ГОРИЗОНТАЛЬНО на дальний лес, φ≈5-15°, где горизонтальная карточка стоит ребром. Это ЦЕНА: неба в верхних 60 % кадра 19.55 → 20.76 %, полог дальней стены реально прореживается. Численно то же: представленная площадь дуба на 0° 424 → 250 м²/дерево, при полу 229 (худший вид ПРИНЯТОЙ сборки) | то же, с `pitch=0.0` |

| `flora-rich-edge-faint-trail-d06db79.png` | РИЧ-КРОМКА ЧИТАЕТСЯ — переснято после того, как core свёл рисуемую и размещаемую землю в одну поверхность (f81690c/f84d9e6). В прежнем архивном кадре кромка не читалась, и это было записано честно; здесь на ходовой высоте видны цветы, грибы и галечные группы вдоль обеих обочин слабой тропы, сама тропа идёт непрерывной канавкой до горизонта. ЧЕГО КАДР НЕ ДОКАЗЫВАЕТ: он НЕ измеряет отношение богатства кромки к дальнему полю (это инструмент BR-3 у core) и НЕ является контролируемым «до/после» — прежний кадр снят с другого ракурса и по другой земле. Дальнее поле на нём заметно бедное, что согласуется с открытым дефектом плотности мха (40 /га против 263 /га) | `DFN_TOUR=1 DFN_STAND=forest DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 ./dfn_app`, кадр `02_path_along_faint_trail`, затем box-average 4×4 |

| `flora-forest-floor-01-path-along-dirt-cc19caf.png` | ТОТ ЖЕ РАКУРС, переснят на единой поверхности. Полотно грунтовой дороги читается и текстурировано. **ПРЕЖНЕЕ НАБЛЮДЕНИЕ УСТОЯЛО, И ЭТО НОВОСТЬ: обочина ПУСТА — ни цветов, ни грибов, только пара галечных групп справа.** Значит дело было НЕ в дефекте двух поверхностей. Ожидание по таблице: у класса Dirt веса богатства 0.55 цветы / 0.45 грибы / 0.50 галька, то есть примерно половина полосы hint-path, а не ноль; на 40 м обзора ждём 4-8 групп цветов. ЧЕГО ЭТОТ КАДР НЕ РЕШАЕТ: одиночный кадр не отличает «правило кладёт мало» от «кладёт в дрейфах, и этот отрезок попал между ними» — нужен подсчёт экземпляров вдоль всего маршрута, он заведён вопросом к core. Ср. `flora-rich-edge-faint-trail-d06db79.png`, где на классе FaintTrail кромка читается | `DFN_TOUR=1 DFN_STAND=forest DFN_TOUR_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 ./dfn_app`, кадр `01_path_along_dirt`, затем box-average 4×4 |

| `core-apron-west300-{BEFORE,AFTER}-687f152.png` | §5.12 АПРОН, ДВА ПЛЕЧА ИЗ ОДНОГО ФАЙЛА ВОССТАНОВЛЕНИЯ (`core-apron-west300-restore.txt`): кадры отличаются ТОЛЬКО правилом апрона (BEFORE — сборка с отключённым `breaks_massif_apron`). Гора читается остриём с видимым подножием в обоих; разница скромная и локальная — левое подножие массива. ЧЕСТНО: этот ракурс НЕ воспроизводит «низкий бесформенный горб», ради которого §5.12 был вынесен, поэтому он показывает работу правила, но не закрывает приёмку. Замер по §5.12b (`tools/measure_ground_junction.py`): самый длинный участок «массив встречает ЗЕМЛЮ» 106 px (без апрона) → 108 px (с апроном) при угловой ширине массива 328 → 357 px | `DFN_RESTORE=docs/acceptance/core-apron-west300-restore.txt DFN_MENU=0 DFN_CAPTURE_AFTER=3 DFN_CAPTURE_DIR=<dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 ./build_core/engine/app/dfn_app`, затем box-average 4×4 |


| `character-crouch-{fwd,lookdown}-{BEFORE,AFTER}-c296fec.png` | **ПРИСЯД: КАМЕРА БЫЛА НА 0.3711 м НИЖЕ СОБСТВЕННОЙ ГОЛОВЫ ТЕЛА — внутри коробки туловища** (жалоба пользователя, второй раз: «при присяди голова в коробку туловища залезает»). Оба плеча из ОДНОГО файла восстановления (`character-crouch-restore.txt`), отличаются ТОЛЬКО местом глаза при полном присяде. ВЗГЛЯД ВНИЗ — тот ракурс, который не может не провалиться, если дефект есть: коричневых пикселей тела 79.6 → 11.1 %, кадр становится «больше половины тело» с ряда 73 из 360 → с ряда 309 (то есть грудь ушла на нижнюю кромку, где ей и место), ног в кадре ДО не видно вовсе. ВПЕРЁД: 59.9 → 2.8 % (остаток — бурая земля и стволы, не тело); в кадре ДО верхняя половина экрана — это ВНУТРЕННЯЯ сторона своей груди. Числа: глаз 0.8500 → 1.2284 при нарисованном 1.2211 (расхождение 7.3 мм — это цена схождения ног у СТОЯЧЕЙ позы, не присяда), шея присевшего 1.0978. ЧЕГО КАДРЫ НЕ ПОКАЗЫВАЮТ: вторую половину правки — голова теперь компенсирует наклон корпуса (взгляд −5.73° вместо −14.3°) — из своего же черепа не видно; это видно на зеркальном двойнике и на будущих NPC | `DFN_RESTORE=docs/acceptance/character-crouch-restore.txt DFN_MENU=0 DFN_CAPTURE_AFTER=4 DFN_CAPTURE_DIR=<НОВЫЙ dir> DFN_INTERNAL_RES=640x360 DFN_PALETTE=0 build_lead/engine/app/dfn_app`, затем box-average 4×4. Взгляд вниз — тот же файл с `pitch = -1.150000`; плечо BEFORE — тот же бинарник с откатом ТОЛЬКО места глаза (см. шапку файла восстановления) |

## Как кадр приводится к 640×360

`IRenderer::save_screenshot` снимает ЗАДНИЙ БУФЕР ОКНА, а это на retina-экране
2560×1440 — внутренние 640×360, растянутые ровно вчетверо. Растяжение —
ближайший сосед (проверено: в выборке 64 000 субпикселей ни одного блока 4×4 с
неоднородным цветом), поэтому усреднение блоков 4×4 обратно в 640×360
восстанавливает внутренний буфер БИТ В БИТ, а не «уменьшает» кадр. Кадры выше
получены именно так. Правило 27 требует родного разрешения; если когда-нибудь
появится путь сохранения прямо из внутреннего буфера, эта оговорка исчезнет.

Проверено ещё раз зоной render на своих шести кадрах, независимо от выборки
character: усреднение блоков 4×4 по ВСЕМ 691 200 субпикселям каждого кадра дало
**ноль** неоднородных блоков — то есть увеличение действительно ближайший сосед,
и обратное усреднение восстанавливает внутренний буфер, а не приближает его.
Важно: `sips -z` для этого не годится — он ресемплит фильтром; нужен именно
box-average по 4×4.
