# Module: docs/acceptance
# File: docs/acceptance/core-great-oak-clearing-pose.txt
#
# Responsibility:
# - The standpoint of the great oak's clearing frames, as a restore sidecar, so
#   the pose is something you can RUN rather than coordinates in a message.
#
# Key items:
# - 200 m north of the giant's site (916.2, 913.1) and 110 m up, looking south
#   and 27.5 deg down onto the oak mass, so the clearing reads as a hole in the
#   canopy. The first cut stood 300 m back at 62 m and 11.5 deg: the intervening
#   crowns filled the frame and the hole was behind them, which is the placement
#   version of photographing a thing from where it cannot be seen.
#
# Dependencies:
# - Uses: App's DFN_RESTORE parser.
# - Used by: docs/acceptance/core-great-oak-clearing-{ARM,CONTROL}-*.png.
#
# AI Agents Notice (must follow):
# - Follow docs/ARCHITECTURE.md strictly.
# - DFN_NULL_PHYSICS=1 IS NOT COSMETIC here: the eye stands 56 m above ground
#   and without it the restored player falls out of the sky before the shutter.
# - The CONTROL arm is DFN_NO_GREAT_OAK=1 on the same binary: no giants, no
#   clearing, everything else identical by construction (Rule 30/48).
captured_at = 12:08:2026 - 23:00:00
build_commit = core-great-oak-clearing
stand = 0
seed = 1
game_seconds = 886.011789
day_fraction = 0.500000
lunar_phase = 0.010987
pos_x = 916.237000
pos_y = 160.000000
pos_z = 763.000000
yaw = 3.141593
pitch = -0.798000
look_x = 0.000000
look_y = -0.716000
look_z = 0.698000
speed_mps = 0.000000
vertical_velocity = 0.000000
stride_phase = 0.000000
gait = 0
locomotion = 0
grounded = 1
crouched = 0
water_depth = 0.000000
internal_w = 640
internal_h = 360
fov_y_rad = 1.309000
head_bob = 1.000000
palette_post = 0
wind_strength = 0.480618
cloud_cover = 0.000000
ambient_darkness = 0.000000
fps = 120.0
frame_ms = 8.3
frame_ms_worst = 8.3
chunks_resident = 9
lod_nodes = 0

# ------------------------------------------------------------------------
# THE FRAMES SHOT FROM THIS POSE (commit 7c33e6d)
#
#   docs/acceptance/core-great-oak-clearing-AFTER-7c33e6d.png
#   docs/acceptance/core-great-oak-clearing-CONTROL-7c33e6d.png
#   ...-AFTER-ZOOM2x-7c33e6d.png / ...-CONTROL-ZOOM2x-7c33e6d.png
#     (the crop that carries the read: x 200..440, y 110..245, doubled)
#
# RECIPE, both arms, ONE binary:
#   DFN_RESTORE=docs/acceptance/core-great-oak-clearing-pose.txt \
#   DFN_NULL_PHYSICS=1 DFN_NULL_AUDIO=1 DFN_INTERNAL_RES=640x360 \
#   DFN_PALETTE=0 DFN_MENU=0 DFN_CAPTURE_DIR=<dir> DFN_CAPTURE_AFTER=5 \
#   ./build_core/engine/app/dfn_app
#   # the CONTROL arm is the same command plus DFN_NO_GREAT_OAK=1
#   # then: python3 tools/archive_frame.py <src> <dst> 640
#
# WHAT TO LOOK FOR: in the CONTROL the oak lattice covers the middle of the
# frame evenly; in the AFTER arm there is a glade about 111 m across with the
# trees standing off its rim. That is the whole claim of this pass.
#
# WHAT THESE FRAMES DO NOT SHOW, AND IT IS NOT A DEFECT OF PLACEMENT: the
# giant itself. render has no mesh bound to ScatterSpecies::GreatOak yet, so
# the world places it, the occlusion model carries it, and nothing draws it.
# The frame will be re-taken from the giant's own read distance once it does.
#
# AND DO NOT PIXEL-DIFF THIS PAIR. Measured, not assumed: two shots of the SAME
# arm differ on 11.9 % of pixels (foliage and LOD dissolve at this altitude),
# against 4.7 % between the arms -- the noise is larger than the signal, so a
# percentage here would be a number that means nothing. The pair is read by EYE
# (Rule 27) and the COUNT comes from the generator (Rule 47, test_great_oak:
# 0 trees inside the clearing against 40 in the control).
