# Module: docs/acceptance
# File: docs/acceptance/palette-thumbs-capture.txt
#
# Responsibility:
# - ДВА КАДРА, доказывающие, что у деталей в меню объектов появилась картинка
#   (заказ 18.08: «у объектов в меню объектов нет всё ещё предпросмотра что это
#   за объекты, только название, нужно чтобы там на белом фоне сам объект был
#   нарисован»).
#
# Key items:
#   palette-thumbs-BEFORE-b82aeb6.png  крючок thumbnail НЕ подключён (контроль)
#   palette-thumbs-AFTER-b82aeb6.png   он подключён
#
# СНЯТО, из корня репозитория:
#     DFN_EDITOR=1 DFN_EDITOR_PARTS=1 DFN_OPEN_MAP=houses/demo \
#     DFN_CAPTURE_DIR=<НОВЫЙ каталог> DFN_CAPTURE_AFTER_FRAMES=180 \
#     build_lead/engine/app/dfn_app
#   затем tools/archive_frame.py <capture_000.png> <dst.png> 1280
#   (2560x1440 нативных, box-average /2).
#
# КОНТРОЛЬНАЯ РУКА (нулевая доза, правило 48) сделана ОДНОЙ правкой в
# App.cpp::wire_editor_panels() — строка `hooks.thumbnail = ...` убрана, сборка
# и тот же прогон, правка возвращена. Отличаться могла ровно она, поэтому
# разница на кадрах не может быть чужой работой.
#
# ЧТО ЧИТАЕТСЯ С КАДРОВ (сетка семейств, правая колонка):
#   ДО    — шесть СЕРЫХ кнопок со словом: «Брус», «Перевязка сруба», «Настил»,
#           «Дверь», «Наличник двери», «Ограда». Ни одного предмета; ровно то
#           состояние, на которое пожаловался пользователь.
#   ПОСЛЕ — те же шесть плиток, в каждой предмет НА БЕЛОМ: брус лежит бруском,
#           перевязка сруба — стопкой венцов, настил — доской, дверь — полотном
#           в раме, ограда — двумя жердями на столбах. Подписи и счётчики
#           (96 / 20 / 24 / 16 / 24 / 24) не изменились.
#
# ПОЧЕМУ ДВЕРЬ DFN_EDITOR_PARTS ВООБЩЕ ЕСТЬ: меню открывается клавишей, а
# беспилотный прогон руки не держит — то есть кадр «меню закрыто» одинаков и у
# работающей картинки, и у полностью сломанной (правило 27). Дверь подаёт то же,
# что подаёт рука, — ОДНО нажатие «меню объектов», — и дальше идёт тем же телом,
# что и клавиша; отдельного пути у неё нет (урок 17.08 в шапке EditorUi.h).
#
# ЧЕГО ЭТИ КАДРЫ НЕ ДОКАЗЫВАЮТ, и потому рядом стоит рукав app_editor_palette:
# что связь 0.25 м и брус 4.6 м кадрируются ОДНИМ правилом, что деталь,
# показанная в трёх местах, выпечена ОДИН раз, и что прокрутка всей полки не
# набирает 356 МБ текстур. Всё три на кадре выглядят одинаково правильно.
